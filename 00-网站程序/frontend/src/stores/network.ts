import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

// 小节接口
interface Section {
  id: string
  title: string
  content: string        // Markdown格式知识点
  mindMap?: string       // Mermaid思维导图（可选）
}

// 章节接口
interface Chapter {
  id: string
  number: number
  title: string
  score: string          // 分值范围
  importance: 'high' | 'medium' | 'low'
  sections: Section[]
  mindMap: string        // Mermaid思维导图
}

export const useNetworkStore = defineStore('network', () => {
  // 当前选中的章节和小节
  const currentChapterId = ref<string>('ch1')
  const currentSectionId = ref<string>('1.1')
  
  // 学习进度记录
  const studiedSections = ref<Set<string>>(new Set())
  
  // 所有章节数据（按王道《计算机网络》8章结构）
  const chapters = ref<Chapter[]>([
    {
      id: 'ch1',
      number: 1,
      title: '计算机网络体系结构',
      score: '5-8分',
      importance: 'medium',
      mindMap: `mindmap
  root((计算机网络体系结构))
    计算机网络概述 ⭐⭐⭐
      概念与组成
      基本概念 ⭐⭐⭐
        实体（Entity）
        协议（Protocol）⭐⭐⭐
          语法
          语义
          时序
        服务（Service）⭐⭐⭐
          面向连接
          无连接
        接口（Interface）
        SDU/PCI/PDU ⭐⭐⭐
      功能
      交换方式 ⭐⭐⭐
        电路交换
        报文交换
        分组交换
      分类
      性能指标 ⭐⭐⭐
        速率
        带宽
        吞吐量
        时延 ⭐⭐⭐
    体系结构 ⭐⭐⭐
      OSI七层模型
      TCP/IP四层模型
      五层参考模型
      各层功能
      封装与解封装`,
      sections: [
        {
          id: '1.1',
          title: '计算机网络概述',
          content: `## 1.1 计算机网络概述

### 一、计算机网络的概念

**计算机网络**：将地理位置不同的具有独立功能的多个计算机及其外部设备，通过通信线路连接起来，在网络操作系统、网络管理软件及网络通信协议的管理和协调下，实现资源共享和信息传递的计算机系统。

### 二、计算机网络的基本概念 ⭐⭐⭐必考

#### 1. 实体（Entity）

**定义**：任何可以发送或接收信息的硬件或软件进程

**常见实体**：
- 📍 **用户进程**：浏览器、邮件客户端等应用程序
- 📍 **系统进程**：操作系统中的网络协议栈
- 📍 **网络设备**：路由器、交换机、网卡等

**理解要点**：
- 实体是通信的参与者
- 同一层中的实体称为**对等实体**（Peer Entity）

---

#### 2. 协议（Protocol）⭐⭐⭐必考

**定义**：控制两个对等实体进行通信的规则的集合

**协议的三要素** ⭐⭐⭐：

##### （1）语法（Syntax）
- **定义**：数据与控制信息的结构或格式
- **例子**：
  - 数据包的头部格式
  - 字段的排列顺序
  - 编码方式（ASCII、UTF-8等）

##### （2）语义（Semantics）
- **定义**：需要发出何种控制信息，完成何种动作以及做出何种响应
- **例子**：
  - SYN表示请求建立连接
  - ACK表示确认收到
  - FIN表示终止连接

##### （3）时序（Timing/Synchronization）
- **定义**：事件实现顺序的详细说明
- **例子**：
  - TCP三次握手的顺序
  - 超时重传的时机
  - 流量控制的节奏

**协议的特点**：
- ✅ **水平方向**：控制对等实体之间的通信
- ✅ **标准化**：由国际组织制定（如ISO、IEEE、IETF）
- ✅ **分层实现**：每层都有自己的协议

**常见协议举例**：
- 应用层：HTTP、FTP、SMTP、DNS
- 传输层：TCP、UDP
- 网络层：IP、ICMP、ARP
- 数据链路层：Ethernet、PPP
- 物理层：RS-232、V.35

---

#### 3. 服务（Service）⭐⭐⭐必考

**定义**：下层向上层提供的功能调用

**服务的两种类型**：

##### （1）面向连接的服务（Connection-Oriented）
- **特点**：
  - 通信前先建立连接
  - 数据传输按序、可靠
  - 通信结束后释放连接
- **例子**：TCP、电话系统
- **类比**：打电话（先拨号→通话→挂断）

##### （2）无连接的服务（Connectionless）
- **特点**：
  - 不需要建立连接
  - 不保证可靠性
  - 每个分组独立传输
- **例子**：UDP、邮政系统
- **类比**：寄信（直接投递，不保证到达）

**服务的特点**：
- ✅ **垂直方向**：下层为上层提供服务
- ✅ **接口调用**：通过SAP（服务访问点）访问
- ✅ **透明性**：上层不需要知道下层如何实现

---

#### 4. 协议与服务的关系 ⭐⭐⭐重要对比

| 对比项 | 协议（Protocol） | 服务（Service） |
|-------|-----------------|----------------|
| **方向** | 水平方向（对等实体之间） | 垂直方向（上下层之间） |
| **可见性** | 对用户透明 | 对用户可见 |
| **作用** | 规定通信规则 | 提供功能接口 |
| **实现** | 由本层内部实现 | 通过下一层实现 |
| **例子** | TCP协议、IP协议 | 可靠传输服务、尽力而为服务 |

**关键理解**：
- 🔑 **协议是内部的**：对等实体之间如何通信，用户看不见
- 🔑 **服务是外部的**：下层给上层提供什么功能，用户可以调用
- 🔑 **协议支撑服务**：通过协议实现服务

**类比理解**：

邮局系统：
- 协议：邮递员之间的交接规则（内部）
- 服务：给用户提供的寄信功能（外部）

餐厅系统：
- 协议：厨师和服务员的配合规则（内部）
- 服务：给顾客提供的点菜功能（外部）

---

#### 5. 接口（Interface）

**定义**：同一结点内相邻两层之间交换信息的连接点

**服务访问点（SAP）**：
- 接口的具体实现
- 上层通过SAP调用下层服务
- 例如：端口号就是传输层的SAP

**接口的特点**：
- 定义了下层向上层提供的原语（Primitive）
- 包括请求、指示、响应、确认四种类型

---

#### 6. SDU、PCI、PDU的关系 ⭐⭐⭐必考

##### （1）SDU（Service Data Unit，服务数据单元）
- **定义**：从上层接收的数据
- **位置**：作为本层的数据部分

##### （2）PCI（Protocol Control Information，协议控制信息）
- **定义**：本层添加的控制信息（首部/尾部）
- **内容**：地址、序号、校验和等

##### （3）PDU（Protocol Data Unit，协议数据单元）
- **定义**：本层实际传输的数据单位
- **公式**：PDU = PCI + SDU

**封装过程示例**：

应用层数据 → TCP段（TCP首部 + 应用数据）
           → IP数据包（IP首部 + TCP段）
           → 以太网帧（帧头 + IP数据包 + 帧尾）

**各层PDU名称**：
- 应用层：**报文**（Message）
- 传输层：**段**（Segment，TCP）或**数据报**（Datagram，UDP）
- 网络层：**数据包**（Packet）或**数据报**（Datagram）
- 数据链路层：**帧**（Frame）
- 物理层：**比特**（Bit）

**例题**：【2018统考】在OSI参考模型中，自下而上第一个提供端到端服务的层次是（）。

A. 数据链路层
B. 传输层
C. 会话层
D. 应用层

**答案**：B

**解析**：传输层是第一个提供端到端（End-to-End）服务的层次，负责进程到进程的通信。数据链路层和网络层提供的是点到点（Point-to-Point）服务。

---

### 三、计算机网络的组成

1. **硬件**
   - 计算机、路由器、交换机、传输介质等

2. **软件**
   - 网络操作系统
   - 网络管理软件
   - 网络通信协议

3. **协议**
   - 网络通信的规则和约定

### 三、计算机网络的功能

1. **数据通信** ⭐⭐⭐
   - 最基本、最重要的功能
   - 电子邮件、文件传输、即时通讯等

2. **资源共享**
   - 硬件资源：打印机、存储设备等
   - 软件资源：应用程序、数据库等
   - 数据资源：文件、数据库记录等

3. **分布式处理**
   - 多台计算机协同完成任务
   - 提高可靠性和处理能力

### 四、三种交换方式 ⭐⭐⭐必考

#### 1. 电路交换（Circuit Switching）

**工作流程**：
- 建立连接 → 通话 → 释放连接

**特点**：
- 独占通信资源
- 实时性强，时延小
- 效率低，线路利用率低

**适用场景**：电话网络

#### 2. 报文交换（Message Switching）

**工作原理**：
- 存储转发方式
- 整个报文作为整体传输

**特点**：
- 不需要建立连接
- 动态分配线路
- 提高线路利用率

**缺点**：
- 时延大
- 对中间结点缓存要求高
- 不适合实时通信

#### 3. 分组交换（Packet Switching）⭐⭐⭐

**工作原理**：
- 将报文划分为多个分组
- 每个分组独立传输
- 采用统计复用技术

**优点**：
- ✅ **高效**：动态分配带宽，线路利用率高
- ✅ **灵活**：每个分组可独立选择路径
- ✅ **迅速**：不必先建立连接，可直接发送
- ✅ **可靠**：完善的网络协议保证可靠性

**缺点**：
- ❌ 时延不确定（排队时延）
- ❌ 需要额外控制信息（首部开销）
- ❌ 可能出现失序、丢失等问题

**三种交换方式对比**：

| 对比项 | 电路交换 | 报文交换 | 分组交换 |
|-------|---------|---------|----------|
| **连接方式** | 面向连接 | 无连接 | 无连接 |
| **资源占用** | 独占 | 共享 | 共享 |
| **时延** | 小（建立连接时延大） | 大 | 中等 |
| **线路利用率** | 低 | 高 | 高 |
| **实时性** | 好 | 差 | 较好 |
| **适用场景** | 电话 | 电报 | 互联网 |

**例题**：【2019统考】下列关于分组交换的说法中，正确的是（）。

A. 分组交换比电路交换的时延小
B. 分组交换适合传输大量数据
C. 分组交换需要预先建立连接
D. 分组交换的线路利用率低

**答案**：B

**解析**：分组交换采用统计复用，线路利用率高，适合突发数据传输。A错误，因为分组交换有排队时延；C错误，分组交换无需建立连接；D错误，分组交换线路利用率高。

### 五、计算机网络的分类

#### 1. 按分布范围分类

- **WAN（广域网）**：覆盖几十到几千公里，如Internet
- **MAN（城域网）**：覆盖一个城市，5-50公里
- **LAN（局域网）**：覆盖较小地理范围，1公里左右
- **PAN（个人区域网）**：围绕个人，10米以内，如蓝牙

#### 2. 按使用者分类

- **公用网**：电信公司建造，付费使用
- **专用网**：特定组织建造，内部使用

#### 3. 按拓扑结构分类

- **总线型**：所有结点连接到一条总线上
- **星型**：所有结点连接到中心结点
- **环型**：结点形成闭合环路
- **网状型**：结点之间有多条路径，可靠性高

### 六、性能指标 ⭐⭐⭐必考

#### 1. 速率（Rate）

**定义**：数据的传送速率，单位为bit/s（bps）

**常用单位换算**：
\`\`\`
1 Kbps = 10³ bit/s = 1,000 bit/s
1 Mbps = 10⁶ bit/s = 1,000,000 bit/s
1 Gbps = 10⁹ bit/s = 1,000,000,000 bit/s
1 Tbps = 10¹² bit/s
\`\`\`

**重要注意**：
- 1 Byte = 8 bits
- 存储容量用Byte，网络速率用bit

#### 2. 带宽（Bandwidth）

**定义**：网络通信线路传送数据的能力，即最高速率

**单位**：bit/s（与速率相同）

**理解要点**：
- 带宽越大，数据传输能力越强
- 类似于水管的粗细

#### 3. 吞吐量（Throughput）

**定义**：单位时间内通过某个网络的实际数据量

**特点**：
- 受限于网络带宽
- 受网络拥塞程度影响
- 通常小于带宽

**类比**：
- 带宽 = 水管的最大流量
- 吞吐量 = 实际流过的水量

#### 4. 时延（Delay）⭐⭐⭐必考

**总时延组成**：
\`\`\`
总时延 = 发送时延 + 传播时延 + 处理时延 + 排队时延
\`\`\`

##### （1）发送时延（Transmission Delay）

**定义**：主机或路由器发送数据帧所需要的时间

**计算公式**：
\`\`\`
发送时延 = 数据长度 / 发送速率
\`\`\`

**特点**：
- 与数据长度成正比
- 与发送速率成反比

##### （2）传播时延（Propagation Delay）

**定义**：电磁波在信道中传播一定距离需要花费的时间

**计算公式**：
\`\`\`
传播时延 = 信道长度 / 电磁波传播速率
\`\`\`

**电磁波传播速率**：
- 铜线：约 2.3×10⁸ m/s
- 光纤：约 2.0×10⁸ m/s

**特点**：
- 与信道长度成正比
- 与数据长度无关

##### （3）处理时延（Processing Delay）

**定义**：主机或路由器收到分组后进行处理所需要的时间

**包括**：
- 分析首部
- 提取数据
- 差错检验
- 查找路由等

##### （4）排队时延（Queuing Delay）

**定义**：分组在路由器输入/输出队列中排队等待处理的时间

**特点**：
- 取决于网络拥塞程度
- 不确定，变化较大

**重要对比**：

| 时延类型 | 影响因素 | 是否可忽略 |
|---------|---------|------------|
| **发送时延** | 数据长度、发送速率 | 大数据时不可忽略 |
| **传播时延** | 信道长度、传播速率 | 远距离时不可忽略 |
| **处理时延** | 设备性能 | 通常较小 |
| **排队时延** | 网络拥塞程度 | 拥塞时很大 |

**例题**：主机甲通过1个路由器与主机乙相连，甲到路由器和路由器到乙的信道长度均为1000km，信号传播速度为2×10⁸m/s，链路带宽为1Gbps。若甲向乙发送一个大小为1000B的数据包，忽略处理时延和排队时延，则总时延为多少？

**解**：

**步骤1**：计算发送时延
\`\`\`
发送时延 = 数据长度 / 发送速率
         = 1000×8 bit / 10⁹ bit/s
         = 8×10³ / 10⁹ s
         = 8 μs
\`\`\`

**步骤2**：计算传播时延
\`\`\`
传播时延 = 信道长度 / 传播速率
         = (2×1000×10³) m / (2×10⁸) m/s
         = 2×10⁶ / 2×10⁸ s
         = 10 ms
\`\`\`

**步骤3**：计算总时延
\`\`\`
总时延 = 发送时延 + 传播时延
       = 8 μs + 10 ms
       ≈ 10 ms  （发送时延可忽略）
\`\`\`

**关键理解**：
- ⚠️ 发送时延与数据长度成正比
- ⚠️ 传播时延与信道长度成正比
- ⚠️ 两者是独立的，不要混淆
- ⚠️ 对于小数据包，传播时延往往占主导

#### 5. 时延带宽积

**定义**：传播时延 × 带宽

**物理意义**：表示链路上能容纳的最大比特数（以比特为单位的链路长度）

**公式**：
\`\`\`
时延带宽积 = 传播时延 × 带宽
\`\`\`

#### 6. 往返时间RTT（Round-Trip Time）

**定义**：从发送方发送数据开始，到发送方收到接收方的确认所经历的时间

**组成**：
- 双向传播时延
- 中间结点处理时延
- 不包括发送时延（通常只考虑ACK的往返）

**应用**：TCP超时重传时间的设置

#### 7. 利用率

**信道利用率**：信道有百分之几的时间被利用

**网络利用率**：全网络信道利用率的加权平均值

**重要结论**：
- 利用率越高，时延越大
- 当利用率接近1时，时延急剧增加
- 实际网络中，利用率通常控制在0.5以下

### 七、考研重点

**高频考点**：
1. ⭐⭐⭐ 三种交换方式的比较
2. ⭐⭐⭐ 时延的计算（发送时延 vs 传播时延）
3. ⭐⭐ 性能指标的定义和单位
4. ⭐ 计算机网络的分类

**典型考题**：
> 【2020统考】下列因素中，不会影响信道数据传输速率的是（）。
> 
> A. 信噪比
> B. 频率带宽
> C. 较高的传输速率
> D. 信号传播速度
> 
> **答案**：D（信号传播速度影响传播时延，不影响数据传输速率）
`        },
        {
          id: '1.2',
          title: '计算机网络体系结构与参考模型',
          content: `## 1.2 计算机网络体系结构与参考模型 ⭐⭐⭐必考

### 一、计算机网络分层结构

**分层思想**：将复杂的网络系统划分为若干个层次，每一层完成特定的功能。

**优点**：
- ✅ 各层之间相互独立
- ✅ 灵活性好
- ✅ 结构上可分割开
- ✅ 易于实现和维护
- ✅ 能促进标准化工作

### 二、OSI七层参考模型 ⭐⭐⭐

**OSI（Open System Interconnection）**：开放系统互连基本参考模型

| 层次 | 名称 | 主要功能 | 典型协议/设备 |
|------|------|----------|---------------|
| 7 | **应用层** | 为用户进程提供服务 | HTTP、FTP、SMTP |
| 6 | **表示层** | 数据格式转换、加密解密 | JPEG、MPEG |
| 5 | **会话层** | 建立、管理和终止会话 | RPC、SQL |
| 4 | **传输层** | 端到端的可靠传输 | TCP、UDP |
| 3 | **网络层** | 路由选择、拥塞控制 | IP、ICMP、路由器 |
| 2 | **数据链路层** | 帧的传输、差错控制 | Ethernet、PPP、交换机 |
| 1 | **物理层** | 比特流的透明传输 | 双绞线、集线器 |

**记忆口诀**："物数网传会表应"（从下到上）

### 三、TCP/IP四层模型 ⭐⭐⭐

| 层次 | 对应OSI层 | 主要协议 |
|------|-----------|----------|
| **应用层** | 应用层+表示层+会话层 | HTTP、FTP、DNS、SMTP |
| **传输层** | 传输层 | TCP、UDP |
| **网际层** | 网络层 | IP、ICMP、ARP |
| **网络接口层** | 数据链路层+物理层 | Ethernet、WiFi |

**特点**：
- ✅ 更简洁实用
- ✅ 互联网的实际标准
- ❌ 不严格区分服务、接口和协议

### 四、五层参考模型 ⭐⭐⭐（教学用）

综合OSI和TCP/IP的优点，用于教学：

| 层次 | 名称 | 数据单位 | 主要功能 |
|------|------|----------|----------|
| 5 | **应用层** | 报文 | 通过应用进程间的交互来完成特定网络应用 |
| 4 | **传输层** | 报文段/用户数据报 | 为两台主机间的应用进程提供通用的数据传输服务 |
| 3 | **网络层** | IP数据报 | 选择合适的路由，使源主机的运输层所传下来的分组能够交付到目的主机 |
| 2 | **数据链路层** | 帧 | 将网络层交下来的IP数据报组装成帧，在两个相邻结点间的链路上传送帧 |
| 1 | **物理层** | 比特 | 利用传输介质为数据链路层提供物理连接，实现比特流的透明传输 |

### 五、各层的主要功能 ⭐⭐⭐

#### 1. 应用层
- 直接为用户的应用进程提供服务
- 常见协议：HTTP、FTP、SMTP、DNS

#### 2. 传输层
- 负责向两个主机中进程之间的通信提供服务
- **关键概念**：端口号、复用、分用
- 主要协议：TCP（可靠）、UDP（不可靠）

#### 3. 网络层
- 负责为分组交换网上的不同主机提供通信服务
- **关键功能**：路由选择、拥塞控制
- 主要协议：IP

#### 4. 数据链路层
- 将网络层交下来的IP数据报组装成帧
- **关键功能**：差错控制、流量控制
- 主要协议：Ethernet、PPP

#### 5. 物理层
- 利用传输介质为数据链路层提供物理连接
- **关键功能**：比特流的透明传输
- 主要设备：集线器、中继器

### 六、封装与解封装过程 ⭐⭐⭐

#### 1. 封装（发送端）

数据从应用层向下传递，每经过一层就添加该层的头部信息：

应用层数据 → 添加传输层头部 → 添加网络层头部 → 添加数据链路层头部和尾部 → 物理层比特流

**数据单位变化**：
- 应用层：报文（Message）
- 传输层：报文段（Segment，TCP）或用户数据报（Datagram，UDP）
- 网络层：IP数据报（Datagram）
- 数据链路层：帧（Frame）
- 物理层：比特（Bit）

#### 2. 解封装（接收端）

数据从物理层向上传递，每经过一层就去除该层的头部信息：

物理层比特流 → 去除链路层头部 → 去除网络层头部 → 去除传输层头部 → 应用层数据

### 七、OSI vs TCP/IP 对比

| 比较项 | OSI模型 | TCP/IP模型 |
|--------|---------|------------|
| 层数 | 7层 | 4层 |
| 产生时间 | 理论先行 | 实践先行 |
| 市场成功 | 失败 | 成功 ✅ |
| 面向连接 | 网络层无连接，传输层面向连接 | 网际层无连接，传输层可选择 |
| 通用性 | 通用性强 | 主要用于互联网 |

### 八、考研重点

**高频考点**：
1. ⭐⭐⭐ OSI七层模型的名称和功能
2. ⭐⭐⭐ TCP/IP四层模型
3. ⭐⭐⭐ 五层参考模型
4. ⭐⭐⭐ 封装与解封装过程
5. ⭐⭐ 各层的数据单位
6. ⭐⭐ OSI与TCP/IP的比较

**典型考题**：
> 【2018统考】在OSI参考模型中，自下而上第一个提供端到端服务的层次是（）。
> 
> A. 数据链路层
> B. 传输层
> C. 会话层
> D. 应用层
> 
> **答案**：B（传输层提供端到端的可靠传输服务）
`        }
      ]
    },
    {
      id: 'ch2',
      number: 2,
      title: '物理层',
      score: '3-5分',
      importance: 'low',
      mindMap: `mindmap
  root((物理层))
    通信基础 ⭐⭐
      数据与信号
      奈奎斯特定理
      香农定理 ⭐⭐⭐
      编码与调制
    传输介质
      导引型
        双绞线
        同轴电缆
        光纤
      非导引型
        无线电波
        微波
    物理层设备
      中继器
      集线器`,
      sections: [
        {
          id: '2.1',
          title: '通信基础',
          content: `## 2.1 通信基础 ⭐⭐

### 一、基本概念

**数据（Data）**：运送消息的实体

**信号（Signal）**：数据的电气或电磁表现

**码元（Code）**：在使用时间域（时域）的波形表示数字信号时，代表不同离散数值的基本波形

### 二、奈奎斯特定理 ⭐⭐

**理想低通信道的最高码元传输速率**：

最高码元传输速率 = 2W Baud

其中W是理想低通信道的带宽（Hz）

**极限数据传输率**（每个码元携带log₂V bit信息）：

极限数据传输率 = 2W × log₂V (bit/s)

其中V是码元离散电平的个数

### 三、香农定理 ⭐⭐⭐必考

**带宽受限且有高斯白噪声干扰的信道的极限数据传输率**：

极限数据传输率 = W × log₂(1 + S/N) (bit/s)

其中：
- W：信道带宽（Hz）
- S：信道内所传信号的平均功率
- N：信道内部的高斯噪声功率
- S/N：信噪比

**信噪比（dB）与S/N的关系**：

信噪比(dB) = 10 × log₁₀(S/N)

**例题**：【2017统考】若信道在无噪声情况下的极限数据传输速率不小于信噪比为30dB条件下的极限数据传输速率，则信号状态数至少是多少？

**解**：
30dB = 10 × log₁₀(S/N) → S/N = 1000

无噪声：2W × log₂V ≥ W × log₂(1 + 1000)
2 × log₂V ≥ log₂(1001) ≈ 10
log₂V ≥ 5
V ≥ 32

所以信号状态数至少是32。

### 四、编码与调制

#### 1. 数字数据编码为数字信号 ⭐⭐

**（1）非归零编码（NRZ, Non-Return-to-Zero）**
- **正电平**表示1，**负电平**表示0
- **优点**：实现简单
- **缺点**：无法同步，长串0或1时无法判断边界

**（2）归零编码（RZ, Return-to-Zero）**
- **前半周期**有电平表示1，**整个周期**无电平表示0
- **优点**：可以同步
- **缺点**：占用带宽大

**（3）反向非归零编码（NRZI）**
- **电平跳变**表示1，**电平不变**表示0
- **优点**：有一定的同步能力

**（4）曼彻斯特编码（Manchester Encoding）** ⭐⭐必考
- **从高到低跳变**表示1，**从低到高跳变**表示0
- **或者相反**（取决于约定）
- **特点**：
  - ✅ 每个比特中间都有跳变，便于同步
  - ✅ 自含时钟信号
  - ❌ 编码效率只有50%
  - ❌ 占用带宽是原始数据的2倍
- **应用**：以太网（10Mbps）

**（5）差分曼彻斯特编码** ⭐⭐
- **比特开始时有跳变**表示0，**无跳变**表示1
- **特点**：
  - ✅ 抗干扰能力强
  - ✅ 便于同步
  - ❌ 实现复杂
- **应用**：令牌环网

**编码方式对比**：

| 编码方式 | 同步能力 | 编码效率 | 抗干扰 | 复杂度 |
|---------|---------|---------|--------|--------|
| NRZ | ❌ 差 | 100% | ❌ 差 | 简单 |
| RZ | ✅ 好 | 50% | ✅ 较好 | 简单 |
| 曼彻斯特 | ✅ 好 | 50% | ✅ 好 | 中等 |
| 差分曼彻斯特 | ✅ 好 | 50% | ✅ 最好 | 复杂 |

#### 2. 数字数据调制为模拟信号

**（1）调幅（AM, Amplitude Modulation）**
- 改变载波的振幅来表示数字信号
- 实现简单，但抗干扰能力差

**（2）调频（FM, Frequency Modulation）**
- 改变载波的频率来表示数字信号
- 抗干扰能力强于AM

**（3）调相（PM, Phase Modulation）**
- 改变载波的相位来表示数字信号
- 抗干扰能力强

**（4）正交振幅调制（QAM）** ⭐⭐
- **同时改变振幅和相位**
- **优点**：频谱利用率高
- **应用**：高速Modem、WiFi、有线电视
- **例如**：16-QAM可以表示4位二进制数（2^4=16种状态）

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 香农定理的计算
2. ⭐⭐ 奈奎斯特定理
3. ⭐⭐ 曼彻斯特编码的特点
4. ⭐ 信噪比的计算
`        },
        {
          id: '2.2',
          title: '传输介质与物理层设备',
          content: `## 2.2 传输介质与物理层设备 ⭐⭐

### 一、传输介质的分类

**传输介质**：数据传输的物理通路，分为导引型和非导引型

| 分类 | 特点 | 示例 |
|------|------|------|
| **导引型** | 信号沿固体介质传播 | 双绞线、同轴电缆、光纤 |
| **非导引型** | 信号在自由空间传播 | 无线电波、微波、红外线 |

### 二、导引型传输介质 ⭐⭐

#### 1. 双绞线（Twisted Pair）

**结构**：两根绝缘铜线按一定密度绞合

**分类**：

| 类型 | 屏蔽 | 速度 | 距离 | 应用 |
|------|------|------|------|------|
| **UTP**（非屏蔽） | 无 | 10Mbps-10Gbps | 100m | 以太网、电话线 |
| **STP**（屏蔽） | 有金属屏蔽层 | 更高 | 100m | 工业环境、抗干扰要求高 |

**优点**：
- ✅ 成本低，易于安装
- ✅ 技术成熟，广泛应用
- ✅ 绞合减少电磁干扰

**缺点**：
- ❌ 传输距离短（100m）
- ❌ 易受外界干扰（UTP）
- ❌ 带宽有限

**类别标准**：
\`\`\`
Cat3：10Mbps（电话线）
Cat5：100Mbps（快速以太网）
Cat5e：1Gbps（千兆以太网）
Cat6：10Gbps（短距离）
Cat6a/Cat7：10Gbps（长距离）
\`\`\`

#### 2. 同轴电缆（Coaxial Cable）

**结构**：
\`\`\`
中心导体 → 绝缘层 → 屏蔽层 → 外护套
\`\`\`

**分类**：
- **基带同轴电缆**：50Ω，用于数字传输（早期以太网）
- **宽带同轴电缆**：75Ω，用于模拟传输（有线电视）

**优点**：
- ✅ 抗干扰能力强于双绞线
- ✅ 带宽较高
- ✅ 传输距离较远

**缺点**：
- ❌ 成本高
- ❌ 安装复杂
- ❌ 已被光纤取代

**应用**：有线电视（CATV）、早期以太网（10Base2、10Base5）

#### 3. 光纤（Optical Fiber）⭐⭐ 重点

**原理**：利用光的全反射原理传输光信号

**结构**：
\`\`\`
纤芯（Core） → 包层（Cladding） → 涂覆层（Coating）
\`\`\`

**分类**：

| 类型 | 纤芯直径 | 传输模式 | 距离 | 应用 |
|------|---------|---------|------|------|
| **多模光纤** | 50/62.5μm | 多个光路 | 2km以内 | 局域网、短距离 |
| **单模光纤** | 8-10μm | 单个光路 | 几十公里 | 长途通信、骨干网 |

**优点**：
- ✅ **带宽极高**：可达Tbps级别
- ✅ **传输距离远**：单模可达几十公里
- ✅ **抗干扰强**：不受电磁干扰
- ✅ **安全性高**：难以窃听
- ✅ **体积小、重量轻**

**缺点**：
- ❌ 成本高（光纤、光电转换器）
- ❌ 安装和维护复杂
- ❌ 需要专用设备（熔接机）

**工作原理**：
\`\`\`
全反射条件：入射角 > 临界角
光在纤芯和包层界面发生全反射，沿光纤传播
\`\`\`

**应用场景**：
- 长途干线网络
- 数据中心互联
- FTTH（光纤到户）
- 海底光缆

### 三、非导引型传输介质

#### 1. 无线电波

**特点**：
-  omnidirectional（全向传播）
- 穿透能力强
- 易受干扰

**应用**：
- AM/FM广播
- WiFi（2.4GHz、5GHz）
- 蓝牙
- 移动通信（4G/5G）

#### 2. 微波

**特点**：
-  directional（定向传播）
- 直线传播，需要视距
- 带宽高

**应用**：
- 微波中继通信
- 卫星通信
- 点对点无线链路

#### 3. 红外线和激光

**红外线**：
- 短距离通信
- 不能穿透障碍物
- 应用：遥控器、IrDA

**激光**：
- 高带宽
- 需要精确对准
- 应用：自由空间光通信

### 四、传输介质对比总结

| 介质 | 带宽 | 距离 | 抗干扰 | 成本 | 安装难度 |
|------|------|------|--------|------|----------|
| **双绞线** | 中 | 短（100m） | 中 | 低 | 简单 |
| **同轴电缆** | 中高 | 中 | 好 | 中 | 中等 |
| **多模光纤** | 高 | 中（2km） | 极好 | 高 | 复杂 |
| **单模光纤** | 极高 | 长（几十km） | 极好 | 很高 | 很复杂 |
| **无线电波** | 中 | 中 | 差 | 低 | 简单 |
| **微波** | 高 | 长 | 中 | 高 | 复杂 |

**选择建议**：
- 🏠 **家庭/办公室**：双绞线（Cat5e/Cat6）
- 🏢 **企业骨干网**：多模光纤
- 🌐 **长途通信**：单模光纤
- 📱 **移动接入**：无线电波（WiFi、5G）

### 五、物理层设备 ⭐

#### 1. 中继器（Repeater）

**功能**：
- 放大和再生信号
- 延长传输距离
- 工作在物理层

**特点**：
- ✅ 简单、成本低
- ❌ 不能隔离冲突域
- ❌ 不能过滤流量

**应用**：延伸网线长度（超过100m时）

#### 2. 集线器（Hub）

**功能**：
- 多端口中继器
- 将收到的信号广播到所有端口

**特点**：
- ✅ 价格便宜
- ❌ 所有端口在同一冲突域
- ❌ 带宽共享（10Mbps Hub，10个端口则每端口平均1Mbps）
- ❌ 半双工通信

**工作层次**：物理层

**与交换机的区别**：

| 对比项 | 集线器（Hub） | 交换机（Switch） |
|-------|--------------|------------------|
| **工作层次** | 物理层 | 数据链路层 |
| **数据传输** | 广播到所有端口 | 根据MAC地址转发 |
| **冲突域** | 所有端口同一冲突域 | 每个端口独立冲突域 |
| **带宽** | 共享带宽 | 独占带宽 |
| **通信方式** | 半双工 | 全双工 |
| **性能** | 低 | 高 |

**现状**：已基本被交换机取代

### 六、考研重点

**高频考点**：
1. ⭐⭐ 光纤的工作原理和分类
2. ⭐⭐ 双绞线的类别和标准
3. ⭐ 各种传输介质的特点和适用场景
4. ⭐ 集线器vs交换机的区别
5. ⭐ 中继器的作用
`        }
      ]
    },
    {
      id: 'ch3',
      number: 3,
      title: '数据链路层',
      score: '8-10分',
      importance: 'high',
      mindMap: `mindmap
  root((数据链路层))
    功能与组帧 ⭐
      三大功能
      组帧方法
    差错控制 ⭐⭐⭐
      检错编码
        奇偶校验
        CRC ⭐⭐⭐
      纠错编码
        海明码 ⭐⭐
    流量控制 ⭐⭐⭐
      停止-等待
      GBN ⭐⭐⭐
      SR ⭐⭐⭐
      滑动窗口
    介质访问控制 ⭐⭐⭐
      FDM/TDM/WDM
      CSMA ⭐⭐⭐
      CSMA/CD ⭐⭐⭐
      CSMA/CA
    局域网 ⭐⭐
      以太网
      MAC地址
      VLAN
    广域网
      PPP ⭐⭐
      HDLC
    链路层设备
      网桥
      交换机 ⭐⭐⭐`,
      sections: [
        {
          id: '3.1',
          title: '数据链路层功能与组帧',
          content: `## 3.1 数据链路层功能与组帧 ⭐⭐ 重点

### 一、数据链路层的三大功能 ⭐⭐⭐

#### 1. 封装成帧（Framing）

**定义**：将网络层的IP数据报加上首部和尾部，构成一个帧

**目的**：
- 界定数据的开始和结束
- 添加控制信息（地址、校验等）

**帧的结构**：
\`\`\`
| 首部 | 数据部分（网络层分组） | 尾部 |
\`\`\`

- **首部**：包含地址、控制信息
- **尾部**：通常包含差错检测码（如CRC）

#### 2. 透明传输（Transparent Transmission）

**定义**：无论数据部分出现什么比特组合，都能正确传输

**问题**：如果数据中出现与帧定界符相同的比特组合怎么办？

**解决方案**：
- 字符填充法：使用转义字符
- 零比特填充法：插入额外的0

**透明性**：数据链路层对上层透明，不关心数据内容

#### 3. 差错控制（Error Control）

**目的**：检测和纠正传输过程中的错误

**方法**：
- **检错编码**：奇偶校验、CRC循环冗余校验
- **纠错编码**：海明码
- **重传机制**：发现错误后请求重传

### 二、组帧方法详解 ⭐⭐ 必考

**组帧**：确定帧的边界，即帧从哪里开始、到哪里结束

#### 1. 字符计数法

**原理**：在帧头部使用一个计数字段标明帧内字符数

**示例**：
\`\`\`
| COUNT | DATA... |
|   5   | ABCDE   |
\`\`\`

**优点**：
- ✅ 实现简单

**缺点**：
- ❌ **致命缺陷**：如果COUNT字段出错，后续所有帧都无法正确分界
- ❌ 无法恢复同步

**应用**：早期系统，现已很少使用

#### 2. 字符填充的首尾定界符法

**原理**：使用特殊字符作为帧的开始和结束标志

**常用定界符**：
- SOH（Start Of Header）：帧开始
- EOT（End Of Transmission）：帧结束

**问题**：数据中出现SOH或EOT怎么办？

**解决**：使用转义字符ESC

**规则**：
\`\`\`
数据中的SOH → ESC + SOH
数据中的EOT → ESC + EOT
数据中的ESC → ESC + ESC
\`\`\`

**示例**：
\`\`\`
原始数据：A SOH B EOT C
填充后：  A ESC SOH B ESC EOT C
\`\`\`

**优点**：
- ✅ 可以处理任意长度的帧

**缺点**：
- ❌ 只适用于字符编码（如ASCII）
- ❌ 不适用于二进制数据
- ❌ 效率低（需要额外字节）

**应用**：面向字符的协议（如BISYNC）

#### 3. 零比特填充的首尾标志法 ⭐⭐⭐ 必考

**原理**：使用特定的比特模式作为帧定界符

**帧标志**：01111110（十六进制0x7E）

**问题**：数据中出现01111110怎么办？

**解决**：零比特填充（Bit Stuffing）

**发送端规则**：
\`\`\`
扫描数据，每遇到连续的5个1，就自动插入一个0
\`\`\`

**接收端规则**：
\`\`\`
扫描数据，每遇到连续的5个1，就删除后面的一个0
如果第6位是0，继续；如果第6位是1，可能是帧结束标志
\`\`\`

**示例**：
\`\`\`
原始数据：01111110  （数据中出现了帧标志）
填充后：  011111010  （在第5个1后插入0）

原始数据：0111110111110110
填充后：  011111001111100110
           ↑插入0    ↑插入0
\`\`\`

**优点**：
- ✅ 适用于任意比特组合
- ✅ 实现简单，硬件容易实现
- ✅ 广泛用于HDLC、PPP等协议

**缺点**：
- ❌ 数据长度不确定（可能增加）

**应用**：HDLC协议、PPP协议

**考研例题**：【2018统考】使用零比特填充法，数据为011111110，填充后的结果是什么？

**解**：
\`\`\`
原始数据：0 11111 110
           ↑连续5个1
填充后：  0 11111 0 110
                  ↑插入0

答案：0111110110
\`\`\`

#### 4. 违规编码法

**原理**：利用物理层编码的违规序列来标识帧边界

**适用条件**：只在某些特定编码中可用

**示例**：曼彻斯特编码

**正常编码**：
- 0：高→低跳变
- 1：低→高跳变

**违规编码**：
- 高→高：违规，用作帧开始
- 低→低：违规，用作帧结束

**优点**：
- ✅ 不需要额外的填充
- ✅ 效率高

**缺点**：
- ❌ 只适用于特定编码
- ❌ 通用性差

**应用**：IEEE 802.3（以太网）

### 三、四种组帧方法对比总结

| 方法 | 适用数据类型 | 透明度 | 效率 | 复杂度 | 应用 |
|------|------------|--------|------|--------|------|
| **字符计数** | 任意 | ❌ 差 | 高 | 简单 | 早期系统 |
| **字符填充** | 字符 | ✅ 好 | 中 | 中等 | BISYNC |
| **零比特填充** | 任意 | ✅ 好 | 中高 | 中等 | HDLC、PPP |
| **违规编码** | 任意 | ✅ 好 | 最高 | 复杂 | 以太网 |

**选择建议**：
- 📝 **面向字符**：字符填充法
- 🔢 **面向比特**：零比特填充法（最常用）
- ⚡ **高性能需求**：违规编码法

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ 零比特填充法的规则（必考计算题）
2. ⭐⭐ 四种组帧方法的比较
3. ⭐⭐ 数据链路层的三大功能
4. ⭐ 透明传输的概念
`        },
        {
          id: '3.2',
          title: '差错控制',
          content: `## 3.2 差错控制 ⭐⭐⭐必考

### 一、差错类型

1. **比特差错**：0变成1或1变成0
2. **分组丢失**：分组在传输过程中消失
3. **分组失序**：分组到达顺序与发送顺序不同
4. **分组重复**：同一分组多次到达

### 二、检错编码 ⭐⭐⭐

#### 1. 奇偶校验码

**原理**：添加一个校验位，使整个码字中1的个数为奇数（奇校验）或偶数（偶校验）

**特点**：
- ✅ 只能检测奇数个比特错误
- ❌ 不能检测偶数个比特错误
- ❌ 不能纠错

#### 2. CRC循环冗余校验 ⭐⭐⭐必考

**CRC（Cyclic Redundancy Check）**：循环冗余校验

**原理**：
\`\`\`
步骤1：选择生成多项式G(x)
步骤2：在数据后面添加r个0（r为G(x)的最高次幂）
步骤3：用模2除法除以G(x)，得到余数R
步骤4：将R附加到数据后面，形成CRC码
\`\`\`

**关键概念**：

**（1）生成多项式G(x)**
- 发送方和接收方事先约定
- 最高位和最低位必须为1
- 例如：G(x) = x⁴ + x + 1 → 二进制：10011

**（2）模2除法**
- **不借位**的除法
- 实际上就是**异或（XOR）**运算
- 规则：
  - 1 XOR 1 = 0
  - 0 XOR 0 = 0
  - 1 XOR 0 = 1
  - 0 XOR 1 = 1

**（3）检错能力**
- 可以检测所有单个比特错误
- 可以检测所有双比特错误
- 可以检测所有奇数个比特错误
- 可以检测长度≤r的突发错误（r为CRC位数）

**详细例题**：【2019统考】要发送的数据为1101011011，采用CRC的生成多项式是P(x)=x⁴+x+1，求应添加在数据后面的余数。

**解**：

**步骤1**：确定生成多项式的二进制表示
\`\`\`
P(x) = x⁴ + x + 1
     = 1·x⁴ + 0·x³ + 0·x² + 1·x¹ + 1·x⁰
     = 10011
\`\`\`

**步骤2**：在数据后面添加r个0
\`\`\`
r = 4（最高次幂）
原始数据：1101011011
添加4个0：11010110110000
\`\`\`

**步骤3**：进行模2除法
\`\`\`
              1100001010    ← 商（不重要）
            ______________
    10011 ) 11010110110000
            10011
            -----
             10011
             10011
             -----
              00001
              00000
              -----
               00010
               00000
               -----
                00101
                00000
                -----
                 01011
                 00000
                 -----
                  10110
                  10011
                  -----
                   01010
                   00000
                   -----
                    10100
                    10011
                    -----
                     01110   ← 余数
\`\`\`

**步骤4**：得出结果
\`\`\`
余数 R = 1110

最终发送的帧：1101011011 1110
              (数据)   (FCS)
\`\`\`

**验证过程**（接收方）：
\`\`\`
接收到的帧：11010110111110
用10011进行模2除法
如果余数为0，则无差错
如果余数不为0，则有差错，丢弃该帧
\`\`\`

**答案**：应添加的余数是**1110**

**重要提示**：
- ⚠️ 模2除法中，每一步都是异或运算，不借位
- ⚠️ 余数的位数必须比除数少1位
- ⚠️ 如果余数不足r位，前面补0

**特点**：
- ✅ 检错能力强
- ✅ 实现简单（硬件实现）
- ❌ 不能纠错

### 三、纠错编码 ⭐⭐

#### 海明码（Hamming Code）⭐⭐

**原理**：通过添加多个校验位，不仅能检错，还能纠错

**海明不等式**：
2^r ≥ k + r + 1
其中k是数据位数，r是校验位数

**特点**：
- ✅ 能检测并纠正1位错误
- ✅ 能检测2位错误
- ❌ 开销较大

**例题**：【2016统考】用海明码对8位信息进行编码，至少需要多少位校验位？

**解**：
k = 8
2^r ≥ 8 + r + 1

r=3: 2³=8 < 8+3+1=12 ✗
r=4: 2⁴=16 ≥ 8+4+1=13 ✓

所以至少需要4位校验位。

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ CRC的计算（必考）
2. ⭐⭐ 海明码的校验位数计算
3. ⭐⭐ 奇偶校验的特点
4. ⭐ 检错vs纠错的区别
`        },
        {
          id: '3.3',
          title: '流量控制与可靠传输',
          content: `## 3.3 流量控制与可靠传输 ⭐⭐⭐必考

### 一、流量控制 vs 可靠传输

**流量控制**：控制发送方的发送速率，使接收方来得及接收

**可靠传输**：确保数据无差错、不丢失、不重复、按序到达

### 二、停止-等待协议 ⭐⭐

**工作原理**：
1. 发送方发送一个分组后等待确认
2. 收到ACK才发送下一个分组
3. 超时重传

**特点**：
- ✅ 简单可靠
- ❌ 信道利用率低

**信道利用率**：
U = T_D / (T_D + RTT + T_A)
其中T_D是数据帧发送时延，RTT是往返时延，T_A是确认帧发送时延

### 三、后退N帧协议（GBN）⭐⭐⭐必考

**GBN（Go-Back-N）**：后退N帧协议

#### 1. 工作原理

**发送方**：
- 可以连续发送多个分组（窗口大小为N）
- 维护一个发送窗口
- 超时后重传所有已发送但未确认的分组

**接收方**：
- 只按序接收分组
- 失序分组直接丢弃
- 使用累积确认（ACK n表示n及之前的所有分组都已正确接收）

**工作流程**：
\`\`\`
发送方：发送0,1,2,3,4（窗口大小=5）
接收方：收到0,1,2，但3丢失
       → 只确认ACK 2
       → 收到4（失序），丢弃
发送方：超时，重传3,4
\`\`\`

#### 2. GBN的特点

**优点**：
- ✅ 提高了信道利用率（相比停止-等待）
- ✅ 实现相对简单
- ✅ 接收方无需缓存

**缺点**：
- ❌ 出错时重传开销大（批量重传）
- ❌ 浪费带宽（可能重传正确的分组）

#### 3. 窗口大小限制

**发送窗口大小**：W_T ≤ 2^n - 1

其中n为序号位数

**原因**：避免新旧窗口序号混淆

**示例**：
- n=3位序号（0-7）
- W_T ≤ 2^3 - 1 = 7
- 实际常用W_T = 4或5

### 四、选择重传协议（SR）⭐⭐⭐必考

**SR（Selective Repeat）**：选择重传协议

#### 1. 工作原理

**发送方**：
- 可以连续发送多个分组
- 维护发送窗口和定时器（每个分组独立定时器）
- 只重传出错或超时的分组

**接收方**：
- 可以接收失序分组
- 缓存失序分组
- 单独确认每个分组
- 当收到连续分组时，一起交付给上层

**工作流程**：
\`\`\`
发送方：发送0,1,2,3,4
接收方：收到0,1,2，但3丢失，收到4
       → 缓存4
       → 单独确认ACK 0, ACK 1, ACK 2, ACK 4
发送方：只重传3
接收方：收到3，与缓存的4一起交付
\`\`\`

#### 2. SR的特点

**优点**：
- ✅ 效率最高（只重传出错分组）
- ✅ 节省带宽
- ✅ 适合高误码率环境

**缺点**：
- ❌ 实现复杂
- ❌ 接收方需要较大缓存
- ❌ 需要为每个分组设置定时器

#### 3. 窗口大小限制

**约束条件**：
\`\`\`
W_T + W_R ≤ 2^n
W_T = W_R ≤ 2^(n-1)
\`\`\`

**原因**：避免接收方无法区分是新分组的序号还是旧分组的序号

**示例**：
- n=3位序号（0-7）
- W_T = W_R ≤ 2^(3-1) = 4
- 即发送窗口和接收窗口最大都为4

### 五、滑动窗口机制 ⭐⭐⭐

**核心思想**：通过窗口控制未确认分组的数量

**窗口的作用**：
- 流量控制：限制发送速率
- 可靠传输：确保数据不丢失
- 提高效率：允许流水线传输

### 六、三种协议详细对比 ⭐⭐⭐必考

| 对比维度 | 停止-等待 | GBN | SR |
|---------|----------|-----|----|
| **发送窗口** | 1 | >1 | >1 |
| **接收窗口** | 1 | 1 | >1 |
| **确认方式** | 逐个确认 | 累积确认 | 逐个确认 |
| **重传策略** | 超时重传 | 批量重传 | 单独重传 |
| **接收方缓存** | 不需要 | 不需要 | 需要 |
| **实现复杂度** | 简单 | 中等 | 复杂 |
| **信道利用率** | 低 | 中 | 高 |
| **适用场景** | 低延迟网络 | 低误码率 | 高误码率 |
| **定时器数量** | 1个 | 1个 | 多个（每分组） |
| **带宽浪费** | 无 | 较多 | 最少 |

**性能对比**：

| 指标 | 停止-等待 | GBN | SR |
|------|----------|-----|----|
| **吞吐量** | 最低 | 中等 | 最高 |
| **延迟** | 最高 | 中等 | 最低 |
| **缓冲区需求** | 最小 | 小 | 大 |
| **协议开销** | 最小 | 小 | 大 |

**选择建议**：
- 🐢 **低速链路、高延迟**：停止-等待（简单可靠）
- 🚀 **高速链路、低误码率**：GBN（平衡性能和复杂度）
- 🌐 **高误码率环境**：SR（效率最高）

### 七、考研重点

**高频考点**：
1. ⭐⭐⭐ GBN和SR的窗口大小计算
2. ⭐⭐⭐ 三种协议的比较（表格记忆）
3. ⭐⭐ 累积确认vs单独确认的区别
4. ⭐⭐ GBN的重传策略
5. ⭐ SR的接收方缓存机制
`        },
        {
          id: '3.4',
          title: '介质访问控制',
          content: `## 3.4 介质访问控制 ⭐⭐⭐必考

### 一、信道划分介质访问控制

#### 1. 频分复用（FDM）

**原理**：将可用带宽划分为多个子信道，每个用户占用一个子信道

**应用**：有线电视、无线电广播

#### 2. 时分复用（TDM）⭐⭐

**原理**：将时间划分为固定长度的时隙，每个用户占用一个时隙

**分类**：
- **静态TDM**：固定分配时隙，可能造成浪费
- **统计TDM**：动态分配时隙，效率更高

**应用**：电话系统、SONET/SDH

#### 3. 波分复用（WDM）

**原理**：光的频分复用，不同波长承载不同信号

**应用**：光纤通信

#### 4. 码分复用（CDM）

**原理**：每个用户使用不同的码序列，可以在同一时间同一频率上传输

**特点**：抗干扰能力强，保密性好

**应用**：CDMA移动通信

### 二、随机访问介质访问控制 ⭐⭐⭐

#### 1. ALOHA协议

**纯ALOHA**：随时发送，冲突后随机等待重传

**时隙ALOHA**：只能在时隙开始时发送

**最大吞吐量**：
- 纯ALOHA：1/(2e) ≈ 18.4%
- 时隙ALOHA：1/e ≈ 36.8%

#### 2. CSMA协议 ⭐⭐⭐

**CSMA（Carrier Sense Multiple Access）**：载波监听多点接入

**三种策略**：
- **1-坚持CSMA**：监听到空闲立即发送，冲突概率高
- **非坚持CSMA**：监听到忙则随机等待，延迟大
- **p-坚持CSMA**：以概率p发送，折中方案

#### 3. CSMA/CD ⭐⭐⭐必考

**CSMA/CD（Collision Detection）**：载波监听多点接入/碰撞检测

**工作流程**：
\`\`\`
1. 先听后发 → 发送前先监听信道是否空闲
2. 边听边发 → 发送过程中持续监听
3. 冲突停发 → 检测到冲突立即停止发送
4. 随机重发 → 等待随机时间后重新尝试
\`\`\`

**最小帧长**：⭐⭐⭐必考

**公式**：
\`\`\`
最小帧长 = 2 × 传播时延 × 数据传输速率
        = 2 × (L / v) × R
\`\`\`

其中：
- L：电缆长度（m）
- v：信号传播速度（m/s），通常为2×10⁸ m/s
- R：数据传输速率（bps）

**物理意义**：
- 确保发送方在发送完整个帧之前能够检测到冲突
- 如果帧太短，可能在检测到冲突前就已经发送完毕
- 这是以太网的**核心设计原则**

**详细计算例题**：【2020统考】以太网的最小帧长为64字节，若数据传输速率为10Mbps，则最大电缆长度约为多少？（信号传播速度为2×10⁸m/s）

**解**：

**步骤1**：将最小帧长转换为比特
\`\`\`
最小帧长 = 64字节 = 64 × 8 = 512 bit
\`\`\`

**步骤2**：代入公式
\`\`\`
512 = 2 × (L / 2×10⁸) × 10×10⁶
\`\`\`

**步骤3**：求解L
\`\`\`
512 = 2 × L × 10×10⁶ / 2×10⁸
512 = L × 10⁷ / 10⁸
512 = L / 10
L = 512 × 10 = 5120 m = 5.12 km
\`\`\`

**步骤4**：考虑实际情况
- 理论最大值：5.12 km
- 实际规定：2500 m（考虑安全系数和信号衰减）

**答案**：最大电缆长度约为 **5.12 km**（理论值），实际规定为 **2500 m**。

**常见变式题**：

| 已知条件 | 求解目标 | 公式变形 |
|---------|---------|----------|
| 最小帧长、速率、长度 | 验证是否满足要求 | 检查：帧长 ≥ 2×(L/v)×R |
| 速率、长度、最小帧长 | 求传播速度 | v = 2×L×R / 帧长 |
| 最小帧长、传播速度、长度 | 求最小速率 | R = 帧长×v / (2×L) |

**记忆技巧**：
- "**2倍往返**"：2 × 传播时延 = 往返时延
- "**帧要够长**"：帧长必须足够让冲突信号返回

#### 4. CSMA/CA ⭐⭐

**CSMA/CA（Collision Avoidance）**：载波监听多点接入/碰撞避免

**应用场景**：无线局域网（802.11）

**为什么不用CSMA/CD？**
- 无线信号衰减大，难以检测冲突
- 存在隐藏终端问题

**工作机制**：
- 使用RTS/CTS握手
- 使用NAV（网络分配向量）

### 三、考研重点

**高频考点**：
1. ⭐⭐⭐ CSMA/CD的最小帧长计算
2. ⭐⭐⭐ CSMA的三种策略比较
3. ⭐⭐ TDM的原理和应用
4. ⭐⭐ CSMA/CA的应用场景
5. ⭐ 各种复用技术的比较
`        }
      ]
    },
    {
      id: 'ch4',
      number: 4,
      title: '网络层',
      score: '10-12分',
      importance: 'high',
      mindMap: `mindmap
  root((网络层))
    功能
      路由与转发
      异构互联
    路由算法 ⭐⭐⭐
      RIP ⭐⭐⭐
      OSPF ⭐⭐⭐
    IPv4 ⭐⭐⭐
      分组格式
      IP地址
      NAT ⭐⭐⭐
      子网划分 ⭐⭐⭐
      ARP ⭐⭐⭐
      DHCP
      ICMP
    IPv6
      特点
      地址表示
    路由协议
      RIP
      OSPF ⭐⭐⭐
      BGP
    网络层设备
      路由器 ⭐⭐⭐
      三层交换机`,
      sections: [
        {
          id: '4.1',
          title: '网络层功能',
          content: `## 4.1 网络层功能

### 一、网络层的两大功能

1. **路由选择**：确定分组从源到目的的最佳路径
2. **分组转发**：将分组从输入端口转移到输出端口

### 二、异构网络互联

网络层屏蔽了各种物理网络的差异，实现了异构网络的互联。

### 三、考研重点

**高频考点**：
1. ⭐⭐ 路由选择vs分组转发
2. ⭐ 网络层的作用
`        },
        {
          id: '4.1b',
          title: '路由器结构与工作原理',
          content: `## 4.1b 路由器结构与工作原理 ⭐⭐⭐必考

### 一、路由器的基本组成

路由器是网络层的核心设备，负责在不同网络之间转发数据包。

**路由器的四大组成部分**：

| 组成部分 | 功能 | 类比 |
|---------|------|------|
| **输入端口** | 接收来自链路的帧，提取IP数据报 | 邮局收件窗口 |
| **交换结构** | 将数据从输入端口转移到输出端口 | 邮局内部分拣系统 |
| **输出端口** | 将数据封装成帧，发送到链路 | 邮局发件窗口 |
| **路由处理器** | 执行路由协议，维护路由表 | 邮局调度中心 |

### 二、输入端口（Input Ports）⭐⭐

#### 1. 主要功能

**（1）物理层处理**
- 接收比特流
- 时钟同步
- 信号解码

**（2）数据链路层处理**
- 帧定界
- 差错检测
- 提取IP数据报

**（3）查找与转发**
- 根据目的IP地址查找转发表
- 确定输出端口
- 排队等待交换

#### 2. 输入端口的工作流程

\`\`\`
步骤1：接收帧 → 物理层接收比特流
       ↓
步骤2：帧处理 → 去除帧头和帧尾，检查校验和
       ↓
步骤3：提取IP数据报 → 交给网络层处理
       ↓
步骤4：查找转发表 → 根据目的IP查找输出端口
       ↓
步骤5：排队 → 进入输入队列，等待通过交换结构
\`\`\`

#### 3. 输入队列的作用

**为什么需要输入队列？**
- 交换结构的速率可能低于输入端口的总速率
- 多个输入端口可能同时向同一输出端口发送数据
- 缓冲突发流量

**问题**：如果队列过长，可能导致丢包（称为输入阻塞）

### 三、交换结构（Switching Fabric）⭐⭐⭐

**作用**：将数据从输入端口转移到输出端口

**三种交换方式**：

#### 1. 经内存交换（Memory-based）

**工作原理**：
\`\`\`
输入端口 → 将数据写入共享内存 → CPU读取 → 写入输出端口
\`\`\`

**特点**：
- ✅ 实现简单
- ❌ 受限于内存带宽
- ❌ CPU成为瓶颈
- 📊 传统路由器使用

#### 2. 经总线交换（Bus-based）

**工作原理**：
\`\`\`
输入端口 → 将数据放到总线上 → 输出端口从总线读取
\`\`\`

**特点**：
- ✅ 比内存交换快
- ❌ 总线带宽限制（只能一个端口同时传输）
- ❌ 冲突时需要重传
- 📊 中低端路由器使用

#### 3. 经互联网络交换（Crossbar/Interconnection Network）⭐⭐⭐

**工作原理**：
\`\`\`
输入端口 → 通过交叉开关矩阵 → 直接连接到输出端口
\`\`\`

**特点**：
- ✅ **并行传输**：多个输入可以同时向不同输出发送
- ✅ **高吞吐量**：无总线争用
- ❌ 实现复杂，成本高
- 📊 高端路由器使用

**对比总结**：

| 交换方式 | 吞吐量 | 并行性 | 复杂度 | 成本 | 应用场景 |
|---------|--------|--------|--------|------|----------|
| **内存交换** | 低 | 无 | 简单 | 低 | 传统路由器 |
| **总线交换** | 中 | 无 | 中等 | 中 | 中低端路由器 |
| **交叉开关** | 高 | 有 | 复杂 | 高 | 高端路由器 |

### 四、输出端口（Output Ports）⭐⭐

#### 1. 主要功能

**（1）缓存管理**
- 存储等待发送的数据报
- 处理拥塞（当到达速率 > 发送速率时）

**（2）数据链路层封装**
- 添加帧头（MAC地址等）
- 添加帧尾（校验和）

**（3）物理层发送**
- 将比特流发送到链路上

#### 2. 输出队列的作用

**为什么需要输出队列？**
- 从交换结构到达的速率可能超过链路发送速率
- 多个输入端口可能同时向同一输出端口发送数据

**问题**：如果队列过长，可能导致丢包（称为输出阻塞）

#### 3. 排队规则

**常见排队策略**：
- **FIFO**（先进先出）：最简单，但不区分优先级
- **优先级排队**：高优先级数据优先发送
- **公平排队**：保证每个流的公平性

### 五、路由处理器（Routing Processor）⭐⭐⭐

**作用**：路由器的“大脑”，负责控制平面功能

#### 1. 主要功能

**（1）运行路由协议**
- RIP、OSPF、BGP等
- 与邻居路由器交换路由信息

**（2）维护路由表**
- 存储到所有目的网络的最佳路径
- 定期更新路由表

**（3）计算转发表**
- 根据路由表生成转发表
- 下发给各个输入端口

**（4）管理功能**
- SNMP监控
- 配置管理
- 故障诊断

#### 2. 路由表 vs 转发表

| 对比项 | 路由表 | 转发表 |
|-------|--------|--------|
| **用途** | 路由选择（控制平面） | 分组转发（数据平面） |
| **内容** | 完整的路由信息 | 简化的转发信息 |
| **位置** | 路由处理器 | 输入端口 |
| **更新频率** | 较低（秒级） | 较高（毫秒级） |
| **大小** | 较大 | 较小（优化后） |

**关系**：
\`\`\`
路由表（路由处理器） → 计算 → 转发表（输入端口）
\`\`\`

### 六、路由器工作流程总结

**完整的数据转发过程**：

\`\`\`
1. 【输入端口】接收帧，提取IP数据报
   ↓
2. 【输入端口】查找转发表，确定输出端口
   ↓
3. 【交换结构】将数据从输入转移到输出
   ↓
4. 【输出端口】缓存数据，等待发送
   ↓
5. 【输出端口】封装成帧，发送到链路
   ↓
6. 【路由处理器】后台运行路由协议，更新路由表
\`\`\`

### 七、性能指标

**关键性能参数**：

| 指标 | 说明 | 典型值 |
|------|------|--------|
| **吞吐量** | 每秒能转发的数据量 | Gbps级别 |
| **延迟** | 数据通过路由器的时间 | 微秒级 |
| **丢包率** | 队列满时丢弃的概率 | < 0.1% |
| **背板带宽** | 交换结构的总带宽 | Tbps级别 |

### 八、考研重点

**高频考点**：
1. ⭐⭐⭐ 路由器的四大组成部分及功能
2. ⭐⭐⭐ 三种交换方式的比较（必考）
3. ⭐⭐ 输入端口的工作流程
4. ⭐⭐ 路由表vs转发表的区别
5. ⭐ 输入队列和输出队列的作用
`        },
        {
          id: '4.2',
          title: '路由算法',
          content: `## 4.2 路由算法 ⭐⭐⭐必考

### 一、静态路由 vs 动态路由

**静态路由**：管理员手动配置，适合小型网络

**动态路由**：路由器自动学习和更新路由表，适合大型网络

### 二、距离-向量路由算法（RIP）⭐⭐⭐必考

**RIP（Routing Information Protocol）**：路由信息协议

#### 1. 工作原理

**核心思想**：每个路由器只与直接相邻的路由器交换信息

**工作流程**：
\`\`\`
1. 初始化：路由器将自己的直连网络加入路由表，距离为1
2. 定期更新：每隔30秒向邻居发送整个路由表
3. 接收更新：收到邻居的路由表后，更新自己的路由表
4. 使用Bellman-Ford算法计算最短路径
\`\`\`

**距离定义**：
- 跳数（Hop Count）：经过的路由器数量
- 直连网络：距离=1
- 最大距离：15跳（16表示不可达）

#### 2. RIP的特点

**优点**：
- ✅ 实现简单，配置容易
- ✅ 开销小，适合小型网络
- ✅ 广泛支持

**缺点**：
- ❌ **收敛慢**：好消息传得快，坏消息传得慢
- ❌ **最大跳数限制**：只能用于直径不超过15跳的网络
- ❌ **路由环路问题**：可能产生计数到无穷大问题
- ❌ **带宽浪费**：定期发送整个路由表

#### 3. 路由环路问题及解决

**问题**：当网络出现故障时，可能导致路由环路

**解决方案**：

**（1）水平分割（Split Horizon）**
- 不从某个接口学到的路由再从该接口发回去

**（2）毒性逆转（Poison Reverse）**
- 从某接口学到的路由，从该接口发出时设置距离为16（不可达）

**（3）触发更新（Triggered Update）**
- 检测到链路故障时立即发送更新，而不是等待30秒

**（4）抑制计时器（Hold-down Timer）**
- 收到不可达信息后，一段时间内不接受关于该路由的其他信息

### 三、链路状态路由算法（OSPF）⭐⭐⭐必考

**OSPF（Open Shortest Path First）**：开放最短路径优先

#### 1. 工作原理

**核心思想**：每个路由器都拥有整个网络的拓扑图

**工作流程**：
\`\`\`
步骤1：发现邻居
  - 发送Hello报文，建立邻居关系
  
步骤2：交换链路状态信息
  - 构建链路状态通告LSA（Link State Advertisement）
  
步骤3：泛洪LSA
  - 将LSA发送给所有邻居（除了来源）
  - 确保所有路由器都有相同的链路状态数据库LSDB
  
步骤4：计算最短路径
  - 使用Dijkstra算法计算到所有目的地的最短路径
  - 构建最短路径树SPT（Shortest Path Tree）
  
步骤5：生成路由表
  - 根据SPT生成路由表
\`\`\`

#### 2. OSPF的特点

**优点**：
- ✅ **收敛快**：链路变化时立即更新
- ✅ **无路由环路**：基于全局拓扑计算
- ✅ **支持大型网络**：无跳数限制
- ✅ **度量灵活**：可以使用带宽、延迟等作为代价
- ✅ **区域划分**：支持分层结构，减小开销
- ✅ **认证机制**：支持报文认证，提高安全性

**缺点**：
- ❌ **实现复杂**：算法复杂度高
- ❌ **开销较大**：需要维护LSDB
- ❌ **占用内存**：存储整个网络拓扑

#### 3. OSPF的区域概念

**为什么需要区域？**
- 减小LSDB规模
- 减小路由表规模
- 限制LSA泛洪范围

**区域类型**：
- **骨干区域（Area 0）**：必须存在，连接其他区域
- **标准区域**：普通区域
- **末节区域（Stub Area）**：不接收外部路由

### 四、RIP vs OSPF 详细对比 ⭐⭐⭐必考

| 对比项 | RIP | OSPF |
|-------|-----|------|
| **算法类型** | 距离-向量算法 | 链路状态算法 |
| **理论基础** | Bellman-Ford算法 | Dijkstra算法 |
| **度量标准** | 跳数（最大15） | 代价（Cost），可基于带宽、延迟等 |
| **收敛速度** | 慢（30秒周期更新） | 快（触发更新） |
| **适用范围** | 小型网络（≤15跳） | 大型网络（无限制） |
| **报文交换内容** | 整个路由表 | 链路状态变化（LSA） |
| **更新方式** | 定期更新（30秒） | 触发更新 + 定期刷新（30分钟） |
| **路由环路** | 可能产生，需特殊机制避免 | 不会产生 |
| **资源消耗** | 低 | 较高（CPU、内存） |
| **配置复杂度** | 简单 | 复杂 |
| **安全性** | 弱（RIPv1无认证） | 强（支持MD5认证） |
| **VLSM支持** | RIPv1不支持，RIPv2支持 | 完全支持 |
| **区域划分** | 不支持 | 支持（减小开销） |
| **负载均衡** | 支持等价多路径 | 支持等价多路径 |

**典型应用场景**：
- **RIP**：小型企业网、校园网、分支机构
- **OSPF**：大型企业网、运营商网络、数据中心

### 五、两种算法的核心区别

#### 1. 信息传递方式

**RIP（距离-向量）**：
- 只知道“到目的地有多远，下一跳是谁”
- 不知道完整的网络拓扑
- 类似于“问路”，只知道方向和距离

**OSPF（链路状态）**：
- 知道整个网络的拓扑结构
- 可以独立计算到任何目的地的最短路径
- 类似于“有地图”，可以自己规划路线

#### 2. 收敛过程

**RIP收敛**：
\`\`\`
时间线：
t=0s:   链路故障
t=30s:  第一次更新（可能还是旧信息）
t=60s:  第二次更新
t=90s:  第三次更新
...
可能需要几分钟才能完全收敛
\`\`\`

**OSPF收敛**：
\`\`\`
时间线：
t=0s:   链路故障
t=0.1s: 立即发送LSA
t=0.2s: LSA泛洪完成
t=0.3s: 重新计算最短路径
t=0.4s: 收敛完成
通常几秒内完成收敛
\`\`\`

### 六、考研重点

**高频考点**：
1. ⭐⭐⭐ RIP和OSPF的比较
2. ⭐⭐ Bellman-Ford算法
3. ⭐⭐ Dijkstra算法
4. ⭐⭐ 路由环路问题
`        },
        {
          id: '4.3',
          title: 'IPv4',
          content: `## 4.3 IPv4 ⭐⭐⭐必考

### 一、IPv4分组格式 ⭐⭐⭐

**首部字段**：
- **版本**（4位）：IPv4为0100
- **首部长度**（4位）：单位是4字节，最小值为5（20字节）
- **服务类型**（8位）：优先级和服务类型
- **总长度**（16位）：首部+数据，最大65535字节
- **标识**（16位）：用于分片重组
- **标志**（3位）：DF（不分片）、MF（更多分片）
- **片偏移**（13位）：分片在原分组中的位置
- **生存时间TTL**（8位）：每经过一个路由器减1
- **协议**（8位）：上层协议（TCP=6, UDP=17）
- **首部校验和**（16位）：只校验首部
- **源IP地址**（32位）
- **目的IP地址**（32位）

**例题**：【2017统考】IPv4首部的最小长度为多少字节？

**答**：20字节（首部长度字段值为5，5×4=20字节）

### 二、IP数据报分片与重组 ⭐⭐⭐必考

**为什么需要分片？**
- 不同网络的MTU（最大传输单元）不同
- 以太网MTU=1500字节，PPP MTU可能更小
- 当IP数据报长度超过链路MTU时，必须分片

#### 1. 分片相关字段

**标识（Identification）**：16位
- 同一原始数据报的所有分片具有相同的标识
- 用于重组时识别哪些分片属于同一个数据报

**标志（Flags）**：3位
- **保留位**：必须为0
- **DF（Don't Fragment）**：不分片标志
  - DF=1：不允许分片，如果超过MTU则丢弃并返回ICMP差错报文
  - DF=0：允许分片
- **MF（More Fragments）**：更多分片标志
  - MF=1：后面还有分片
  - MF=0：这是最后一个分片

**片偏移（Fragment Offset）**：13位
- 表示该分片在原始数据报中的位置
- **单位**：8字节
- 例如：片偏移=100，表示该分片从第800字节（100×8）开始

#### 2. 分片过程

**分片规则**：
\`\`\`
1. 除最后一片外，其他分片的数据部分长度必须是8字节的整数倍
2. 每个分片都包含完整的IP首部（部分字段需要修改）
3. 所有分片的标识字段相同
4. 片偏移按8字节为单位计算
\`\`\`

**分片示例**：

假设原始IP数据报总长度=4000字节（首部20字节+数据3980字节），经过MTU=1500字节的网络：

\`\`\`
原始数据报：
| IP首部(20) | 数据(3980) |
总长度 = 4000字节

分片后：

分片1：
| IP首部(20) | 数据(1480) |
总长度 = 1500
标识 = X, MF=1, 片偏移=0

分片2：
| IP首部(20) | 数据(1480) |
总长度 = 1500
标识 = X, MF=1, 片偏移=185 (1480/8)

分片3：
| IP首部(20) | 数据(1020) |
总长度 = 1040
标识 = X, MF=0, 片偏移=370 (2960/8)
\`\`\`

**计算验证**：
- 分片1数据：0 ~ 1479（1480字节）
- 分片2数据：1480 ~ 2959（1480字节）
- 分片3数据：2960 ~ 3979（1020字节）
- 总数据：1480 + 1480 + 1020 = 3980字节 ✓

#### 3. 重组过程

**重组位置**：目的主机（不在中间路由器重组）

**重组步骤**：
\`\`\`
1. 根据标识字段，将属于同一原始数据报的分片归类
2. 根据片偏移字段，确定每个分片的位置
3. 等待所有分片到达（MF=0的分片到达且所有前面的分片都已收到）
4. 按片偏移顺序拼接数据部分
5. 去除各分片的IP首部，恢复原始数据报
\`\`\`

**重组超时**：
- 如果一定时间内未收到所有分片，则丢弃已收到的分片
- 通常超时时间为30-60秒

#### 4. 分片的优缺点

**优点**：
- ✅ 适应不同MTU的网络
- ✅ 提高网络灵活性

**缺点**：
- ❌ 增加路由器负担
- ❌ 任何一个分片丢失，整个数据报都要重传
- ❌ 容易被利用进行攻击（如Teardrop攻击）

**现代网络的改进**：
- **路径MTU发现（PMTUD）**：发送方探测路径上的最小MTU，避免分片
- **IPv6**：只在源主机分片，中间路由器不分片

**例题**：【2016统考】一个IP数据报总长度为4000字节（固定首部长度20字节）。现经过一个物理网络传送，该物理网络的最大传送单元MTU为1500字节。请问应当分为几个分片？各分片的片偏移是多少？

**解**：

**步骤1**：计算数据部分长度
数据长度 = 4000 - 20 = 3980字节

**步骤2**：计算分片数量
每个分片最大数据长度 = 1500 - 20 = 1480字节
分片数 = ⌈3980 / 1480⌉ = 3个分片

**步骤3**：计算各分片的片偏移
- 分片1：片偏移 = 0 / 8 = 0
- 分片2：片偏移 = 1480 / 8 = 185
- 分片3：片偏移 = 2960 / 8 = 370

**答案**：分为3个分片，片偏移分别为0、185、370

### 三、IP地址分类 ⭐⭐

**A类地址**：0开头，1.0.0.0~126.255.255.255
- 网络号：8位，主机号：24位
- 适合大型网络

**B类地址**：10开头，128.0.0.0~191.255.255.255
- 网络号：16位，主机号：16位
- 适合中型网络

**C类地址**：110开头，192.0.0.0~223.255.255.255
- 网络号：24位，主机号：8位
- 适合小型网络

**D类地址**：1110开头，224.0.0.0~239.255.255.255（组播）

**E类地址**：1111开头，240.0.0.0~255.255.255.255（保留）

### 三、NAT技术 ⭐⭐⭐必考

**NAT（Network Address Translation）**：网络地址转换

**作用**：
- 解决IPv4地址不足问题
- 隐藏内部网络结构，提高安全性

**工作原理**：
- 内网使用私有地址（10.x.x.x、172.16-31.x.x、192.168.x.x）
- NAT路由器维护地址转换表
- 出站时替换源IP，入站时替换目的IP

**类型**：
- **静态NAT**：一对一映射
- **动态NAT**：多对多映射
- **NAPT**：多对一映射（最常用）

### 四、子网划分与CIDR ⭐⭐⭐必考

**子网划分**：从主机号借用若干位作为子网号

**子网掩码**：网络号和子网号对应位为1，主机号对应位为0

**例题**：【2019统考】某公司申请到一个C类IP地址，但要连接6个子公司，最大的一个子公司有26台计算机，每个子公司在一个网段中，则子网掩码应设为多少？

**解**：
最大子公司26台主机，需要至少5位主机位（2^5-2=30>=26）
C类地址默认24位网络位，借用3位作为子网位（2^3=8>=6个子网）
所以子网掩码为24+3=27位，即255.255.255.224

**CIDR（无分类编址）**：
- 消除传统的A、B、C类划分
- 使用斜线记法：192.168.1.0/24
- 支持路由聚合，减小路由表规模

### 五、ARP协议 ⭐⭐⭐必考

**ARP（Address Resolution Protocol）**：地址解析协议

**作用**：根据IP地址获取MAC地址

**工作原理**：
1. 主机A发送ARP请求广播："谁是192.168.1.2？"
2. 主机B收到后回复ARP响应："我是，我的MAC是XX:XX:XX"
3. 主机A缓存ARP表项

**特点**：
- 工作在数据链路层和网络层之间
- ARP表有老化时间（通常20分钟）

### 六、DHCP协议 ⭐⭐

**DHCP（Dynamic Host Configuration Protocol）**：动态主机配置协议

**作用**：自动分配IP地址

**工作流程（DORA）**：
1. **Discover**：客户端广播发现DHCP服务器
2. **Offer**：服务器提供IP地址
3. **Request**：客户端请求使用该IP
4. **Acknowledge**：服务器确认分配

### 七、ICMP协议 ⭐⭐

**ICMP（Internet Control Message Protocol）**：网际控制报文协议

**应用**：
- **ping**：测试连通性（使用ICMP回送请求和回答）
- **traceroute**：追踪路径（利用TTL超时）

### 八、考研重点

**高频考点**：
1. ⭐⭐⭐ IPv4首部格式和各字段含义
2. ⭐⭐⭐ 子网划分和CIDR计算
3. ⭐⭐⭐ NAT的工作原理
4. ⭐⭐⭐ ARP的工作过程
5. ⭐⭐ DHCP的DORA流程
6. ⭐⭐ ICMP的应用
`        },
        {
          id: '4.4',
          title: 'IPv6',
          content: `## 4.4 IPv6

### 一、IPv6的特点

1. **更大的地址空间**：128位地址
2. **简化的首部格式**：固定40字节
3. **更好的安全性**：内置IPSec支持
4. **即插即用**：支持自动配置
5. **更好的QoS支持**

### 二、IPv6地址表示

**冒号十六进制表示法**：
2001:0DB8:0000:0000:0000:0000:0000:0001

**压缩表示**：
2001:DB8::1

**规则**：
- 每组前面的0可以省略
- 连续的全0组可以用::代替（只能用一次）

### 三、IPv4向IPv6过渡

**双栈策略**：同时运行IPv4和IPv6

**隧道技术**：将IPv6分组封装在IPv4分组中传输

**翻译技术**：在IPv4和IPv6之间进行协议转换

### 四、考研重点

**高频考点**：
1. ⭐⭐ IPv6的地址表示
2. ⭐⭐ IPv6的特点
3. ⭐ 过渡技术
`        }
      ]
    },
    {
      id: 'ch5',
      number: 5,
      title: '传输层',
      score: '8-10分',
      importance: 'high',
      mindMap: `mindmap
  root((传输层))
    传输层服务
      进程通信
      端口号 ⭐⭐
      复用分用
    UDP ⭐⭐
      特点
      首部格式
      应用
    TCP ⭐⭐⭐
      特点
      连接管理 ⭐⭐⭐
        三次握手
        四次挥手
      可靠传输
      流量控制 ⭐⭐⭐
      拥塞控制 ⭐⭐⭐
        慢开始
        拥塞避免
        快重传
        快恢复`,
      sections: [
        {
          id: '5.1',
          title: '传输层服务',
          content: `## 5.1 传输层服务

### 一、传输层的核心功能

**进程间的逻辑通信**：传输层为运行在不同主机上的应用进程提供直接的逻辑通信服务。

### 二、端口号 ⭐⭐

**端口号的作用**：标识主机中的应用进程

**端口号范围**：0~65535（16位）

**分类**：
- **熟知端口号**：0~1023（HTTP: 80, FTP: 21, DNS: 53）
- **登记端口号**：1024~49151
- **短暂端口号**：49152~65535

### 三、复用与分用

**复用**：发送方的不同应用进程都可以使用同一个传输层协议传送数据

**分用**：接收方的传输层在剥去报文首部后，能够把这些数据正确交付到目的应用进程

### 四、考研重点

**高频考点**：
1. ⭐⭐ 端口号的作用和分类
2. ⭐⭐ 复用与分用的概念
3. ⭐ 传输层与网络层的区别
`        },
        {
          id: '5.2',
          title: 'UDP',
          content: `## 5.2 UDP ⭐⭐

### 一、UDP的特点

1. **无连接**：发送数据前不需要建立连接
2. **不可靠**：不保证数据到达，不重传
3. **面向报文**：应用层给多大就发多大
4. **支持一对一、一对多**
5. **首部开销小**：只有8字节

### 二、UDP首部格式

**4个字段**（各2字节）：
- 源端口
- 目的端口
- 长度
- 校验和

### 三、UDP的应用场景

- 实时应用：IP电话、视频会议
- 简单请求-响应：DNS、DHCP
- 广播和多播应用

### 四、考研重点

**高频考点**：
1. ⭐⭐ UDP的特点
2. ⭐⭐ UDP首部格式
3. ⭐ UDP vs TCP的比较
`        },
        {
          id: '5.3',
          title: 'TCP',
          content: `## 5.3 TCP ⭐⭐⭐必考

### 一、TCP的特点

1. **面向连接**：传输前需要建立连接
2. **可靠传输**：无差错、不丢失、不重复、按序到达
3. **面向字节流**：不保留消息边界
4. **全双工通信**：双方都可以发送和接收
5. **点对点通信**：一对一

### 二、TCP报文段首部格式 ⭐⭐⭐必考

**TCP首部结构**（最小20字节，最大60字节）：

| 字段 | 长度 | 说明 |
|------|------|------|
| **源端口** | 16位 | 发送方端口号 |
| **目的端口** | 16位 | 接收方端口号 |
| **序号（seq）** | 32位 | 本报文段所发送的数据的第一个字节的序号 |
| **确认号（ack）** | 32位 | 期望收到对方下一个报文段的第一个数据字节的序号 |
| **首部长度** | 4位 | TCP首部长度，单位是4字节，最小值为5（20字节） |
| **保留** | 6位 | 保留为今后使用，目前置0 |
| **控制位** | 6位 | URG、ACK、PSH、RST、SYN、FIN |
| **窗口** | 16位 | 接收窗口大小，用于流量控制 |
| **校验和** | 16位 | 检验首部和数据，发送方计算，接收方验证 |
| **紧急指针** | 16位 | 指出本报文段中紧急数据的字节数 |
| **选项** | 可变 | 最长40字节，如MSS、窗口扩大因子等 |

**重要字段详解**：

#### 1. 序号和确认号

**序号（Sequence Number）**：
- 表示本报文段所发送数据的第一个字节的序号
- TCP连接中传送的字节流中的每个字节都按顺序编号
- 初始序号ISN（Initial Sequence Number）在建立连接时随机生成

**确认号（Acknowledgment Number）**：
- 期望收到对方下一个报文段的第一个数据字节的序号
- 例如：确认号N表示到序号N-1为止的所有数据都已正确收到
- 只有当ACK=1时，确认号字段才有效

**例题**：主机A向主机B发送一个TCP报文段，其序号为501，携带200字节的数据。请问：
1. 该报文段最后一个字节的序号是多少？
2. 主机B收到后，返回的确认号应该是多少？

**解**：
1. 最后一个字节序号 = 501 + 200 - 1 = 700
2. 确认号 = 701（期望收到下一个字节的序号）

#### 2. 控制位（6个标志位）

| 标志位 | 名称 | 含义 |
|--------|------|------|
| **URG** | 紧急 | URG=1时，紧急指针有效，表明有紧急数据 |
| **ACK** | 确认 | ACK=1时，确认号字段有效 |
| **PSH** | 推送 | PSH=1时，接收方应尽快将数据交给应用层 |
| **RST** | 复位 | RST=1时，表明TCP连接出现严重错误，必须释放连接 |
| **SYN** | 同步 | SYN=1时，表明这是一个连接请求或连接接受报文 |
| **FIN** | 终止 | FIN=1时，表明此报文段的发送方的数据已发送完毕，要求释放连接 |

**典型组合**：
- **SYN=1, ACK=0**：连接请求
- **SYN=1, ACK=1**：连接接受
- **FIN=1, ACK=1**：连接释放
- **ACK=1**：普通确认

#### 3. 窗口字段

**作用**：用于流量控制，告诉对方自己的接收缓冲区大小

**单位**：字节

**工作机制**：
- 接收方根据当前缓冲区剩余空间设置窗口值
- 发送方根据窗口值调整发送速率
- 窗口=0时，发送方停止发送（但会定期发送探测报文）

#### 4. 选项字段

**常见选项**：

**（1）最大报文段长度MSS（Maximum Segment Size）**
- 仅在SYN报文中出现
- 告诉对方本端能够接收的最大报文段长度
- MSS = MTU - IP首部(20) - TCP首部(20)
- 以太网中，MTU=1500，所以MSS通常为1460

**（2）窗口扩大因子**
- 解决16位窗口字段不够用的问题
- 实际窗口 = 窗口字段值 × 2^扩大因子

**（3）时间戳**
- 用于计算RTT和防止旧报文段混淆
- 每个报文段都携带时间戳

### 三、TCP连接管理 ⭐⭐⭐必考

#### 1. 三次握手 ⭐⭐⭐

**过程**：
1. 客户端 → 服务器：SYN=1, seq=x
2. 服务器 → 客户端：SYN=1, ACK=1, seq=y, ack=x+1
3. 客户端 → 服务器：ACK=1, seq=x+1, ack=y+1

**为什么需要三次？**
- 防止已失效的连接请求突然传到服务器
- 确保双方的收发能力都正常

**例题**：【2020统考】TCP三次握手中，第二次握手服务器发送的报文段中，哪些标志位被置1？

**答**：SYN=1, ACK=1

#### 2. 四次挥手 ⭐⭐⭐

**过程**：
1. 客户端 → 服务器：FIN=1, seq=u
2. 服务器 → 客户端：ACK=1, seq=v, ack=u+1
3. 服务器 → 客户端：FIN=1, ACK=1, seq=w, ack=u+1
4. 客户端 → 服务器：ACK=1, seq=u+1, ack=w+1

**为什么需要四次？**
- TCP是全双工的，每个方向都需要单独关闭
- 服务器收到FIN后可能还有数据要发送

**TIME-WAIT状态**：
- 客户端最后等待2MSL（最长报文段寿命）
- 确保服务器收到最后一个ACK
- 让旧连接的报文段在网络中消失

### 三、TCP可靠传输 ⭐⭐⭐

**机制**：
1. **校验和**：检测数据错误
2. **序号**：解决乱序问题
3. **确认应答**：确认数据到达
4. **超时重传**：处理丢失
5. **累积确认**：减少确认报文数量

### 四、TCP流量控制 ⭐⭐⭐

**目的**：控制发送方的发送速率，使接收方来得及接收

**机制**：滑动窗口

**工作原理**：
- 接收方在ACK中通告自己的接收窗口大小（rwnd）
- 发送方根据rwnd调整发送窗口
- rwnd=0时，发送方停止发送（但会定期发送探测报文）

### 五、TCP拥塞控制 ⭐⭐⭐必考

**目的**：防止过多数据注入网络，避免网络链路或路由器过载

**四种算法**：

#### 1. 慢开始（Slow Start）

- 初始拥塞窗口cwnd=1
- 每收到一个ACK，cwnd加1（指数增长）
- 达到慢开始门限ssthresh时，转入拥塞避免

#### 2. 拥塞避免（Congestion Avoidance）

- cwnd每经过一个RTT加1（线性增长）
- 当发生超时重传时：
  - ssthresh = cwnd / 2
  - cwnd = 1
  - 重新执行慢开始

#### 3. 快重传（Fast Retransmit）

- 收到3个重复ACK时，立即重传丢失的报文段
- 不必等待超时

#### 4. 快恢复（Fast Recovery）

- 收到3个重复ACK时：
  - ssthresh = cwnd / 2
  - cwnd = ssthresh（不是1！）
  - 执行拥塞避免

**例题**：【2018统考】TCP拥塞控制中，当发生超时事件时，慢开始门限ssthresh和拥塞窗口cwnd如何变化？

**答**：
ssthresh = cwnd / 2
cwnd = 1

### 六、考研重点

**高频考点**：
1. ⭐⭐⭐ TCP三次握手和四次挥手的过程
2. ⭐⭐⭐ 拥塞控制的四种算法
3. ⭐⭐⭐ 流量控制的滑动窗口机制
4. ⭐⭐ TIME-WAIT状态的作用
5. ⭐⭐ TCP vs UDP的比较
6. ⭐⭐ 可靠传输的机制
`        }
      ]
    },
    {
      id: 'ch6',
      number: 6,
      title: '应用层',
      score: '5-8分',
      importance: 'medium',
      mindMap: `mindmap
  root((应用层))
    网络应用模型
      C/S模型
      P2P模型
    DNS系统 ⭐⭐⭐
      域名结构
      查询过程
      服务器类型
    FTP ⭐⭐
      工作原理
      控制与数据连接
    电子邮件 ⭐⭐
      SMTP
      POP3
      IMAP
    WWW ⭐⭐⭐
      HTTP ⭐⭐⭐
      HTTPS
      Cookie/Session`,
      sections: [
        {
          id: '6.1',
          title: '网络应用模型',
          content: `## 6.1 网络应用模型

### 一、C/S模型（Client/Server）

**特点**：
- 服务器始终开机，具有固定IP地址
- 客户端之间不直接通信
- 扩展性差

**应用**：Web、Email、FTP

### 二、P2P模型（Peer-to-Peer）

**特点**：
- 任意端系统之间可以直接通信
- 无专用服务器
- 扩展性好

**应用**：BitTorrent、Skype

### 三、考研重点

**高频考点**：
1. ⭐⭐ C/S与P2P的比较
2. ⭐ 两种模型的特点
`        },
        {
          id: '6.2',
          title: 'DNS系统',
          content: `## 6.2 DNS系统 ⭐⭐⭐必考

### 一、域名结构

**层次结构**：
根域名 → 顶级域名 → 二级域名 → 三级域名

**示例**：www.baidu.com
- com：顶级域名
- baidu：二级域名
- www：三级域名（主机名）

**顶级域名分类**：
- **国家顶级域名**：cn、us、uk等
- **通用顶级域名**：com、org、net、edu等

### 二、DNS服务器类型

1. **根域名服务器**：13个，知道所有顶级域名服务器
2. **顶级域名服务器**：管理注册在该顶级域下的所有二级域名
3. **权威域名服务器**：每个主机的权威域名服务器
4. **本地域名服务器**：ISP提供，缓存查询结果

### 三、DNS查询过程 ⭐⭐⭐

**两种方式**：

#### 1. 递归查询

- 主机向本地域名服务器查询
- 本地域名服务器代替主机继续查询
- 最终将结果返回给主机

**特点**：主机只发送一次请求

#### 2. 迭代查询

- 本地域名服务器向根域名服务器查询
- 根域名服务器返回下一个要查询的服务器地址
- 本地域名服务器继续查询

**特点**：减轻根域名服务器负担

**实际过程**：
1. 主机向本地域名服务器发起递归查询
2. 本地域名服务器向根域名服务器发起迭代查询
3. 根 → 顶级 → 权威，逐级查询
4. 结果缓存到本地域名服务器

**例题**：【2019统考】DNS递归查询和迭代查询的区别是什么？

**答**：
- 递归查询：服务器代替客户继续查询，客户只发一次请求
- 迭代查询：服务器返回下一个要查询的服务器地址，由客户继续查询

### 四、DNS报文格式

**主要字段**：
- 标识符：16位，标识查询和响应
- 标志：区分查询/响应、递归/迭代
- 问题数、回答RR数、授权RR数、额外RR数
- 问题部分、回答部分、授权部分、额外部分

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ DNS查询过程（递归vs迭代）
2. ⭐⭐ DNS服务器类型
3. ⭐⭐ 域名层次结构
4. ⭐ DNS报文格式
`        },
        {
          id: '6.3',
          title: 'FTP',
          content: `## 6.3 FTP ⭐⭐

### 一、FTP工作原理

**FTP（File Transfer Protocol）**：文件传输协议

**特点**：
- 使用TCP可靠传输
- 交互式协议
- 支持多种文件类型

### 二、控制连接与数据连接 ⭐⭐

**控制连接**：
- 端口号：21
- 整个会话期间保持打开
- 传输控制信息（用户名、密码、命令）

**数据连接**：
- 端口号：20（主动模式）
- 每次传输文件时建立，传输完成后关闭
- 传输文件内容

**两种模式**：

#### 1. 主动模式（PORT）

- 客户端开启一个端口监听
- 服务器从20端口主动连接到客户端
- 可能被防火墙阻止

#### 2. 被动模式（PASV）

- 服务器开启一个端口监听
- 客户端主动连接到服务器
- 更适合防火墙环境

### 三、考研重点

**高频考点**：
1. ⭐⭐ FTP的两个连接（控制+数据）
2. ⭐⭐ 主动模式vs被动模式
3. ⭐ FTP使用的端口号
`        },
        {
          id: '6.4',
          title: '电子邮件',
          content: `## 6.4 电子邮件 ⭐⭐

### 一、电子邮件系统组成

1. **用户代理（UA）**：Outlook、Foxmail等
2. **邮件服务器**：存储和转发邮件
3. **邮件发送协议**：SMTP
4. **邮件读取协议**：POP3、IMAP

### 二、SMTP协议 ⭐⭐

**SMTP（Simple Mail Transfer Protocol）**：简单邮件传输协议

**特点**：
- 推协议（Push）：发送方主动推送
- 使用TCP端口25
- 只能发送ASCII码
- 命令/响应交互

**工作流程**：
1. 建立TCP连接
2. 握手（HELO）
3. 发送邮件（MAIL FROM、RCPT TO、DATA）
4. 关闭连接（QUIT）

### 三、POP3协议 ⭐⭐

**POP3（Post Office Protocol v3）**：邮局协议

**特点**：
- 拉协议（Pull）：接收方主动拉取
- 使用TCP端口110
- 下载后删除服务器上的邮件（默认）
- 离线工作

### 四、IMAP协议

**IMAP（Internet Message Access Protocol）**：网际消息访问协议

**特点**：
- 使用TCP端口143
- 邮件保留在服务器上
- 支持在线操作
- 可以只下载邮件头部

### 五、三种协议的比较

| 协议 | 作用 | 端口 | 特点 |
|------|------|------|------|
| SMTP | 发送邮件 | 25 | 推协议 |
| POP3 | 接收邮件 | 110 | 下载后删除 |
| IMAP | 接收邮件 | 143 | 保留在服务器 |

### 六、考研重点

**高频考点**：
1. ⭐⭐ SMTP、POP3、IMAP的作用和区别
2. ⭐⭐ 推协议vs拉协议
3. ⭐ 各协议使用的端口号
`        },
        {
          id: '6.5',
          title: 'WWW',
          content: `## 6.5 WWW ⭐⭐⭐必考

### 一、WWW概述

**WWW（World Wide Web）**：万维网

**三个核心技术**：
1. **URL**：统一资源定位符
2. **HTTP**：超文本传输协议
3. **HTML**：超文本标记语言

### 二、HTTP协议 ⭐⭐⭐必考

**HTTP（HyperText Transfer Protocol）**：超文本传输协议

**特点**：
- 无状态协议
- 使用TCP端口80
- 请求-响应模式
- 持久连接（HTTP/1.1默认）

#### 1. HTTP报文格式

**请求报文**：
GET /index.html HTTP/1.1
Host: www.example.com
User-Agent: Mozilla/5.0
...

**响应报文**：
HTTP/1.1 200 OK
Content-Type: text/html
Content-Length: 1234
...

#### 2. HTTP方法

- **GET**：获取资源（幂等）
- **POST**：提交数据（非幂等）
- **PUT**：上传资源
- **DELETE**：删除资源
- **HEAD**：获取头部信息

**GET vs POST**：
- GET参数在URL中，POST在报文体中
- GET有长度限制，POST无限制
- GET可缓存，POST不可缓存
- GET幂等，POST非幂等

#### 3. HTTP状态码

- **1xx**：信息性状态码
- **2xx**：成功（200 OK）
- **3xx**：重定向（301永久重定向、302临时重定向）
- **4xx**：客户端错误（404 Not Found）
- **5xx**：服务器错误（500 Internal Server Error）

#### 4. HTTP版本演进 ⭐⭐⭐

##### HTTP/1.0（1996年）

**特点**：
- 非持久连接：每个请求都要建立新的TCP连接
- 无管道化：必须等待前一个响应完成才能发送下一个请求
- 基本缓存控制

**缺点**：
- 队头阻塞（Head-of-Line Blocking）
- 多次TCP握手开销大
- 慢启动问题

##### HTTP/1.1（1997年，当前主流）⭐⭐⭐

**改进**：
- ✅ **持久连接**：默认Keep-Alive，多个请求复用同一个TCP连接
- ✅ **管道化**：允许客户端在不等待响应的情况下发送多个请求
- ✅ **分块传输编码**：支持流式传输
- ✅ **增强缓存控制**：Cache-Control头部
- ✅ **范围请求**：支持断点续传（Range头部）
- ✅ **主机头**：支持虚拟主机（Host头部）

**仍然存在的问题**：
- ❌ 队头阻塞仍然存在（应用层）
- ❌ 头部冗余：每个请求都携带相同的头部信息
- ❌ 单一队列：所有请求按顺序处理

##### HTTP/2.0（2015年）

**重大改进**：
- ✅ **多路复用**：多个请求可以在同一个TCP连接上并行传输
- ✅ **二进制分帧**：将HTTP消息分解为更小的帧
- ✅ **头部压缩**：使用HPACK算法压缩头部
- ✅ **服务器推送**：服务器可以主动向客户端推送资源
- ✅ **优先级**：可以为请求设置优先级

**性能提升**：
- 页面加载速度提升30%-50%
- 减少延迟
- 更好的网络利用率

**三个版本对比**：

| 特性 | HTTP/1.0 | HTTP/1.1 | HTTP/2.0 |
|------|----------|----------|----------|
| **连接方式** | 非持久 | 持久连接 | 持久连接 |
| **多路复用** | ❌ | ❌ | ✅ |
| **管道化** | ❌ | ✅（有队头阻塞） | ✅（无队头阻塞） |
| **头部压缩** | ❌ | ❌ | ✅（HPACK） |
| **二进制协议** | ❌ | ❌ | ✅ |
| **服务器推送** | ❌ | ❌ | ✅ |
| **加密要求** | 不强制 | 不强制 | 推荐（大部分实现需要TLS） |

### 三、HTTPS详解 ⭐⭐⭐必考

**HTTPS**：HTTP over SSL/TLS

**为什么需要HTTPS？**
- 🔒 **机密性**：防止数据被窃听
- 🔐 **完整性**：防止数据被篡改
- 👤 **身份认证**：防止中间人攻击

#### 1. HTTPS工作原理

**工作流程**：

**步骤1**：TCP三次握手
- 客户端与服务器建立TCP连接（端口443）

**步骤2**：SSL/TLS握手（简化版）
\`\`\`
1. Client Hello：客户端发送支持的加密套件、随机数Client Random
2. Server Hello：服务器选择加密套件、发送证书、随机数Server Random
3. 客户端验证证书：检查证书是否有效、是否被篡改
4. 密钥交换：生成预主密钥Pre-Master Secret
5. 生成会话密钥：Client Random + Server Random + Pre-Master Secret
6. 切换加密：双方使用会话密钥进行对称加密通信
\`\`\`

**步骤3**：HTTP通信
- 所有HTTP请求和响应都经过加密传输

#### 2. SSL/TLS握手详细过程

**关键概念**：

**（1）非对称加密**（握手阶段使用）
- 公钥加密，私钥解密
- 用于安全地交换对称密钥
- 速度慢，不适合大量数据传输

**（2）对称加密**（通信阶段使用）
- 同一密钥加密和解密
- 速度快，适合大量数据传输
- 密钥需要通过非对称加密安全交换

**（3）数字证书**
- 由CA（证书授权中心）签发
- 包含服务器的公钥和身份信息
- 客户端通过验证证书确认服务器身份

**完整握手流程**：
\`\`\`
客户端                          服务器
  | --- Client Hello -----------> |
  |     (加密套件列表)            |
  |                               |
  | <--- Server Hello ----------- |
  |     (选择的加密套件)          |
  | <--- Certificate ------------ |
  |     (服务器证书)              |
  | <--- Server Key Exchange ---- |
  |     (公钥参数)                |
  |                               |
  | --- Client Key Exchange ----> |
  |     (用公钥加密的预主密钥)    |
  |                               |
  | --- Change Cipher Spec -----> |
  |     (切换到加密模式)          |
  | --- Finished ----------------> |
  |                               |
  | <--- Change Cipher Spec ----- |
  | <--- Finished --------------- |
  |                               |
  | === 加密HTTP通信 =========== |
\`\`\`

#### 3. HTTP vs HTTPS对比

| 对比项 | HTTP | HTTPS |
|-------|------|--------|
| **端口** | 80 | 443 |
| **安全性** | 明文传输，不安全 | 加密传输，安全 |
| **证书** | 不需要 | 需要SSL证书 |
| **速度** | 快 | 稍慢（握手开销） |
| **SEO** | 不利 | Google优先排名 |
| **应用场景** | 内部系统 | 电商、银行、登录等 |

### 四、Cookie和Session详解 ⭐⭐

**为什么需要Cookie/Session？**
- HTTP是无状态协议，无法记住用户身份
- 需要一种机制来跟踪用户会话

#### 1. Cookie

**定义**：服务器发送到用户浏览器并保存在本地的一小块数据

**特点**：
- 📍 **存储位置**：客户端（浏览器）
- 📏 **大小限制**：每个Cookie最大4KB
- 🔢 **数量限制**：每个域名最多20-50个Cookie
- ⏱️ **生命周期**：可设置过期时间，否则关闭浏览器后失效
- 🔓 **安全性**：较低，可被查看和篡改
- 🚀 **传输方式**：每次请求自动携带在HTTP头部

**工作流程**：
\`\`\`
1. 客户端首次访问服务器
2. 服务器在响应中添加 Set-Cookie 头部
3. 浏览器保存Cookie
4. 后续请求自动携带 Cookie 头部
\`\`\`

**示例**：
\`\`\`
// 服务器响应
Set-Cookie: sessionId=abc123; Expires=Wed, 01 Jan 2025 00:00:00 GMT; Path=/

// 客户端请求
Cookie: sessionId=abc123
\`\`\`

#### 2. Session

**定义**：服务器端存储的用户会话信息

**特点**：
- 📍 **存储位置**：服务器端
- 📏 **大小限制**：无明确限制（取决于服务器内存）
- 🔢 **数量限制**：取决于服务器资源
- ⏱️ **生命周期**：默认30分钟无活动后失效
- 🔐 **安全性**：较高，数据存储在服务器
- 🚀 **传输方式**：通过Cookie或URL传递Session ID

**工作流程**：
\`\`\`
1. 客户端首次访问
2. 服务器创建Session，生成唯一的Session ID
3. 服务器将Session ID通过Cookie发送给客户端
4. 客户端后续请求携带Session ID
5. 服务器根据Session ID查找对应的Session数据
\`\`\`

#### 3. Cookie vs Session详细对比

| 对比项 | Cookie | Session |
|-------|--------|----------|
| **存储位置** | 客户端（浏览器） | 服务器端 |
| **安全性** | 低（可被篡改） | 高（服务器控制） |
| **性能** | 好（不占用服务器资源） | 一般（占用服务器内存） |
| **容量** | 小（4KB） | 大（无限制） |
| **有效期** | 可长期保存 | 短期（默认30分钟） |
| **传输开销** | 每次请求都携带 | 只传Session ID |
| **跨域支持** | 受限 | 需特殊处理 |
| **适用场景** | 记住用户名、偏好设置 | 登录状态、购物车 |

#### 4. Session的实现方式

**（1）基于Cookie的Session**
- Session ID存储在Cookie中
- 最常用，但要求客户端支持Cookie

**（2）URL重写**
- Session ID附加在URL后面
- 例如：http://example.com/page?sessionId=abc123
- 适用于Cookie禁用的情况

**（3）隐藏表单字段**
- Session ID存储在表单的隐藏字段中
- 仅适用于表单提交

#### 5. Session的问题与解决

**问题1**：服务器集群时Session共享
- **解决**：使用Redis等集中存储Session

**问题2**：Session固定攻击
- **解决**：登录后重新生成Session ID

**问题3**：Session劫持
- **解决**：使用HTTPS、HttpOnly标志

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ HTTP请求/响应报文格式
2. ⭐⭐⭐ GET vs POST的区别
3. ⭐⭐⭐ HTTP状态码
4. ⭐⭐ HTTP/1.0 vs HTTP/1.1
5. ⭐⭐ HTTPS的工作原理
6. ⭐⭐ Cookie vs Session
`        }
      ]
    },
    {
      id: 'ch7',
      number: 7,
      title: '网络安全',
      score: '3-5分',
      importance: 'low',
      mindMap: `mindmap
  root((网络安全))
    安全概念
      威胁
      服务
    加密技术 ⭐⭐
      对称加密
      非对称加密
      数字签名
    认证技术
    安全协议 ⭐⭐
      SSL/TLS
      IPSec
      PGP`,
      sections: [
        {
          id: '7.1',
          title: '网络安全概念',
          content: `## 7.1 网络安全概念

### 一、主要安全威胁

1. **截获**：未经授权获取信息
2. **中断**：破坏系统资源
3. **篡改**：修改信息内容
4. **伪造**：假冒身份

### 二、安全服务

1. **机密性**：防止信息泄露
2. **完整性**：防止信息被篡改
3. **可用性**：确保授权用户能访问
4. **认证**：验证身份
5. **不可否认性**：防止抵赖

### 三、考研重点

**高频考点**：
1. ⭐ 四种安全威胁
2. ⭐ 五种安全服务
`        },
        {
          id: '7.2',
          title: '加密技术',
          content: `## 7.2 加密技术 ⭐⭐

### 一、对称加密

**特点**：
- 加密和解密使用同一密钥
- 速度快，适合大数据量
- 密钥分发困难

**算法**：
- DES（数据加密标准）
- AES（高级加密标准）
- 3DES

### 二、非对称加密

**特点**：
- 使用公钥和私钥
- 公钥公开，私钥保密
- 速度慢，适合小数据量

**算法**：
- RSA
- ECC（椭圆曲线密码）

**应用**：
- 数字签名
- 密钥交换

### 三、数字签名 ⭐⭐

**作用**：
- 认证：确认发送方身份
- 完整性：检测是否被篡改
- 不可否认性：防止抵赖

**过程**：
1. 对消息计算摘要（Hash）
2. 用私钥加密摘要得到签名
3. 接收方用公钥解密签名，验证摘要

### 四、考研重点

**高频考点**：
1. ⭐⭐ 对称加密vs非对称加密
2. ⭐⭐ 数字签名的原理和作用
3. ⭐ 常见加密算法
`        },
        {
          id: '7.3',
          title: '安全协议',
          content: `## 7.3 安全协议 ⭐⭐

### 一、SSL/TLS协议

**SSL（Secure Sockets Layer）**：安全套接字层
**TLS（Transport Layer Security）**：传输层安全

**作用**：为TCP连接提供安全通信

**工作流程**：
1. TCP三次握手
2. SSL/TLS握手：
   - 协商加密算法
   - 服务器发送证书
   - 客户端验证证书
   - 生成会话密钥
3. 加密数据传输

**应用**：HTTPS

### 二、IPSec协议

**IPSec（Internet Protocol Security）**：网际协议安全

**作用**：在网络层提供安全服务

**两种模式**：
- **传输模式**：只加密数据部分
- **隧道模式**：加密整个IP分组

**应用**：VPN

### 三、PGP

**PGP（Pretty Good Privacy）**：优良保密协议

**作用**：电子邮件加密

**特点**：
- 结合对称和非对称加密
- 提供加密和数字签名

### 四、考研重点

**高频考点**：
1. ⭐⭐ SSL/TLS的工作流程
2. ⭐⭐ IPSec的两种模式
3. ⭐ PGP的应用
`        }
      ]
    },
    {
      id: 'ch8',
      number: 8,
      title: '网络管理与新技术',
      score: '0-2分',
      importance: 'low',
      mindMap: `mindmap
  root((网络管理与新技术))
    网络管理
      SNMP
      管理模型
    新技术
      SDN
      CDN
      IoT`,
      sections: [
        {
          id: '8.1',
          title: '网络管理',
          content: `## 8.1 网络管理

### 一、SNMP协议

**SNMP（Simple Network Management Protocol）**：简单网络管理协议

**组成**：
- **管理站**：管理者
- **代理**：被管理设备
- **MIB**：管理信息库

**操作**：
- Get：获取信息
- Set：设置参数
- Trap：主动报告异常

### 二、网络管理功能

1. **故障管理**：检测和修复故障
2. **配置管理**：配置网络设备
3. **性能管理**：监控网络性能
4. **安全管理**：保护网络安全
5. **计费管理**：统计资源使用

### 三、考研重点

**高频考点**：
1. ⭐ SNMP的基本组成
2. ⭐ 网络管理的五大功能
`        },
        {
          id: '8.2',
          title: '新技术',
          content: `## 8.2 新技术

### 一、SDN（软件定义网络）

**特点**：
- 控制平面与数据平面分离
- 集中控制
- 可编程

**架构**：
- 应用层
- 控制层
- 基础设施层

### 二、CDN（内容分发网络）

**作用**：将内容缓存到边缘节点，加速访问

**原理**：
- 就近访问
- 负载均衡
- 减少骨干网流量

### 三、IoT（物联网）

**特点**：
- 万物互联
- 感知层、网络层、应用层
- 海量设备

### 四、考研重点

**高频考点**：
1. ⭐ SDN的特点
2. ⭐ CDN的原理
3. ⭐ IoT的架构
`        }
      ]
    }
  ])
  
  // 计算属性
  const currentChapter = computed(() => {
    return chapters.value.find(ch => ch.id === currentChapterId.value)
  })
  
  const currentSection = computed(() => {
    if (!currentChapter.value) return null
    const section = currentChapter.value.sections.find(sec => sec.id === currentSectionId.value)
    
    // 如果找不到匹配的小节（章节和小节不匹配），自动选择当前章节的第一个小节
    if (!section && currentChapter.value.sections.length > 0) {
      console.warn('小节ID不匹配，自动切换到章节的第一个小节')
      currentSectionId.value = currentChapter.value.sections[0].id
      return currentChapter.value.sections[0]
    }
    
    return section
  })
  
  const overallProgress = computed(() => {
    let totalSections = 0
    let studiedCount = 0
    
    chapters.value.forEach(chapter => {
      chapter.sections.forEach(section => {
        totalSections++
        if (studiedSections.value.has(section.id)) {
          studiedCount++
        }
      })
    })
    
    return totalSections > 0 ? Math.round((studiedCount / totalSections) * 100) : 0
  })
  
  // 方法
  const selectChapter = (chapterId: string) => {
    currentChapterId.value = chapterId
    const chapter = chapters.value.find(ch => ch.id === chapterId)
    if (chapter && chapter.sections.length > 0) {
      currentSectionId.value = chapter.sections[0].id
    }
  }
  
  const selectSection = (sectionId: string) => {
    // 根据小节ID自动推断章节ID
    const chapterNum = sectionId.split('.')[0] // 例如 '5.1' -> '5'
    const newChapterId = `ch${chapterNum}`
    
    // 先设置章节ID，再设置小节ID
    currentChapterId.value = newChapterId
    currentSectionId.value = sectionId
  }
  
  const toggleSectionStudied = (sectionId: string) => {
    if (studiedSections.value.has(sectionId)) {
      studiedSections.value.delete(sectionId)
    } else {
      studiedSections.value.add(sectionId)
    }
  }
  
  const isSectionStudied = (sectionId: string) => {
    return studiedSections.value.has(sectionId)
  }
  
  return {
    // 状态
    currentChapterId,
    currentSectionId,
    studiedSections,
    chapters,
    
    // 计算属性
    currentChapter,
    currentSection,
    overallProgress,
    
    // 方法
    selectChapter,
    selectSection,
    toggleSectionStudied,
    isSectionStudied
  }
})
