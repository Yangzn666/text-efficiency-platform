import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

interface Section {
  id: string
  title: string
  content: string        // Markdown格式
  mindMap?: string       // Mermaid语法（可选）
}

interface Chapter {
  id: string
  number: number
  title: string
  score: string          // 分值范围
  importance: 'high' | 'medium' | 'low'
  sections: Section[]
  mindMap: string        // Mermaid语法
}

export const useDataStructureStore = defineStore('dataStructure', () => {
  // 当前选中的章节和小节
  const currentChapterId = ref<string>('ch1')
  const currentSectionId = ref<string>('1.1')
  
  // 学习进度记录
  const studiedSections = ref<Set<string>>(new Set())
  
  // 所有章节数据（按王道《数据结构》8章结构）
  const chapters = ref<Chapter[]>([
    {
      id: 'ch1',
      number: 1,
      title: '绪论',
      score: '0-2分',
      importance: 'low',
      mindMap: `mindmap
  root((绪论))
    基本概念
      数据
      数据元素
      数据对象
      数据结构
    三要素
      逻辑结构
        集合
        线性
        树形
        图状
      存储结构
        顺序存储
        链式存储
        索引存储
        散列存储
      数据的运算
    算法
      特性
        有穷性
        确定性
        可行性
        输入
        输出
      评价指标
        时间复杂度 ⭐⭐
        空间复杂度`,
      sections: [
        {
          id: '1.1',
          title: '数据结构的基本概念',
          content: `## 1.1 数据结构的基本概念

### 一、基本概念

**数据**：信息的载体，能被计算机识别、存储和加工

**数据元素**：数据的基本单位，也称为元素、结点、记录

**数据项**：构成数据元素的不可分割的最小单位

**数据对象**：性质相同的数据元素的集合

**数据结构**：相互之间存在一种或多种特定关系的数据元素的集合

### 二、数据结构的三要素 ⭐⭐

#### 1. 逻辑结构（面向问题）

指数据元素之间的逻辑关系，与存储无关

| 类型 | 说明 | 示例 |
|------|------|------|
| **集合结构** | 元素之间除同属一个集合外无其他关系 | 集合 |
| **线性结构** | 一对一关系 | 线性表、栈、队列 |
| **树形结构** | 一对多关系 | 树、二叉树 |
| **图状结构** | 多对多关系 | 图、网 |

#### 2. 存储结构（物理结构）

数据结构在计算机中的表示（映像）

| 类型 | 说明 | 特点 |
|------|------|------|
| **顺序存储** | 逻辑相邻，物理也相邻 | 随机访问，插入删除慢 |
| **链式存储** | 逻辑相邻，物理不一定相邻 | 插入删除快，不支持随机访问 |
| **索引存储** | 建立索引表 | 检索快，增加存储空间 |
| **散列存储** | 根据关键字计算地址 | 检索、插入、删除都快 |

#### 3. 数据的运算

施加在数据上的运算，包括：
- 检索、排序、插入、删除、修改等
- 运算的定义针对逻辑结构
- 运算的实现针对存储结构

### 三、逻辑结构与存储结构的关系 ⭐⭐⭐必考

**重要结论**：
1. ✅ **逻辑结构独立于存储结构** - 同一逻辑结构可以有多种存储实现
2. ✅ 存储结构依赖于逻辑结构 - 必须先有逻辑结构，才能设计存储结构
3. ✅ 数据的逻辑结构唯一，但存储结构不唯一

**易混淆点**：
- ❌ 错误：顺序表是逻辑结构，链表是逻辑结构
- ✅ 正确：顺序表和链表都是**存储结构**（物理结构）
- ❌ 错误：逻辑结构决定存储结构
- ✅ 正确：逻辑结构不唯一决定存储结构，一种逻辑结构可以有多种存储实现

**常见逻辑结构举例**：
- 线性结构：线性表、栈、队列、串
- 树形结构：树、二叉树
- 图状结构：图、网

**常见存储结构举例**：
- 顺序存储：顺序表、顺序栈、循环队列
- 链式存储：单链表、双链表、循环链表、链栈、链队列
- 散列存储：哈希表

### 💡 知识点卡片：数据结构全景图

> 📌 **学习路线指引**：这里提到的所有数据结构将在后续章节详细讲解
> 
> **线性结构**（第2-4章）：
> - 线性表 → 第2章（顺序表、链表）
> - 栈和队列 → 第3章（受限的线性表）
> - 串 → 第4章
> 
> **树形结构**（第5章）：
> - 树和二叉树 → 第5章（重点章节，8-12分）
> 
> **图状结构**（第6章）：
> - 图 → 第6章（最复杂的数据结构）
> 
> **应用层**（第7-8章）：
> - 查找 → 第7章（哈希表、二叉排序树）
> - 排序 → 第8章（各种排序算法）
> 
> 💪 **建议**：先掌握线性结构，再学习树和图，最后学习查找和排序算法

### 四、数据存储的重要原则 ⭐⭐

**核心要点**：
- 存储数据时，不仅要存储**数据元素的值**，还要存储**数据元素之间的关系**
- 顺序存储：通过物理位置相邻表示关系
- 链式存储：通过指针表示关系

**例题**：
> 在存储数据时，通常不仅要存储各数据元素的值，还要存储（）。
> A. 数据的操作方法  B. 数据元素的类型  C. 数据元素之间的关系  D. 数据的存取方法
> 
> **答案**：C
> **解析**：数据结构的核心就是研究数据元素之间的关系，存储时必须体现这种关系。

### 五、考研真题精选 ⭐⭐⭐

> 【2020统考】一个完整的数据结构通常应包含以下哪些要素（）？
> A. 数据元素及其存储方式
> B. 数据的逻辑结构和物理结构
> C. 数据的逻辑结构、存储结构以及在其上定义的基本操作
> D. 数据对象和数据元素之间的关系
> 
> **答案**：C
> **解析**：数据结构包含三要素：逻辑结构、存储结构（物理结构）、数据的运算（基本操作）。缺一不可。

### 六、重要考点总结

**易错点**：
1. ⚠️ 数据项是最小单位，数据元素是基本单位
2. ⚠️ **逻辑结构与存储结构独立** - 这是高频考点！
3. ⚠️ 运算定义在逻辑结构上，实现在存储结构上
4. ⚠️ 存储数据必须存储元素之间的关系
5. ⚠️ 顺序表、链表是存储结构，不是逻辑结构

**记忆口诀**：
- 数据结构三要素：**逻、存、运**（逻辑、存储、运算）
- 逻辑结构四种：**集、线、树、图**
- 存储结构四种：**顺、链、索、散**
`        },
        {
          id: '1.2',
          title: '算法和算法评价',
          content: `## 1.2 算法和算法评价 ⭐⭐

### 一、算法的五个特性

1. **有穷性**：算法必须在执行有限步后结束
2. **确定性**：每条指令必须有确切的含义
3. **可行性**：算法中描述的操作都可以通过已经实现的基本运算执行
4. **输入**：零个或多个输入
5. **输出**：一个或多个输出

### 二、算法效率的度量 ⭐⭐⭐必考

#### 1. 时间复杂度

**定义**：算法中基本操作重复执行的次数

**计算方法**：
- 找出基本语句（执行次数最多的语句）
- 计算基本语句的执行次数的数量级

**常见的时间复杂度**（从小到大）：
\`
O(1) < O(log₂n) < O(n) < O(nlog₂n) < O(n²) < O(n³) < O(2ⁿ) < O(n!) < O(nⁿ)
\`

**例题**：
\`\`\`c
// 例1：O(1)
x = x + 1;

// 例2：O(n)
for (i = 0; i < n; i++) {
    x = x + 1;
}

// 例3：O(n²)
for (i = 0; i < n; i++) {
    for (j = 0; j < n; j++) {
        x = x + 1;
    }
}

// 例4：O(log₂n)
i = 1;
while (i <= n) {
    i = i * 2;
}
\`\`\`

#### 2. 空间复杂度

**定义**：算法所需存储空间的度量

**公式**：S(n) = O(f(n))

**组成**：
- 固定部分：指令空间、常量、简单变量
- 可变部分：动态分配的空间、递归栈空间

**例题**：
\`\`\`c
// 空间复杂度 O(1)
int sum(int a[], int n) {
    int s = 0;
    for (int i = 0; i < n; i++)
        s += a[i];
    return s;
}

// 空间复杂度 O(n)
int* createArray(int n) {
    int* arr = (int*)malloc(n * sizeof(int));
    return arr;
}
\`\`\`

### 三、算法的特性详解 ⭐⭐

**五大特性**：
1. **有穷性**：算法必须在执行有限步后结束，且每一步都在可接受时间内完成
   - ⚠️ 注意：程序算法，操作系统可以是无限循环的程序，但不是算法
2. **确定性**：每条指令必须有确切的含义，相同的输入只能得到相同的输出
3. **可行性**：算法中描述的操作都可以通过已经实现的基本运算执行
4. **输入**：零个或多个输入（可以没有输入）
5. **输出**：一个或多个输出（必须有输出）

**易错点**：
- ❌ 错误：算法的时间效率取决于算法执行所花的CPU时间
- ✅ 正确：时间效率用**时间复杂度**衡量，与具体机器无关
- ❌ 错误：在算法设计中不允许用牺牲空间效率的方式来换取好的时间效率
- ✅ 正确：**时空权衡**是常用策略（如空间换时间）

### 四、时间复杂度详解 ⭐⭐⭐必考

#### 1. 时间复杂度的本质

**核心概念**：
- 时间复杂度不是计算实际运行时间，而是计算**基本操作的执行次数**
- 使用大O记号表示**数量级**，忽略常数项和低阶项
- 关注**最坏情况**的时间复杂度（除非特别说明）

#### 2. 常见时间复杂度类型

| 复杂度 | 名称 | 典型场景 | 示例 |
|--------|------|----------|------|
| O(1) | 常数阶 | 基本运算 | 数组访问、算术运算 |
| O(log n) | 对数阶 | 二分查找 | 每次减半的循环 |
| O(n) | 线性阶 | 单重循环 | 遍历数组 |
| O(n log n) | 线性对数阶 | 高效排序 | 快速排序、归并排序 |
| O(n²) | 平方阶 | 双重循环 | 冒泡排序、选择排序 |
| O(n³) | 立方阶 | 三重循环 | 矩阵乘法 |
| O(2ⁿ) | 指数阶 | 递归 | 斐波那契数列（朴素递归） |

#### 3. 考研真题精选 ⭐⭐⭐

> **【2017统考真题1】** 下列函数的时间复杂度是（）。
> 
> \`\`\`c
> int func(int n){
>     int i=0, sum=0;
>     while(sum<n) sum += ++i;
>     return i;
> }
> \`\`\`
> A. O(log n)  B. O(n^(1/2))  C. O(n)  D. O(n log n)
> 
> **答案**：B
> **解析**：sum = 1+2+3+...+i = i(i+1)/2，当 sum ≥ n 时，i(i+1)/2 ≈ n，解得 i ≈ √(2n)，所以时间复杂度为 O(n^(1/2))。

> **【2019统考真题1】** 设 n 是描述问题规模的非负整数，下列程序段的时间复杂度是（）。
> **\`\`\`c
> x=0;
> while(n >= (x+1)*(x+1))
>     x = x + 1;
> **\`\`\`
> A. O(log n)  B. O(n^(1/2))  C. O(n)  D. O(n²)
> 
> **答案**：B
> **解析**：循环条件 (x+1)² ≤ n，即 x+1 ≤ √n，x ≤ √n - 1，循环执行次数约为 √n，所以时间复杂度为 O(n^(1/2))。

> **【2022统考真题1】** 下列程序段的时间复杂度是（）。
> **\`\`\`c
> int sum=0;
> for(int i=1; i<n; i*=2)
>     for(int j=0; j<i; j++)
>         sum++;
> **\`\`\`
> A. O(log₂n)  B. O(n)  C. O(n log₂n)  D. O(n²)
> 
> **答案**：B
> **解析**：外层循环 i = 1, 2, 4, 8, ...，内层循环执行 i 次。总执行次数 = 1+2+4+8+...+2^k < n，这是等比数列求和，总和 < 2n，所以时间复杂度为 O(n)。

> **【2025统考真题1】** 下列程序段的时间复杂度是（）。
> **\`\`\`c
> int count = 0, i, j;
> for(i = 1; i * i <= n; i++)
>     for(j = 1; j <= i; j++)
>         count++;
> **\`\`\`
> A. O(log n)  B. O(n)  C. O(n log n)  D. O(n²)
> 
> **答案**：B
> **解析**：外层循环 i 从 1 到 √n，内层循环执行 i 次。总执行次数 = 1+2+3+...+√n = n(√n+1)/2 ≈ n/2，所以时间复杂度为 O(n)。

#### 4. 时间复杂度计算技巧

**常见循环结构**：
\`\`\`c
// 技巧1：等差数列求和
for(i=1; i<=n; i++)
    for(j=1; j<=i; j++)
        // 执行次数 = 1+2+...+n = n(n+1)/2 = O(n²)

// 技巧2：等比数列求和
for(i=1; i<=n; i*=2)
    // 执行次数 = log₂n

// 技巧3：嵌套循环独立
for(i=1; i<=n; i++)
    for(j=1; j<=n; j*=2)
        // 执行次数 = n × log₂n = O(n log n)

// 技巧4：条件循环
x=0;
while(x*x <= n)
    x++;
// 执行次数 = √n = O(n^(1/2))
\`\`\`

### 五、空间复杂度详解 ⭐⭐

#### 1. 空间复杂度的本质

**核心概念**：
- 空间复杂度不是计算实际占用的字节数，而是计算**额外需要的辅助空间**
- **O(1)** 表示所需的辅助空间大小与问题规模 n **无关**（可以有常量级的空间）
- 不包括输入数据本身占用的空间

#### 2. O(1) 的正确理解 ⭐⭐⭐必考

**常见误区**：
- ❌ 错误：O(1) 表示不需要任何辅助空间
- ❌ 错误：O(1) 表示不需要任何空间
- ✅ 正确：O(1) 表示**所需辅助空间大小与问题规模 n 无关**

**例题**：
> 某算法的空间复杂度为 O(1)，则表示该算法（）。
> A. 不需要任何辅助空间
> B. 所需辅助空间大小与问题规模 n 无关
> C. 不需要任何空间
> D. 所需空间大小与问题规模 n 无关
> 
> **答案**：B
> **解析**：O(1) 表示空间复杂度是常数阶，即所需辅助空间是固定的，不随 n 变化。可以有变量，但变量个数是固定的。

#### 3. 空间复杂度计算

\`\`\`c
// 空间复杂度 O(1)
int sum(int a[], int n) {
    int s = 0;          // 只需要固定个数的变量
    for (int i = 0; i < n; i++)
        s += a[i];
    return s;
}

// 空间复杂度 O(n)
int* createArray(int n) {
    int* arr = (int*)malloc(n * sizeof(int));  // 动态分配与 n 相关的空间
    return arr;
}

// 空间复杂度 O(n)
void recursive(int n) {
    if (n <= 0) return;
    recursive(n-1);  // 递归深度为 n，需要 n 层栈空间
}
\`\`\`

### 六、考研重点总结

**高频考点**：
1. ⭐⭐⭐ 时间复杂度的计算（**必考！**）
2. ⭐⭐⭐ 特殊循环结构的时间复杂度（等差、等比数列）
3. ⭐⭐ O(1) 的正确理解
4. ⭐⭐ 最好、最坏、平均时间复杂度
5. ⭐ 空间复杂度的分析

**答题技巧**：
- 看到 while 循环中有乘法或平方 → 考虑 O(log n) 或 O(n^(1/2))
- 看到 for 循环嵌套，外层等比、内层线性 → 考虑 O(n)
- 空间复杂度 O(1) → 记住关键词“与 n 无关”
`        }
      ]
    },
    {
      id: 'ch2',
      number: 2,
      title: '线性表',
      score: '3-5分',
      importance: 'medium',
      mindMap: `mindmap
  root((线性表))
    基本概念
      定义
        有限序列
        相同数据类型
      基本操作
        初始化
        销毁
        插入
        删除
        查找
        求长度
    顺序表 ⭐
      特点
        随机访问 O(1)
        存储密度高
      插入删除
        平均移动 n/2 个元素
        时间复杂度 O(n)
      动态分配
        malloc/realloc
    链表 ⭐⭐
      单链表
        头指针 vs 头结点
        插入删除 O(1)
      双链表
        前后指针
        可双向遍历
      循环链表
        尾指针指向头
        无NULL指针
      静态链表
        数组实现
        next为游标
    比较与应用
      顺序表优势
        随机访问快
        缓存友好
      链表优势
        插入删除灵活
        无需连续空间
      应用场景
        频繁查找→顺序表
        频繁增删→链表`,
      sections: [
        {
          id: '2.1',
          title: '线性表的定义和基本操作',
          content: `## 2.1 线性表的定义和基本操作

### 一、线性表的定义

**线性表（Linear List）**：具有**相同数据类型**的 n（n≥0）个数据元素的**有限序列**。

- **记作**：L = (a₁, a₂, ..., aₙ)
- **n**：表长，n=0 时为空表
- **a₁**：表头元素
- **aₙ**：表尾元素
- **位序**：元素 aᵢ 的位序为 i（从1开始）

**逻辑特性**：
- ✅ 除第一个元素外，每个元素有且仅有一个**直接前驱**
- ✅ 除最后一个元素外，每个元素有且仅有一个**直接后继**
- ✅ 元素之间存在**一对一**的线性关系

### 二、线性表的基本操作 ⭐必考

#### 1. InitList(&L) - 初始化
- 构造一个空的线性表 L
- 分配内存空间

#### 2. DestroyList(&L) - 销毁
- 销毁线性表 L
- 释放内存空间

#### 3. ListInsert(&L, i, e) - 插入 ⭐⭐
- 在表 L 的第 i 个位置插入元素 e
- **时间复杂度**：O(n)
- **注意**：i 的取值范围为 1 ≤ i ≤ n+1

#### 4. ListDelete(&L, i, &e) - 删除 ⭐⭐
- 删除表 L 的第 i 个位置的元素，并用 e 返回
- **时间复杂度**：O(n)
- **注意**：i 的取值范围为 1 ≤ i ≤ n

#### 5. LocateElem(L, e) - 按值查找
- 查找值为 e 的元素，返回其位序
- **时间复杂度**：O(n)

#### 6. GetElem(L, i) - 按位查找
- 获取表 L 中第 i 个位置的元素
- **时间复杂度**：取决于存储结构
  - 顺序表：O(1) ✅
  - 链表：O(n)

#### 7. Length(L) - 求表长
- 返回线性表 L 的长度 n
- **时间复杂度**：O(1)

#### 8. PrintList(L) - 输出
- 按顺序输出线性表 L 的所有元素值

#### 9. Empty(L) - 判空
- 判断线性表 L 是否为空
- 空返回 true，否则返回 false

### 三、重要考点

**易错点**：
1. ⚠️ 位序从 1 开始，数组下标从 0 开始
2. ⚠️ 插入位置 i 可以是 n+1（表尾插入）
3. ⚠️ 删除位置 i 最大为 n（不能删除不存在的元素）
4. ⚠️ 引用符号 & 表示参数会被修改

**考研真题示例**：
> 【2019统考】在线性表的下列运算中，不改变数据元素之间结构关系的运算是（）。
> A. 插入  B. 删除  C. 排序  D. 置空
> 
> **答案**：D（置空只是清空元素，不改变结构关系）

### 🔗 知识延伸：栈和队列是特殊的线性表

> 💡 **重要概念**：栈和队列都是**操作受限的线性表**
> 
> - **栈（Stack）**：只能在**一端**进行插入和删除
>   - 特点：后进先出（LIFO）
>   - 应用：函数调用、表达式求值、括号匹配
>   - 📖 详见：3.1 栈的基本概念
> 
> - **队列（Queue）**：在**一端插入**，在**另一端删除**
>   - 特点：先进先出（FIFO）
>   - 应用：缓冲区管理、层次遍历、BFS算法
>   - 📖 详见：3.4 队列的基本概念
> 
> ⚠️ **注意**：虽然栈和队列的操作受限，但它们的逻辑结构仍然是线性结构！
`
        },
        {
          id: '2.2',
          title: '顺序表',
          content: `## 2.2 顺序表 ⭐⭐

### 一、顺序表的定义

**顺序表**：用**顺序存储**的方式实现线性表。

- **存储方式**：把逻辑上相邻的元素存储在物理位置也相邻的存储单元中
- **实现方式**：通常使用**数组**实现
- **特点**：元素地址连续

### 二、顺序表的两种实现

#### 1. 静态分配

\`\`\`c
#define MaxSize 50  // 定义最大长度
typedef struct {
    ElemType data[MaxSize];  // 用静态数组存放元素
    int length;               // 顺序表当前长度
} SqList;
\`\`\`

**特点**：
- ✅ 实现简单
- ❌ 存储空间固定，不可扩展
- ❌ 空间浪费或溢出

#### 2. 动态分配 ⭐推荐

\`\`\`c
#define InitSize 100  // 初始长度
typedef struct {
    ElemType *data;     // 指示动态分配数组的指针
    int MaxSize;        // 顺序表的最大容量
    int length;         // 顺序表当前长度
} SeqList;
\`\`\`

**初始化**：
\`\`\`c
L.data = (ElemType*)malloc(sizeof(ElemType) * InitSize);
L.length = 0;
L.MaxSize = InitSize;
\`\`\`

**扩容**：
\`\`\`c
// 当空间不足时，增加容量
ElemType *p = L.data;
L.data = (ElemType*)malloc(sizeof(ElemType) * (L.MaxSize + Increment));
for (int i = 0; i < L.length; i++) {
    L.data[i] = p[i];  // 复制数据
}
free(p);  // 释放原空间
L.MaxSize += Increment;
\`\`\`

### 三、顺序表的基本操作实现 ⭐⭐必考

#### 1. 插入操作 ListInsert(&L, i, e)

**算法思想**：
1. 判断 i 是否合法（1 ≤ i ≤ L.length+1）
2. 判断存储空间是否已满
3. 将第 i 个及之后的元素后移一位
4. 在位置 i 放入元素 e
5. 表长加 1

**代码实现**：
\`\`\`c
bool ListInsert(SeqList &L, int i, ElemType e) {
    if (i < 1 || i > L.length + 1)  // 判断范围
        return false;
    if (L.length >= L.MaxSize)      // 判断是否已满
        return false;
    
    for (int j = L.length; j >= i; j--)  // 后移元素
        L.data[j] = L.data[j-1];
    
    L.data[i-1] = e;   // 插入元素（注意下标）
    L.length++;        // 长度加1
    return true;
}
\`\`\`

**时间复杂度分析**：
- **最好情况**：在表尾插入（i = n+1），不需移动元素，O(1)
- **最坏情况**：在表头插入（i = 1），需移动 n 个元素，O(n)
- **平均情况**：平均移动 n/2 个元素，**O(n)** ⭐

**例题**：在长度为 n 的顺序表中，在第 i 个位置插入元素，需要移动多少个元素？
**答**：n - i + 1 个元素

#### 2. 删除操作 ListDelete(&L, i, &e)

**算法思想**：
1. 判断 i 是否合法（1 ≤ i ≤ L.length）
2. 取出被删除的元素
3. 将第 i+1 个及之后的元素前移一位
4. 表长减 1

**代码实现**：
\`\`\`c
bool ListDelete(SeqList &L, int i, ElemType &e) {
    if (i < 1 || i > L.length)  // 判断范围
        return false;
    
    e = L.data[i-1];  // 保存被删除元素
    
    for (int j = i; j < L.length; j++)  // 前移元素
        L.data[j-1] = L.data[j];
    
    L.length--;  // 长度减1
    return true;
}
\`\`\`

**时间复杂度分析**：
- **最好情况**：删除表尾元素（i = n），不需移动，O(1)
- **最坏情况**：删除表头元素（i = 1），需移动 n-1 个元素，O(n)
- **平均情况**：平均移动 (n-1)/2 个元素，**O(n)** ⭐

#### 3. 按位查找 GetElem(L, i)

\`\`\`c
ElemType GetElem(SeqList L, int i) {
    if (i < 1 || i > L.length)
        return ERROR;
    return L.data[i-1];  // 随机访问，O(1)
}
\`\`\`

**时间复杂度**：**O(1)** ✅ 这是顺序表的最大优势！

#### 4. 按值查找 LocateElem(L, e)

\`\`\`c
int LocateElem(SeqList L, ElemType e) {
    for (int i = 0; i < L.length; i++) {
        if (L.data[i] == e)
            return i + 1;  // 返回位序（从1开始）
    }
    return 0;  // 未找到
}
\`\`\`

**时间复杂度**：**O(n)**

### 四、顺序表的特点总结

| 特点 | 说明 |
|------|------|
| **优点** | |
| 随机访问 | 通过下标可在 O(1) 时间内找到指定元素 ✅ |
| 存储密度高 | 只存储数据元素，不存储额外信息 |
| 缓存友好 | 物理位置连续，CPU 缓存命中率高 |
| **缺点** | |
| 插入删除慢 | 需要移动大量元素，O(n) ❌ |
| 需预分配空间 | 可能造成浪费或溢出 |
| 扩展困难 | 动态扩容需要复制数据 |

### 五、考研重点 ⭐⭐⭐

**必考知识点**：
1. ⭐⭐ 插入/删除操作的平均移动次数
2. ⭐⭐ 时间复杂度分析（最好、最坏、平均）
3. ⭐ 静态分配 vs 动态分配的区别
4. ⭐ 顺序表的优缺点
5. ⭐⭐⭐ 顺序表的代码实现（插入、删除、查找）
6. ⭐⭐ 动态扩容的实现原理

**典型考题**：
> 【2017统考】对于一个长度为 n 的顺序表，删除第 i 个元素需要移动（）个元素。
> A. i  B. n-i  C. n-i+1  D. n-i-1
> 
> **答案**：B（删除第 i 个，需要将后面的 n-i 个元素前移）

### 六、顺序表常见算法题 ⭐⭐

#### 1. 删除所有值为x的元素

**题目**：删除顺序表中所有值为 x 的元素

**方法一：暴力法**
\`\`\`c
void DeleteAllX(SeqList &L, ElemType x) {
    int k = 0;  // 记录值为x的元素个数
    for (int i = 0; i < L.length; i++) {
        if (L.data[i] == x) {
            k++;
        } else {
            L.data[i - k] = L.data[i];  // 前移k个位置
        }
    }
    L.length -= k;
}
\`\`\`

**方法二：双指针法（推荐）**
\`\`\`c
void DeleteAllX_V2(SeqList &L, ElemType x) {
    int k = 0;  // k记录不等于x的元素个数
    for (int i = 0; i < L.length; i++) {
        if (L.data[i] != x) {
            L.data[k] = L.data[i];
            k++;
        }
    }
    L.length = k;
}
\`\`\`

**时间复杂度**：O(n)  
**空间复杂度**：O(1)

**考研要点**：
- ⚠️ 不要每删除一个就移动后面所有元素，那样是O(n²)
- ⚠️ 使用"覆盖"思想，只遍历一次

#### 2. 逆置顺序表

**题目**：将顺序表就地逆置（不使用额外空间）

**算法思想**：首尾交换，向中间靠拢

\`\`\`c
void Reverse(SeqList &L) {
    ElemType temp;
    for (int i = 0; i < L.length / 2; i++) {
        temp = L.data[i];
        L.data[i] = L.data[L.length - 1 - i];
        L.data[L.length - 1 - i] = temp;
    }
}
\`\`\`

**时间复杂度**：O(n)  
**空间复杂度**：O(1)

**考研要点**：
- ⚠️ 只需遍历一半即可
- ⚠️ 这是数组逆置的经典算法

#### 3. 合并两个有序顺序表

**题目**：将两个递增有序的顺序表合并为一个递增有序的顺序表

**算法思想**：归并排序的合并步骤

\`\`\`c
bool Merge(SeqList A, SeqList B, SeqList &C) {
    if (A.length + B.length > C.MaxSize)
        return false;  // 空间不足
    
    int i = 0, j = 0, k = 0;
    
    while (i < A.length && j < B.length) {
        if (A.data[i] <= B.data[j])
            C.data[k++] = A.data[i++];
        else
            C.data[k++] = B.data[j++];
    }
    
    // 处理剩余部分
    while (i < A.length)
        C.data[k++] = A.data[i++];
    while (j < B.length)
        C.data[k++] = B.data[j++];
    
    C.length = k;
    return true;
}
\`\`\`

**时间复杂度**：O(m+n)  
**空间复杂度**：O(m+n)，需要新空间存储结果

**考研要点**：
- ⭐⭐ 归并思想是重要考点
- ⚠️ 注意边界条件判断
- ⚠️ 可以优化为原地合并（但更复杂）

#### 4. 查找第k小的元素

**题目**：在无序顺序表中查找第k小的元素

**方法一：排序后查找**
- 先排序 O(n log n)，再直接访问 O(1)
- 总时间复杂度：O(n log n)

**方法二：快速选择算法（推荐）**
- 基于快速排序的partition思想
- 平均时间复杂度：O(n)

\`\`\`c
ElemType FindKthSmallest(SeqList L, int k) {
    // 简化版，实际需要使用partition函数
    // 这里仅展示思路
    int left = 0, right = L.length - 1;
    
    while (left <= right) {
        int pivot = Partition(L.data, left, right);
        if (pivot == k - 1)
            return L.data[pivot];
        else if (pivot < k - 1)
            left = pivot + 1;
        else
            right = pivot - 1;
    }
    return ERROR;
}
\`\`\`

**考研要点**：
- ⭐⭐ 快速选择算法比排序更高效
- ⚠️ 面试常考，需要掌握partition的实现
`
        },
        {
          id: '2.3',
          title: '链表',
          content: `## 2.3 链表 ⭐⭐⭐

### 一、链表的定义

**链表**：用**链式存储**的方式实现线性表。

- **存储方式**：逻辑上相邻的元素在物理位置上可以不相邻
- **节点结构**：数据域 + 指针域
- **特点**：通过指针链接各个节点

### 二、单链表 ⭐⭐

#### 1. 节点定义

\`\`\`c
typedef struct LNode {
    ElemType data;      // 数据域
    struct LNode *next; // 指针域，指向下一个节点
} LNode, *LinkList;
\`\`\`

#### 2. 头指针 vs 头结点 ⭐必考

**头指针**：
- 指向链表第一个节点的指针
- 标识链表，必不可少
- 若链表为空，头指针为 NULL

**头结点**：
- 在第一个数据节点之前附加的节点
- 数据域可以不存信息或存表长
- 指针域指向第一个数据节点
- **优点**：统一空表和非空表的处理，简化操作

**对比**：
| | 带头结点 | 不带头结点 |
|---|---|---|
| 空表判断 | head->next == NULL | head == NULL |
| 首节点操作 | 与其他节点一致 | 需要特殊处理 |
| 推荐使用 | ✅ 推荐 | ❌ 不推荐 |

#### 3. 单链表的基本操作

##### (1) 头插法建立单链表

**算法思想**：每次将新节点插入到头结点之后

\`\`\`c
LinkList List_HeadInsert(LinkList &L) {
    LNode *s;
    int x;
    L = (LinkList)malloc(sizeof(LNode));  // 创建头结点
    L->next = NULL;
    
    scanf("%d", &x);
    while (x != 9999) {  // 输入9999表示结束
        s = (LNode*)malloc(sizeof(LNode));
        s->data = x;
        s->next = L->next;  // 新节点指向原首节点
        L->next = s;        // 头结点指向新节点
        scanf("%d", &x);
    }
    return L;
}
\`\`\`

**特点**：读入数据的顺序与生成的链表中元素的顺序**相反**

**时间复杂度**：O(n)

##### (2) 尾插法建立单链表 ⭐推荐

**算法思想**：每次将新节点插入到链表尾部，需要维护一个尾指针

\`\`\`c
LinkList List_TailInsert(LinkList &L) {
    int x;
    L = (LinkList)malloc(sizeof(LNode));
    LNode *s, *r = L;  // r为表尾指针
    
    scanf("%d", &x);
    while (x != 9999) {
        s = (LNode*)malloc(sizeof(LNode));
        s->data = x;
        r->next = s;  // 尾节点指向新节点
        r = s;        // 更新尾指针
        scanf("%d", &x);
    }
    r->next = NULL;  // 尾节点指针置空
    return L;
}
\`\`\`

**特点**：读入数据的顺序与生成的链表中元素的顺序**相同** ✅

**时间复杂度**：O(n)

##### (3) 按序号查找 GetElem(L, i)

\`\`\`c
LNode *GetElem(LinkList L, int i) {
    if (i <= 0) return NULL;
    LNode *p = L->next;  // 从第一个节点开始
    int j = 1;
    
    while (p && j < i) {
        p = p->next;
        j++;
    }
    return p;  // 返回第i个节点的指针，若不存在则返回NULL
}
\`\`\`

**时间复杂度**：**O(n)** ❌ 这是链表的劣势

##### (4) 按值查找 LocateElem(L, e)

\`\`\`c
LNode *LocateElem(LinkList L, ElemType e) {
    LNode *p = L->next;
    while (p && p->data != e) {
        p = p->next;
    }
    return p;  // 找到返回节点指针，否则返回NULL
}
\`\`\`

**时间复杂度**：**O(n)**

##### (5) 插入操作 ListInsert(&L, i, e) ⭐⭐

**算法思想**：
1. 找到第 i-1 个节点（前驱节点）
2. 创建新节点
3. 新节点指向原第 i 个节点
4. 第 i-1 个节点指向新节点

\`\`\`c
bool ListInsert(LinkList &L, int i, ElemType e) {
    if (i < 1) return false;
    
    LNode *p = L;  // 从头结点开始
    int j = 0;
    
    // 找到第 i-1 个节点
    while (p && j < i - 1) {
        p = p->next;
        j++;
    }
    
    if (!p) return false;  // i 超过表长
    
    LNode *s = (LNode*)malloc(sizeof(LNode));
    s->data = e;
    s->next = p->next;  // 新节点指向原第i个节点
    p->next = s;        // 第i-1个节点指向新节点
    return true;
}
\`\`\`

**关键点**：
- ⚠️ 必须**先连后断**：先让 s->next = p->next，再 p->next = s
- 如果已知前驱节点指针 p，插入操作的时间复杂度为 **O(1)** ✅

**时间复杂度**：
- 已知前驱节点：O(1)
- 需要查找前驱：O(n)

##### (6) 删除操作 ListDelete(&L, i, &e) ⭐⭐

**算法思想**：
1. 找到第 i-1 个节点（前驱节点）
2. 保存第 i 个节点的值
3. 前驱节点指向第 i+1 个节点
4. 释放第 i 个节点的内存

\`\`\`c
bool ListDelete(LinkList &L, int i, ElemType &e) {
    if (i < 1) return false;
    
    LNode *p = L;
    int j = 0;
    
    // 找到第 i-1 个节点
    while (p->next && j < i - 1) {
        p = p->next;
        j++;
    }
    
    if (!p->next) return false;  // 第i个节点不存在
    
    LNode *q = p->next;  // q指向待删除节点
    e = q->data;         // 保存值
    p->next = q->next;   // 断开连接
    free(q);             // 释放内存
    return true;
}
\`\`\`

**关键点**：
- ⚠️ 必须先保存待删除节点指针，再断开连接
- 如果已知前驱节点指针 p，删除操作的时间复杂度为 **O(1)** ✅

**时间复杂度**：
- 已知前驱节点：O(1)
- 需要查找前驱：O(n)

##### (7) 求表长 Length(L)

\`\`\`c
int Length(LinkList L) {
    int len = 0;
    LNode *p = L->next;
    while (p) {
        len++;
        p = p->next;
    }
    return len;
}
\`\`\`

**时间复杂度**：O(n)

### 三、双链表 ⭐

#### 1. 节点定义

\`\`\`c
typedef struct DNode {
    ElemType data;           // 数据域
    struct DNode *prior;     // 前驱指针
    struct DNode *next;      // 后继指针
} DNode, *DLinkList;
\`\`\`

#### 2. 特点
- ✅ 可以**双向遍历**
- ✅ 删除节点时无需查找前驱（通过 prior 指针）
- ❌ 每个节点多一个指针域，空间开销大

#### 3. 插入操作（在 p 节点后插入 s 节点）

\`\`\`c
s->next = p->next;
if (p->next) p->next->prior = s;
s->prior = p;
p->next = s;
\`\`\`

**注意**：顺序不能错！

#### 4. 删除操作（删除 p 的后继节点 q）

\`\`\`c
p->next = q->next;
if (q->next) q->next->prior = p;
free(q);
\`\`\`

### 四、循环链表 ⭐

#### 1. 循环单链表
- 最后一个节点的 next 指针指向**头结点**
- 判空条件：L->next == L
- 可以从任意节点出发遍历整个链表

#### 2. 循环双链表
- 头结点的 prior 指向**尾节点**
- 尾节点的 next 指向**头结点**
- 判空条件：L->next == L && L->prior == L

### 五、静态链表 ⭐

**定义**：用**数组**描述的链表，适用于不支持指针的语言（如 BASIC、Java早期版本）

\`\`\`c
#define MaxSize 50
typedef struct {
    ElemType data;  // 数据域
    int next;       // 游标，表示下一个元素的数组下标
} SLinkList[MaxSize];
\`\`\`

**特点**：
- ✅ 不需要指针
- ❌ 需要预先分配较大空间
- 以 next == -1 表示链表结束

### 六、顺序表 vs 链表对比 ⭐⭐⭐必考

| 比较项 | 顺序表 | 链表 |
|--------|--------|------|
| **存取方式** | 随机访问 O(1) ✅ | 顺序访问 O(n) |
| **插入删除** | 需移动元素 O(n) | 只需修改指针 O(1)* |
| **空间分配** | 需预分配，可能浪费 | 动态分配，灵活 |
| **存储密度** | 高（只存数据）✅ | 低（还需存指针） |
| **缓存性能** | 好（连续存储）✅ | 差（分散存储） |

**选择原则**：
- 🔹 频繁查找 → 顺序表
- 🔹 频繁增删 → 链表
- 🔹 表长变化大 → 链表
- 🔹 对空间要求高 → 顺序表

### 七、考研重点 ⭐⭐⭐

**高频考点**：
1. ⭐⭐⭐ 单链表的头插法和尾插法
2. ⭐⭐⭐ 插入和删除操作的代码实现
3. ⭐⭐ 头指针和头结点的区别
4. ⭐⭐ 顺序表和链表的比较
5. ⭐ 双链表和循环链表的特点
6. ⭐⭐⭐ 链表相关算法题（逆置、合并、查找倒数第k个节点等）
7. ⭐⭐ 静态链表的实现原理

**典型考题**：
> 【2020统考】已知一个带有表头节点的单链表，假设该链表只给出了头指针 L，在不改变链表的前提下，请设计一个尽可能高效的算法，查找链表中倒数第 k 个位置上的节点。
> 
> **思路**：使用双指针法，两个指针相距 k 个节点，同时向后移动

### 八、链表常见算法题 ⭐⭐⭐必考

#### 1. 链表逆置（反转）

**题目**：将带头结点的单链表就地逆置

**算法思想**：遍历链表，将每个节点的next指向前一个节点

\`\`\`c
LinkList Reverse(LinkList &L) {
    LNode *p = L->next;  // p指向第一个数据节点
    LNode *r;
    L->next = NULL;      // 断开头结点与后续节点的连接
    
    while (p) {
        r = p->next;     // 保存p的后继
        p->next = L->next;  // 将p插入到头结点之后
        L->next = p;
        p = r;           // p指向下一个待处理节点
    }
    return L;
}
\`\`\`

**时间复杂度**：O(n)  
**空间复杂度**：O(1)

**考研要点**：
- ⚠️ 这是"头插法"的变形应用
- ⚠️ 需要三个指针：p（当前节点）、r（后继节点）、L（头结点）
- ⚠️ 注意初始化 L->next = NULL

#### 2. 合并两个有序链表

**题目**：将两个递增有序的单链表合并为一个递增有序的单链表

**算法思想**：归并思想，每次取较小的节点尾插到新链表

\`\`\`c
LinkList Merge(LinkList &A, LinkList &B) {
    LNode *pa = A->next;  // pa指向A的第一个数据节点
    LNode *pb = B->next;  // pb指向B的第一个数据节点
    LNode *r = A;         // r始终指向新链表的尾节点
    
    A->next = NULL;       // A作为新链表的头结点
    
    while (pa && pb) {
        if (pa->data <= pb->data) {
            r->next = pa;
            r = pa;
            pa = pa->next;
        } else {
            r->next = pb;
            r = pb;
            pb = pb->next;
        }
    }
    
    // 将剩余部分链接到新链表
    if (pa) r->next = pa;
    if (pb) r->next = pb;
    
    free(B);  // 释放B的头结点
    return A;
}
\`\`\`

**时间复杂度**：O(m+n)，其中m、n分别为两个链表的长度  
**空间复杂度**：O(1)，利用原节点空间

**考研要点**：
- ⚠️ 这是"尾插法"的应用
- ⚠️ 不需要创建新节点，直接修改指针
- ⚠️ 注意处理剩余部分

#### 3. 查找倒数第k个节点（双指针法）

**题目**：查找单链表中倒数第k个节点

**算法思想**：设置两个指针p和q，初始都指向头结点。先让p走k步，然后p和q同时前进，当p到达末尾时，q指向的就是倒数第k个节点。

\`\`\`c
LNode* SearchKthFromEnd(LinkList L, int k) {
    LNode *p = L->next;
    LNode *q = L->next;
    
    // p先走k步
    for (int i = 0; i < k && p; i++) {
        p = p->next;
    }
    
    if (!p && i < k) return NULL;  // k超过链表长度
    
    // p和q同时前进
    while (p) {
        p = p->next;
        q = q->next;
    }
    
    return q;  // q即为倒数第k个节点
}
\`\`\`

**时间复杂度**：O(n)，只需遍历一次链表  
**空间复杂度**：O(1)

**考研要点**：
- ⭐⭐⭐ 双指针法是链表算法的重要技巧
- ⚠️ 注意边界条件判断（k是否合法）
- ⚠️ 这个方法比"先求长度再查找"更高效

#### 4. 判断链表是否有环

**题目**：判断单链表中是否存在环

**算法思想**：快慢指针法（Floyd判圈算法）。设置快指针fast（每次走2步）和慢指针slow（每次走1步），如果存在环，两者必定相遇。

\`\`\`c
bool HasCycle(LinkList L) {
    LNode *slow = L->next;
    LNode *fast = L->next;
    
    while (fast && fast->next) {
        slow = slow->next;        // 慢指针走1步
        fast = fast->next->next;  // 快指针走2步
        
        if (slow == fast) {
            return true;  // 相遇，有环
        }
    }
    
    return false;  // 无环
}
\`\`\`

**时间复杂度**：O(n)  
**空间复杂度**：O(1)

**考研要点**：
- ⭐⭐⭐ 快慢指针是经典算法
- ⚠️ 如果fast遇到NULL，说明无环
- ⚠️ 如果有环，可以进一步求环的入口点和长度

#### 5. 删除链表中的重复元素

**题目**：删除递增有序链表中的重复元素

**算法思想**：由于链表有序，重复元素必然相邻。遍历时比较当前节点和后继节点的值，若相同则删除后继节点。

\`\`\`c
void DeleteDuplicates(LinkList &L) {
    LNode *p = L->next;
    
    while (p && p->next) {
        if (p->data == p->next->data) {
            LNode *q = p->next;     // q指向待删除节点
            p->next = q->next;      // 跳过q
            free(q);                // 释放内存
        } else {
            p = p->next;            // 继续遍历
        }
    }
}
\`\`\`

**时间复杂度**：O(n)  
**空间复杂度**：O(1)

**考研要点**：
- ⚠️ 注意删除后不要移动p，因为新的后继可能仍然重复
- ⚠️ 如果不有序，需要使用哈希表或双重循环

### 九、易错点总结

**常见错误**：
1. ❌ 链表插入时忘记"先连后断"，导致链表断裂
2. ❌ 删除节点后忘记free()，造成内存泄漏
3. ❌ 访问空指针（未判断p是否为NULL就直接访问p->next）
4. ❌ 混淆位序和下标（位序从1开始，数组下标从0开始）
5. ❌ 循环链表判空条件错误（应该是L->next == L，不是L->next == NULL）
6. ❌ 双链表插入时指针修改顺序错误

**答题技巧**：
- ✅ 画图辅助理解指针变化
- ✅ 先写伪代码，再写具体实现
- ✅ 注意边界条件（空表、只有一个节点、操作首/尾节点）
- ✅ 标注时间复杂度和空间复杂度

### 🔗 知识延伸：链表思想在树和图中的应用

> 💡 **重要概念**：链表的**指针链接思想**广泛应用于树和图的存储
> 
> - **二叉链表**（孩子表示法）：每个节点有两个指针域
>   - 📖 详见：5.3 二叉树的存储结构
>   - 结构：data + lchild + rchild
> 
> - **孩子兄弟表示法**：用链表存储一般树
>   - 📖 详见：5.6 树、森林
>   - 结构：data + firstchild + nextsibling
> 
> - **邻接表**：图的链式存储
>   - 📖 详见：6.2 图的存储结构
>   - 每个顶点对应一个链表，存储其邻接点
> 
> 💡 **理解要点**：掌握链表后，学习树和图的存储会更容易！
`
        },
        {
          id: '2.4',
          title: '顺序表和链表的比较',
          content: `## 2.4 顺序表和链表的比较 ⭐⭐

### 一、综合对比表

| 比较维度 | 顺序表 | 链表 |
|----------|--------|------|
| **逻辑结构** | 都是线性表，元素间是一对一的线性关系 |
| **存储结构** | 顺序存储（物理相邻） | 链式存储（物理可不相邻） |
| **基本操作** | |
| 按位查找 | O(1) ✅ | O(n) |
| 按值查找 | O(n) | O(n) |
| 插入 | O(n) | O(1)* |
| 删除 | O(n) | O(1)* |
| **空间性能** | |
| 存储密度 | 1（只存数据）✅ | <1（还需存指针） |
| 空间分配 | 静态/动态预分配 | 动态按需分配 |
| 扩容代价 | 需复制全部数据 | 无需扩容 |
| **其他特性** | |
| 缓存友好性 | 好（局部性好）✅ | 差（跳跃访问） |
| 实现难度 | 简单 | 较复杂 |
| 边界处理 | 需注意越界 | 需注意空指针 |

*注：链表的 O(1) 是已知前驱节点的情况

### 二、选择策略

#### 基于运算特征选择

**选择顺序表**：
1. ✅ 稳定，很少进行插入删除操作
2. ✅ 需要频繁按序号访问元素
3. ✅ 事先知道表的大致长度

**选择链表**：
1. ✅ 需要频繁进行插入删除操作
2. ✅ 表长变化较大，难以估计
3. ✅ 不关心随机访问效率

#### 基于环境因素选择

**考虑语言支持**：
- C/C++：两者都方便实现
- Java/Python：内置动态数组，优先使用顺序表思想

**考虑硬件特性**：
- 现代 CPU 缓存机制使得顺序表的实际性能往往优于理论分析
- 小数据量时，顺序表的常数因子更小

### 三、实际应用建议

**工程实践**：
- 📌 大多数情况下，优先使用**动态数组**（如 C++ 的 vector、Java 的 ArrayList）
- 📌 只有在频繁插入删除的场景才考虑链表
- 📌 现代编程语言的标准库已经做了很好的优化

**考研答题**：
- 📝 明确写出选择的理由（基于时间复杂度、空间复杂度）
- 📝 结合具体场景分析（如"频繁查找"、"表长不确定"等关键词）
- 📝 必要时给出复杂度分析

### 四、常见误区

❌ **误区1**：链表插入删除一定是 O(1)
- ✅ 正解：只有已知前驱节点时才是 O(1)，如果需要查找前驱则是 O(n)

❌ **误区2**：链表比顺序表更节省空间
- ✅ 正解：链表每个节点需要额外的指针域，存储密度更低

❌ **误区3**：顺序表不能动态扩容
- ✅ 正解：可以通过 realloc 或重新 malloc 实现动态扩容，只是代价较大

### 五、考研真题精选

> 【2018统考】下列关于线性表的说法中，正确的是（）。
> A. 线性表的顺序存储结构优于链式存储结构
> B. 线性表的链式存储结构优于顺序存储结构
> C. 在实际应用中，应根据具体问题的特点选择合适的存储结构
> D. 以上都不对
> 
> **答案**：C（没有绝对优劣，只有适合与否）

> 【2016统考】对于长度为 n 的线性表，在最坏情况下，下列各排序算法所对应的比较次数正确的是（）。
> （此题考查不同存储结构对算法的影响）
> 
> **要点**：顺序表支持随机访问，某些算法（如二分查找）效率更高
`
        }
      ]
    },
    {
      id: 'ch3',
      number: 3,
      title: '栈和队列',
      score: '5-8分',
      importance: 'high',
      mindMap: `mindmap
  root((栈和队列))
    栈 ⭐⭐
      特点
        LIFO
        只在一端操作
      存储结构
        顺序栈
        链栈
      应用
        括号匹配
        表达式求值
        递归
    队列 ⭐⭐
      特点
        FIFO
        两端操作
      存储结构
        顺序队列
          循环队列 ⭐
        链队列
      应用
        层次遍历
        缓冲区
    特殊队列
      双端队列
      优先队列`,
      sections: [
        {
          id: '3.1',
          title: '栈的基本概念',
          content: `## 3.1 栈的基本概念 ⭐⭐

### 一、栈的定义

**栈（Stack）**：只允许在**一端**进行插入或删除操作的**线性表**。

- **栈顶（Top）**：允许插入和删除的一端
- **栈底（Bottom）**：不允许操作的一端
- **特点**：**后进先出**（LIFO - Last In First Out）

### 二、栈的基本操作

1. **InitStack(&S)** - 初始化栈
2. **StackEmpty(S)** - 判断栈是否为空
3. **Push(&S, x)** - 进栈（入栈）⭐
4. **Pop(&S, &x)** - 出栈（退栈）⭐
5. **GetTop(S, &x)** - 读取栈顶元素
6. **DestroyStack(&S)** - 销毁栈

**注意**：
- ⚠️ 入栈前判断栈是否已满（上溢）
- ⚠️ 出栈前判断栈是否为空（下溢）

### 三、栈的数学性质 ⭐⭐⭐必考

**n个不同元素进栈，出栈元素不同排列的个数**：

\`\`\`
Catalan数 = C(2n,n)/(n+1) = (2n)!/((n+1)!×n!)
\`\`\`

**例题1**：3个元素a,b,c进栈，有多少种出栈序列？

**解**：Catalan(3) = C(6,3)/4 = 20/4 = 5种

分别为：abc, acb, bac, bca, cba（cab不可能）

**例题2**：若栈的输入序列是P₁,P₂,...,Pₙ，输出序列是1,2,3,...,n
- 若P=1，则P₁**不可能是2**
  - 原因：P₃=1说明1是第三个入栈的，那么P₁和P₂必须在1之前入栈
  - 如果P₁=2，则2比1先入栈，但输出要求1先出，矛盾
  - 所以P₁不可能是2，只能是大于1的数
  
- 若P₃=3，则P₁**可能是2**
  - 例如：P₁=2, P₂=1, P₃=3，入栈顺序为2,1,3
  - 出栈：3先出（栈顶），然后1出，最后2出，得到3,1,2
  - 或者P₁=1, P₂=2, P₃=3，入栈顺序1,2,3，出栈3,2,1
  - 所以P₁可能是1也可能是2

**判断合法出栈序列的方法**：
- 对于输出序列中的每个元素，检查其后面比它小的元素是否按递减顺序排列
- 例如：3,1,2是非法的（1后面有2，但2>1）
- 例如：3,2,1是合法的（2和1都小于3，且2>1递减）

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ 栈的LIFO特性
2. ⭐⭐⭐ Catalan数计算合法出栈序列
3. ⭐⭐ 栈的应用场景
`        },
        {
          id: '3.2',
          title: '顺序栈和链栈',
          content: `## 3.2 顺序栈和链栈 ⭐⭐

### 一、顺序栈

#### 1. 定义

用**顺序存储**实现的栈，即分配一块连续的存储区域存放栈中的元素。

\`\`\`c
#define MaxSize 50
typedef struct {
    ElemType data[MaxSize];  // 存放栈中元素
    int top;                  // 栈顶指针
} SqStack;
\`\`\`

#### 2. 初始化

\`\`\`c
void InitStack(SqStack &S) {
    S.top = -1;  // 初始化栈顶指针
}
\`\`\`

#### 3. 判栈空

\`\`\`c
bool StackEmpty(SqStack S) {
    if (S.top == -1)
        return true;
    else
        return false;
}
\`\`\`

#### 4. 进栈操作 ⭐⭐

\`\`\`c
bool Push(SqStack &S, ElemType x) {
    if (S.top == MaxSize - 1)  // 栈满
        return false;
    S.data[++S.top] = x;  // 指针先加1，再入栈
    return true;
}
\`\`\`

**时间复杂度**：O(1)

#### 5. 出栈操作 ⭐⭐

\`\`\`c
bool Pop(SqStack &S, ElemType &x) {
    if (S.top == -1)  // 栈空
        return false;
    x = S.data[S.top--];  // 先出栈，指针再减1
    return true;
}
\`\`\`

**时间复杂度**：O(1)

#### 6. 读栈顶元素

\`\`\`c
bool GetTop(SqStack S, ElemType &x) {
    if (S.top == -1)
        return false;
    x = S.data[S.top];
    return true;
}
\`\`\`

**时间复杂度**：O(1)

#### 7. 共享栈 ⭐⭐⭐必考

**定义**：利用栈底位置相对不变的特性，让两个顺序栈共享一个一维数组空间。

\`\`\`c
typedef struct {
    ElemType data[MaxSize];
    int top0;  // 0号栈栈顶（初始值为-1）
    int top1;  // 1号栈栈顶（初始值为MaxSize或n）
} ShStack;
\`\`\`

**栈满条件**：top1 - top0 == 1（或top0 + 1 == top1）

**解释**：
- 栈0从左向右增长（top0从-1开始递增）
- 栈1从右向左增长（top1从n开始递减）
- 当两个栈顶指针相邻时（差值为1），说明空间已满
- ⚠️ 注意：不是top0 == top1，因为这样会重叠一个位置

**优点**：更有效地利用存储空间

### 二、链栈

#### 1. 定义

用**链式存储**实现的栈，通常采用**单链表**实现。

\`\`\`c
typedef struct Linknode {
    ElemType data;
    struct Linknode *next;
} *LiStack;
\`\`\`

**注意**：所有操作都在**链表头部**进行（头插法）

#### 2. 优点

- ✅ 不存在栈满上溢的情况（除非内存耗尽）
- ✅ 适合多个栈共享空间
- ❌ 每个结点需要额外的指针域

### 三、顺序栈 vs 链栈

| 比较项 | 顺序栈 | 链栈 |
|--------|--------|------|
| 存储空间 | 预先分配，可能浪费 | 动态分配，灵活 |
| 栈满判断 | 需要判断 | 几乎不会满 |
| 存取速度 | 快（连续存储） | 稍慢（指针跳转） |
| 扩展性 | 差 | 好 |

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ 顺序栈的进栈、出栈操作代码
2. ⭐⭐ 栈顶指针的变化（++top vs top++）
3. ⭐⭐ 共享栈的栈满条件
4. ⭐ 链栈的操作特点
`        },
        {
          id: '3.3',
          title: '栈的应用',
          content: `## 3.3 栈的应用 ⭐⭐⭐

### 一、括号匹配 ⭐⭐

**问题**：检查表达式中的括号是否正确匹配

**算法思想**：
1. 遇到左括号，入栈
2. 遇到右括号，弹出栈顶元素并匹配
3. 最后栈应为空

\`\`\`c
bool bracketCheck(char str[], int length) {
    SqStack S;
    InitStack(S);
    
    for (int i = 0; i < length; i++) {
        if (str[i] == '(' || str[i] == '[' || str[i] == '{') {
            Push(S, str[i]);  // 左括号入栈
        } else {
            if (StackEmpty(S))  // 栈空，匹配失败
                return false;
            
            char topElem;
            Pop(S, topElem);
            
            // 检查是否匹配
            if (str[i] == ')' && topElem != '(')
                return false;
            if (str[i] == ']' && topElem != '[')
                return false;
            if (str[i] == '}' && topElem != '{')
                return false;
        }
    }
    
    return StackEmpty(S);  // 栈空则匹配成功
}
\`\`\`

**时间复杂度**：O(n)

### 二、表达式求值 ⭐⭐⭐必考

#### 1. 三种表达式

| 类型 | 说明 | 示例 |
|------|------|------|
| **中缀表达式** | 运算符在操作数中间 | A + B |
| **前缀表达式** | 运算符在操作数前面 | + A B |
| **后缀表达式** | 运算符在操作数后面 | A B + |

#### 2. 中缀转后缀（逆波兰表达式）⭐⭐⭐

**算法思想**（使用栈）：
1. 遇到操作数，直接输出
2. 遇到运算符：
   - 若栈空或栈顶为'('，直接入栈
   - 若优先级高于栈顶，入栈
   - 否则，弹出栈顶并输出，直到可以入栈
3. 遇到'('，直接入栈
4. 遇到')'，弹出栈顶并输出，直到遇到'('

**例题**：中缀表达式 A + B × (C - D) - E / F 的后缀表达式？

**解**：A B C D - × + E F / -

#### 3. 后缀表达式求值 ⭐⭐

**算法思想**：
1. 从左到右扫描后缀表达式
2. 遇到操作数，入栈
3. 遇到运算符，弹出两个操作数，计算后结果入栈
4. 最后栈中剩下的就是结果

### 三、递归 ⭐⭐

**递归**：函数直接或间接调用自身

**栈的作用**：系统使用**递归工作栈**保存每层递归的信息

**递归的三个要素**：
1. 递归出口（终止条件）
2. 递归体（缩小问题规模）
3. 递归调用

**例题**：斐波那契数列

\`\`\`c
int Fib(int n) {
    if (n == 0) return 0;      // 递归出口
    if (n == 1) return 1;      // 递归出口
    return Fib(n-1) + Fib(n-2); // 递归体
}
\`\`\`

**缺点**：可能存在大量重复计算

**改进**：使用动态规划或记忆化搜索

### 💡 重要应用：栈在树遍历中的作用

> 🔗 **核心关联**：树的**非递归遍历**算法都依赖栈实现
> 
> - **先序遍历（非递归）**：使用栈模拟递归过程
>   - 📖 详见：5.4 二叉树的遍历
> 
> - **中序遍历（非递归）**：利用栈保存未访问的节点
>   - 📖 详见：5.4 二叉树的遍历
> 
> - **后序遍历（非递归）**：需要两个栈或标记法
>   - 📖 详见：5.4 二叉树的遍历
> 
> 💡 **理解要点**：递归的本质就是系统栈的调用，非递归遍历是手动维护栈

### 四、栈的应用场景总结 ⭐⭐⭐

**栈的典型应用**：
1. ✅ **括号匹配** - 检查表达式括号是否匹配
2. ✅ **表达式求值** - 中缀转后缀、后缀求值
3. ✅ **递归实现** - 系统使用递归工作栈
4. ✅ **深度优先搜索（DFS）** - 图的遍历
5. ✅ **函数调用** - 系统栈保存返回地址
6. ✅ **浏览器后退** - 历史记录用栈存储
7. ✅ **撤销操作** - 编辑器用栈保存操作历史

**非栈的应用**：
- ❌ **缓冲区** - 缓冲区通常使用**队列**（FIFO），而不是栈
- ❌ **层次遍历（BFS）** - 使用队列

**考研重点提示**：
- 缓冲区、消息队列、进程调度等都使用**队列**而非栈
- 递归算法 vs 非递归算法：非递归通常效率更高（避免函数调用开销）

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 括号匹配算法
2. ⭐⭐⭐ 中缀转后缀算法
3. ⭐⭐⭐ 后缀表达式求值
4. ⭐⭐ 递归的实现机制
5. ⭐ 栈在DFS中的应用
`        },
        {
          id: '3.4',
          title: '队列的基本概念',
          content: `## 3.4 队列的基本概念 ⭐⭐

### 一、队列的定义

**队列（Queue）**：只允许在**一端**进行插入，在**另一端**进行删除的**线性表**。

- **队头（Front）**：允许删除的一端
- **队尾（Rear）**：允许插入的一端
- **特点**：**先进先出**（FIFO - First In First Out）

### 二、队列的基本操作

1. **InitQueue(&Q)** - 初始化队列
2. **QueueEmpty(Q)** - 判断队列是否为空
3. **EnQueue(&Q, x)** - 入队 ⭐
4. **DeQueue(&Q, &x)** - 出队 ⭐
5. **GetHead(Q, &x)** - 读取队头元素

**注意**：
- ⚠️ 入队前判断队列是否已满
- ⚠️ 出队前判断队列是否为空

### 三、队列的数学性质

**n个不同元素进队，出队顺序唯一**（与栈不同）

因为队列是FIFO，所以出队顺序只能是进入的顺序。

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ 队列的FIFO特性
2. ⭐⭐ 队列与栈的区别
3. ⭐⭐ 循环队列的实现
`        },
        {
          id: '3.5',
          title: '顺序队列和链队列',
          content: `## 3.5 顺序队列和链队列 ⭐⭐⭐

### 一、顺序队列

#### 1. 定义

\`\`\`c
#define MaxSize 50
typedef struct {
    ElemType data[MaxSize];  // 存放队列元素
    int front, rear;          // 队头指针和队尾指针
} SqQueue;
\`\`\`

#### 2. 初始化

\`\`\`c
void InitQueue(SqQueue &Q) {
    Q.front = Q.rear = 0;  // 初始时队头队尾指向0
}
\`\`\`

#### 3. 判队空

\`\`\`c
bool QueueEmpty(SqQueue Q) {
    if (Q.front == Q.rear)
        return true;
    else
        return false;
}
\`\`\`

#### 4. 入队操作 ⭐⭐

\`\`\`c
bool EnQueue(SqQueue &Q, ElemType x) {
    if ((Q.rear + 1) % MaxSize == Q.front)  // 队满
        return false;
    Q.data[Q.rear] = x;
    Q.rear = (Q.rear + 1) % MaxSize;  // 队尾指针加1取模
    return true;
}
\`\`\`

**时间复杂度**：O(1)

#### 5. 出队操作 ⭐⭐

\`\`\`c
bool DeQueue(SqQueue &Q, ElemType &x) {
    if (Q.front == Q.rear)  // 队空
        return false;
    x = Q.data[Q.front];
    Q.front = (Q.front + 1) % MaxSize;  // 队头指针加1取模
    return true;
}
\`\`\`

**时间复杂度**：O(1)

### 二、循环队列 ⭐⭐⭐必考

#### 1. 为什么要用循环队列？

**问题**：普通顺序队列存在"假溢出"现象
- 队尾指针已到数组末尾，但前面还有空闲空间
- 无法继续入队

**解决**：将顺序队列想象为一个环，即**循环队列**

#### 2. 循环队列的关键操作 ⭐⭐⭐必考

**重要概念**：
- 数组A[0...n]表示数组有**n+1个元素**（从0到n）
- 数组A[0...n-1]表示数组有**n个元素**（从0到n-1）

**队头指针进1**：Q.front = (Q.front + 1) % MaxSize

**队尾指针进1**：Q.rear = (Q.rear + 1) % MaxSize

**队列长度**：(Q.rear - Q.front + MaxSize) % MaxSize

**循环队列的初始化**（2011统考真题⭐⭐⭐）：
- 若数组为A[0...n-1]，front指向队头元素，rear指向队尾元素
- 要求第一个元素存储在A[0]处
- 初始时：front = 0, rear = n-1
- 解释：rear = n-1表示rear指向最后一个位置，当第一个元素入队时
  - rear = (n-1 + 1) % n = 0，正好指向A[0]

**特殊数组表示法**：
- 若数组为A[0...n]（注意是到n，共n+1个元素）
- 入队操作：rear = (rear + 1) mod (n+1)
- ️ 注意区分A[0...n]和A[0...n-1]的模运算

#### 3. 队满和队空的判断方法 ⭐⭐⭐必考

**方法1**：牺牲一个存储单元（最常用）⭐⭐⭐
- 队满条件：(Q.rear + 1) % MaxSize == Q.front
- 队空条件：Q.front == Q.rear
- **解释**：当rear的下一个位置是front时，认为队满（留一个空位）

**方法2**：增加size变量记录元素个数
- 队满条件：size == MaxSize
- 队空条件：size == 0

**方法3**：增加tag变量标记最近操作
- tag=1表示最近进行了入队，tag=0表示最近进行了出队
- 队满：front == rear && tag == 1
- 队空：front == rear && tag == 0

**front和rear的含义变化**：
- **情况A**：front指向队头元素的前一位置，rear指向队尾元素
  - 队空：Q.rear == Q.front
- **情况B**：front指向队头元素，rear指向队尾元素的下一位置
  - 队空：Q.rear == Q.front
- ️ 注意题目中front和rear的具体定义！

### 三、链队列

#### 1. 定义

\`\`\`c
typedef struct LinkNode {
    ElemType data;
    struct LinkNode *next;
} LinkNode;

typedef struct {
    LinkNode *front, *rear;  // 队头和队尾指针
} LinkQueue;
\`\`\`

#### 2. 初始化

\`\`\`c
void InitQueue(LinkQueue &Q) {
    Q.front = Q.rear = (LinkNode*)malloc(sizeof(LinkNode));
    Q.front->next = NULL;
}
\`\`\`

#### 3. 入队（尾插法）

\`\`\`c
void EnQueue(LinkQueue &Q, ElemType x) {
    LinkNode *s = (LinkNode*)malloc(sizeof(LinkNode));
    s->data = x;
    s->next = NULL;
    Q.rear->next = s;  // 新节点接到队尾
    Q.rear = s;        // 更新队尾指针
}
\`\`\`

#### 4. 出队（头删法）

\`\`\`c
bool DeQueue(LinkQueue &Q, ElemType &x) {
    if (Q.front == Q.rear)
        return false;
    
    LinkNode *p = Q.front->next;
    x = p->data;
    Q.front->next = p->next;
    
    if (Q.rear == p)  // 如果只有一个元素
        Q.rear = Q.front;
    
    free(p);
    return true;
}
\`\`\`

### 四、顺序队列 vs 链队列 ⭐⭐必考

| 比较项 | 顺序队列（循环队列） | 链队列 |
|--------|----------|--------|
| 空间 | 预先分配，可能浪费 | 动态分配，灵活 |
| 队满判断 | 需要判断 | 几乎不会满 |
| 存取速度 | 快（连续存储） | 稍慢（指针跳转） |
| **队列长度计算** | **可通过front和rear计算** | **不能直接计算，需遍历** ⭐ |
| 适用场景 | 元素个数可预估 | 元素个数不确定 |

**链队列的重要特性**：
- ✅ 优点：队列长度不受限制（动态分配）
- ❌ 缺点：不能根据front和rear指针直接计算队列长度（需遍历链表）
- ❌ 缺点：需要额外的指针域存储空间

**最适合用作队列的链表**：
- ✅ **带头尾指针的非循环单链表**（队头在链表头部，队尾在链表尾部）
- ❌ 只带头指针的非循环双链表（无法高效在队尾入队）

**单链表实现队列的要点**：
- 队头设在**链头**（便于出队操作O(1)）
- 队尾设在**链尾**（便于入队操作O(1)）
- 需要同时维护front和rear两个指针

**链队列删除操作**：
- 一般情况下：仅修改头指针（删除第一个节点）
- 特殊情况：当队列只有一个元素时，删除后队列为空
  - 此时需要同时修改头指针和尾指针（rear = front）
- 所以答案是：**头尾指针可能都要修改**

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 循环队列的队满、队空条件
2. ⭐⭐⭐ 循环队列的长度计算
3. ⭐⭐ 循环队列的入队、出队操作
4. ⭐⭐ 链队列的操作
5. ⭐ 假溢出问题及解决

### 💡 核心应用：队列在图算法中的作用

> 🔗 **最重要应用**：队列是**广度优先搜索（BFS）**的核心数据结构
> 
> - **图的BFS遍历**：使用队列实现层次遍历
>   - 📖 详见：6.3 图的遍历
>   - 思想：先访问的顶点，其邻接点也先被访问
> 
> - **树的层次遍历**：同样使用队列
>   - 📖 详见：5.4 二叉树的遍历
>   - 按层从上到下、从左到右访问节点
> 
> - **最短路径算法（BFS版）**：无权图的最短路径
>   - 📖 详见：6.5 最短路径
> 
> 💡 **理解要点**：队列的FIFO特性保证了“先发现的节点先扩展”
`        },
        {
          id: '3.6',
          title: '双端队列和应用',
          content: `## 3.6 双端队列和应用 ⭐

### 一、双端队列

**双端队列（Deque）**：允许在**两端**都可以进行入队和出队操作的队列。

#### 1. 分类

| 类型 | 说明 |
|------|------|
| **输入受限** | 一端可输入输出，另一端只可输出 |
| **输出受限** | 一端可输入输出，另一端只可输入 |

#### 2. 合法出队序列

**n个不同元素通过双端队列，可能的出队序列数**：
- 比栈多（Catalan数），比全排列少

### 二、队列的应用 ⭐⭐

#### 1. 层次遍历（BFS）⭐⭐⭐

**树的层次遍历**：
\`\`\`c
void LevelOrder(BiTree T) {
    LinkQueue Q;
    InitQueue(Q);
    
    if (T) {
        EnQueue(Q, T);
        while (!QueueEmpty(Q)) {
            BiTree p;
            DeQueue(Q, p);
            visit(p);  // 访问节点
            
            if (p->lchild)
                EnQueue(Q, p->lchild);
            if (p->rchild)
                EnQueue(Q, p->rchild);
        }
    }
}
\`\`\`

**图的广度优先搜索（BFS）**也使用队列

#### 2. 缓冲区管理

- CPU缓冲区
- 打印机队列
- 消息队列

#### 3. 其他应用

- 操作系统中的进程调度
- 网络数据包传输
- 广度优先搜索算法

### 三、考研重点

**高频考点**：
1. ⭐⭐⭐ 队列在层次遍历中的应用
2. ⭐⭐ 双端队列的特点
3. ⭐⭐ BFS算法中的队列使用
4. ⭐ 队列的实际应用场景
`        }
      ]
    },
    {
      id: 'ch4',
      number: 4,
      title: '串、数组和广义表',
      score: '2-5分',
      importance: 'low',
      mindMap: `mindmap
  root((串、数组和广义表))
    串 ⭐
      定义
      存储结构
      模式匹配
        BF算法
        KMP算法 ⭐⭐
    数组
      顺序存储
      矩阵压缩
        对称矩阵
        三角矩阵
        稀疏矩阵
    广义表
      定义
      存储结构
      基本运算`,
      sections: [
        {
          id: '4.1',
          title: '串的基本概念',
          content: `## 4.1 串的基本概念 ⭐

### 一、串的定义

**串（String）**：由零个或多个字符组成的**有限序列**。

- 记作：S = 'a₁a₂...aₙ'（n≥0）
- **串长**：n
- **空串**：n=0，记作 '' 或 Φ
- **空格串**：由一个或多个空格组成，如 '   '
- **子串**：串中任意个连续的字符组成的子序列
- **主串**：包含子串的串

**注意**：
- ⚠️ 空串 ≠ 空格串
- ⚠️ 子串必须是连续的

### 二、串的存储结构

#### 1. 定长顺序存储

\`\`\`c
#define MAXLEN 255
typedef struct {
    char ch[MAXLEN + 1];  // 存储串的一维数组
    int length;             // 串的当前长度
} SString;
\`\`\`

**特点**：简单，但可能截断

#### 2. 堆分配存储（动态数组）⭐推荐

\`\`\`c
typedef struct {
    char *ch;   // 按串长分配存储区，ch指向基地址
    int length; // 串的长度
} HString;
\`\`\`

**初始化**：
\`\`\`c
S.ch = (char*)malloc(n * sizeof(char));
\`\`\`

**优点**：灵活，不会截断

#### 3. 块链存储

每个结点可以存放多个字符，用链表连接。

**存储密度** = 数据域占比 / (数据域 + 指针域)

### 三、串的基本操作

1. **StrAssign(&T, chars)** - 赋值
2. **StrCopy(&T, S)** - 复制
3. **StrEmpty(S)** - 判空
4. **StrCompare(S, T)** - 比较
5. **StrLength(S)** - 求长度
6. **Concat(&T, S1, S2)** - 连接
7. **SubString(&Sub, S, pos, len)** - 求子串
8. **Index(S, T)** - 定位（模式匹配）⭐⭐⭐
9. **StrInsert(&S, pos, T)** - 插入
10. **StrDelete(&S, pos, len)** - 删除
11. **Replace(&S, T, V)** - 替换
12. **ClearString(&S)** - 清空
13. **DestroyString(&S)** - 销毁

### 四、考研重点

**高频考点**：
1. ⭐⭐ 串的三种存储结构
2. ⭐⭐ 基本操作的时间复杂度
3. ⭐⭐⭐ 模式匹配算法（BF和KMP）
`        },
        {
          id: '4.2',
          title: '串的模式匹配算法',
          content: `## 4.2 串的模式匹配算法 ⭐⭐⭐必考

### 一、BF算法（Brute-Force，暴力匹配）

**思想**：从主串的第一个字符开始，逐个与模式串比较，不匹配则回溯。

\`\`\`c
int Index_BF(SString S, SString T) {
    int i = 1, j = 1;  // i为主串指针，j为模式串指针
    
    while (i <= S.length && j <= T.length) {
        if (S.ch[i] == T.ch[j]) {
            i++;
            j++;  // 继续比较后续字符
        } else {
            i = i - j + 2;  // 主串指针回溯
            j = 1;           // 模式串指针重置
        }
    }
    
    if (j > T.length)
        return i - T.length;  // 匹配成功，返回位置
    else
        return 0;  // 匹配失败
}
\`\`\`

**时间复杂度**：
- 最好情况：O(m)（m为模式串长度）
- 最坏情况：O(n×m)（n为主串长度）
- 平均情况：O(n+m)

**缺点**：存在大量回溯，效率低

### 二、KMP算法 ⭐⭐⭐必考

**改进点**：主串指针不回溯，利用已匹配的信息

#### 1. next数组 ⭐⭐⭐

**定义**：next[j]表示模式串中前j-1个字符的最长相等前后缀长度+1

**计算公式**：
\`\`\`
next[j] = max{k | 1 < k < j 且 'p₁...pₖ₋₁' = 'pⱼ₋ₖ₊₁...pⱼ₋₁'}
\`\`\`

**手工计算方法**：
1. next[1] = 0
2. next[2] = 1
3. 对于j>2，找前j-1个字符的最长相等前后缀长度k，next[j] = k+1

**例题**：模式串 'abaabc' 的next数组？

| j | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| 模式串 | a | b | a | a | b | c |
| next[j] | 0 | 1 | 1 | 2 | 2 | 3 |

**计算过程**：
- next[1] = 0（规定）
- next[2] = 1（规定）
- next[3]：'ab'，无相等前后缀，next[3] = 1
- next[4]：'aba'，最长相等前后缀'a'，长度1，next[4] = 2
- next[5]：'abaa'，最长相等前后缀'a'，长度1，next[5] = 2
- next[6]：'abaab'，最长相等前后缀'ab'，长度2，next[6] = 3

#### 2. KMP算法实现

\`\`\`c
int Index_KMP(SString S, SString T, int next[]) {
    int i = 1, j = 1;
    
    while (i <= S.length && j <= T.length) {
        if (j == 0 || S.ch[i] == T.ch[j]) {
            i++;
            j++;  // 继续比较后续字符
        } else {
            j = next[j];  // 模式串向右移动，主串指针不变
        }
    }
    
    if (j > T.length)
        return i - T.length;  // 匹配成功
    else
        return 0;  // 匹配失败
}
\`\`\`

**时间复杂度**：O(n+m) ✅ 比BF算法快

#### 3. nextval数组（改进的next）⭐⭐

**问题**：某些情况下next数组仍有冗余比较

**改进**：如果p[j] == p[next[j]]，则nextval[j] = nextval[next[j]]

**计算步骤**：
1. nextval[1] = 0
2. 若p[j] == p[next[j]]，则nextval[j] = nextval[next[j]]
3. 否则，nextval[j] = next[j]

**例题**：模式串 'aaaab' 的next和nextval？

| j | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 模式串 | a | a | a | a | b |
| next[j] | 0 | 1 | 2 | 3 | 4 |
| nextval[j] | 0 | 0 | 0 | 0 | 4 |

### 三、BF vs KMP 对比

| 比较项 | BF算法 | KMP算法 |
|--------|---------|----------|
| 主串指针 | 会回溯 | 不回溯 ✅ |
| 时间复杂度 | O(n×m) | O(n+m) ✅ |
| 空间复杂度 | O(1) | O(m) |
| 实现难度 | 简单 | 较复杂 |
| 适用场景 | 短串、随机文本 | 长串、重复模式 |

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ next数组的手工计算
2. ⭐⭐⭐ KMP算法的执行过程
3. ⭐⭐ nextval数组的计算
4. ⭐⭐ BF和KMP的时间复杂度分析
5. ⭐ KMP算法的代码实现

**典型考题**：
> 【2015统考】已知字符串S为'abaabaabacacaabaabcc'，模式串T为'abaabc'。采用KMP算法进行匹配，第一次出现"失配"时，i=j=5，则下次开始匹配时，i和j的值分别是（）。
> 
> **解**：需要计算next[5]，然后j=next[5]，i不变
`        },
        {
          id: '4.3',
          title: '数组的存储结构',
          content: `## 4.3 数组的存储结构 ⭐

### 一、数组的定义

**数组**：由类型相同的数据元素构成的有序集合。

- **一维数组**：线性结构
- **多维数组**：线性结构的推广

### 二、数组的顺序存储

**特点**：
- ✅ 支持随机访问
- ❌ 插入删除困难

**地址计算**（以二维数组为例）：

#### 1. 行优先存储（C/C++、Java）

\`\`\`
LOC(i,j) = LOC(0,0) + (i×n + j)×L
\`\`\`

其中：
- i：行号（从0开始）
- j：列号（从0开始）
- n：每行的元素个数（列数）
- L：每个元素占用的存储单元

#### 2. 列优先存储（Fortran）

\`\`\`
LOC(i,j) = LOC(0,0) + (j×m + i)×L
\`\`\`

其中 m 为行数

**例题**：二维数组A[6][8]，每个元素占6个字节，按行优先存储，首地址为1000，求A[3][4]的地址？

**解**：
\`\`\`
LOC(3,4) = 1000 + (3×8 + 4)×6 = 1000 + 28×6 = 1000 + 168 = 1168
\`\`\`

### 三、特殊矩阵的压缩存储 ⭐⭐

#### 1. 对称矩阵 ⭐⭐

**特点**：a[i][j] = a[j][i]

**压缩方法**：只存储下三角（包括对角线）

**地址映射**（行优先）：
\`\`\`
k = i×(i+1)/2 + j  （当i≥j时）
k = j×(j+1)/2 + i  （当i<j时，利用对称性）
\`\`\`

其中 k 为一维数组的下标（从0开始）

#### 2. 三角矩阵 ⭐

**上三角矩阵**：i>j时，a[i][j]=c（常数）

**下三角矩阵**：i<j时，a[i][j]=c（常数）

**存储**：存储三角部分 + 常数c

#### 3. 对角矩阵（带状矩阵）

**特点**：非零元素集中在对角线附近

**三对角矩阵**：只有主对角线及其上下两条对角线有非零元素

**压缩方法**：按行优先存储非零元素

#### 4. 稀疏矩阵 ⭐⭐

**定义**：非零元素个数远小于总元素个数的矩阵

**三元组表**：
\`\`\`c
typedef struct {
    int i, j;      // 行号、列号
    ElemType e;    // 元素值
} Triple;

typedef struct {
    Triple data[MAXSIZE + 1];  // 三元组表
    int mu, nu, tu;             // 行数、列数、非零元个数
} TSMatrix;
\`\`\`

**十字链表**：适合频繁修改的稀疏矩阵

### 四、考研重点

**高频考点**：
1. ⭐⭐ 二维数组的地址计算（行优先/列优先）
2. ⭐⭐ 对称矩阵的压缩存储
3. ⭐⭐ 稀疏矩阵的三元组表示
4. ⭐ 三角矩阵和对角矩阵的压缩
`        },
        {
          id: '4.4',
          title: '广义表',
          content: `## 4.4 广义表 ⭐

### 一、广义表的定义

**广义表（Lists）**：n（n≥0）个元素a₁, a₂, ..., aₙ的有限序列。

- 元素可以是**原子**（单个元素）或**子表**（广义表）
- 记作：LS = (a₁, a₂, ..., aₙ)

**重要概念**：
- **表头（Head）**：第一个元素a₁
- **表尾（Tail）**：除表头外的其余元素组成的表(a₂, ..., aₙ)

**注意**：
- ⚠️ 表头可以是原子或子表
- ⚠️ 表尾一定是表（即使是空表）

**例题**：广义表 A = (a, (b, c), d)
- Head(A) = a
- Tail(A) = ((b, c), d)

### 二、广义表的性质

1. **有序性**：元素之间有顺序关系
2. **递归性**：广义表可以是其自身的子表
3. **共享性**：可以被其他广义表共享

### 三、广义表的存储结构

**头尾链表存储**：

\`\`\`c
typedef enum {ATOM, LIST} ElemTag;  // ATOM=0:原子, LIST=1:子表

typedef struct GLNode {
    ElemTag tag;  // 标志位
    union {
        AtomType atom;  // 原子结点的值域
        struct {
            struct GLNode *hp, *tp;
        } ptr;  // 子表结点的指针域，hp指向表头，tp指向表尾
    };
} *GList;
\`\`\`

### 四、广义表的深度和长度

**长度**：最外层元素的个数

**深度**：括号嵌套的最大层数

**例题**：A = (a, (b, (c, d)), e)
- 长度：3（a、(b,(c,d))、e）
- 深度：3（三层括号）

### 五、考研重点

**高频考点**：
1. ⭐⭐ 广义表的表头和表尾
2. ⭐⭐ 广义表的深度和长度
3. ⭐ 广义表的存储结构
4. ⭐ 广义表的递归定义

**典型考题**：
> 广义表 ((a,b,c),d,e,f) 的长度和深度分别是（）。
> 
> **解**：长度=4，深度=2
`        }
      ]
    },
    {
      id: 'ch5',
      number: 5,
      title: '树和二叉树',
      score: '8-12分',
      importance: 'high',
      mindMap: `mindmap
  root((树和二叉树))
    树的基本概念
      定义
      基本术语
        结点
        度
        深度/高度
    二叉树 ⭐⭐⭐
      定义
      性质 ⭐⭐
      存储结构
        顺序存储
        链式存储
      遍历 ⭐⭐⭐
        前序
        中序
        后序
        层序
      线索化 ⭐⭐
    树和森林
      存储结构
      遍历
      与二叉树的转换
    哈夫曼树 ⭐⭐
      定义
      构造算法
      哈夫曼编码
    并查集⭐⭐
      Find查找
      Union合并
      路径压缩`,
      sections: [
        {
          id: '5.1',
          title: '树的基本概念',
          content: `## 5.1 树的基本概念 ⭐

### 一、树的定义

**树（Tree）**：n（n≥0）个结点的**有限集合**。

- n=0：空树
- n>0：有且仅有一个特定的称为**根（Root）**的结点
- 当n>1时，其余结点可分为m（m>0）个互不相交的有限集合T₁, T₂, ..., Tₘ，其中每一个集合本身又是一棵树

**特点**：
- ✅ 递归定义
- ✅ 一对多的层次结构

### 二、基本术语 ⭐⭐

#### 1. 结点关系
- **结点的度**：结点拥有的子树数
- **叶子结点（终端结点）**：度为0的结点
- **分支结点（非终端结点）**：度不为0的结点
- **内部结点**：除根结点外的分支结点

#### 2. 树的属性
- **树的度**：树内各结点的度的**最大值**
- **孩子/双亲**：结点的子树的根称为该结点的孩子，该结点称为孩子的双亲
- **兄弟**：同一双亲的孩子之间互称兄弟
- **祖先**：从根到该结点所经分支上的所有结点
- **子孙**：以某结点为根的子树中的任一结点

#### 3. 层次和深度
- **结点的层次**：从根开始，根为第1层
- **树的深度（高度）**：树中结点的**最大层次**

**注意**：
- ⚠️ 深度/高度从1开始计数
- ⚠️ 树的度是各结点度的最大值

### 三、树的性质

**性质1**：树中的结点数 = 所有结点的度数 + 1

\`\`\`
n = n₀ + n₁ + n₂ + ... + nₘ
边数 = n - 1 = 0×n₀ + 1×n₁ + 2×n₂ + ... + m×nₘ
\`\`\`

### 四、考研重点

**高频考点**：
1. ⭐⭐ 树的基本术语（度、深度、层次）
2. ⭐⭐ 树的性质（结点数与度数的关系）
3. ⭐ 树的递归定义
`        },
        {
          id: '5.2',
          title: '二叉树的概念和性质',
          content: `## 5.2 二叉树的概念和性质 ⭐⭐⭐必考

### 一、二叉树的定义

**二叉树（Binary Tree）**：每个结点最多有**两棵**子树（即二叉树中不存在度大于2的结点），且二叉树的子树有**左右之分**。

**五种基本形态**：
1. 空二叉树
2. 只有根结点
3. 只有左子树
4. 只有右子树
5. 左右子树都有

**注意**：
- ⚠️ 二叉树不是树的特殊情况（有序 vs 无序）
- ⚠️ 左子树和右子树不能颠倒

### 二、几种特殊的二叉树 ⭐⭐⭐

#### 1. 满二叉树

**定义**：高度为h，且含有2ʰ-1个结点的二叉树。

**特点**：
- 每层都达到最大结点数
- 叶子结点都在最底层
- 编号规律：结点i的左孩子为2i，右孩子为2i+1

#### 2. 完全二叉树 ⭐⭐⭐必考

**定义**：高度为h、有n个结点的二叉树，当且仅当其每个结点都与高度为h的满二叉树中编号为1~n的结点一一对应。

**特点**：
- ✅ 叶子结点只可能在层次最大的两层上出现
- ✅ 对于任一结点，若其右分支下的子孙的最大层次为l，则其左分支下的子孙的最大层次必为l或l+1
- ✅ 按层序编号，不会出现"断号"

**判断方法**：
- 层次遍历，遇到第一个叶子结点后，后续结点必须都是叶子

#### 3. 二叉排序树（BST）

**定义**：左子树上所有结点的关键字均小于根结点的关键字；右子树上所有结点的关键字均大于根结点的关键字。

#### 4. 平衡二叉树（AVL）

**定义**：树上任一结点的左子树和右子树的深度之差不超过1。

### 三、二叉树的性质 ⭐⭐⭐必考

**性质1**：非空二叉树上的叶子结点数等于度为2的结点数加1

\`\`\`
n₀ = n₂ + 1
\`\`\`

**证明**：
\`\`\`
n = n₀ + n₁ + n₂ （结点总数）
n - 1 = n₁ + 2n₂ （边数 = 总度数）
联立得：n₀ = n₂ + 1
\`\`\`

**性质2**：非空二叉树上第k层上至多有2ᵏ⁻¹个结点（k≥1）

**性质3**：高度为h的二叉树至多有2ʰ-1个结点

**性质4**：具有n个结点的完全二叉树的高度为⌊log₂n⌋+1或⌈log₂(n+1)⌉

**性质5**（重要）⭐⭐⭐：对完全二叉树按从上到下、从左到右的顺序依次编号1,2,...,n，则有以下关系：

- 当i>1时，结点i的双亲的编号为⌊i/2⌋
- 当2i≤n时，结点i的左孩子编号为2i，否则无左孩子
- 当2i+1≤n时，结点i的右孩子编号为2i+1，否则无右孩子

**例题**：完全二叉树有1001个结点，求叶子结点个数？

**解**：
\`\`\`
n = n₀ + n₁ + n₂ = 1001
n₀ = n₂ + 1
完全二叉树中n₁只能是0或1

若n₁=0：n₀ + 0 + (n₀-1) = 1001 → 2n₀ = 1002 → n₀ = 501
若n₁=1：n₀ + 1 + (n₀-1) = 1001 → 2n₀ = 1001 → n₀ = 500.5（舍去）

所以n₀ = 501
\`\`\`

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ 二叉树的5个性质（特别是性质1和性质5）
2. ⭐⭐⭐ 满二叉树vs完全二叉树的判断
3. ⭐⭐⭐ 完全二叉树的结点计算
4. ⭐⭐ 特殊二叉树的定义
`        },
        {
          id: '5.3',
          title: '二叉树的存储结构',
          content: `## 5.3 二叉树的存储结构 ⭐

### 一、顺序存储

**适用**：完全二叉树和满二叉树

**方法**：用一组地址连续的存储单元依次自上而下、自左至右存储完全二叉树上的结点元素。

**缺点**：
- ❌ 一般二叉树会造成大量空间浪费
- ❌ 插入删除困难

### 二、链式存储 ⭐⭐推荐

#### 1. 二叉链表（最常用）

\`\`\`c
typedef struct BiTNode {
    ElemType data;                    // 数据域
    struct BiTNode *lchild, *rchild;  // 左右孩子指针
} BiTNode, *BiTree;
\`\`\`

**特点**：
- n个结点的二叉链表共有n+1个空链域
- 这些空链域可用于构造线索二叉树

#### 2. 三叉链表

\`\`\`c
typedef struct TriTNode {
    ElemType data;
    struct TriTNode *lchild, *rchild, *parent;  // 增加父指针
} TriTNode, *TriTree;
\`\`\`

**优点**：方便查找父结点

### 三、考研重点

**高频考点**：
1. ⭐⭐ 二叉链表的存储结构
2. ⭐⭐ n个结点的二叉链表有n+1个空链域
3. ⭐ 顺序存储的适用场景
`        },
        {
          id: '5.4',
          title: '二叉树的遍历',
          content: `## 5.4 二叉树的遍历 ⭐⭐⭐必考

### 一、遍历的定义

**遍历**：按某条搜索路径访问树中每个结点，使得每个结点均被访问一次，而且仅被访问一次。

### 二、四种遍历方式 ⭐⭐⭐

#### 1. 先序遍历（PreOrder）⭐⭐⭐

**访问顺序**：根 → 左 → 右（NLR）

**递归算法**：
\`\`\`c
void PreOrder(BiTree T) {
    if (T != NULL) {
        visit(T);           // 访问根结点
        PreOrder(T->lchild); // 递归遍历左子树
        PreOrder(T->rchild); // 递归遍历右子树
    }
}
\`\`\`

**例题**：先序序列为 A B D H I E J C F K G

#### 2. 中序遍历（InOrder）⭐⭐⭐

**访问顺序**：左 → 根 → 右（LNR）

**递归算法**：
\`\`\`c
void InOrder(BiTree T) {
    if (T != NULL) {
        InOrder(T->lchild);  // 递归遍历左子树
        visit(T);            // 访问根结点
        InOrder(T->rchild);  // 递归遍历右子树
    }
}
\`\`\`

**例题**：中序序列为 H D I B J E A K F C G

**应用**：二叉排序树的中序遍历得到递增序列 ✅

#### 3. 后序遍历（PostOrder）⭐⭐⭐

**访问顺序**：左 → 右 → 根（LRN）

**递归算法**：
\`\`\`c
void PostOrder(BiTree T) {
    if (T != NULL) {
        PostOrder(T->lchild);  // 递归遍历左子树
        PostOrder(T->rchild);  // 递归遍历右子树
        visit(T);              // 访问根结点
    }
}
\`\`\`

**例题**：后序序列为 H I D J E B K F G C A

**应用**：求树的深度、释放二叉树

#### 4. 层序遍历（LevelOrder）⭐⭐

**访问顺序**：从上到下、从左到右

**算法思想**（使用队列）：
1. 初始化一个辅助队列
2. 根结点入队
3. 若队列非空，则队头结点出队并访问，然后将该结点的左、右孩子依次入队
4. 重复3直到队列为空

\`\`\`c
void LevelOrder(BiTree T) {
    LinkQueue Q;
    InitQueue(Q);
    
    if (T) {
        EnQueue(Q, T);
        while (!QueueEmpty(Q)) {
            BiTree p;
            DeQueue(Q, p);
            visit(p);
            
            if (p->lchild)
                EnQueue(Q, p->lchild);
            if (p->rchild)
                EnQueue(Q, p->rchild);
        }
    }
}
\`\`\`

**例题**：层序序列为 A B C D E F G H I J K

### 三、由遍历序列构造二叉树 ⭐⭐⭐必考

**结论**：
- ✅ 先序 + 中序 → 唯一确定一棵二叉树
- ✅ 后序 + 中序 → 唯一确定一棵二叉树
- ✅ 层序 + 中序 → 唯一确定一棵二叉树
- ❌ 先序 + 后序 → 不能唯一确定（除非是满二叉树）

**算法思想**（先序+中序为例）：
1. 先序序列的第一个元素是根结点
2. 在中序序列中找到根结点，左边是左子树，右边是右子树
3. 递归构造左子树和右子树

**例题**：
- 先序：A B D H I E J C F K G
- 中序：H D I B J E A K F C G

**构造过程**：
1. A是根，中序中A左边{H,D,I,B,J,E}是左子树，右边{K,F,C,G}是右子树
2. 左子树的先序是{B,D,H,I,E,J}，B是左子树的根
3. 递归构造...

### 四、遍历算法的应用 ⭐⭐

#### 1. 求二叉树的深度

\`\`\`c
int treeDepth(BiTree T) {
    if (T == NULL)
        return 0;
    else {
        int ldepth = treeDepth(T->lchild);
        int rdepth = treeDepth(T->rchild);
        return (ldepth > rdepth ? ldepth : rdepth) + 1;
    }
}
\`\`\`

#### 2. 统计叶子结点数

\`\`\`c
int countLeaf(BiTree T) {
    if (T == NULL)
        return 0;
    if (T->lchild == NULL && T->rchild == NULL)
        return 1;
    return countLeaf(T->lchild) + countLeaf(T->rchild);
}
\`\`\`

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 三种递归遍历的代码实现
2. ⭐⭐⭐ 由遍历序列构造二叉树
3. ⭐⭐⭐ 层序遍历的队列实现
4. ⭐⭐ 遍历算法的应用（求深度、统计结点数等）
5. ⭐⭐ 不同遍历序列的特点

**典型考题**：
> 【2017统考】要使一棵非空二叉树的先序序列与中序序列相同，则所有非叶结点须满足的条件是（）。
> 
> **解**：只有右子树（因为先序是NLR，中序是LNR，要相同则L为空）

### 💡 重要扩展：二叉树在查找中的应用

> 🔗 **核心关联**：二叉树是许多**高效查找算法**的基础
> 
> - **二叉排序树（BST）**：利用中序遍历得到有序序列
>   - 📖 详见：7.3 二叉排序树和平衡二叉树
>   - 特点：左子树 < 根 < 右子树
>   - 平均查找长度：O(log₂n)
> 
> - **平衡二叉树（AVL）**：保证查找效率不退化
>   - 📖 详见：7.3 二叉排序树和平衡二叉树
>   - 特点：左右子树高度差 ≤ 1
>   - 查找、插入、删除均为 O(log₂n)
> 
> - **哈夫曼树**：最优二叉树，用于数据压缩
>   - 📖 详见：5.7 哈夫曼树和哈夫曼编码
>   - 应用：哈夫曼编码、文件压缩
> 
> 💡 **理解要点**：二叉树不仅是存储结构，更是实现高效查找的关键！
`        },
        {
          id: '5.5',
          title: '线索二叉树',
          content: `## 5.5 线索二叉树 ⭐⭐

### 一、线索二叉树的基本概念

**问题**：二叉链表中有n+1个空链域，浪费了空间

**解决**：利用空链域存放指向结点在某种遍历次序下的前驱和后继结点的指针（这种附加的指针称为"线索"）

**线索化**：对二叉树以某种次序遍历使其变为线索二叉树的过程

### 二、线索二叉树的存储结构

\`\`\`c
typedef struct ThreadNode {
    ElemType data;
    struct ThreadNode *lchild, *rchild;
    int ltag, rtag;  // 线索标记
} ThreadNode, *ThreadTree;
\`\`\`

**线索标记**：
- ltag=0：lchild指向左孩子
- ltag=1：lchild指向前驱（线索）
- rtag=0：rchild指向右孩子
- rtag=1：rchild指向后继（线索）

### 三、中序线索二叉树 ⭐⭐

#### 1. 中序线索化

\`\`\`c
void InThread(ThreadTree &p, ThreadTree &pre) {
    if (p != NULL) {
        InThread(p->lchild, pre);  // 递归线索化左子树
        
        if (p->lchild == NULL) {   // 左子树为空，建立前驱线索
            p->lchild = pre;
            p->ltag = 1;
        }
        
        if (pre != NULL && pre->rchild == NULL) {  // 建立前驱结点的后继线索
            pre->rchild = p;
            pre->rtag = 1;
        }
        
        pre = p;  // 标记当前结点成为刚刚访问过的结点
        InThread(p->rchild, pre);  // 递归线索化右子树
    }
}

void CreateInThread(ThreadTree T) {
    ThreadTree pre = NULL;
    if (T != NULL) {
        InThread(T, pre);
        pre->rchild = NULL;  // 处理最后一个结点
        pre->rtag = 1;
    }
}
\`\`\`

#### 2. 中序线索二叉树上找前驱和后继

**找后继**：
- 若rtag==1，则rchild指向后继
- 若rtag==0，则后继为右子树中最左下的结点

**找前驱**：
- 若ltag==1，则lchild指向前驱
- 若ltag==0，则前驱为左子树中最右下的结点

### 四、考研重点

**高频考点**：
1. ⭐⭐ 线索二叉树的存储结构
2. ⭐⭐ 中序线索化的算法
3. ⭐⭐ 在线索二叉树中找前驱和后继
4. ⭐ 线索化的目的（利用空链域）
`        },
        {
          id: '5.6',
          title: '树、森林',
          content: `## 5.6 树、森林 ⭐⭐

### 一、树的存储结构

#### 1. 双亲表示法

\`\`\`c
#define MAX_TREE_SIZE 100
typedef struct {
    ElemType data;       // 结点数据
    int parent;          // 双亲位置域
} PTNode;

typedef struct {
    PTNode nodes[MAX_TREE_SIZE];  // 双亲表示
    int n;                         // 结点数
} PTree;
\`\`\`

**优点**：找双亲容易 O(1)
**缺点**：找孩子困难 O(n)

#### 2. 孩子表示法

将每个结点的孩子排列起来，看成一个线性表，采用单链表存储。

**优点**：找孩子容易
**缺点**：找双亲困难

#### 3. 孩子兄弟表示法（最重要）⭐⭐⭐

\`\`\`c
typedef struct CSNode {
    ElemType data;
    struct CSNode *firstchild, *nextsibling;  // 第一个孩子和右兄弟指针
} CSNode, *CSTree;
\`\`\`

**优点**：
- ✅ 可以方便地实现树和二叉树的转换
- ✅ 灵活的指针设置，可以找到任意孩子和兄弟

### 二、树、森林与二叉树的转换 ⭐⭐⭐必考

#### 1. 树 → 二叉树（左孩子右兄弟）

**规则**：
- 加线：在所有兄弟结点之间加一条连线
- 抹线：对每个结点，除了其左孩子外，去除其与其余孩子之间的关系
- 旋转：以树的根结点为轴心，将整树顺时针旋转45°

**结果**：树对应的二叉树，根结点一定没有右子树

#### 2. 森林 → 二叉树

**规则**：
1. 将每棵树转换为二叉树
2. 第一棵二叉树不动，从第二棵二叉树开始，依次把后一棵二叉树的根结点作为前一棵二叉树的根结点的右孩子

#### 3. 二叉树 → 树/森林

**判断**：
- 若二叉树根结点有左孩子，则能转换为树
- 若二叉树根结点有右孩子，则能转换为森林

**规则**：逆向操作即可

### 三、树和森林的遍历

#### 1. 树的遍历

| 遍历方式 | 访问顺序 | 对应二叉树 |
|----------|----------|------------|
| 先根遍历 | 根 → 子树 | 先序遍历 |
| 后根遍历 | 子树 → 根 | 中序遍历 |

#### 2. 森林的遍历

| 遍历方式 | 访问顺序 | 对应二叉树 |
|----------|----------|------------|
| 先序遍历 | 第一棵树 → 其余森林 | 先序遍历 |
| 中序遍历 | 第一棵树的子树森林 → 第一棵树的根 → 其余森林 | 中序遍历 |

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ 孩子兄弟表示法
2. ⭐⭐⭐ 树、森林与二叉树的转换
3. ⭐⭐ 树的存储结构比较
4. ⭐⭐ 树和森林的遍历与二叉树遍历的对应关系
`        },
        {
          id: '5.7',
          title: '哈夫曼树',
          content: `## 5.7 哈夫曼树和哈夫曼编码 ⭐⭐⭐必考

### 一、哈夫曼树的定义

**带权路径长度（WPL）**：树中所有叶子结点的带权路径长度之和。

\`\`\`
WPL = Σ(wᵢ × lᵢ)
\`\`\`

其中wᵢ为第i个叶子结点的权值，lᵢ为该叶子结点到根的路径长度。

**哈夫曼树（最优二叉树）**：给定n个权值作为n个叶子结点，构造一棵二叉树，若该树的带权路径长度达到最小，则称这样的二叉树为哈夫曼树。

### 二、哈夫曼树的构造算法 ⭐⭐⭐

**算法思想**（贪心算法）：
1. 将这n个结点分别作为n棵仅含一个结点的二叉树，构成森林F
2. 构造一个新结点，从F中选取两棵根结点权值最小的树作为新结点的左、右子树，并且将新结点的权值置为左、右子树上根结点的权值之和
3. 从F中删除刚才选出的两棵树，同时将新得到的树加入F中
4. 重复步骤2和3，直至F中只剩下一棵树为止

**例题**：权值集合{5, 7, 2, 13}，构造哈夫曼树

**步骤**：
1. 选最小的2和5，合并为7
2. 选7和7，合并为14
3. 选13和14，合并为27

**WPL** = 2×3 + 5×3 + 7×2 + 13×1 = 6 + 15 + 14 + 13 = 48

### 三、哈夫曼编码 ⭐⭐⭐

**前缀编码**：没有一个编码是另一个编码的前缀

**哈夫曼编码**：根据哈夫曼树得到的前缀编码

**编码规则**：
- 左分支标0，右分支标1（或相反）
- 从根到叶子结点的路径上的0/1序列即为该字符的编码

**特点**：
- ✅ 是最优前缀编码
- ✅ 权值越大的字符编码越短
- ✅ 可以唯一解码

**例题**：字符集{A,B,C,D}，频率{5,7,2,13}，求哈夫曼编码

**解**：根据上面的哈夫曼树
- D(13): 0
- B(7): 10
- A(5): 110
- C(2): 111

**平均编码长度** = (13×1 + 7×2 + 5×3 + 2×3) / 27 = 48/27 ≈ 1.78

### 四、哈夫曼树的性质

1. 每个初始结点最终都成为叶子结点
2. 权值越小的结点，路径长度越大
3. 哈夫曼树中不存在度为1的结点
4. n个叶子结点的哈夫曼树共有2n-1个结点
5. 哈夫曼树不唯一，但WPL相同

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 哈夫曼树的构造过程
2. ⭐⭐⭐ WPL的计算
3. ⭐⭐⭐ 哈夫曼编码的生成
4. ⭐⭐ 哈夫曼树的性质
5. ⭐ 哈夫曼编码的最优性证明

**典型考题**：
> 【2018统考】已知字符集{a,b,c,d,e,f}，各字符出现次数分别为{6,3,8,2,10,4}，则哈夫曼编码的平均长度为（）。
> 
> **解**：先构造哈夫曼树，计算WPL，再除以总次数
`        },
        {
          id: '5.8',
          title: '并查集',
          content: `## 5.8 并查集 ⭐⭐

### 一、并查集的概念

**并查集（Union-Find / Disjoint Set）**：一种管理**不相交集合**的树形数据结构。

**核心思想**：
- 每个集合用一棵**树**表示，树根作为集合的**代表元素**
- 用数组 parent[] 存储每个结点的父结点，根结点的 parent 指向自己

\`\`\`
集合 {1,2,3}: 1为根, 2、3为孩子    parent[1]=1, parent[2]=1, parent[3]=1
集合 {4,5}:   4为根, 5为孩子        parent[4]=4, parent[5]=5
\`\`\`

### 二、基本操作 ⭐⭐⭐

#### 1. 初始化

\`\`\`c
void Init(int n) {
    for (int i = 1; i <= n; i++)
        parent[i] = i;  // 每个元素自成一集合
}
\`\`\`

#### 2. Find（查找根节点）

\`\`\`c
int Find(int x) {
    while (parent[x] != x)
        x = parent[x];
    return x;
}
\`\`\`

**路径压缩优化** ⭐⭐：

\`\`\`c
int Find(int x) {
    if (parent[x] != x)
        parent[x] = Find(parent[x]);  // 路径上所有点直接挂到根
    return parent[x];
}
\`\`\`

- 查找时把路径上所有结点**直接挂到根**下
- 优化后树近似扁平，均摊复杂度接近 O(1)

#### 3. Union（合并集合）

\`\`\`c
void Union(int a, int b) {
    int ra = Find(a), rb = Find(b);
    if (ra != rb)
        parent[ra] = rb;  // 一棵树的根挂到另一棵
}
\`\`\`

**按秩合并**：将矮树挂到高树下，防止退化成链。

### 三、应用场景

| 应用 | 说明 |
|------|------|
| **Kruskal算法** ⭐⭐⭐ | 判断边的两端点是否已连通（是否成环） |
| 连通分量判定 | 判断图中两结点是否在同一连通分量 |
| 等价类划分 | 合并有等价关系的元素 |

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ 并查集在Kruskal算法中的作用（判断是否形成环）
2. ⭐⭐ 路径压缩的思想与效果
3. ⭐⭐ Find/Union操作的代码实现
4. ⭐ 并查集的树形（双亲表示法）存储结构

**注意**：并查集只支持**合并**与**查询**，不支持**分割**集合。`
        }
      ]
    },
    {
      id: 'ch6',
      number: 6,
      title: '图',
      score: '8-12分',
      importance: 'high',
      mindMap: `mindmap
  root((图))
    基本概念
      定义
      术语
        顶点
        边
        度
    存储结构 ⭐⭐
      邻接矩阵
      邻接表 ⭐
      十字链表
      邻接多重表
    图的遍历 ⭐⭐⭐
      BFS
      DFS
    图的应用 ⭐⭐⭐
      最小生成树
        Prim算法
        Kruskal算法
      最短路径
        Dijkstra算法
        Floyd算法
      拓扑排序
      关键路径`,
      sections: [
        {
          id: '6.1',
          title: '图的基本概念',
          content: `## 6.1 图的基本概念 ⭐⭐

### 一、图的定义

**图（Graph）**：由顶点集合V和边集合E组成，记作G=(V,E)。

- **无向图**：边没有方向，(v,w) = (w,v)
- **有向图**：边有方向，<v,w> ≠ <w,v>

### 二、基本术语 ⭐⭐

#### 1. 顶点和边
- **邻接**：若(v,w)∈E，则v和w互为邻接点
- **度（Degree）**：与顶点v关联的边的数目
  - 无向图：TD(v) = 度
  - 有向图：TD(v) = ID(v) + OD(v)（入度+出度）

#### 2. 特殊图
- **完全图**：任意两个顶点之间都有边
  - 无向完全图：n个顶点有n(n-1)/2条边
  - 有向完全图：n个顶点有n(n-1)条弧
- **稠密图**：边数接近完全图
- **稀疏图**：边数远小于完全图

#### 3. 路径和连通性
- **路径**：顶点序列 v, v₁, v₂, ..., vₙ, w
- **路径长度**：路径上边的数目
- **简单路径**：序列中顶点不重复出现的路径
- **回路（环）**：第一个顶点和最后一个顶点相同的路径

#### 4. 连通性
- **无向图**：
  - **连通**：从顶点v到w有路径
  - **连通图**：图中任意两个顶点都连通
  - **连通分量**：无向图中的极大连通子图

- **有向图**：
  - **强连通**：从v到w和从w到v都有路径
  - **强连通图**：图中任意两个顶点都强连通
  - **强连通分量**：有向图中的极大强连通子图

#### 5. 生成树
- **生成树**：包含图中全部n个顶点的极小连通子图
- **性质**：n个顶点的生成树有且仅有n-1条边

### 三、图的性质

**性质1**：对于无向图，所有顶点的度数之和等于边数的2倍

\`\`\`
Σ TD(v) = 2e
\`\`\`

**性质2**：对于有向图，所有顶点的入度之和 = 出度之和 = 边数

\`\`\`
Σ ID(v) = Σ OD(v) = e
\`\`\`

### 四、考研重点

**高频考点**：
1. ⭐⭐ 图的基本术语（度、连通、完全图）
2. ⭐⭐ 完全图的边数计算
3. ⭐⭐ 连通图和强连通图的区别
4. ⭐ 生成树的性质
`        },
        {
          id: '6.2',
          title: '图的存储结构',
          content: `## 6.2 图的存储结构 ⭐⭐⭐必考

### 一、邻接矩阵法 ⭐⭐

#### 1. 定义

用两个数组分别存储顶点信息和边（弧）信息。

\`\`\`c
#define MaxVertexNum 100
typedef char VertexType;
typedef int EdgeType;

typedef struct {
    VertexType Vex[MaxVertexNum];           // 顶点表
    EdgeType Edge[MaxVertexNum][MaxVertexNum]; // 邻接矩阵
    int vexnum, arcnum;                      // 顶点数和边数
} MGraph;
\`\`\`

#### 2. 特点

**无向图**：对称矩阵，可压缩存储

**有向图**：不一定对称

**空间复杂度**：O(n²)

#### 3. 优点和缺点

| 优点 | 缺点 |
|------|------|
| 直观、简单 | 空间复杂度高 O(n²) |
| 方便判断顶点间是否有边 | 稀疏图浪费空间 |
| 方便计算顶点的度 | 增加/删除顶点困难 |

**时间复杂度**：
- 判断两顶点是否有边：O(1) ✅
- 求顶点的度：O(n)

### 二、邻接表法 ⭐⭐⭐推荐

#### 1. 定义

顺序分配与链式分配相结合的存储方法。

\`\`\`c
// 边表结点
typedef struct ArcNode {
    int adjvex;              // 该弧所指向的顶点的位置
    struct ArcNode *nextarc; // 指向下一条弧的指针
    InfoType info;           // 权值等信息
} ArcNode;

// 顶点表结点
typedef struct VNode {
    VertexType data;         // 顶点信息
    ArcNode *firstarc;       // 指向第一条依附该顶点的弧的指针
} VNode, AdjList[MaxVertexNum];

typedef struct {
    AdjList vertices;        // 邻接表
    int vexnum, arcnum;      // 图的顶点数和边数
} ALGraph;
\`\`\`

#### 2. 特点

**无向图**：每个边在邻接表中出现两次

**有向图**：
- 邻接表：记录出边（出度）
- 逆邻接表：记录入边（入度）

**空间复杂度**：O(|V|+|E|)

#### 3. 优点和缺点

| 优点 | 缺点 |
|------|------|
| 空间效率高 O(|V|+|E|) | 判断两顶点是否有边慢 O(n) |
| 适合稀疏图 | 计算度不方便（无向图除外） |
| 方便找邻接点 |  |

**时间复杂度**：
- 找顶点v的所有邻接点：O(d(v)) ✅
- 判断两顶点是否有边：O(n) ❌

### 三、其他存储方法

#### 1. 十字链表（有向图）

同时存储出边和入边，方便计算入度和出度。

#### 2. 邻接多重表（无向图）

每条边只用一个结点表示，避免邻接表中边重复存储。

### 四、邻接矩阵 vs 邻接表

| 比较项 | 邻接矩阵 | 邻接表 |
|--------|----------|--------|
| 空间复杂度 | O(n²) | O(|V|+|E|) |
| 判断边存在 | O(1) ✅ | O(n) |
| 找邻接点 | O(n) | O(d(v)) ✅ |
| 计算度 | O(n) | O(n)或O(1) |
| 适用场景 | 稠密图 | 稀疏图 ✅ |

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 邻接表和邻接矩阵的存储结构
2. ⭐⭐⭐ 两种存储方法的优缺点比较
3. ⭐⭐ 空间复杂度和时间复杂度分析
4. ⭐⭐ 根据图画出邻接表/邻接矩阵
5. ⭐ 十字链表和邻接多重表
`        },
        {
          id: '6.3',
          title: '图的遍历',
          content: `## 6.3 图的遍历 ⭐⭐⭐必考

### 一、广度优先搜索（BFS）⭐⭐⭐

**思想**：类似于树的层次遍历，使用**队列**实现。

**算法步骤**：
1. 从图中某个顶点v出发，访问v
2. 依次访问v的各个未曾访问过的邻接点
3. 分别从这些邻接点出发，依次访问它们的邻接点
4. 重复3，直到所有顶点都被访问

**代码实现**：
\`\`\`c
bool visited[MAX_VERTEX_NUM];  // 访问标记数组

void BFS(Graph G, int v) {
    visit(v);                   // 访问初始顶点
    visited[v] = true;          // 标记已访问
    
    InitQueue(Q);               // 初始化辅助队列
    EnQueue(Q, v);              // 初始顶点入队
    
    while (!QueueEmpty(Q)) {
        DeQueue(Q, v);          // 队头元素出队
        
        for (w = FirstNeighbor(G, v); w >= 0; w = NextNeighbor(G, v, w)) {
            if (!visited[w]) {  // w为v的尚未访问的邻接顶点
                visit(w);
                visited[w] = true;
                EnQueue(Q, w);
            }
        }
    }
}
\`\`\`

**时间复杂度**：
- 邻接矩阵：O(n²)
- 邻接表：O(|V|+|E|)

**应用**：
- 无权图的最短路径
- 连通性判断
- 层次遍历

### 二、深度优先搜索（DFS）⭐⭐⭐

**思想**：类似于树的先序遍历，使用**递归**或**栈**实现。

**算法步骤**：
1. 从图中某个顶点v出发，访问v
2. 找出刚访问过的顶点的第一个未被访问的邻接点，访问该顶点
3. 以该顶点为新顶点，重复2，直到当前访问的顶点的所有邻接点都被访问过
4. 退回到最近被访问过的顶点，若它还有未被访问的邻接点，则从该邻接点继续2
5. 重复4，直到所有顶点都被访问

**代码实现**（递归）：
\`\`\`c
void DFS(Graph G, int v) {
    visit(v);                   // 访问顶点v
    visited[v] = true;          // 标记已访问
    
    for (w = FirstNeighbor(G, v); w >= 0; w = NextNeighbor(G, v, w)) {
        if (!visited[w]) {      // w为v的尚未访问的邻接顶点
            DFS(G, w);
        }
    }
}
\`\`\`

**时间复杂度**：
- 邻接矩阵：O(n²)
- 邻接表：O(|V|+|E|)

**应用**：
- 连通性判断
- 拓扑排序
- 关键路径
- 求解迷宫问题

### 三、BFS vs DFS

| 比较项 | BFS | DFS |
|--------|-----|-----|
| 数据结构 | 队列 | 栈/递归 |
| 搜索策略 | 层层推进 | 一条路走到黑 |
| 最短路径 | 可以找到（无权图）✅ | 不能保证 |
| 空间复杂度 | 较高 | 较低 ✅ |
| 适用场景 | 求最短路径 | 连通性、回溯 |

### 四、遍历序列的唯一性

**结论**：
- ❌ 给定一个图，其BFS/DFS序列**不唯一**
- ✅ 给定起始顶点和存储结构，遍历序列**唯一**

**原因**：邻接点的访问顺序取决于存储结构

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ BFS和DFS的代码实现
2. ⭐⭐⭐ BFS和DFS的时间复杂度
3. ⭐⭐⭐ 根据图写出BFS/DFS序列
4. ⭐⭐ BFS和DFS的比较
5. ⭐⭐ 遍历序列的唯一性

**典型考题**：
> 【2019统考】以下关于图的遍历说法中，正确的是（）。
> 
> A. DFS适合用于判断图中是否存在环
> B. BFS一定比DFS快
> C. 图的遍历序列是唯一的
> D. DFS不能使用非递归实现
> 
> **答案**：A
`        },
        {
          id: '6.4',
          title: '最小生成树',
          content: `## 6.4 最小生成树 ⭐⭐⭐必考

### 一、基本概念

**生成树**：包含图中全部n个顶点的极小连通子图，有n-1条边。

**最小生成树（MST）**：带权连通图中，所有生成树中权的总和最小的生成树。

**性质**：
1. 最小生成树不是唯一的（当各边权值不相同时唯一）
2. 最小生成树的边的权值之和总是唯一的
3. 最小生成树的边数为顶点数减1

### 二、Prim算法（普里姆算法）⭐⭐⭐

**思想**：从某一个顶点开始构建生成树，每次将代价最小的新顶点纳入生成树，直到所有顶点都纳入为止。

**算法步骤**：
1. 从图中任取一个顶点加入生成树
2. 在所有连接生成树内顶点和生成树外顶点的边中，选择一条权值最小的边
3. 将该边连接的生成树外顶点加入生成树
4. 重复2-3，直到所有顶点都加入生成树

**时间复杂度**：O(n²)（适合稠密图）

**代码框架**：
\`\`\`c
void Prim(G, T) {
    T = ∅;                          // 初始化空树
    U = {w};                        // 添加任一顶点w
    
    while ((V-U) != ∅) {           // 若树中不含全部顶点
        设(u,v)是使u∈U与v∈(V-U)，且权值最小的边;
        T = T∪{(u,v)};             // 边归入树
        U = U∪{v};                 // 顶点归入树
    }
}
\`\`\`

### 三、Kruskal算法（克鲁斯卡尔算法）⭐⭐⭐

**思想**：每次选择一条权值最小的边，使这条边的两头连通（原本不连通），直到所有结点都连通。

**算法步骤**：
1. 将所有边按权值从小到大排序
2. 依次考察每条边，若该边连接的两个顶点不在同一连通分量中，则将该边加入生成树
3. 重复2，直到生成树中有n-1条边

**关键技术**：并查集（判断两个顶点是否连通）

**时间复杂度**：O(|E|log|E|)（适合稀疏图）

**代码框架**：
\`\`\`c
void Kruskal(V, T) {
    T = V;                         // 初始化树T，仅含顶点
    numS = n;                      // 连通分量数
    
    while (numS > 1) {             // 若连通分量数大于1
        从E中取出权值最小的边(v,u);
        if (v和u属于T中不同的连通分量) {
            T = T∪{(v,u)};         // 将此边加入生成树
            numS--;                // 连通分量数减1
        }
    }
}
\`\`\`

### 四、Prim vs Kruskal

| 比较项 | Prim算法 | Kruskal算法 |
|--------|----------|-------------|
| 时间复杂度 | O(n²) | O(|E|log|E|) |
| 适用场景 | 稠密图 ✅ | 稀疏图 ✅ |
| 核心思想 | 从顶点出发 | 从边出发 |
| 数据结构 | 数组 | 并查集 |

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ Prim算法的执行过程
2. ⭐⭐⭐ Kruskal算法的执行过程
3. ⭐⭐ 两种算法的比较和适用场景
4. ⭐⭐ 最小生成树的性质
5. ⭐ 手工构造最小生成树

**典型考题**：
> 【2016统考】使用Prim算法求最小生成树时，需要维护的信息是（）。
> 
> **解**：需要维护每个顶点到生成树的最小距离
`        },
        {
          id: '6.5',
          title: '最短路径',
          content: `## 6.5 最短路径 ⭐⭐⭐必考

### 一、单源最短路径 - Dijkstra算法 ⭐⭐⭐

**问题**：求图中某一顶点到其余各顶点的最短路径。

**限制**：**不能有负权边**

**思想**：基于贪心策略，逐步确定从源点到各顶点的最短路径。

**算法步骤**：
1. 初始化：设置集合S存放已求得最短路径的顶点，初始时S={v₀}
2. 从V-S中选择距离源点最近的顶点u，加入S
3. 更新V-S中各顶点的距离：若经过u到达某顶点的距离更短，则更新
4. 重复2-3，直到S=V

**时间复杂度**：O(n²)

**代码框架**：
\`\`\`c
void Dijkstra(Graph G, int v) {
    // 初始化
    for (i = 0; i < G.vexnum; i++) {
        final[i] = false;         // 均未求得最短路径
        dist[i] = G.Edge[v][i];   // 初始化距离
        path[i] = (dist[i] < INF) ? v : -1;  // 初始化路径
    }
    final[v] = true;
    dist[v] = 0;
    
    // 主循环
    for (i = 1; i < G.vexnum; i++) {
        // 选择最小距离的顶点
        min = INF;\n        for (w = 0; w < G.vexnum; w++) {
            if (!final[w] && dist[w] < min) {
                u = w;
                min = dist[w];
            }
        }
        
        final[u] = true;  // 加入S
        
        // 更新距离
        for (w = 0; w < G.vexnum; w++) {
            if (!final[w] && dist[u] + G.Edge[u][w] < dist[w]) {
                dist[w] = dist[u] + G.Edge[u][w];
                path[w] = u;
            }
        }
    }
}
\`\`\`

**注意**：Dijkstra算法**不能处理负权边**！

### 二、多源最短路径 - Floyd算法 ⭐⭐⭐

**问题**：求图中每一对顶点之间的最短路径。

**思想**：动态规划，逐步考虑中间顶点。

**算法步骤**：
1. 初始化：A⁻¹[i][j] = G.Edge[i][j]
2. 依次考虑每个顶点k作为中间点
3. 若A[i][k] + A[k][j] < A[i][j]，则更新A[i][j]
4. 重复2-3，直到考虑完所有顶点

**时间复杂度**：O(n³)

**空间复杂度**：O(n²)

**代码框架**：
\`\`\`c
void Floyd(Graph G) {
    // 初始化
    for (i = 0; i < G.vexnum; i++) {
        for (j = 0; j < G.vexnum; j++) {
            A[i][j] = G.Edge[i][j];
            path[i][j] = -1;
        }
    }
    
    // 三重循环
    for (k = 0; k < G.vexnum; k++) {
        for (i = 0; i < G.vexnum; i++) {
            for (j = 0; j < G.vexnum; j++) {
                if (A[i][k] + A[k][j] < A[i][j]) {
                    A[i][j] = A[i][k] + A[k][j];
                    path[i][j] = k;
                }
            }
        }
    }
}
\`\`\`

**优点**：可以处理负权边（但不能有负权回路）

### 三、Dijkstra vs Floyd

| 比较项 | Dijkstra | Floyd |
|--------|----------|-------|
| 问题类型 | 单源最短路径 | 多源最短路径 |
| 时间复杂度 | O(n²) | O(n³) |
| 负权边 | ❌ 不支持 | ✅ 支持（无负权回路） |
| 算法思想 | 贪心 | 动态规划 |

### 四、考研重点

**高频考点**：
1. ⭐⭐⭐ Dijkstra算法的执行过程
2. ⭐⭐⭐ Floyd算法的执行过程
3. ⭐⭐⭐ Dijkstra不能处理负权边
4. ⭐⭐ 两种算法的时间复杂度
5. ⭐⭐ 手工模拟算法过程

**典型考题**：
> 【2020统考】下列关于最短路径算法的说法中，正确的是（）。
> 
> A. Dijkstra算法可以处理负权边
> B. Floyd算法的时间复杂度为O(n²)
> C. Dijkstra算法基于贪心策略
> D. Floyd算法不能处理负权边
> 
> **答案**：C
`        },
        {
          id: '6.6',
          title: '拓扑排序和关键路径',
          content: `## 6.6 拓扑排序和关键路径 ⭐⭐⭐必考

### 一、AOV网和拓扑排序 ⭐⭐⭐

**AOV网（Activity On Vertex）**：用顶点表示活动，用弧表示活动间优先关系的有向图。

**拓扑排序**：将有向无环图（DAG）的所有顶点排成一个线性序列，使得若存在弧<vᵢ,vⱼ>，则序列中vᵢ必在vⱼ之前。

**算法步骤**：
1. 从AOV网中选择一个没有前驱（入度为0）的顶点并输出
2. 从网中删除该顶点和所有以它为尾的弧
3. 重复1-2，直到网为空，或网中不存在无前驱的顶点为止（后一种情况说明有环）

**时间复杂度**：O(|V|+|E|)

**代码框架**：
\`\`\`c
bool TopologicalSort(Graph G) {
    InitStack(S);                  // 初始化栈，存放入度为0的顶点
    
    for (i = 0; i < G.vexnum; i++) {
        if (indegree[i] == 0)
            Push(S, i);            // 入度为0的顶点入栈
    }
    
    count = 0;                     // 计数，记录输出的顶点数
    
    while (!StackEmpty(S)) {
        Pop(S, i);                 // 弹出栈顶顶点
        print[count++] = i;        // 输出顶点i
        
        for (p = G.vertices[i].firstarc; p; p = p->nextarc) {
            v = p->adjvex;         // 获取邻接点
            if (!(--indegree[v]))  // 入度减1，若为0则入栈
                Push(S, v);
        }
    }
    
    if (count < G.vexnum)
        return false;              // 有环
    else
        return true;               // 拓扑排序成功
}
\`\`\`

**应用**：
- 课程安排
- 工程计划
- 编译顺序
- 检测有向图是否有环

### 二、AOE网和关键路径 ⭐⭐⭐

**AOE网（Activity On Edge）**：用顶点表示事件，用有向边表示活动，边上的权值表示活动持续时间的带权有向无环图。

**关键路径**：从源点到汇点的最长路径，决定了整个工程的最短完成时间。

**四个重要参数**：

1. **事件vₖ的最早发生时间ve(k)**
   - ve(源点) = 0
   - ve(k) = max{ve(j) + Weight(vⱼ,vₖ)}

2. **事件vₖ的最迟发生时间vl(k)**
   - vl(汇点) = ve(汇点)
   - vl(k) = min{vl(j) - Weight(vₖ,vⱼ)}

3. **活动aᵢ的最早开始时间e(i)**
   - e(i) = ve(起点)

4. **活动aᵢ的最迟开始时间l(i)**
   - l(i) = vl(终点) - Weight

**关键活动**：满足e(i) = l(i)的活动

**关键路径**：由关键活动组成的路径

**算法步骤**：
1. 拓扑排序，计算ve()
2. 逆拓扑排序，计算vl()
3. 计算e()和l()
4. 找出e(i)=l(i)的关键活动
5. 由关键活动构成关键路径

**时间复杂度**：O(|V|+|E|)

### 三、考研重点

**高频考点**：
1. ⭐⭐⭐ 拓扑排序的算法和执行过程
2. ⭐⭐⭐ 关键路径的计算（ve、vl、e、l）
3. ⭐⭐⭐ 判断有向图是否有环
4. ⭐⭐ 拓扑排序的应用场景
5. ⭐⭐ 关键路径的意义

**典型考题**：
> 【2021统考】下列关于拓扑排序的说法中，正确的是（）。
> 
> A. 拓扑排序的结果唯一
> B. 有环的图也可以进行拓扑排序
> C. 拓扑排序可以用于检测图中是否有环
> D. 拓扑排序只能用于有向图
> 
> **答案**：C、D
`        }
      ]
    },
    {
      id: 'ch7',
      number: 7,
      title: '查找',
      score: '5-8分',
      importance: 'medium',
      mindMap: `mindmap
  root((查找))
    基本概念
      查找表
      关键字
      平均查找长度 ASL ⭐
    线性表的查找
      顺序查找 ⭐
      折半查找 ⭐⭐
      分块查找
    树表的查找
      二叉排序树 ⭐⭐
      平衡二叉树 ⭐⭐
      B树和B+树 ⭐⭐
    散列表 ⭐⭐⭐
      散列函数
      冲突处理
        开放定址法
        拉链法`,
      sections: [
        {
          id: '7.1',
          title: '查找的基本概念',
          content: `## 7.1 查找的基本概念 ⭐

### 一、基本概念

**查找表**：由同一类型的数据元素构成的集合

**关键字**：数据元素中某个数据项的值，可用来标识一个数据元素

**查找**：根据给定的某个值，在查找表中确定一个其关键字等于给定值的数据元素

**平均查找长度（ASL）**⭐⭐⭐：为确定记录在查找表中的位置，需和给定值进行比较的关键字个数的期望值

\`\`\`
ASL = Σ pᵢ × cᵢ
\`\`\`

其中pᵢ为查找第i个元素的概率，cᵢ为找到第i个元素所需比较次数。

### 二、查找的分类

| 类型 | 说明 | 典型算法 |
|------|------|----------|
| **静态查找** | 只查找，不修改查找表 | 顺序查找、折半查找 |
| **动态查找** | 查找的同时进行修改 | 二叉排序树、散列表 |

### 三、考研重点

**高频考点**：
1. ⭐⭐⭐ ASL的计算
2. ⭐⭐ 静态查找vs动态查找
3. ⭐ 查找成功的ASL vs 查找失败的ASL
`        },
        {
          id: '7.2',
          title: '顺序查找和折半查找',
          content: `## 7.2 顺序查找和折半查找 ⭐⭐⭐必考

### 一、顺序查找 ⭐⭐

**思想**：从表的一端开始，逐个检查每个关键字是否与给定值相等。

**代码实现**：
\`\`\`c
int Search_Seq(SSTable ST, KeyType key) {
    ST.elem[0].key = key;  // 哨兵
    
    for (i = ST.length; ST.elem[i].key != key; i--);
    
    return i;  // 若找到，返回位置；否则返回0
}
\`\`\`

**性能分析**：
- **查找成功**：ASL = (n+1)/2
- **查找失败**：ASL = n+1
- **时间复杂度**：O(n)

**优点**：简单，对表结构无要求
**缺点**：慢

### 二、折半查找（二分查找）⭐⭐⭐必考

**前提条件**：
1. 必须采用**顺序存储**结构
2. 必须按关键字**有序**排列

**思想**：每次将查找范围缩小一半。

**代码实现**：
\`\`\`c
int Binary_Search(SeqList L, ElemType key) {
    int low = 0, high = L.length - 1, mid;
    
    while (low <= high) {
        mid = (low + high) / 2;
        
        if (L.elem[mid] == key)
            return mid;           // 查找成功
        else if (L.elem[mid] > key)
            high = mid - 1;       // 在左半部分查找
        else
            low = mid + 1;        // 在右半部分查找
    }
    
    return -1;  // 查找失败
}
\`\`\`

**性能分析**：
- **时间复杂度**：O(log₂n) ✅
- **空间复杂度**：O(1)
- **ASL**：log₂(n+1)-1

**判定树**：折半查找的过程可以用一棵二叉树来描述，称为判定树。

**例题**：有序表{1,3,5,7,9,11,13,15}，查找11需要比较几次？

**解**：
1. mid=3，elem[3]=7 < 11，查右半部分
2. mid=5，elem[5]=11 = 11，找到！
   共比较2次

### 三、分块查找（索引顺序查找）⭐

**思想**：结合顺序查找和折半查找的优点。

**要求**：
1. 分块有序：块内可以无序，但块间必须有序
2. 建立索引表

**性能分析**：
- 若分为b块，每块s个元素
- ASL ≈ log₂(b) + s/2
- 当s=√n时，ASL最小，为O(√n)

### 四、三种查找方法比较

| 查找方法 | 时间复杂度 | 存储结构 | 表的要求 |
|----------|------------|----------|----------|
| 顺序查找 | O(n) | 顺序/链式 | 无要求 |
| 折半查找 | O(log₂n) ✅ | 顺序 | 有序 |
| 分块查找 | O(√n) | 顺序+索引 | 分块有序 |

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 折半查找的代码实现
2. ⭐⭐⭐ 折半查找的ASL计算
3. ⭐⭐⭐ 折半查找的判定树
4. ⭐⭐ 三种查找方法的比较
5. ⭐⭐ 顺序查找的哨兵技巧

**典型考题**：
> 【2014统考】对长度为n的有序表进行折半查找，其时间复杂度为（）。
> 
> **答案**：O(log₂n)
`        },
        {
          id: '7.3',
          title: '二叉排序树和平衡二叉树',
          content: `## 7.3 二叉排序树和平衡二叉树 ⭐⭐⭐必考

### 一、二叉排序树（BST）⭐⭐⭐

**定义**：一棵二叉树，若满足以下性质：
1. 若左子树不空，则左子树上所有结点的值均小于根结点的值
2. 若右子树不空，则右子树上所有结点的值均大于根结点的值
3. 左右子树也分别为二叉排序树

**特点**：**中序遍历**得到递增序列 ✅

#### 1. 查找操作

\`\`\`c
BSTNode *BST_Search(BiTree T, ElemType key) {
    while (T != NULL && key != T->data) {
        if (key < T->data)
            T = T->lchild;  // 在左子树中查找
        else
            T = T->rchild;  // 在右子树中查找
    }
    return T;
}
\`\`\`

**时间复杂度**：O(h)，h为树高
- 最好：O(log₂n)（平衡时）
- 最坏：O(n)（单支树）

#### 2. 插入操作

\`\`\`c
int BST_Insert(BiTree &T, KeyType k) {
    if (T == NULL) {  // 原树为空，新插入的记录为根结点
        T = (BiTree)malloc(sizeof(BSTNode));
        T->key = k;
        T->lchild = T->rchild = NULL;
        return 1;  // 插入成功
    }
    else if (k == T->key)  // 树中存在相同关键字的结点
        return 0;  // 插入失败
    else if (k < T->key)
        return BST_Insert(T->lchild, k);  // 插入左子树
    else
        return BST_Insert(T->rchild, k);  // 插入右子树
}
\`\`\`

#### 3. 删除操作 ⭐⭐

**三种情况**：
1. **叶子结点**：直接删除
2. **只有左子树或右子树**：用子树代替该结点
3. **左右子树都有**：用直接前驱（左子树中最右下结点）或直接后继（右子树中最左下结点）代替

#### 4. 性能分析

- **查找成功ASL**：与树的形态有关
- 最好情况：O(log₂n)（平衡二叉树）
- 最坏情况：O(n)（单支树）

### 二、平衡二叉树（AVL树）⭐⭐⭐

**定义**：树上任一结点的左子树和右子树的深度之差不超过1。

**平衡因子（BF）**：左子树高度 - 右子树高度

**平衡条件**：|BF| ≤ 1

#### 1. 失衡调整（旋转）⭐⭐⭐

**LL平衡旋转（右单旋转）**：
- 在A的左孩子的左子树上插入结点导致失衡
- 将A的左孩子B向右上旋转代替A成为根结点

**RR平衡旋转（左单旋转）**：
- 在A的右孩子的右子树上插入结点导致失衡
- 将A的右孩子B向左上旋转代替A成为根结点

**LR平衡旋转（先左后右双旋转）**：
- 在A的左孩子的右子树上插入结点导致失衡
- 先左旋，再右旋

**RL平衡旋转（先右后左双旋转）**：
- 在A的右孩子的左子树上插入结点导致失衡
- 先右旋，再左旋

#### 2. 性能分析

- **查找、插入、删除**：O(log₂n) ✅
- **高度**：h = O(log₂n)

### 三、考研重点

**高频考点**：
1. ⭐⭐⭐ BST的查找、插入、删除操作
2. ⭐⭐⭐ AVL树的四种旋转调整
3. ⭐⭐ BST的ASL计算
4. ⭐⭐ BST的中序遍历是递增序列
5. ⭐⭐ AVL树的平衡因子

**典型考题**：
> 【2017统考】在有n个结点的二叉排序树上进行查找，最坏情况下的时间复杂度为（）。
> 
> **答案**：O(n)（当树退化为单支树时）
`        },
        {
          id: '7.4',
          title: 'B树和B+树',
          content: `## 7.4 B树和B+树 ⭐⭐

### 一、B树（多路平衡查找树）⭐⭐

**m阶B树的定义**：
1. 每个结点至多有m棵子树
2. 若根结点不是叶子结点，则至少有2棵子树
3. 除根之外的所有非叶子结点至少有⌈m/2⌉棵子树
4. 所有的叶子结点都出现在同一层次上

**m阶B树的结点结构**：
\`\`\`
n, P₀, K₁, P₁, K₂, P₂, ..., Kₙ, Pₙ
\`\`\`

其中：
- n：关键字个数
- Kᵢ：关键字
- Pᵢ：指向子树的指针

**约束条件**：⌈m/2⌉-1 ≤ n ≤ m-1

#### 1. B树的查找

类似于二叉排序树，但在每个结点内进行顺序查找或折半查找。

**时间复杂度**：O(logₘn × log₂m) = O(log₂n)

#### 2. B树的插入

**步骤**：
1. 定位：找到插入位置
2. 插入：将关键字插入到相应结点
3. 分裂：若结点关键字数超过m-1，则分裂

**分裂过程**：
- 将结点分成两部分
- 中间关键字上升到父结点
- 若父结点也满了，继续分裂

#### 3. B树的删除

**步骤**：
1. 定位：找到要删除的关键字
2. 删除：
   - 若在叶子结点，直接删除
   - 若在非叶子结点，用直接后继替代后删除
3. 调整：若结点关键字数少于⌈m/2⌉-1，则需要调整
   - 向兄弟借关键字
   - 与兄弟合并

### 二、B+树 ⭐⭐

**特点**：
1. 有n棵子树的结点中含有n个关键字
2. 所有的叶子结点包含了全部关键字及指向相应记录的指针
3. 所有的叶子结点按关键字大小顺序链接
4. 所有非叶子结点可以看成是索引部分

**B树 vs B+树**：

| 比较项 | B树 | B+树 |
|--------|-----|------|
| 关键字分布 | 所有结点 | 只在叶子结点 |
| 查询方式 | 任意位置 | 只能从叶子结点 |
| 范围查询 | 较慢 | 快 ✅ |
| 应用场景 | 文件系统 | 数据库索引 ✅ |

### 三、考研重点

**高频考点**：
1. ⭐⭐ B树的定义和性质
2. ⭐⭐ B树的插入和分裂过程
3. ⭐⭐ B树的删除和合并过程
4. ⭐⭐ B+树的特点
5. ⭐ B树和B+树的区别

**典型考题**：
> 【2016统考】在一棵具有15个关键字的4阶B树中，含关键字的结点数最多是（）。
> 
> **解**：要使结点数最多，每个结点的关键字数应最少
> 4阶B树，非根结点至少⌈4/2⌉-1 = 1个关键字
> 所以最多15个结点
`        },
        {
          id: '7.5',
          title: '散列表',
          content: `## 7.5 散列表（Hash表）⭐⭐⭐必考

### 一、基本概念

**散列函数**：将关键字映射到存储位置的函数 h(key)

**冲突**：不同关键字映射到同一位置

**同义词**：发生冲突的两个关键字

**装填因子α**：α = 表中记录数 / 散列表长度

**ASL影响因素**：
1. 散列函数的好坏
2. 处理冲突的方法
3. 装填因子α

### 二、散列函数的构造方法 ⭐⭐

#### 1. 直接定址法

h(key) = key 或 h(key) = a×key + b

**适用**：关键字分布连续且规模不大

#### 2. 除留余数法 ⭐⭐⭐最常用

h(key) = key % p

**关键**：p的选择
- p应为不大于表长m的**最大质数**
- p不应接近2的幂

**例题**：表长m=13，p应选择？

**解**：p=13（13是质数）

#### 3. 数字分析法

抽取关键字中分布均匀的若干位作为散列地址

#### 4. 平方取中法

取关键字平方后的中间几位作为散列地址

#### 5. 折叠法

将关键字分割成位数相同的几部分，然后取叠加和作为散列地址

### 三、处理冲突的方法 ⭐⭐⭐

#### 1. 开放定址法 ⭐⭐

**线性探测法**：
\`\`\`
hᵢ = (h(key) + i) % m  （i=1,2,...,m-1）
\`\`\`

**缺点**：容易产生"聚集"现象

**二次探测法**：
\`\`\`
hᵢ = (h(key) ± i²) % m
\`\`\`

**优点**：减少了聚集

**伪随机探测法**：
\`\`\`
hᵢ = (h(key) + 伪随机序列) % m
\`\`\`

#### 2. 拉链法（链地址法）⭐⭐⭐推荐

将所有同义词存储在一个线性链表中，由散列地址唯一标识。

**优点**：
- ✅ 不会产生聚集
- ✅ 适合经常插入和删除的情况
- ✅ 装填因子α可以大于1

**缺点**：
- ❌ 指针占用额外空间

### 四、散列表的性能分析 ⭐⭐⭐

**ASL计算公式**：

**线性探测法**：
- 查找成功：ASL ≈ (1 + 1/(1-α)) / 2
- 查找失败：ASL ≈ (1 + 1/(1-α)²) / 2

**拉链法**：
- 查找成功：ASL ≈ 1 + α/2
- 查找失败：ASL ≈ α

**结论**：
- α越小，ASL越小，查找越快
- α越大，空间利用率越高，但查找越慢
- 通常取α=0.5~0.85

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 散列函数的构造（特别是除留余数法）
2. ⭐⭐⭐ 线性探测法的ASL计算
3. ⭐⭐⭐ 拉链法的ASL计算
4. ⭐⭐ 处理冲突的方法比较
5. ⭐⭐ 装填因子的影响
6. ⭐ 手工模拟散列表的构建过程

**典型考题**：
> 【2019统考】用哈希表存储元素时，需要进行冲突处理。当采用开放定址法处理冲突时，以下说法正确的是（）。
> 
> A. 删除元素时可以直接删除
> B. 删除元素时需要做特殊标记
> C. 装填因子可以大于1
> D. 不会产生聚集现象
> 
> **答案**：B（删除时需要标记，否则会影响后续查找）
`        }
      ]
    },
    {
      id: 'ch8',
      number: 8,
      title: '排序',
      score: '5-8分',
      importance: 'medium',
      mindMap: `mindmap
  root((排序))
    基本概念
      稳定性
      内部排序 vs 外部排序
    插入排序 ⭐⭐
      直接插入
      折半插入
      希尔排序
    交换排序 ⭐⭐⭐
      冒泡排序
      快速排序 ⭐⭐⭐
    选择排序 ⭐⭐
      简单选择
      堆排序 ⭐⭐
    归并排序 ⭐⭐
    基数排序
    算法比较 ⭐⭐⭐
      时间复杂度
      空间复杂度
      稳定性
    外部排序⭐⭐
      多路归并
      置换选择排序
      最佳归并树`,
      sections: [
        {
          id: '8.1',
          title: '排序的基本概念',
          content: `## 8.1 排序的基本概念 ⭐⭐

### 一、排序的定义

**排序**：重新排列表中的元素，使表中的元素满足按关键字有序的过程。

**稳定性**：若待排序表中有两个元素Rᵢ和Rⱼ，其对应的关键字相同即keyᵢ=keyⱼ，且在排序前Rᵢ在Rⱼ前面：
- **稳定排序**：排序后Rᵢ仍然在Rⱼ前面
- **不稳定排序**：排序后Rᵢ可能在Rⱼ后面

### 二、内部排序 vs 外部排序

| 类型 | 说明 | 数据位置 |
|------|------|----------|
| **内部排序** | 数据全部在内存中进行排序 | 内存 |
| **外部排序** | 数据太多，需要访问外存 | 内存+外存 |

### 三、考研重点

**高频考点**：
1. ⭐⭐ 稳定性的概念
2. ⭐⭐ 内部排序vs外部排序
3. ⭐⭐⭐ 各种排序算法的比较（时间、空间、稳定性）
`        },
        {
          id: '8.2',
          title: '插入排序',
          content: `## 8.2 插入排序 ⭐⭐

### 一、直接插入排序 ⭐⭐

**思想**：每次将一个待排序的记录按其关键字大小插入到前面已经排好序的子序列中。

**代码实现**：
\`\`\`c
void InsertSort(ElemType A[], int n) {
    for (i = 2; i <= n; i++) {
        if (A[i].key < A[i-1].key) {
            A[0] = A[i];  // 哨兵
            for (j = i - 1; A[0].key < A[j].key; j--)
                A[j+1] = A[j];
            A[j+1] = A[0];
        }
    }
}
\`\`\`

**性能**：最好O(n)，最坏O(n²)，平均O(n²)，空间O(1)，✅稳定

### 二、希尔排序 ⭐⭐

**思想**：先将表分割成若干子表分别排序，待基本有序时再整体进行一次直接插入排序。

**性能**：约O(n¹·³)，空间O(1)，❌不稳定

### 三、考研重点

**高频考点**：
1. ⭐⭐ 直接插入排序的代码
2. ⭐⭐ 希尔排序的步长变化
3. ⭐ 稳定性分析
`        },
        {
          id: '8.3',
          title: '交换排序',
          content: `## 8.3 交换排序 ⭐⭐⭐必考

### 一、冒泡排序 ⭐⭐

**思想**：从后往前两两比较，逆序则交换。

**性能**：最好O(n)，最坏O(n²)，平均O(n²)，空间O(1)，✅稳定

### 二、快速排序 ⭐⭐⭐必考

**思想**：基于分治法，选择枢轴将表分为两部分，递归排序。

**代码实现**：
\`\`\`c
int Partition(ElemType A[], int low, int high) {
    ElemType pivot = A[low];
    while (low < high) {
        while (low < high && A[high] >= pivot) high--;
        A[low] = A[high];
        while (low < high && A[low] <= pivot) low++;
        A[high] = A[low];
    }
    A[low] = pivot;
    return low;
}
\`\`\`

**性能**：最好O(nlog₂n)，最坏O(n²)，平均O(nlog₂n)，空间O(log₂n)，❌不稳定

### 三、考研重点

**高频考点**：
1. ⭐⭐⭐ 快速排序的代码和划分过程
2. ⭐⭐⭐ 第一趟排序的结果
3. ⭐⭐ 时间复杂度分析
4. ⭐ 最坏情况（已有序）
`        },
        {
          id: '8.4',
          title: '选择排序',
          content: `## 8.4 选择排序 ⭐⭐

### 一、简单选择排序 ⭐⭐

**思想**：每趟选择最小的记录放在已排序序列的最后。

**性能**：O(n²)，空间O(1)，❌不稳定

### 二、堆排序 ⭐⭐⭐必考

**堆的定义**：
- **大根堆**：L(i) ≥ L(2i) 且 L(i) ≥ L(2i+1)

**代码框架**：
\`\`\`c
void HeapSort(ElemType A[], int len) {
    BuildMaxHeap(A, len);  // 建堆
    for (i = len; i > 1; i--) {
        swap(A[i], A[1]);     // 输出堆顶
        HeadAdjust(A, 1, i-1); // 调整
    }
}
\`\`\`

**性能**：O(nlog₂n)，空间O(1)，❌不稳定

### 三、考研重点

**高频考点**：
1. ⭐⭐⭐ 堆排序的代码实现
2. ⭐⭐ 大根堆/小根堆的判断
3. ⭐⭐ 建堆时间O(n)
4. ⭐ 稳定性
`        },
        {
          id: '8.5',
          title: '归并排序和基数排序',
          content: `## 8.5 归并排序和基数排序 ⭐⭐

### 一、归并排序 ⭐⭐

**思想**：将两个有序表合并成一个新的有序表。

**性能**：O(nlog₂n)，空间O(n)，✅稳定

**应用**：外部排序、链表排序

### 二、基数排序 ⭐

**思想**：基于关键字各位的大小进行分配和收集。

**性能**：O(d(n+r))，空间O(r)，✅稳定

**优点**：可突破O(nlog₂n)下界

### 三、考研重点

**高频考点**：
1. ⭐⭐ 归并排序的时间空间复杂度
2. ⭐⭐ 归并排序的稳定性
3. ⭐ 基数排序的思想

### 💡 知识关联：堆排序与优先队列

> 🔗 **重要应用**：堆排序中的“堆”是**完全二叉树**的应用
> 
> - **堆的性质**：大根堆/小根堆是完全二叉树
>   - 📖 详见：5.2 二叉树的概念和性质
>   - 特点：父节点 ≥（或 ≤）子节点
> 
> - **优先队列**：基于堆实现的高效数据结构
>   - 📖 详见：3.6 双端队列和应用
>   - 操作：插入O(log n)、删除最大/最小O(log n)
> 
> - **Dijkstra算法**：使用优先队列优化最短路径
>   - 📖 详见：6.5 最短路径
>   - 时间复杂度从O(n²)优化到O((n+m)log n)
> 
> 💡 **理解要点**：堆不仅是排序算法，更是实现优先队列的基础！
`        },
        {
          id: '8.6',
          title: '各种排序算法的比较',
          content: `## 8.6 各种排序算法的比较 ⭐⭐⭐必考

### 一、排序算法综合对比表

>  **重要表格**（来源：B站 蓝不过海呀）—— 建议收藏，考前必背

| 排序类别 | 排序算法 | 最好 | 最坏 | 平均 | 辅助空间 | 稳定性 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **插入排序** | 直接插入排序 | O(n) | O(n²) | O(n²) | O(1) | ✅ 稳定 |
| | 折半插入排序 | O(n) | O(n²) | O(n²) | O(1) | ✅ 稳定 |
| | 希尔排序 | O(n) | O(n²) | O(n¹·³) | O(1) | ❌ 不稳定 |
| **交换排序** | 冒泡排序 | O(n) | O(n²) | O(n²) | O(1) | ✅ 稳定 |
| | 快速排序 | O(nlog₂n) | O(n²) | O(nlog₂n) | O(log₂n) | ❌ 不稳定 |
| **选择排序** | 简单选择排序 | O(n²) | O(n²) | O(n²) | O(1) | ❌ 不稳定 |
| | 堆排序 | O(nlog₂n) | O(nlog₂n) | O(nlog₂n) | O(1) | ❌ 不稳定 |
| **归并排序** | 归并排序 | O(nlog₂n) | O(nlog₂n) | O(nlog₂n) | O(n) | ✅ 稳定 |
| **基数排序** | 基数排序 | O(d(n+r)) | O(d(n+r)) | O(d(n+r)) | O(r) | ✅ 稳定 |

**记忆口诀**：
- 稳定排序：**插冒归基**（插入、冒泡、归并、基数）
- 不稳定排序：**快选希堆**（快速、选择、希尔、堆）
- 空间 O(1)：除快排 O(log₂n)、归并 O(n)、基数 O(r) 外，其余均为 O(1)
- 任何情况都 O(nlog₂n)：**堆排序、归并排序**

### 二、时间复杂度总结

| 排序算法 | 最好 | 平均 | 最坏 |
|----------|------|------|------|
| 直接插入 | O(n) | O(n²) | O(n²) |
| 希尔排序 | - | O(n¹·³) | - |
| 冒泡排序 | O(n) | O(n²) | O(n²) |
| 快速排序 | O(nlog₂n) | O(nlog₂n) | O(n²) |
| 简单选择 | O(n²) | O(n²) | O(n²) |
| 堆排序 | O(nlog₂n) | O(nlog₂n) | O(nlog₂n) ✅ |
| 归并排序 | O(nlog₂n) | O(nlog₂n) | O(nlog₂n) ✅ |
| 基数排序 | O(d(n+r)) | O(d(n+r)) | O(d(n+r)) |

### 三、空间复杂度总结

| O(1) ✅ | O(log₂n) | O(n)  |
|---------|-----------|----------|
| 直接插入 | 快速排序 | 归并排序 |
| 折半插入 |  |  |
| 希尔排序 |  |  |
| 冒泡排序 |  |  |
| 简单选择 |  |  |
| 堆排序 |  |  |

### 四、稳定性总结

**稳定排序 ✅**：直接插入、折半插入、冒泡、归并、基数

**不稳定排序 ❌**：希尔、快速、简单选择、堆

**记忆口诀**："快选希堆"不稳定

### 五、选择建议

| 场景 | 推荐算法 |
|------|----------|
| n较小 | 直接插入、简单选择 |
| n较大，要求稳定 | 归并排序 |
| n较大，不要求稳定 | 快速排序（平均最快） |
| 初始基本有序 | 直接插入、冒泡 |
| 任何情况都要求O(nlogn) | 堆排序、归并排序 |

### 六、考研重点

**高频考点**：
1. ⭐⭐⭐ 各种排序算法的时间、空间、稳定性（综合对比表必背）
2. ⭐⭐ 根据场景选择合适的排序算法
3. ⭐⭐ 第一趟排序后的结果
4. ⭐ 排序算法的选择依据

**典型考题**：
> 【2020统考】下列排序算法中，平均时间复杂度为O(nlog₂n)且稳定的是（）。
>
> A. 快速排序  B. 堆排序  C. 归并排序  D. 希尔排序
>
> **答案**：C
`        },
        {
          id: '8.7',
          title: '外部排序',
          content: `## 8.7 外部排序 ⭐⭐

### 一、外部排序的基本思想

**为什么需要外部排序**：
- 待排序数据量超过内存容量，无法一次性载入
- 数据存放在外存（磁盘），需**内外存结合**完成排序

**基本流程**（两步走）：

1. **生成初始归并段**：分批读入内存，用内部排序生成有序段，写回外存
2. **多路归并**：将多个初始归并段逐步合并，直到整体有序

**性能瓶颈**：外存的**I/O次数**（磁盘读写远慢于内存）

### 二、多路归并 ⭐⭐⭐

**k路归并**：每次从k个归并段的当前最小元素中选最小者输出。

| 项目 | 说明 |
|------|------|
| 归并趟数 | s = ⌈logₖm⌉（m为初始归并段数） |
| 内部比较 | 每元素需 k-1 次比较（用败者树降为 ⌈log₂k⌉） |
| I/O次数 | 每趟对全部数据读写一遍，共 2×m×s 次块I/O |

**关键结论** ⭐⭐⭐：
- 增加归并路数k → **减少归并趟数** → **减少I/O次数**（主要矛盾）
- 但k增大 → 内部比较次数增加 → 用**败者树**优化比较

**例题**：m=32个初始段：
- 2路归并：s = ⌈log₂32⌉ = 5趟
- 8路归并：s = ⌈log₈32⌉ = 2趟

### 三、置换-选择排序 ⭐⭐

**目的**：生成**更长的初始归并段**，减少初始段数量。

**思想**：
- 类似堆排序，从输入缓冲中选出满足条件的最小元素输出
- 当缓冲区中所有元素都不满足条件时，当前归并段结束，开始新段
- 输出的归并段**段内有序、段间无序**

**效果** ⭐⭐：初始归并段平均长度 = **2w**（w为工作区大小），是简单内部排序（w）的2倍 → 初始段数量减半。

### 四、最佳归并树 ⭐⭐

**问题**：各初始归并段长度不等时，不同归并顺序导致I/O总量不同。

**方法**：仿照**哈夫曼树**的构造思想：
- 以各归并段长度为权值构造哈夫曼树
- **短的段先合并，长的段后合并**
- 带权路径长度WPL最小 → 总I/O次数最少

**补充虚段**：若 (m-1) % (k-1) ≠ 0，需补充长度为0的**虚段**，凑满k叉哈夫曼树。

### 五、考研重点

**高频考点**：
1. ⭐⭐⭐ 归并趟数与I/O次数的计算（s = ⌈logₖm⌉）
2. ⭐⭐ 增加归并路数对性能的影响（减趟数、减I/O）
3. ⭐⭐ 置换-选择排序生成初始段的平均长度（2w）
4. ⭐ 最佳归并树与哈夫曼树的联系

**典型考题**：
> 设有16个初始归并段，采用4路归并，需要几趟归并？
> 
> **解**：s = ⌈log₄16⌉ = 2趟`
        }
      ]
    }
  ])
  
  // 计算属性
  const currentChapter = computed(() => {
    return chapters.value.find(ch => ch.id === currentChapterId.value)
  })
  
  const currentSection = computed(() => {
    if (!currentChapter.value) return null
    return currentChapter.value.sections.find(sec => sec.id === currentSectionId.value)
  })
  
  const overallProgress = computed(() => {
    let totalSections = 0
    let studiedCount = 0
    
    chapters.value.forEach(chapter => {
      chapter.sections.forEach(section => {
        totalSections++
        if (studiedSections.value.has(section.id)) {
          studiedCount++
        }
      })
    })
    
    return totalSections > 0 ? Math.round((studiedCount / totalSections) * 100) : 0
  })
  
  // 方法
  const selectChapter = (chapterId: string) => {
    currentChapterId.value = chapterId
    const chapter = chapters.value.find(ch => ch.id === chapterId)
    if (chapter && chapter.sections.length > 0) {
      currentSectionId.value = chapter.sections[0].id
    }
  }
  
  const selectSection = (sectionId: string) => {
    currentSectionId.value = sectionId
    // 根据小节ID自动推断章节ID
    const chapterNum = sectionId.split('.')[0]
    const newChapterId = `ch${chapterNum}`
    currentChapterId.value = newChapterId
  }
  
  const toggleSectionStudied = (sectionId: string) => {
    if (studiedSections.value.has(sectionId)) {
      studiedSections.value.delete(sectionId)
    } else {
      studiedSections.value.add(sectionId)
    }
  }
  
  const isSectionStudied = (sectionId: string) => {
    return studiedSections.value.has(sectionId)
  }
  
  return {
    // 状态
    currentChapterId,
    currentSectionId,
    studiedSections,
    chapters,
    
    // 计算属性
    currentChapter,
    currentSection,
    overallProgress,
    
    // 方法
    selectChapter,
    selectSection,
    toggleSectionStudied,
    isSectionStudied
  }
})
