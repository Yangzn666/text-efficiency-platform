import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

interface Section {
  id: string
  title: string
  content: string        // Markdown格式
  mindMap?: string       // Mermaid语法（可选）
}

interface Chapter {
  id: string
  number: number
  title: string
  score: string          // 分值范围
  importance: 'high' | 'medium' | 'low'
  sections: Section[]
  mindMap: string        // Mermaid语法
}

export const useOperatingSystemStore = defineStore('operatingSystem', () => {
  // 当前选中的章节和小节
  const currentChapterId = ref<string>('ch1')
  const currentSectionId = ref<string>('1.1')
  
  // 学习进度记录 - 立即从localStorage加载
  const studiedSections = ref<Set<string>>(new Set())
  
  // 立即加载进度,防止被空Set覆盖
  const savedProgress = localStorage.getItem('os_studied_sections')
  if (savedProgress) {
    studiedSections.value = new Set(JSON.parse(savedProgress))
  }
  
  // 所有章节数据 - 按照王道操作系统教材目录（5章）
  const chapters = ref<Chapter[]>([
    {
      id: 'ch1',
      number: 1,
      title: '计算机系统概述',
      score: '2-4分',
      importance: 'medium',
      mindMap: `mindmap
  root((计算机系统概述))
    操作系统的基本概念
      操作系统的概念
      操作系统的功能和目标
      操作系统的特征
        并发
        共享
        虚拟
        异步
    操作系统的发展
      手工操作阶段
      批处理阶段
      分时操作系统
      实时操作系统
      网络操作系统和分布式系统
      微机操作系统
    操作系统的运行环境
      操作系统内核
      处理机的双重工作模式
      中断和异常的概念
      系统调用
    操作系统结构
    操作系统引导
    虚拟机
      虚拟机的基本概念
      虚拟机的实现`,
      sections: [
        {
          id: '1.1',
          title: '操作系统的基本概念',
          content: `## 1.1 操作系统的基本概念 ⭐⭐ 重点

### 一、操作系统的定义

**定义**：操作系统（Operating System, OS）是指控制和管理整个计算机系统的硬件和软件资源，并合理地组织调度计算机的工作和资源的分配，以提供给用户和其他软件方便的接口和环境，它是计算机系统中最基本的系统软件。

**核心理解**：
-  **位置**：紧贴硬件之上的第一层软件
- 📍 **地位**：最基础、最重要、最复杂的系统软件
- 📍 **作用**：承上启下，管理硬件，服务用户

**与其他软件的关系**：
\`\`\`
用户应用程序
    ↓
其他系统软件（数据库、编译器等）
    ↓
操作系统 ← 最核心的系统软件
    ↓
计算机硬件
\`\`\`

### 二、操作系统的功能和目标 ⭐⭐⭐ 必考

#### 1. 作为计算机系统资源的管理者

操作系统管理的四大资源：

| 资源类型 | 管理内容 | 主要功能 |
|---------|---------|----------|
| **处理机** | CPU时间 | 进程管理、线程管理、CPU调度 |
| **存储器** | 内存空间 | 内存分配、回收、保护、扩充 |
| **文件** | 磁盘空间 | 文件管理、目录管理、存储空间管理 |
| **设备** | I/O设备 | 设备分配、缓冲管理、驱动管理 |

**详解**：

1. **处理机管理**（进程管理）
   - 进程控制：创建、撤销、状态转换
   - 进程同步：协调进程间的执行顺序
   - 进程通信：进程间信息交换
   - 死锁处理：预防、避免、检测、解除
   - 线程管理：线程创建、调度、同步

2. **存储器管理**（内存管理）
   - 内存分配与回收
   - 地址映射（逻辑地址→物理地址）
   - 内存保护（防止越界访问）
   - 内存扩充（虚拟内存技术）

3. **文件管理**
   - 文件存储空间管理
   - 目录管理
   - 文件读写管理
   - 文件共享与保护

4. **设备管理**
   - 缓冲管理
   - 设备分配
   - 设备处理（驱动程序）
   - 虚拟设备技术（SPOOLing）

#### 2. 作为用户与计算机硬件系统之间的接口

操作系统提供三种接口：

| 接口类型 | 面向用户 | 使用方式 | 示例 |
|---------|---------|---------|------|
| **命令接口** | 普通用户 | 键盘输入命令 | Windows CMD、Linux Shell |
| **程序接口** | 程序员 | 系统调用（System Call） | open(), read(), write() |
| **图形接口** | 普通用户 | 鼠标点击、触摸 | Windows GUI、macOS Finder |

**重要概念**：
- ✅ **命令接口**：联机命令接口（交互式）、脱机命令接口（批处理）
- ✅ **程序接口**：由一组**系统调用**组成，是应用程序获取操作系统服务的唯一途径
- ✅ **图形接口**：现代操作系统的主流接口，如Windows的资源管理器

#### 3. 实现了对计算机资源的抽象

**裸机**：只有硬件，无软件的计算机

**操作系统的作用**：
- 覆盖在裸机之上，构成**虚拟机**
- 隐藏硬件的复杂性和多样性
- 提供简单、方便的使用接口

\`\`\`
用户视角：简单、方便、统一的接口
    ↓
操作系统：资源管理 + 抽象层
    ↓
硬件视角：复杂、多样的物理设备
\`\`\`

**抽象层次**：
- 第一次抽象：将裸机扩展为功能更强的虚拟机
- 第二次抽象：提供用户接口，使虚拟机更易使用

### 三、操作系统的四个特征 ⭐⭐ 必考

#### 1. 并发（Concurrency）

**定义**：两个或多个事件在**同一时间间隔内**发生

**关键点**：
- ⏰ **时间间隔** vs **时刻**：并发是间隔，不是时刻
- 🔄 **宏观 vs 微观**：
  - 宏观上：多个程序同时执行
  - 微观上：程序交替执行（分时复用CPU）

**并发 vs 并行**：

| 特性 | 并发（Concurrency） | 并行（Parallelism） |
|-----|---------------------|---------------------|
| **定义** | 同一时间间隔内发生 | 同一时刻同时发生 |
| **执行方式** | 交替执行 | 真正同时执行 |
| **硬件要求** | 单核CPU即可 | 需要多核CPU |
| **示例** | 单核CPU多任务 | 多核CPU多任务 |

**生活中的例子**：
-  **并发**：一个人同时做饭、接电话、看孩子（交替进行）
- 🍳 **并行**：三个人分别做饭、接电话、看孩子（同时进行）

#### 2. 共享（Sharing）

**定义**：系统中的资源可供内存中多个并发执行的进程共同使用

**两种共享方式**：

| 共享方式 | 特点 | 示例 |
|---------|------|------|
| **互斥共享** | 某时间段只允许一个进程访问 | 打印机、磁带机 |
| **同时访问** | 宏观上多个进程同时访问 | 磁盘文件、共享内存 |

**互斥共享（临界资源）**：
- 某些资源一段时间只允许一个进程访问
- 访问期间其他进程必须等待
- 典型设备：打印机、扫描仪等独占设备

**同时访问**：
- 某些资源允许多个进程“同时”访问
- 宏观上同时，微观上交替
- 典型资源：磁盘、文件、共享内存

**并发与共享的关系**：
- 互为存在条件，相辅相成
- 没有并发，就不需要共享
- 没有共享，并发的意义不大

#### 3. 虚拟（Virtual）

**定义**：通过某种技术将一个物理实体映射为多个逻辑对应物

**两种虚拟技术**：

| 虚拟技术 | 实现方法 | 示例 |
|---------|---------|------|
| **空分复用** | 资源空间分割 | 虚拟内存、虚拟设备 |
| **时分复用** | 时间片轮流使用 | 虚拟处理器、虚拟显示器 |

**虚拟技术示例**：

1. **虚拟处理器**（时分复用）
   - 物理：1个CPU
   - 虚拟：多个逻辑CPU（进程觉得各自独占CPU）
   - 实现：进程调度、时间片轮转

2. **虚拟存储器**（空分复用 + 时分复用）
   - 物理：4GB内存
   - 虚拟：每个进程看到4GB地址空间
   - 实现：分页、分段、段页式管理

3. **虚拟设备**（SPOOLing技术）
   - 物理：1台打印机
   - 虚拟：每个进程都有专属打印机
   - 实现：磁盘缓冲区 + 后台打印服务

#### 4. 异步（Asynchronism）

**定义**：在多道程序环境下，进程的执行不是一气呵成的，而是走走停停，以不可预知的速度向前推进

**特点**：
-  **不可预知性**：进程何时执行、执行多久、何时阻塞都不确定
- ⚡ **异步性**：由并发引起，因为资源有限，进程需要等待
- ✅ **可再现性**：尽管异步，但结果应该确定（操作系统需保证）

**示例**：
\`\`\`
进程A：运行10ms → 等待I/O（100ms） → 运行5ms → 结束
进程B：等待CPU（10ms） → 运行20ms → 等待I/O（50ms） → 运行10ms → 结束
\`\`\`

### 四、操作系统的四大特征之间的关系

\`\`\`
并发（基础）
  ↓
共享（互依）
  ↓
虚拟（技术）
  ↓
异步（结果）
\`\`\`

**关系总结**：
1. **并发**和**共享**是操作系统最基本的两个特征，互为存在条件
2. **虚拟**以并发和共享为前提，是实现技术
3. **异步**是并发和共享的必然结果

### 五、操作系统的目标

1. **方便性**：提供友好接口，使计算机更易使用
2. **有效性**：提高系统资源利用率和系统吞吐量
3. **可扩充性**：方便添加新功能和模块
4. **开放性**：遵循国际标准，实现互操作

---

 **学习建议**：
- ✅ 操作系统的四个特征是408考试的高频考点
- ✅ 理解并发与并行的区别
- ✅ 掌握互斥共享与同时访问的区别
- ✅ 了解虚拟技术的两种实现方式`
        },
        {
          id: '1.2',
          title: '操作系统的发展',
          content: `## 1.2 操作系统的发展

### 一、手工操作阶段（无操作系统）

**时代背景**：1940s-1950s，第一代计算机（电子管）

**特点**：
- 🔧 **人工操作**：程序员直接插拔线路、操作开关
-  **CPU等待**：CPU大量时间等待人工操作
- 📉 **利用率低**：CPU利用率不到5%
- 📝 **独占使用**：一次只能运行一个程序

**工作流程**：
\`\`\`
1. 程序员将程序卡片放入读卡器
2. CPU执行程序
3. 程序结束，输出结果
4. 程序员取走结果，准备下一个程序
\`\`\`

**问题**：
- 人机矛盾：CPU速度 >> 人工操作速度
- 资源浪费：CPU大量空闲等待

### 二、批处理阶段

#### 1. 单道批处理系统

**产生背景**：为了解决人机矛盾

**核心思想**：
- 引入**监督程序**（Monitor）
- 自动完成作业过渡
- 减少人工干预

**特点**：
- 📚 **单道性**：内存中仅有一道程序运行
- 🔄 **自动性**：作业自动过渡，无需人工干预
-  **顺序性**：作业按顺序执行

**工作流程**：
\`\`\`
输入井 → 监督程序 → 内存（1道程序） → CPU执行 → 输出井
\`\`\`

**优点**：
- ✅ 减少了人工干预
- ✅ 提高了CPU利用率（约20-30%）

**缺点**：
- ❌ 资源利用率仍然不高
- ❌ CPU与I/O设备串行工作
- ❌ 内存中只有一道程序

**示例**：
\`\`\`
程序A：CPU计算 10s → I/O操作 20s → CPU计算 5s
程序B：等待程序A完成后再执行
→ CPU在程序A的I/O操作期间空闲！
\`\`\`

#### 2. 多道批处理系统

**产生背景**：为了进一步提高资源利用率

**核心思想**：
- 内存中同时装入**多道程序**
- 宏观上并行，微观上串行
- CPU与I/O设备并行工作

**特点**：
- 📚 **多道性**：内存中同时有多道程序
- 🔄 **宏观并行**：用户感觉多个程序同时执行
- ⚡ **微观串行**：单核CPU交替执行
-  **自动调度**：操作系统自动调度

**工作原理**：
\`\`\`
程序A：CPU计算 10s → I/O操作 20s → CPU计算 5s
程序B：               CPU计算 15s → I/O操作 10s
程序C：                              CPU计算 8s

CPU利用：
[程序A 10s][程序B 15s][程序A 5s][程序C 8s]
         ↑              ↑
      程序A做I/O      程序B做I/O
      程序B用CPU      程序C用CPU
\`\`\`

**优点**：
- ✅ 资源利用率高（CPU、内存、I/O设备）
- ✅ 系统吞吐量大
- ✅ CPU与I/O设备并行

**缺点**：
- ❌ **交互性差**：用户无法与程序交互
- ❌ **响应时间长**：作业需要等待
- ❌ **无实时性**：不适合实时控制

**性能指标对比**：

| 指标 | 单道批处理 | 多道批处理 |
|-----|-----------|----------|
| CPU利用率 | 20-30% | 70-90% |
| 吞吐量 | 低 | 高 |
| 资源利用率 | 较低 | 高 |
| 交互性 | 无 | 无 |

### 三、分时操作系统

**产生背景**：为了满足人机交互需求

**核心思想**：
- **时间片轮转**（Round Robin）
- 每个用户分配一个时间片
- 轮流使用CPU

**四大特征**：

| 特征 | 说明 | 示例 |
|-----|------|------|
| **多路性** | 同时为多个用户服务 | 多个终端同时登录 |
| **独立性** | 用户之间互不干扰 | 每个用户感觉独占系统 |
| **及时性** | 快速响应（秒级） | 用户输入立即响应 |
| **交互性** | 人机对话 | 用户可随时干预程序 |

**时间片轮转调度**：
\`\`\`
CPU时间轴：
[用户A 100ms][用户B 100ms][用户C 100ms][用户A 100ms]...

每个用户感觉：
- 我的程序一直在运行
- 输入立即得到响应
- 好像独占了整个系统
\`\`\`

**关键技术**：
- 时间片：通常10-100ms
- 进程切换：保存/恢复现场
- 中断机制：时间片到，产生中断

**代表系统**：
- UNIX（最著名）
- Linux
- Windows（部分特性）

**优点**：
- ✅ 良好的交互性
- ✅ 及时响应
- ✅ 用户友好

**缺点**：
- ❌ 实时性不够（不适合实时控制）
- ❌ 开销较大（频繁切换）

### 四、实时操作系统

**产生背景**：为了满足实时控制需求

**核心思想**：
- 在**严格时间限制**内响应外部事件
- 高可靠性、高实时性

**分类**：

| 类型 | 特点 | 应用场景 | 示例 |
|-----|------|---------|------|
| **硬实时** | 必须严格按时完成 | 工业控制、航空航天 | 导弹制导、汽车ABS |
| **软实时** | 偶尔可以接受延迟 | 多媒体、网络传输 | 视频播放、在线游戏 |

**特点**：
- ⏰ **及时性**：毫秒级甚至微秒级响应
-  **可靠性**：高可靠性，不能崩溃
- 🎯 **确定性**：响应时间可预测
- 🚫 **交互性弱**：通常无人工交互

**实时调度算法**：
- EDF（Earliest Deadline First）
- RMS（Rate Monotonic Scheduling）

**应用场景**：
- 工业控制（机器人、自动化生产线）
- 航空航天（飞行控制、导航系统）
- 军事系统（雷达、导弹制导）
- 医疗设备（心脏起搏器、呼吸机）
- 汽车电子（ABS、ESP）

### 五、分时 vs 实时对比 ⭐⭐ 重点

| 对比项 | 分时操作系统 | 实时操作系统 |
|-------|-------------|------------|
| **设计目标** | 交互性、公平性 | 及时性、可靠性 |
| **响应时间** | 秒级 | 毫秒/微秒级 |
| **调度策略** | 时间片轮转 | 优先级调度 |
| **可靠性** | 一般 | 极高 |
| **交互性** | 强 | 弱 |
| **应用** | 通用计算 | 专用控制 |

### 六、网络操作系统和分布式系统

#### 1. 网络操作系统（NOS）

**特点**：
- 多台计算机通过网络连接
- 资源共享（文件、打印机等）
- 用户透明访问

**代表**：
- Windows Server
- Novell NetWare
- Linux NFS

#### 2. 分布式操作系统

**特点**：
- 多台计算机协同工作
- 对用户透明（像一台计算机）
- 高可靠性、高可用性

**与网络操作系统的区别**：
- 网络OS：用户知道多台计算机存在
- 分布式OS：用户感觉只有一台计算机

### 七、微机操作系统

**发展**：
- MS-DOS（命令行）
- Windows（图形界面）
- macOS
- Linux（桌面版）

**特点**：
- 单用户为主
- 图形界面友好
- 功能丰富

### 八、现代操作系统的发展趋势

1. **微内核架构**：Windows NT、macOS X
2. **虚拟化技术**：VMware、Docker
3. **云计算**：AWS、阿里云
4. **移动操作系统**：Android、iOS
5. **物联网OS**：FreeRTOS、RT-Thread

---

 **学习提示**：
- ✅ 理解单道 vs 多道批处理的区别
- ✅ 掌握分时系统的四大特征
- ✅ 区分硬实时和软实时
- ✅ 了解各种操作系统的优缺点`,
        },
        {
          id: '1.3',
          title: '操作系统的运行环境',
          content: `## 1.3 操作系统的运行环境 ⭐⭐ 重点

### 一、操作系统内核

**定义**：内核是操作系统最基本、最核心的部分，**常驻内存**

**内核的功能**：

| 功能 | 说明 | 示例 |
|-----|------|------|
| **支撑功能** | 为内核的其他部分提供支持 | 中断处理、时钟管理、原语操作 |
| **资源管理功能** | 管理系统资源 | 进程管理、存储器管理、设备管理 |

**内核模式（核心态）**：
- 操作系统内核程序运行在核心态
- 可以执行**特权指令**
- 可以访问所有系统资源

**特权指令**：
- 只能在核心态执行的指令
- 例如：I/O指令、启动设备指令、修改寄存器指令
- 用户程序执行特权指令会引发异常
- 💡 点击【特权指令】【修改寄存器】【PSW】等关键词查看详细解释

### 二、处理机的双重工作模式 ⭐⭐ 必考

#### 1. 用户态（目态）

**特点**：
- 🚫 **限制**：不能执行特权指令
- 🚫 **限制**：不能访问核心资源
- ✅ **用途**：运行用户程序

**访问范围**：
- 只能访问用户地址空间
- 只能执行非特权指令

#### 2. 核心态（系统态、管态、内核态）

**特点**：
- ✅ **权限**：可以执行特权指令
- ✅ **权限**：可以访问所有资源
- ✅ **用途**：运行操作系统内核程序

**访问范围**：
- 可以访问所有地址空间
- 可以执行所有指令

#### 3. 模式切换 ⭐ 重点

**用户态 → 核心态**：

| 切换方式 | 说明 | 示例 |
|---------|------|------|
| **系统调用** | 用户程序主动请求 | open(), read(), write() |
| **异常** | 程序运行出错 | 除零错误、缺页异常 |
| **外围设备中断** | 外部设备完成I/O | 磁盘读写完成、键盘输入 |

**核心态 → 用户态**：
- 操作系统完成服务后，主动切换回用户态
- 执行**特权指令**修改程序状态字（PSW）

**模式切换 vs 进程切换**：

| 对比项 | 模式切换 | 进程切换 |
|-------|---------|--------|
| **切换内容** | 仅切换工作模式 | 切换整个进程上下文 |
| **涉及范围** | 用户态↔核心态 | 进程A→进程B |
| **时间开销** | 较小 | 较大 |
| **必然关系** | 模式切换不一定进程切换 | 进程切换一定伴随模式切换 |

**重要结论**：
- ✅ 进程切换 **一定** 会引发模式切换
- ❌ 模式切换 **不一定** 引发进程切换

### 三、中断和异常的概念 ⭐⭐ 必考

#### 1. 中断（Interrupt）

**定义**：来自CPU**外部**的异步事件

**特点**：
- 来自外部（硬件设备）
- 发生时间**不可预知**（异步）
- 与当前执行的指令**无关**

**中断源**：

| 中断源 | 说明 | 示例 |
|-------|------|------|
| **I/O中断** | 外部设备完成操作 | 磁盘读写完成、打印机就绪 |
| **硬件故障中断** | 硬件出错 | 电源故障、内存校验错误 |
| **时钟中断** | 定时器到期 | 时间片用完 |

**时钟中断详解**：
- 📍 **硬件基础**：由硬件定时器（Timer）产生，定时发出中断请求
- 📍 **作用**：
  - 实现**时间片轮转调度**（分时系统的核心）
  - 提供**系统时钟**（维护当前时间）
  - 实现**延时和定时**功能
  - 防止进程**长时间占用CPU**
- 📍 **处理流程**：
  - 定时器到期 → 发出时钟中断 → CPU暂停当前进程
  - 操作系统执行时钟中断处理程序
  - 更新时间信息、检查时间片是否用完
  - 如果时间片用完，进行进程调度
  - 恢复被中断进程或切换到新进程
- 📍 **重要性**：是操作系统实现**并发**和**分时**的基础

**中断处理过程**：

<pre>
1. CPU收到中断请求
2. 保存当前执行状态（断点、寄存器）
3. 切换到核心态
4. 执行中断服务程序
5. 恢复现场，返回用户态
</pre>

**中断隐指令（硬件自动完成）**：
-  **保存断点**：将PC值（程序计数器）保存到堆栈
-  **保存PSW**：将程序状态字保存到堆栈
- 📍 **关中断**：禁止新的中断（保证中断处理的原子性）
-  **转中断服务程序**：根据中断向量表跳转到对应的处理程序

**操作系统保存的内容**：
- 📍 **通用寄存器**：由中断服务程序保存到进程控制块（PCB）
-  **其他状态信息**：如标志寄存器、段寄存器等

**重要区别**：
- 💡 **PC值**由**中断隐指令（硬件）**自动保存
- 💡 **通用寄存器**由**操作系统（软件）**保存

#### 2. 异常（Exception）

**定义**：来自CPU**内部**的同步事件

**特点**：
- 来自内部（CPU自身）
- 发生时间**可预知**（同步）
- 与当前执行的指令**有关**

**异常分类**：

| 类型 | 说明 | 示例 |
|-----|------|------|
| **故障（Fault）** | 可修复，恢复后重新执行 | 缺页异常、除零错误 |
| **陷阱（Trap）** | 报告事件，继续执行 | 系统调用、断点调试 |
| **终止（Abort）** | 不可修复，终止程序 | 硬件故障、非法指令 |

#### 3. 中断 vs 异常对比 ⭐⭐ 重点

| 对比项 | 中断（Interrupt） | 异常（Exception） |
|-------|------------------|------------------|
| **来源** | CPU外部（硬件设备） | CPU内部（程序自身） |
| **同步性** | 异步（不可预知） | 同步（可预知） |
| **与指令关系** | 无关 | 有关 |
| **处理时机** | 当前指令执行完后 | 立即处理 |
| **返回地址** | 下一条指令 | 可能是当前指令 |
| **示例** | I/O完成、时钟中断 | 缺页、除零、系统调用 |

**记忆口诀**：
-  **中断**：**外**部**异**步
- 📍 **异常**：**内**部**同**步

### 四、系统调用 ⭐⭐⭐ 必考

#### 1. 系统调用的概念

**定义**：系统调用是操作系统为应用程序使用内核功能所提供的接口

**本质理解**：
- 📍 **应用程序**不能直接访问硬件和内核资源
- 📍 必须通过**系统调用**请求操作系统提供服务
- 📍 是用户态程序进入核心态的唯一合法途径
- 📍 用户程序获取操作系统服务的**唯一途径**

**系统调用 vs 库函数**：

| 对比项 | 系统调用 | 库函数 |
|-------|--------|------|
| **定义** | 操作系统提供的内核接口 | 程序员编写的函数库 |
| **执行态** | 核心态 | 用户态 |
| **性能** | 较慢（需要模式切换） | 快（直接调用） |
| **可移植性** | 依赖操作系统 | 跨平台 |
| **示例** | open(), read(), fork() | printf(), malloc(), strlen() |

**重要关系**：
- 💡 库函数**可能调用**系统调用（如printf()内部调用write()）
- 💡 库函数**也可能不调用**系统调用（如strlen()只是计算字符串长度）
- 💡 系统调用是**操作系统提供的**，库函数是**程序员提供的**

**特点**：
- 是**程序接口**的具体体现
- 用户程序通过系统调用请求操作系统服务
- 执行系统调用会引发**陷阱（Trap）**
- 从用户态切换到核心态

#### 2. 系统调用的实现过程

\`\`\`
用户程序调用系统调用
    ↓
陷入指令（Trap指令）
    ↓
CPU切换到核心态
    ↓
操作系统执行服务例程
    ↓
返回用户态
    ↓
用户程序继续执行
\`\`\`

**关键步骤**：

1. **传递参数**：
   - 方式1：通过寄存器传递
   - 方式2：通过内存块传递
   - 方式3：通过栈传递

2. **执行陷入指令**：
   - 触发陷阱（Trap）
   - CPU切换到核心态

3. **查找系统调用表**：
   - 根据系统调用号查找对应的服务例程

4. **执行服务例程**：
   - 完成请求的服务

5. **返回结果**：
   - 返回用户态
   - 继续执行用户程序

#### 3. 系统调用 vs 普通函数调用

| 对比项 | 系统调用 | 普通函数调用 |
|-------|---------|------------|
| **运行模式** | 核心态 | 用户态 |
| **入口** | 陷入指令 | 函数调用指令 |
| **开销** | 较大（模式切换） | 较小 |
| **权限** | 可以执行特权指令 | 不能执行特权指令 |
| **实现** | 操作系统提供 | 程序自身实现 |

#### 4. 系统调用的类型

**常见系统调用**：

| 类型 | 功能 | 示例 |
|-----|------|------|
| **进程控制** | 创建、终止进程 | fork(), exit() |
| **文件管理** | 文件操作 | open(), read(), write(), close() |
| **设备管理** | 设备操作 | ioctl() |
| **信息维护** | 获取系统信息 | getpid(), time() |
| **通信** | 进程间通信 | pipe(), shmget() |

### 五、命令解释程序（Shell） ⭐⭐ 重点

**定义**：命令解释程序是操作系统提供的用户接口，用于接收、解析和执行用户输入的命令

**功能**：
- 📍 接收用户输入的命令
- 📍 解析命令并提取参数
- 📍 调用相应的系统调用或程序
- 📍 显示执行结果

**实现方式**：

| 方式 | 说明 | 示例 |
|-----|------|------|
| **联机命令接口** | 交互式，逐条执行 | Windows CMD、Linux Shell |
| **脱机命令接口** | 批处理，一次执行多条 | 批处理脚本、Shell脚本 |

**Shell的工作流程**：

<pre>
用户输入命令（如 ls -l）
    ↓
Shell解析命令（提取命令名和参数）
    ↓
Shell调用fork()创建子进程
    ↓
子进程调用exec()执行命令
    ↓
命令执行完成，返回结果
    ↓
Shell显示结果，等待下一条命令
</pre>

**常见Shell**：
- **Windows**：CMD、PowerShell
- **Linux/Unix**：bash、sh、csh、zsh

**Shell vs 系统调用**：
- 📍 Shell是**用户态程序**，系统调用是**内核接口**
- 📍 Shell通过调用系统调用来实现命令功能
-  用户可以直接调用系统调用，也可以通过Shell间接调用

### 六、用户态到核心态的转换途径 ⭐⭐ 必考

**三种途径**：

| 途径 | 说明 | 示例 |
|-----|------|------|
| **系统调用** | 用户程序主动请求 | 执行open()读取文件 |
| **异常** | 程序运行出错 | 除零错误、缺页异常 |
| **外围设备中断** | 外部设备完成I/O | 磁盘读写完成 |

**记忆技巧**：
- 📍 **主动**：系统调用（用户程序请求）
- 📍 **被动**：异常（程序出错）、中断（外部事件）

---

 **学习提示**：
- ✅ 用户态和核心态的区别是高频考点
- ✅ 中断和异常的对比必考
- ✅ 系统调用的实现过程要理解
- ✅ 模式切换和进程切换的关系要掌握`,
        },
        {
          id: '1.4',
          title: '操作系统结构',
          content: `## 1.4 操作系统结构

### 一、操作系统的结构设计目标

**设计目标**：
- 🎯 **正确性**：功能正确，无逻辑错误
- 🎯 **高效性**：资源利用率高，响应快
- 🎯 **可维护性**：易于修改和扩展
- 🎯 **灵活性**：适应不同硬件和应用

### 二、大内核（宏内核）vs 微内核 ⭐⭐ 重点

#### 1. 大内核（Monolithic Kernel）

**特点**：
- 📦 **集成度**：所有功能集成在内核中
- ⚡ **性能**：高（函数直接调用）
- 📏 **代码规模**：大（百万行级）
- 🔧 **可维护性**：差（牵一发而动全身）

**架构**：
\`\`\`
─────────────────────────────────┐
│           用户空间               │
├─────────────────────────────────┤
│        大内核（全部功能）         │
│  ┌─────┬─────┬─────┬─────┐    │
│  │进程  │内存  │文件  │设备  │    │
│  │管理  │管理  │管理  │管理  │    │
│  └──────────┴─────┴─────    │
└─────────────────────────────────┘
\`\`\`

**代表系统**：
- Linux
- UNIX
- Windows（部分）

**优点**：
- ✅ 性能高（无需上下文切换）
- ✅ 实现简单

**缺点**：
- ❌ 代码庞大，难以维护
- ❌ 可靠性低（一个模块崩溃导致整个系统崩溃）
- ❌ 可扩展性差

#### 2. 微内核（Microkernel）

**特点**：
- 📦 **集成度**：只保留最基本功能
- ⚡ **性能**：较低（需要消息传递）
- 📏 **代码规模**：小（几千到几万行）
- 🔧 **可维护性**：好（模块化设计）

**架构**：
\`\`\`
┌─────────────────────────────────┐
│           用户空间               │
│  ┌─────┬─────┬─────┬─────┐    │
│  │进程  │内存  │文件  │设备  │    │
│  │服务  │服务  │服务  │服务  │    │
│  └─────┴─────┴─────┴─────┘    │
├─────────────────────────────────┤
│        微内核（最小功能集）       │
│  ┌─────┬─────┬─────┐          │
│  │进程  │通信  │中断  │          │
│  │调度  │      │处理  │          │
│  └─────┴─────┴─────┘          │
─────────────────────────────────┘
\`\`\`

**微内核的基本功能**：
- 进程/线程管理（最基本的调度）
- 进程间通信（IPC）
- 中断处理
- 内存管理（基本的地址映射）

**其他功能**：
- 文件系统、设备驱动、网络协议等作为**用户态服务**实现

**代表系统**：
- Minix
- Mach（macOS的基础）
- QNX（实时系统）
- Windows NT（混合内核）

**优点**：
- ✅ 代码小，易于维护
- ✅ 可靠性高（一个服务崩溃不影响其他）
- ✅ 可扩展性好
- ✅ 安全性高

**缺点**：
- ❌ 性能较低（频繁的消息传递和上下文切换）
- ❌ 实现复杂

#### 3. 大内核 vs 微内核对比 ⭐⭐ 必考

| 对比项 | 大内核 | 微内核 |
|-------|--------|--------|
| **代码规模** | 大（百万行） | 小（几千-几万行） |
| **性能** | 高（直接调用） | 低（消息传递） |
| **可靠性** | 低（一个崩溃全崩溃） | 高（服务隔离） |
| **可维护性** | 差 | 好 |
| **可扩展性** | 差 | 好 |
| **安全性** | 低 | 高 |
| **代表系统** | Linux、UNIX | Minix、Mach |

### 三、分层式结构

**思想**：将操作系统分为若干层，每层只使用下层提供的服务

**架构示例**：
\`\`\`
第5层：用户程序
    ↓
第4层：用户接口（命令接口、图形接口）
    ↓
第3层：文件管理
    ↓
第2层：进程管理
    ↓
第1层：硬件抽象层
    ↓
第0层：硬件
\`\`\`

**优点**：
- ✅ 结构清晰，层次分明
- ✅ 便于调试和维护
- ✅ 每层功能单一

**缺点**：
- ❌ 效率较低（逐层调用）
- ❌ 层次划分困难

### 四、模块化结构 ⭐ 重点

**思想**：将操作系统划分为多个模块，模块间通过接口通信

**特点**：
- 📍 模块**高内聚、低耦合**：每个模块功能单一，接口明确
-  模块可以**动态加载和卸载**：运行时按需加载
- 📍 模块间通过**定义良好的接口**通信

**模块分类**：

| 模块类型 | 功能 | 示例 |
|---------|------|------|
| **进程管理模块** | 进程调度、同步 | 调度器、信号量 |
| **内存管理模块** | 内存分配、回收 | 分页管理、伙伴系统 |
| **文件系统模块** | 文件操作、目录管理 | ext4、NTFS驱动 |
| **设备驱动模块** | 硬件控制 | 显卡驱动、网卡驱动 |
| **网络协议模块** | 网络通信 | TCP/IP协议栈 |

**模块间通信方式**：
- 📍 **函数调用**：直接调用其他模块的函数
-  **消息传递**：通过消息队列通信
- 📍 **共享内存**：通过共享数据区交换数据

**代表系统**：
- **Linux**的可加载内核模块（LKM）：
  - 使用insmod命令加载模块
  - 使用rmmod命令卸载模块
  - 模块文件扩展名：.ko（Kernel Object）
  - 示例：文件系统驱动、硬件驱动可以作为模块加载
- **Windows**的驱动程序：
  - 使用.sys文件
  - 可以动态加载和卸载

**模块化 vs 分层式**：
-  **分层式**：严格层次关系，上层只能调用下层
- 📍 **模块化**：平等关系，模块间可以相互调用（通过接口）

**优点**：
- ✅ 结构清晰，易于理解和维护
- ✅ 模块独立开发、测试
- ✅ 可动态扩展功能
- ✅ 便于移植（替换特定模块）

**缺点**：
- ❌ 模块间可能存在循环依赖
-  接口设计困难
- ❌ 模块过多时管理复杂

### 五、外核（Exokernel）⭐ 了解

**思想**：最小化内核，将资源管理交给应用程序

**核心理念**：
- 📍 内核只负责**资源保护**和**资源复用**
-  **应用程序**自行管理资源的分配和使用
-  打破传统OS的“一刀切”资源管理策略

**架构特点**：

<pre>
┌─────────────────────────────────┐
│           应用程序               │
│  ┌───────────────────────────  │
│  │  应用程序自行管理资源      │  │
│  │  （内存、文件、网络等）    │  │
│  ───────────────────────────  │
─────────────────────────────────┤
│        外核（最小功能）         │
│  ┌───────────────────────────┐  │
│  │  资源保护（安全检查）    │  │
│  │  资源复用（多路复用）    │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│         物理硬件                  │
└─────────────────────────────────┘
</pre>

**外核的基本功能**：

| 功能 | 说明 | 示例 |
|-----|------|------|
| **资源保护** | 确保应用程序不能访问不属于自己的资源 | 内存访问检查、权限验证 |
| **资源复用** | 允许多个应用共享物理资源 | 时间片分配、磁盘块分配 |
| **安全检查** | 验证应用程序的资源请求是否合法 | 地址范围检查、权限检查 |

**与传统OS的对比**：

| 对比项 | 传统OS | 外核 |
|-------|--------|------|
| **资源管理** | OS统一管理 | 应用程序自行管理 |
| **灵活性** | 低（固定策略） | 高（自定义策略） |
| **性能** | 中 | 极高 |
| **编程复杂度** | 低 | 高 |
| **安全性** | OS保证 | 应用自己负责 |

**库操作系统（LibOS）**：
-  **概念**：外核的配套机制，应用程序链接库操作系统
- 📍 **功能**：提供传统OS的高级功能（文件系统、网络协议等）
-  **工作方式**：
  - 应用调用LibOS的高级接口
  - LibOS调用外核的基本接口
  - 外核执行安全检查后访问硬件

**代表系统**：
- MIT Exokernel（研究项目）
- XOK
- Nemesis

**优点**：
- ✅ **性能极高**：应用直接管理资源，无OS overhead
- ✅ **灵活性极强**：应用可以根据需求定制资源管理策略
- ✅ **适合特殊应用**：数据库、Web服务器等可以优化资源管理

**缺点**：
-  **编程复杂**：应用需要自己管理资源
- ❌ **应用开发难度大**：需要深厚的系统知识
-  **安全性责任转移**：应用自己负责安全
- ❌ **不适合普通应用**：普通应用不需要这种灵活性

**应用场景**：
- 📍 高性能服务器（数据库、Web服务器）
- 📍 嵌入式系统
- 📍 研究实验

### 六、混合内核（Hybrid Kernel）

**思想**：结合大内核和微内核的优点

**特点**：
- 核心功能在内核态（性能）
- 非核心功能在用户态（可靠性）

**代表系统**：
- Windows NT
- macOS X

---

 **学习提示**：
- ✅ 大内核和微内核的对比是高频考点
- ✅ 理解两种架构的优缺点
- ✅ 了解代表系统`,
        },
        {
          id: '1.5',
          title: '操作系统引导',
          content: `## 1.5 操作系统引导过程

### 一、引导过程概述

**引导（Boot）**：计算机从开机到操作系统完全加载的过程

**目标**：
- 🎯 初始化硬件
-  加载操作系统内核
-  启动第一个用户进程

### 二、引导步骤详解 ⭐⭐ 重点

#### 1. BIOS/UEFI启动 ⭐ 重点

**BIOS（Basic Input/Output System）**：
-  存储在主板**ROM芯片**中的固件
- 📍 **工作模式**：16位实模式，只能访问1MB内存
- 📍 **功能**：硬件自检、初始化、引导
- 📍 **存储位置**：主板上的Flash ROM
- 📍 **配置**：CMOS芯片（需要电池供电）

**UEFI（Unified Extensible Firmware Interface）**：
- 📍 **新一代**固件接口，替代BIOS
-  **工作模式**：32位或64位保护模式
- 📍 **特点**：
  - 图形界面（支持鼠标）
  - 支持大容量硬盘（>2TB）
  - 启动速度快（并行初始化硬件）
  - 支持安全引导（Secure Boot）
  - 模块化设计，可扩展
- 📍 **存储位置**：EFI系统分区（FAT32格式）

**BIOS vs UEFI对比**：

| 对比项 | BIOS | UEFI |
|-------|------|------|
| **工作模式** | 16位实模式 | 32/64位保护模式 |
| **启动速度** | 慢 | 快 |
| **硬盘支持** | MBR（最大2TB） | GPT（无限制） |
| **界面** | 文本界面 | 图形界面 |
| **安全性** | 低 | 高（Secure Boot） |
| **扩展性** | 差 | 好（模块化） |

**POST（Power-On Self-Test）详解**：

<div class="process-flow">
开机<br>
↓<br>
CPU从固定地址(FFFF0h)读取BIOS代码<br>
↓<br>
执行POST自检程序<br>
↓<br>
检查关键硬件:<br>
&nbsp;&nbsp;- CPU(寄存器、指令集)<br>
&nbsp;&nbsp;- 内存(读写测试)<br>
&nbsp;&nbsp;- 显卡(显存、显示)<br>
&nbsp;&nbsp;- 键盘、鼠标<br>
&nbsp;&nbsp;- 硬盘、光驱<br>
↓<br>
如果硬件故障 → 发出报警声(Beep Code)<br>
↓<br>
硬件正常 → 初始化硬件设备<br>
↓<br>
查找引导设备(按启动顺序)<br>
↓<br>
读取引导设备的第一个扇区(MBR)<br>
↓<br>
将控制权交给引导程序
</div>

#### 2. 引导加载程序（Bootloader）

**功能**：
- 加载操作系统内核到内存
- 提供启动菜单（多系统选择）

**常见Bootloader**：

| Bootloader | 适用系统 | 特点 |
|-----------|---------|------|
| **GRUB** | Linux | 功能强大，支持多系统 |
| **LILO** | Linux | 较老，功能简单 |
| **Windows Boot Manager** | Windows | 专用于Windows |
| **rEFInd** | macOS/Linux | 图形界面美观 |

**GRUB启动流程**：
\`\`\`
1. Stage 1：MBR中的引导代码（512字节）
    ↓
2. Stage 1.5：过渡代码
    ↓
3. Stage 2：GRUB主程序
    ↓
4. 显示启动菜单
    ↓
5. 加载内核
\`\`\`

#### 3. 加载内核

**内核加载过程**：
\`\`\`
Bootloader → 从磁盘读取内核 → 加载到内存指定位置
    ↓
解压内核（如果压缩）
    ↓
初始化内核数据结构
\`\`\`

**内核镜像格式**：
- Linux：vmlinuz
- Windows：ntoskrnl.exe

#### 4. 初始化内核

**内核初始化步骤**：

| 步骤 | 内容 | 说明 |
|-----|------|------|
| **1. 硬件初始化** | CPU、内存、中断 | 设置基本运行环境 |
| **2. 内存管理** | 页表、分区 | 建立内存管理系统 |
| **3. 进程管理** | 创建init进程 | 第一个用户进程 |
| **4. 设备驱动** | 加载驱动 | 识别硬件设备 |
| **5. 文件系统** | 挂载根文件系统 | 建立文件系统 |

#### 5. 启动用户进程

**Linux系统**：
\`\`\`
内核初始化完成
    ↓
创建init进程（PID=1）
    ↓
init进程读取/etc/inittab或systemd配置
    ↓
启动系统服务（网络、SSH、GUI等）
    ↓
启动登录界面（getty/display manager）
    ↓
用户登录
\`\`\`

**Windows系统**：
\`\`\`
内核初始化完成
    ↓
创建System进程
    ↓
创建SMSS.EXE（会话管理器）
    ↓
启动CSRSS.EXE（客户端/服务器运行时）
    ↓
启动Winlogon（登录管理器）
    ↓
显示登录界面
\`\`\`

### 三、引导扇区结构

**MBR（Master Boot Record）**：
- 位置：硬盘第0扇区（512字节）
- 结构：
  - 引导代码：446字节
  - 分区表：64字节（4个分区项）
  - 结束标志：2字节（0x55AA）

**GPT（GUID Partition Table）**：
- UEFI标准，支持大容量硬盘（>2TB）
- 支持128个分区
- 有备份分区表

### 四、安全引导

**Secure Boot**：
- UEFI的功能
- 只加载签名的操作系统
- 防止恶意软件篡改引导过程

---

 **学习提示**：
- ✅ 理解引导的完整流程
- ✅ 了解BIOS和UEFI的区别
- ✅ 知道Bootloader的作用`,
        },
        {
          id: '1.6',
          title: '虚拟机',
          content: `## 1.6 虚拟机

### 一、虚拟机的基本概念 ⭐⭐ 重点

**虚拟机（Virtual Machine, VM）**：通过软件模拟的具有完整硬件系统功能的、运行在一个完全隔离环境中的计算机系统

**本质**：
- 软件实现的**抽象层**
- 提供与物理机相同的功能
- 多个虚拟机共享物理资源

**虚拟化技术**：将物理资源抽象、转换为虚拟资源的技术

### 二、虚拟机的分类

#### 1. 系统虚拟机（完全虚拟化）

**特点**：
- 模拟完整的硬件系统
- 可以运行**完整的操作系统**
- 对客户OS透明（不需要修改）

**代表**：
- VMware Workstation
- VirtualBox
- KVM（Kernel-based VM）

**架构**：
\`\`\`
┌─────────────────────────────────┐
│         客户OS（Guest OS）       │
│  ┌───────────────────────────┐  │
│  │  应用程序                  │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │  未修改的OS内核            │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│        虚拟机监控器（VMM）       │
│  ┌───────────────────────────  │
│  │  硬件模拟层                │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│        宿主OS（Host OS）         │
├─────────────────────────────────┤
│         物理硬件                  │
└─────────────────────────────────┘
\`\`\`

**优点**：
- ✅ 兼容性好（支持任意OS）
- ✅ 隔离性强
- ✅ 易于迁移

**缺点**：
- ❌ 性能开销大（需要模拟硬件）
- ❌ 资源占用多

#### 2. 进程虚拟机（半虚拟化）

**特点**：
- 只提供**单个进程**的执行环境
- 不模拟完整硬件
- 需要修改客户OS

**代表**：
- JVM（Java Virtual Machine）
- .NET CLR（Common Language Runtime）
- Python VM

**架构**：
\`\`\`
应用程序（Java/Python/.NET）
    ↓
进程虚拟机（JVM/Python VM）
    ↓
宿主操作系统
    ↓
物理硬件
\`\`\`

**优点**：
- ✅ 性能好（无需完整硬件模拟）
- ✅ 跨平台（一次编写，到处运行）
- ✅ 资源占用少

**缺点**：
- ❌ 需要修改程序（编译为字节码）
- ❌ 只能运行特定语言

### 三、虚拟化实现技术 ⭐⭐ 重点

#### 1. 完全虚拟化（Full Virtualization）

**原理**：
- VMM完全模拟物理硬件
- 客户OS认为自己运行在真实硬件上
- 特权指令通过**陷入-模拟**机制处理

**陷入-模拟（Trap-and-Emulate）**：
\`\`\`
客户OS执行特权指令
    ↓
触发陷阱（Trap）到VMM
    ↓
VMM模拟该指令的效果
    ↓
返回结果给客户OS
\`\`\`

**代表**：VMware、VirtualBox

#### 2. 半虚拟化（Paravirtualization）

**原理**：
- 修改客户OS内核
- 用**超调用（Hypercall）**替代特权指令
- 客户OS知道自己运行在虚拟环境中

**超调用**：
- 类似系统调用，但调用的是VMM
- 直接请求VMM提供服务
- 无需陷入-模拟，性能更好

**代表**：Xen（早期版本）

#### 3. 硬件辅助虚拟化

**原理**：
- CPU提供**硬件支持**（Intel VT-x、AMD-V）
- 新增CPU模式：**根模式**（VMM）和**非根模式**（客户OS）
- 特权指令自动陷入VMM，无需软件模拟

**技术**：
- Intel VT-x（Virtualization Technology）
- AMD-V（AMD Virtualization）
- ARM Virtualization Extensions

**优点**：
- ✅ 性能接近物理机
- ✅ 无需修改客户OS
- ✅ 支持完全虚拟化

**代表**：现代VMware、KVM、Hyper-V

### 四、虚拟化技术对比 ⭐⭐ 必考

| 对比项 | 完全虚拟化 | 半虚拟化 | 硬件辅助虚拟化 |
|-------|-----------|---------|-------------|
| **性能** | 低 | 中 | 高 |
| **兼容性** | 好（任意OS） | 差（需修改OS） | 好（任意OS） |
| **实现复杂度** | 高 | 中 | 低（硬件支持） |
| **代表技术** | VMware、VirtualBox | Xen（早期） | KVM、Hyper-V |

### 五、虚拟机的优点

| 优点 | 说明 | 应用场景 |
|-----|------|--------|
| **资源隔离** | 多个VM互不影响 | 服务器多租户 |
| **便于迁移** | VM可以轻松迁移 | 云计算、负载均衡 |
| **快照和备份** | 可以随时保存状态 | 测试、开发 |
| **资源利用率** | 提高硬件利用率 | 数据中心 |
| **安全性** | 隔离恶意软件 | 安全测试 |

### 六、虚拟机监控器（VMM/Hypervisor）⭐⭐ 重点

**定义**：创建和运行虚拟机的软件层，也称为Hypervisor

**VMM的核心功能**：

#### 1. 处理器虚拟化  重点

**任务**：让多个客户OS共享物理CPU

**实现方式**：
- 📍 **完全虚拟化**：使用陷入-模拟（Trap-and-Emulate）
  - 客户OS执行特权指令 → 触发陷阱 → VMM模拟
  - 性能开销大
- 📍 **半虚拟化**：修改客户OS，使用超调用（Hypercall）
  - 客户OS直接调用VMM服务
  - 性能更好
-  **硬件辅助**：CPU提供根模式/非根模式
  - Intel VT-x、AMD-V
  - 性能接近物理机

**关键技术**：
- **影子页表（Shadow Page Table）**：VMM维护客户OS页表的影子副本
- **扩展页表（EPT）/嵌套页表（NPT）**：硬件支持的地址转换

#### 2. 内存虚拟化 ⭐ 重点

**任务**：让每个VM拥有独立的物理地址空间

**地址转换层次**：

<div class="process-flow">
客户OS虚拟地址(GVA)<br>
↓ 客户OS页表<br>
客户OS物理地址(GPA) - 客户机物理地址<br>
↓ VMM页表(影子页表或EPT)<br>
真实物理地址(HPA) - 宿主机物理地址
</div>

**实现方式**：
-  **影子页表（Shadow Page Table）**：
  - VMM维护GVA到HPA的直接映射
  - 客户OS修改页表时，VMM同步更新影子页表
  - 性能开销大
- 📍 **硬件辅助内存虚拟化**：
  - Intel EPT（Extended Page Table）
  - AMD NPT（Nested Page Table）
  - 硬件自动完成GPA到HPA的转换
  - 性能好

#### 3. I/O虚拟化  了解

**任务**：让多个VM共享物理I/O设备

**实现方式**：

| 方式 | 说明 | 性能 | 示例 |
|-----|------|------|------|
| **完全模拟** | VMM完全模拟硬件设备 | 低 | VirtualBox默认网卡 |
| **半虚拟化** | 客户OS使用特殊驱动 | 中 | Xen的virtio |
| **直接分配** | 设备直接分配给VM | 高 | SR-IOV、PCI Passthrough |
| **共享物理设备** | 多VM共享，VMM调度 | 中 | 现代虚拟化平台 |

**SR-IOV（Single Root I/O Virtualization）**：
- 📍 硬件支持的技术
- 📍 一个物理设备可以虚拟出多个虚拟功能（VF）
- 📍 每个VF可以直接分配给不同的VM
-  性能接近原生

#### 4. 虚拟机管理

**功能**：
- 📍 **创建/删除**：动态创建和销毁VM
- 📍 **启动/停止**：控制VM的运行状态
-  **迁移**：将VM从一台物理机迁移到另一台
  - **实时迁移（Live Migration）**：VM不停机迁移
  - **冷迁移**：VM关机后迁移
-  **快照**：保存VM的某个状态，可以随时恢复
- 📍 **资源调度**：动态分配CPU、内存资源

**分类**：

| 类型 | 特点 | 代表 |
|-----|------|------|
| **Type 1（裸金属）** | 直接运行在硬件上，性能好 | VMware ESXi、Xen、KVM、Hyper-V |
| **Type 2（宿主型）** | 运行在宿主OS上，方便 | VMware Workstation、VirtualBox、Parallels |

**Type 1 vs Type 2对比**：

| 对比项 | Type 1 | Type 2 |
|-------|--------|--------|
| **运行位置** | 直接在硬件上 | 在宿主OS上 |
| **性能** | 高 | 较低 |
| **应用场景** | 服务器、数据中心 | 桌面、开发测试 |
| **代表性** | 生产环境 | 个人使用 |

### 七、容器技术（轻量级虚拟化）

**容器 vs 虚拟机**：

| 对比项 | 虚拟机 | 容器 |
|-------|--------|------|
| **隔离级别** | 硬件级 | 进程级 |
| **启动速度** | 慢（分钟级） | 快（秒级） |
| **资源开销** | 大（完整OS） | 小（共享内核） |
| **性能** | 较低 | 接近物理机 |
| **代表技术** | VMware、KVM | Docker、LXC |

**容器特点**：
- 共享宿主OS内核
- 轻量级隔离（命名空间、cgroups）
- 启动快、资源占用少

---

 **学习提示**：
- ✅ 理解虚拟机的基本概念
- ✅ 掌握三种虚拟化技术的区别
- ✅ 了解虚拟机和容器的对比`,
        },
      ]
    },
    {
      id: 'ch2',
      number: 2,
      title: '进程与线程',
      score: '15-20分',
      importance: 'high',
      mindMap: `mindmap
  root((进程与线程))
    进程与线程简介
      进程的概念和特征
      进程的组成
      进程的内存映像
      进程的状态与转换
      进程控制
      进程的通信
      线程和多线程模型
    CPU调度
      调度的概念
      进程调度
      调度的目标
      进程切换
      CPU调度算法
        FCFS
        SJF
        HRRN
        RR
        多级反馈队列
      多处理机调度
    同步与互斥
      同步与互斥的基本概念
      实现临界区互斥的基本方法
      互斥锁
      信号量
      经典同步问题
        生产者-消费者
        读者-写者
        哲学家进餐
    死锁
      死锁的概念
      死锁预防
      死锁避免
      死锁检测与解除`,
      sections: [
        {
          id: '2.1',
          title: '进程与线程简介',
          content: `## 2.1 进程与线程简介 ⭐⭐⭐ 必考

### 一、进程的概念 ⭐⭐ 重点

**定义**：进程是程序的一次执行过程，是系统进行**资源分配和调度**的基本单位

**本质理解**：
- 📍 **动态性**：进程是"活"的，程序是"死"的
- 📍 **独立性**：每个进程有独立的地址空间
- 📍 **并发性**：多个进程可以并发执行

**进程 vs 程序**：

| 对比项 | 程序 | 进程 |
|-------|------|------|
| **性质** | 静态的代码文件 | 动态的执行过程 |
| **存在位置** | 磁盘上 | 内存中 |
| **生命周期** | 永久存在 | 临时存在（创建→运行→终止） |
| **组成** | 代码+数据 | 程序段+数据段+PCB |
| **关系** | 一个程序可对应多个进程 | 一个进程只对应一个程序 |

**生活中的例子**：
- 📱 **程序**：微信APP安装包（存储在手机上）
- 🚀 **进程**：你打开微信后运行的实例（占用内存和CPU）
- 💡 你可以同时打开3个微信窗口 = 3个进程

### 二、进程的特征 ⭐⭐ 必考

#### 1. 动态性（最基本特征）

**含义**：进程是程序的一次执行过程，是**动态产生、动态消亡**的

**生命周期**：
\`\`\`
创建 → 就绪 → 运行 → 阻塞/终止
\`\`\`

**对比**：
- ✅ 进程：动态的（有生命周期）
- ❌ 程序：静态的（永久存在）

#### 2. 并发性

**含义**：多个进程可以在单处理器上**并发执行**

**并发执行的特点**：
- 宏观上：多个进程同时执行
- 微观上：进程交替执行（分时复用CPU）

**并发 vs 并行**：

| 特性 | 并发 | 并行 |
|-----|------|------|
| **时间** | 同一时间间隔 | 同一时刻 |
| **硬件要求** | 单核即可 | 需要多核 |
| **执行方式** | 交替执行 | 真正同时 |

#### 3. 独立性

**含义**：进程是系统**独立运行**和**独立获得资源**的基本单位

**体现**：
- 每个进程有独立的**地址空间**
- 进程间互不干扰（除非主动通信）
- 一个进程崩溃不影响其他进程

#### 4. 异步性

**含义**：进程按各自**独立的、不可预知的速度**向前推进

**原因**：
- 进程何时被调度不确定
- I/O操作完成时间不确定
- 中断发生时间不确定

**结果**：
- 进程的执行顺序不可预测
- 需要**同步机制**协调进程间合作

#### 5. 结构性

**含义**：进程由**程序段、数据段和PCB**三部分组成

### 三、进程的组成 ⭐⭐ 重点

#### 1. 程序段（Code Segment）

**内容**：描述进程要完成的功能（代码）

**特点**：
- 可以被多个进程共享（如库函数）
- 通常是只读的

#### 2. 数据段（Data Segment）

**内容**：程序执行时所需的数据和工作区

**包括**：
- 全局变量
- 静态变量
- 堆（动态分配的内存）
- 栈（局部变量、函数调用）

#### 3. 进程控制块PCB（Process Control Block）⭐⭐⭐ 必考

**定义**：系统感知进程存在的**唯一标志**

**重要性**：
- 🎯 PCB是进程存在的唯一标识
- 🎯 系统通过PCB来管理和控制进程
- 🎯 创建进程时创建PCB，撤销进程时撤销PCB

**PCB包含的信息**：

| 信息类型 | 具体内容 |
|---------|--------|
| **进程标识符** | PID（进程ID）、PPID（父进程ID） |
| **处理机状态** | 通用寄存器、指令计数器、程序状态字PSW |
| **进程调度信息** | 进程状态、优先级、等待时间 |
| **进程控制信息** | 程序和数据的地址、资源清单、同步机制 |

**PCB的组织方式**：
- 链接方式：将相同状态的PCB链接成队列
- 索引方式：建立索引表

**生活中的比喻**：
- 🆔 **PCB** = 学生的学籍档案
- 📝 记录学生的一切信息（姓名、学号、成绩、状态）
- 🏫 学校通过学籍档案管理学生

### 四、进程的状态与转换 ⭐⭐⭐ 必考

#### 1. 三状态模型

**三种基本状态**：

| 状态 | 说明 | 特征 |
|-----|------|------|
| **就绪态（Ready）** | 已获得除CPU外的所有资源 | 万事俱备，只欠CPU |
| **运行态（Running）** | 已获得CPU，正在执行 | 正在使用CPU |
| **阻塞态（Blocked）** | 因等待某事件而暂停 | 等待I/O、信号量等 |

**状态转换图**：
\`\`\`
        时间片到/被抢占
    ┌──────────────────────┐
    │                      ↓
  就绪 ←──────────→ 运行
    ↑     调度         ↓
    │                  │ 等待事件
    │                  ↓
    └────────── 阻塞
       事件发生
\`\`\`

**状态转换详解**：

| 转换 | 触发条件 | 说明 |
|-----|---------|------|
| **就绪 → 运行** | 进程被调度选中 | 获得CPU |
| **运行 → 就绪** | 时间片用完/被抢占 | 让出CPU |
| **运行 → 阻塞** | 等待事件（I/O、信号量） | 主动放弃CPU |
| **阻塞 → 就绪** | 等待的事件发生 | I/O完成、信号量可用 |

**重要规则**：
- ❌ **阻塞 → 运行**：不可能！必须先经过就绪态
- ❌ **就绪 → 阻塞**：不可能！只有运行态才能阻塞

**💡 关键理解：进程被唤醒后的状态**
- ✅ **进程被唤醒** = 从阻塞态进入**就绪态**（不是运行态）
- 💡 唤醒只是表示等待的事件发生了，但还需要等待CPU调度
- 🔄 唤醒流程：阻塞态 → 就绪态 → (等待调度) → 运行态

#### 💡 关键概念：并发进程的不可再现性

**含义**：并发进程的执行结果与执行速度有关，具有**不可再现性**

**原因**：
- 进程按各自独立、不可预知的速度推进（异步性）
- 进程间的相对执行顺序不确定
- 访问共享资源时可能产生竞争

**示例**：
\`\`\`python
# 两个并发进程同时执行 i = i + 1
# 初始 i = 0
# 进程A: 读取i=0, 计算0+1=1, 写入i=1
# 进程B: 读取i=0, 计算0+1=1, 写入i=1
# 结果: i=1 (期望是2)
\`\`\`

**解决方法**：使用**同步机制**（信号量、互斥锁等）确保正确性

#### 2. 五状态模型

在三状态基础上增加：

| 新增状态 | 说明 |
|---------|------|
| **创建态（New）** | 进程正在被创建 |
| **终止态（Terminated）** | 进程正在被撤销 |

**完整生命周期**：
\`\`\`
创建 → 就绪 ↔ 运行 → 阻塞
              ↓
            终止
\`\`\`

#### 3. 挂起状态（七状态模型）

**挂起的原因**：
- 终端用户请求
- 父进程请求
- 操作系统需要（交换技术）

**挂起状态**：
- **就绪挂起**：进程在磁盘上，处于就绪状态
- **阻塞挂起**：进程在磁盘上，处于阻塞状态

### 五、进程控制 ⭐⭐ 重点

**进程控制**：对系统中所有进程实施管理

**原语（Primitive）**：
- 由若干条指令组成的程序段
- **原子操作**：要么全部执行，要么全部不执行
- 执行期间**不允许中断**

**常见的进程控制原语**：

#### 1. 进程创建

**系统调用**：fork()、CreateProcess()

**创建过程**：
\`\`\`
1. 申请空白PCB
    ↓
2. 为新进程分配资源（内存空间、I/O设备等）
    ↓
3. 初始化PCB
    ↓
4. 将新进程插入就绪队列
\`\`\`

**⚠️ 重要说明**：
- ✅ 进程创建时分配的资源包括：**内存空间**、文件描述符、I/O设备等
- ❌ **不包括CPU**：CPU是在进程被调度时才分配的
- 💡 进程在就绪态时已有内存，但没有CPU

**fork()的特点**：
- 创建一个子进程
- 子进程复制父进程的地址空间
- fork()返回两次：父进程返回子进程PID，子进程返回0

**💡 父进程与子进程的关系**：

| 特性 | 说明 |
|-----|------|
| **创建关系** | 父进程通过fork()创建子进程 |
| **资源继承** | 子进程继承父进程的大部分资源（代码、数据、打开的文件） |
| **独立性** | 创建后两者独立运行，互不影响 |
| **标识符** | 子进程有独立的PID，PCB中记录PPID（父进程ID） |
| **终止关系** | 父进程终止时，通常需要终止或托管子进程 |

**生活中的例子**：
- 👨‍👦 **父进程** = 父亲
- 👶 **子进程** = 孩子
- 💡 孩子继承父亲的基因和资源，但长大后独立生活

#### 💡 作业 vs 进程

**作业（Job）**：用户提交给系统的一个计算任务，是**用户视角**的概念

**进程（Process）**：作业的执行实例，是**系统视角**的概念

| 对比项 | 作业 | 进程 |
|-------|------|------|
| **视角** | 用户角度 | 系统角度 |
| **生命周期** | 从提交到完成 | 从创建到终止 |
| **组成** | 程序+数据+作业说明书 | 程序段+数据段+PCB |
| **关系** | 一个作业可分解为多个进程 | 一个进程属于某个作业 |

**示例**：
- 📝 **作业**：编译并运行一个C程序（包含编辑、编译、链接、运行等多个步骤）
- ⚙️ **进程**：编译器进程、链接器进程、执行程序进程

---#### 2. 进程终止

**系统调用**：exit()、TerminateProcess()

**终止原因**：
- 正常结束
- 异常结束（错误、越界、保护错）
- 外界干预（用户杀死、父进程终止）

**终止过程**：
\`\`\`
1. 根据被终止进程的标识符，检索PCB
    ↓
2. 若进程正在运行，立即终止
    ↓
3. 若有子孙进程，终止子孙进程
    ↓
4. 释放资源
    ↓
5. 将PCB从队列中移除
\`\`\`

#### 3. 进程阻塞与唤醒

**阻塞原语**：block()
- 进程主动调用，让自己进入阻塞态
- 例如：等待I/O完成、等待信号量

**唤醒原语**：wakeup()
- 当等待的事件发生时，阻塞进程被唤醒
- 例如：I/O完成中断、V操作

**💡 信号的处理**：

**信号（Signal）**：一种软中断机制，用于通知进程某事件发生

**常见信号**：

| 信号 | 编号 | 说明 |
|-----|-----|------|
| **SIGINT** | 2 | 中断信号（Ctrl+C） |
| **SIGKILL** | 9 | 强制终止（不能被捕获） |
| **SIGTERM** | 15 | 终止信号（可被捕获） |
| **SIGSTOP** | 19 | 停止信号 |
| **SIGCHLD** | 17 | 子进程状态改变 |

**信号的处理方式**：

1. **默认处理**：执行系统默认动作（通常是终止）
2. **忽略信号**：不采取任何行动（SIGKILL除外）
3. **捕获信号**：注册信号处理函数，自定义处理方式

**系统调用**：
\`\`\`c
signal(SIGINT, handler);  // 注册信号处理函数
kill(pid, SIGTERM);       // 向指定进程发送信号
\`\`\`

**生活中的例子**：
- 📱 **信号** = 手机铃声
- 🔔 铃声响起（信号到达）
- 📞 你可以接听（捕获）、挂断（忽略）、或让它响完（默认）

### 六、进程的通信 ⭐⭐ 重点

**进程通信**：进程之间交换信息

#### 1. 共享存储器系统

**原理**：多个进程共享一块内存区域

**实现方式**：
- 基于数据结构的共享（低级）
- 基于共享存储区的共享（高级）

**优点**：速度快
**缺点**：需要同步机制

#### 2. 消息传递系统 ⭐ 常用

**原理**：进程间以消息为单位进行通信

**实现方式**：
- **直接通信**：send/receive指定进程名
- **间接通信**：通过信箱（mailbox）

**系统调用**：
\`\`\`
send(receiver, message)
receive(sender, message)
\`\`\`

**优点**：
- 无需共享内存
- 易于实现同步

#### 3. 管道通信

**原理**：连接读写进程的一个共享文件（pipe文件）

**特点**：
- 半双工通信（单向）
- 先进先出（FIFO）
- 容量有限

**Linux命令**：
\`\`\`bash
ps aux | grep chrome  # 管道符 |
\`\`\`

**💡 管道的本质**：
- ✅ **管道是一种特殊的文件**（存在于内存中的虚拟文件）
- 📁 在Linux中，管道文件类型为 \`p\` (pipe)
- 🔍 查看管道：\`ls -l /proc/PID/fd/\` 可以看到管道文件

**管道的类型**：
- **匿名管道**：只能用于有亲缘关系的进程（父子进程）
- **命名管道（FIFO）**：可用于任意进程间通信

---

### 七、线程的概念 ⭐⭐ 重点

#### 1. 引入线程的原因

**问题**：
- 进程是资源分配单位，也是调度单位
- 进程切换开销大（保存/恢复整个地址空间）
- 并发度受限

**解决**：引入**线程**作为调度单位

#### 2. 线程的定义

**线程**：是进程中的一个执行单元，是**CPU调度和分派**的基本单位

**特点**：
- 线程**共享**进程的资源
- 线程**独立**调度
- 线程切换开销小

**💡 线程的重要特性**：

1. **一个线程只能属于一个进程**
   - 线程不能跨进程存在
   - 线程的生命周期依附于所属进程
   - 进程终止时，所有线程也随之终止

2. **线程的CPU现场（Context）**
   - **定义**：线程执行时的CPU状态信息
   - **包括**：
     - 程序计数器（PC）
     - 寄存器集合
     - 栈指针
   - **作用**：线程切换时保存和恢复执行状态
   - **存储位置**：线程控制块（TCB）中

3. **线程共享进程的资源**
   - ✅ 共享：地址空间、全局变量、文件描述符、信号处理
   - ❌ 不共享：栈、寄存器、程序计数器（每个线程独立）

#### 3. 线程 vs 进程 ⭐⭐ 必考

| 对比项 | 进程 | 线程 |
|-------|------|------|
| **资源拥有** | 独立拥有资源 | 共享进程资源 |
| **调度单位** | 不是（传统OS） | 是 |
| **并发性** | 较低 | 更高 |
| **系统开销** | 大（创建/切换） | 小 |
| **独立性** | 高（独立地址空间） | 低（共享地址空间） |
| **通信方式** | 复杂（IPC） | 简单（共享内存） |

**生活中的比喻**：
- 🏭 **进程** = 工厂（拥有设备、原料等资源）
- 👷 **线程** = 工人（共享工厂资源，独立工作）
- 💡 一个工厂可以有多个工人

#### 4. 线程的实现方式

| 方式 | 说明 | 代表 |
|-----|------|------|
| **用户级线程** | 内核不知道线程存在 | 早期Java |
| **内核级线程** | 内核支持线程 | Linux、Windows |
| **混合式** | 结合两者优点 | Solaris |

---

 **学习提示**：
- ✅ 进程的特征是高频考点
- ✅ PCB的内容和作用必考
- ✅ 三状态模型的转换图必须掌握
- ✅ 进程vs线程的对比常考选择题`,
        },
        {
          id: '2.2',
          title: 'CPU调度',
          content: `## 2.2 CPU调度 ⭐⭐⭐ 必考

### 一、调度的基本概念

**调度（Scheduling）**：按照某种算法，从就绪队列中选择一个进程分配给CPU

#### 1. 三级调度

| 调度级别 | 名称 | 频率 | 说明 |
|---------|------|-----|------|
| **高级调度** | 作业调度 | 低（分钟级） | 从外存→内存，创建进程 |
| **中级调度** | 内存调度 | 中 | 内存↔外存交换（挂起/激活） |
| **低级调度** | 进程调度 | 高（毫秒级） | 从就绪队列选择进程分配CPU |

**注意**：
- ✅ **低级调度**是OS必须具有的调度
- ❌ 高级和中级调度不是必须的

#### 2. 调度时机 ⭐ 重点

**何时进行调度**：

| 时机 | 类型 | 说明 |
|-----|------|------|
| **进程终止** | 主动 | 进程执行完毕 |
| **进程阻塞** | 主动 | 等待I/O、信号量 |
| **时间片用完** | 被动 | RR算法 |
| **更高优先级进程到达** | 被动 | 抢占式调度 |
| **中断处理结束** | - | I/O完成中断 |

**不能调度的情况**：
- ❌ 在中断处理过程中
- ❌ 在原子操作（原语）执行期间
- ❌ 在内核临界区

#### 3. 调度方式

| 方式 | 说明 | 特点 |
|-----|------|------|
| **非抢占式** | 一旦分配CPU就一直运行，直到主动放弃 | 简单、开销小 |
| **抢占式** | 可以暂停正在执行的进程 | 灵活、响应快 |

**抢占原则**：
- 优先权原则：更高优先级进程到达
- 短作业优先原则：更短作业到达
- 时间片原则：时间片用完

### 二、进程切换 ⭐ 重点

**定义**：保存当前进程的上下文，恢复新进程的上下文

**过程**：

\`\`\`
1. 保存当前进程的PCB（寄存器、PC等）
    ↓
2. 将当前进程移入相应队列
    ↓
3. 选择新进程
    ↓
4. 恢复新进程的PCB
    ↓
5. 跳转到新进程的断点继续执行
\`\`\`

**💡 进程上下文（Process Context）**

**定义**：进程执行时的全部状态信息

**包括三个部分**：

| 组成部分 | 内容 | 说明 |
|---------|------|------|
| **用户级上下文** | 程序、数据、用户栈 | 用户空间的资源 |
| **寄存器上下文** | 通用寄存器、PC、PSW | CPU内部状态 |
| **系统级上下文** | PCB、内核栈 | 内核空间的数据结构 |

**上下文切换**：
- 保存旧进程的上下文
- 恢复新进程的上下文
- 开销较大（涉及内存访问、TLB失效等）

**💡 CPU繁忙型 vs I/O繁忙型作业**

| 类型 | 特点 | 示例 | 调度策略 |
|-----|------|------|----------|
| **CPU繁忙型** | 大量CPU计算，少量I/O | 科学计算、图像处理 | 适合长时间片 |
| **I/O繁忙型** | 频繁I/O操作，少量CPU | 数据库查询、Web服务 | 适合短时间片 |

**调度优化**：
- 🎯 CPU繁忙型：给予较长CPU时间，减少切换开销
- 🎯 I/O繁忙型：优先调度，提高I/O设备利用率

### 三、调度目标

**主要目标**：

| 目标 | 说明 |
|-----|------|
| **CPU利用率** | 尽可能让CPU忙碌 |
| **系统吞吐量** | 单位时间内完成的作业数 |
| **周转时间** | 作业提交到完成的时间 |
| **等待时间** | 作业在就绪队列等待的时间 |
| **响应时间** | 从提交请求到首次产生响应的时间 |

**计算公式**：

| 指标 | 公式 |
|-----|------|
| **周转时间** | 完成时间 - 提交时间 |
| **带权周转时间** | 周转时间 / 运行时间 |
| **平均周转时间** | Σ周转时间 / n |

### 四、CPU调度算法 ⭐⭐⭐ 必考

#### 1. 先来先服务（FCFS, First-Come First-Served）

**原理**：按进程到达的先后顺序调度

**特点**：
- ✅ 简单、公平
- ❌ 对短作业不利（护航效应）
- ❌ 平均等待时间长

**示例**：
\`\`\`
进程 | 到达时间 | 服务时间
 A   |    0     |    24
 B   |    0     |     3
 C   |    0     |     3

执行顺序：A → B → C
平均等待时间 = (0 + 24 + 27) / 3 = 17
\`\`\`

**适用场景**：批处理系统

#### 2. 短作业优先（SJF, Shortest Job First）

**原理**：优先调度运行时间短的作业

**特点**：
- ✅ 平均等待时间最短（最优）
- ❌ 对长作业不利（可能饥饿）
- ❌ 难以预知运行时间

**示例**：
\`\`\`
进程 | 到达时间 | 服务时间
 A   |    0     |    24
 B   |    0     |     3
 C   |    0     |     3

执行顺序：B → C → A（短作业优先）
平均等待时间 = (3 + 6 + 0) / 3 = 3 ← 比FCFS好！
\`\`\`

**变种**：
- **非抢占式SJF**：一旦开始运行，不中断
- **抢占式SJF（SRTF）**：新作业更短则抢占

#### 3. 高响应比优先（HRRN, Highest Response Ratio Next）

**原理**：选择响应比最高的进程

**响应比公式**：
\`\`\`
响应比 = (等待时间 + 要求服务时间) / 要求服务时间
       = 1 + 等待时间 / 要求服务时间
\`\`\`

**特点**：
- ✅ 兼顾长短作业
- ✅ 避免长作业饥饿
- ✅ 随着等待时间增加，响应比提高

**分析**：
- 短作业：分母小，响应比高 → 优先
- 长作业：等待时间长，分子大 → 最终也会被调度

**示例**：
\`\`\`
假设A已等待10，需要5；B刚到达，需要2
A的响应比 = 1 + 10/5 = 3
B的响应比 = 1 + 0/2 = 1
结果：A优先（虽然长，但等待久了）
\`\`\`

#### 4. 时间片轮转（RR, Round Robin）

**原理**：每个进程分配一个时间片，轮流执行

**特点**：
- ✅ 公平、响应快
- ✅ 适合分时系统
- ❌ 时间片大小影响性能

**时间片选择**：

| 时间片 | 效果 |
|-------|------|
| **太大** | 退化为FCFS |
| **太小** | 频繁切换，开销大 |
| **适中** | 一般取10-100ms |

**示例**（时间片=4）：
\`\`\`
进程 | 服务时间
 A   |    6
 B   |    3
 C   |    7

执行顺序：A(4) → B(3) → C(4) → A(2) → C(3)
\`\`\`

#### 5. 多级反馈队列（MLFQ, Multilevel Feedback Queue）⭐⭐ 重点

**原理**：设置多个优先级队列，进程可在队列间移动

**结构**：
\`\`\`
队列1（最高优先级）：时间片=1
    ↓ 未完成的进程
队列2：时间片=2
    ↓ 未完成的进程
队列3：时间片=4
    ↓ ...
队列n（最低优先级）：FCFS
\`\`\`

**规则**：
1. 新进程进入队列1
2. 队列1采用RR，时间片最小
3. 时间片用完未完成，降级到下一队列
4. 高优先级队列为空时，才调度低优先级队列
5. 新进程到达可抢占低优先级进程

**特点**：
- ✅ 综合性能好
- ✅ 短作业快速响应
- ✅ 长作业不会饥饿
- ✅ 无需预知运行时间

**代表系统**：UNIX、Linux、Windows

### 五、调度算法对比 ⭐⭐ 必考

| 算法 | 公平性 | 效率 | 响应时间 | 实现难度 | 适用场景 |
|-----|-------|-----|---------|---------|--------|
| **FCFS** | 公平 | 低 | 差 | 简单 | 批处理 |
| **SJF** | 不公平 | 高 | 较好 | 中等 | 批处理 |
| **HRRN** | 较公平 | 较高 | 好 | 中等 | 通用 |
| **RR** | 公平 | 中等 | 好 | 简单 | 分时系统 |
| **MLFQ** | 较公平 | 高 | 好 | 复杂 | 通用 |

### 六、多处理机调度 ⭐⭐

**背景**：现代计算机普遍采用多核CPU，调度需考虑多个处理机之间的任务分配。

**与单处理机调度的区别**：

| 对比项 | 单处理机 | 多处理机 |
|--------|----------|----------|
| 调度对象 | 1个CPU | 多个CPU |
| 核心问题 | 选哪个进程 | 选哪个进程 + 放哪个CPU |
| 额外考虑 | 无 | 负载均衡、处理器亲和性 |

**调度方式**：
- **对称多处理（SMP）**：所有CPU平等，任一CPU都可执行任何进程
- **非对称多处理（ASMP）**：主从结构，主CPU负责调度

**处理器亲和性（Affinity）** ⭐⭐：
- **软亲和性**：调度器尽量让进程留在原CPU（利用Cache热数据），但不保证
- **硬亲和性**：通过系统调用强制绑定进程到指定CPU
- **原因**：进程切换CPU后原CPU的Cache数据失效，重新加载代价大

**负载均衡** ⭐：

| 方式 | 说明 |
|------|------|
| **推迁移（Push）** | 过载CPU主动把进程推给空闲CPU |
| **拉迁移（Pull）** | 空闲CPU主动从过载CPU拉取进程 |

**多核调度策略**：
1. **全局调度**：所有CPU共享一个就绪队列（负载均衡好，但锁竞争开销大）
2. **分区调度**：每个CPU一个私有队列（开销小，但可能负载不均）

**💡 记忆要点**：多处理机调度 = 单处理机调度 + 亲和性 + 负载均衡

---

 **学习提示**：
- ✅ 调度算法的计算题必考（周转时间、等待时间）
- ✅ 掌握每种算法的优缺点
- ✅ 理解多级反馈队列的工作原理
- ✅ 会画甘特图分析调度过程`,
        },
        {
          id: '2.3',
          title: '同步与互斥',
          content: `## 2.3 同步与互斥 ⭐⭐⭐ 必考

### 一、基本概念

#### 1. 临界资源（Critical Resource）

**定义**：一次仅允许一个进程使用的资源

**示例**：
- 打印机、磁带机等硬件设备
- 变量、表格、队列等软件资源

#### 2. 临界区（Critical Section）

**定义**：进程中访问临界资源的那段代码

**结构**：
\`\`\`
do {
    进入区;     // 检查能否进入临界区
    临界区;     // 访问临界资源
    退出区;     // 释放临界资源使用权
    剩余区;     // 其他代码
} while (true);
\`\`\`

**💡 临界区中进程的特点**：
1. **互斥性**：同一时刻最多只有一个进程在临界区内执行
2. **空闲让进**：临界区空闲时，请求进入的进程可以立即进入
3. **忙则等待**：临界区被占用时，其他进程必须等待
4. **有限等待**：等待进入临界区的时间是有限的（不会饥饿）
5. **让权等待**：等待时应释放CPU，避免忙等（可选优化）

#### 3. 互斥（Mutual Exclusion）

**定义**：当一个进程正在使用某资源时，其他进程不能使用该资源

**本质**：间接制约关系（竞争关系）

**示例**：
- 🖨️ 打印机：同一时刻只能有一个进程打印
- 💾 共享变量：避免读写冲突

#### 4. 同步（Synchronization）

**定义**：多个进程为完成同一任务，需要协调它们的工作次序

**本质**：直接制约关系（合作关系）

**示例**：
- 🔄 生产者-消费者：生产后才能消费
- 📝 读者-写者：写完才能读

### 二、实现临界区互斥的方法 ⭐⭐ 重点

#### 💡 临界区管理的四个原则

在讨论具体实现方法之前，首先需要了解**临界区互斥应遵循的四个基本原则**：

1. **空闲让进**：当无进程处于临界区时，应允许一个请求进入临界区的进程立即进入
2. **忙则等待**：当已有进程进入临界区时，其他试图进入临界区的进程必须等待
3. **有限等待**：对请求访问的进程，应保证能在有限时间内进入临界区（不会饥饿）
4. **让权等待**：当进程不能进入临界区时，应立即释放处理机，防止进程忙等

**说明**：
- 原则1、2、3是**必须满足**的基本要求
- 原则4是**优化建议**，不是强制要求（Peterson算法就不满足让权等待）

---

#### 1. 软件方法

##### （1）单标志法

**思想**：设置一个共享变量turn，表示允许进入的进程号

**代码**：
\`\`\`c
// 进程Pi
while (turn != i);    // 进入区
critical section;      // 临界区
turn = j;              // 退出区
remainder section;     // 剩余区
\`\`\`

**问题**：
- ❌ **不满足“空闲让进”**：强制轮流，即使另一个进程不想进入
- ✅ 满足互斥

##### （2）双标志先检查法

**思想**：每个进程设置一个flag，表示是否想进入

**代码**：
\`\`\`c
// 进程Pi
while (flag[j]);       // 检查对方
flag[i] = true;        // 标记自己想进入
critical section;
flag[i] = false;
\`\`\`

**问题**：
- ❌ **可能违反互斥**：两个进程同时检查通过后都进入

##### （3）双标志后检查法

**代码**：
\`\`\`c
// 进程Pi
flag[i] = true;        // 先标记
while (flag[j]);       // 再检查
critical section;
flag[i] = false;
\`\`\`

**问题**：
- ❌ **可能导致饥饿**：两个进程都标记后互相等待

##### （4）Peterson算法 ⭐ 经典

**思想**：结合双标志和单标志

**代码**：
\`\`\`c
// 进程Pi
flag[i] = true;        // 我想进入
turn = j;              // 让你优先
while (flag[j] && turn == j);  // 如果你也想且你优先，则等待
critical section;
flag[i] = false;
\`\`\`

**优点**：
- ✅ 满足前三个原则（空闲让进、忙则等待、有限等待）
- ❌ 不满足让权等待（忙等）
- ✅ 纯软件实现

**说明**：Peterson算法通过while循环忙等，没有释放CPU，因此不满足“让权等待”原则。但在实际应用中，前三个原则是必须满足的，让权等待是优化建议。

#### 2. 硬件方法

##### （1）中断屏蔽方法

**原理**：进入临界区前关中断，退出后开中断

**代码**：
\`\`\`c
关中断;
临界区;
开中断;
\`\`\`

**💡 开中断 vs 关中断的区别**：

| 对比项 | 关中断（CLI） | 开中断（STI） |
|-------|------------|------------|
| **作用** | 禁止CPU响应外部中断 | 允许CPU响应外部中断 |
| **使用场景** | 进入临界区、执行原子操作 | 退出临界区、恢复正常执行 |
| **特权级别** | 特权指令，只能在核心态执行 | 特权指令，只能在核心态执行 |
| **影响范围** | 仅影响当前CPU | 仅影响当前CPU |

**注意事项**：
- ⚠️ 关中断时间不能太长，否则影响系统响应
- ⚠️ 只适用于单处理机，多处理机无效
- ⚠️ 用户态进程不能使用（特权指令）

**问题**：
- ❌ 不适用于多处理机
- ❌ 影响系统效率

##### （2）TestAndSet指令（TS指令）

**定义**：原子操作，测试并设置

**代码**：
\`\`\`c
boolean TestAndSet(boolean *lock) {
    boolean old = *lock;
    *lock = true;
    return old;
}

// 使用
while (TestAndSet(&lock));  // 忙等待
critical section;
lock = false;
\`\`\`

**优点**：适用于多处理机

##### （3）Swap指令（XCHG指令）

**原理**：交换两个变量的值（原子操作）

**代码**：
\`\`\`c
void Swap(boolean *a, boolean *b) {
    boolean temp = *a;
    *a = *b;
    *b = temp;
}

// 使用
key = true;
do {
    Swap(&lock, &key);
} while (key);
critical section;
lock = false;
\`\`\`

### 三、互斥锁（Mutex Lock）⭐⭐ 重点

**定义**：一种简单的同步机制，用于实现临界区互斥

**基本操作**：
- **acquire()**：获取锁，如果锁已被占用则等待
- **release()**：释放锁，唤醒等待的进程

**代码示例**：
\`\`\`c
// 初始化
mutex lock = FREE;

// 进程Pi
acquire(lock);      // 进入临界区前获取锁
critical section;   // 临界区
release(lock);      // 退出临界区后释放锁
remainder section;
\`\`\`

**💡 线程可以有互斥锁吗？**

**答案**：✅ **可以！**

**详细说明**：
1. **线程可以使用互斥锁**
   - 互斥锁不仅可用于进程间同步
   - 也可用于同一进程内的线程间同步
   
2. **线程互斥锁的特点**
   - 保护共享数据不被多个线程同时访问
   - 线程切换开销小，但互斥锁仍然必要
   - 避免竞态条件（Race Condition）

3. **常见实现**
   - POSIX线程：\`pthread_mutex_t\`
   - Java：\`synchronized\`关键字、\`ReentrantLock\`
   - C++：\`std::mutex\`

**互斥锁 vs 信号量**：

| 对比项 | 互斥锁 | 信号量 |
|-------|--------|--------|
| **用途** | 仅用于互斥 | 可用于互斥和同步 |
| **拥有者** | 有拥有者概念 | 无拥有者概念 |
| **释放限制** | 只能由持有者释放 | 任何进程都可V操作 |
| **复杂度** | 简单 | 较复杂 |

---

### 四、信号量机制 ⭐⭐⭐ 必考

#### 1. 整型信号量

**定义**：用一个整数表示资源数量

**操作**：
\`\`\`c
wait(S):  // P操作
    while (S <= 0);  // 忙等待
    S--;

signal(S):  // V操作
    S++;
\`\`\`

**问题**：
- ❌ **忙等待**：浪费CPU时间
- ❌ 不满足“让权等待”

#### 2. 记录型信号量 ⭐ 重要

**定义**：增加等待队列，避免忙等待

**结构**：
\`\`\`c
typedef struct {
    int value;           // 资源数量
    struct process *L;   // 等待队列
} semaphore;
\`\`\`

**P操作（wait）**：
\`\`\`c
void wait(semaphore S) {
    S.value--;           // 申请一个资源
    if (S.value < 0) {   // 无可用资源
        block(S.L);      // 阻塞，加入等待队列
    }
}
\`\`\`

**V操作（signal）**：
\`\`\`c
void signal(semaphore S) {
    S.value++;           // 释放一个资源
    if (S.value <= 0) {  // 有进程在等待
        wakeup(S.L);     // 唤醒一个等待进程
    }
}
\`\`\`

**value的含义**：

| value值 | 含义 |
|---------|------|
| **> 0** | 可用资源数量 |
| **= 0** | 无可用资源，但无等待进程 |
| **< 0** | \|value\| 表示等待进程数 |

**重要性质**：
- ✅ P、V操作是**原子操作**（不可中断）
- ✅ 成对出现：P在前，V在后
- ✅ 在同一进程中，P、V操作数量相等

#### 3. AND型信号量

**思想**：一次性申请所有资源

**操作**：
\`\`\`c
Swait(S1, S2, ..., Sn);   // 同时申请多个资源
Ssignal(S1, S2, ..., Sn); // 同时释放多个资源
\`\`\`

**优点**：避免死锁

#### 4. 信号量集

**思想**：一次申请N个资源

**操作**：
\`\`\`c
Swait(S, d, t);   // 申请d个资源，下限t
Ssignal(S, d);    // 释放d个资源
\`\`\`

### 四、经典同步问题 ⭐⭐⭐ 必考

#### 1. 生产者-消费者问题

**问题描述**：
- 一组生产者生产产品，放入缓冲区
- 一组消费者从缓冲区取产品消费
- 缓冲区容量有限

**信号量设置**：
\`\`\`c
semaphore mutex = 1;    // 互斥访问缓冲区
semaphore empty = n;    // 空闲缓冲区数量
semaphore full = 0;     // 已用缓冲区数量
\`\`\`

**生产者进程**：
\`\`\`c
producer() {
    while (true) {
        生产一个产品;
        wait(empty);      // 申请空缓冲区
        wait(mutex);      // 互斥访问
        将产品放入缓冲区;
        signal(mutex);
        signal(full);     // 增加满缓冲区计数
    }
}
\`\`\`

**消费者进程**：
\`\`\`c
consumer() {
    while (true) {
        wait(full);       // 申请满缓冲区
        wait(mutex);      // 互斥访问
        从缓冲区取产品;
        signal(mutex);
        signal(empty);    // 增加空缓冲区计数
        消费产品;
    }
}
\`\`\`

**注意**：
- ⚠️ P操作的顺序不能颠倒！先P资源信号量，再P互斥信号量
- ⚠️ 否则可能导致死锁

#### 2. 读者-写者问题

**问题描述**：
- 多个读者可以同时读
- 写者必须独占访问
- 读者和写者互斥

##### （1）读者优先

**特点**：只要有读者在读，写者就必须等待

**信号量**：
\`\`\`c
semaphore rw = 1;    // 写者互斥
semaphore mutex = 1; // 保护readcount
int readcount = 0;   // 读者数量
\`\`\`

**读者进程**：
\`\`\`c
reader() {
    while (true) {
        wait(mutex);
        if (readcount == 0) wait(rw);  // 第一个读者阻止写者
        readcount++;
        signal(mutex);
        
        读数据;
        
        wait(mutex);
        readcount--;
        if (readcount == 0) signal(rw);  // 最后一个读者允许写者
        signal(mutex);
    }
}
\`\`\`

**写者进程**：
\`\`\`c
writer() {
    while (true) {
        wait(rw);      // 申请写权限
        写数据;
        signal(rw);
    }
}
\`\`\`

**问题**：
- ❌ 写者可能饥饿（读者源源不断）

##### （2）写者优先

**特点**：一旦有写者等待，后续读者必须等待

**额外信号量**：
\`\`\`c
semaphore w = 1;     // 保证写者优先
\`\`\`

#### 3. 哲学家进餐问题

**问题描述**：
- 5个哲学家围坐圆桌
- 每人左右各有一根筷子
- 需要两根筷子才能进餐

**简单解法（有问题）**：
\`\`\`c
semaphore chopstick[5] = {1, 1, 1, 1, 1};

philosopher(i) {
    while (true) {
        wait(chopstick[i]);         // 拿左筷
        wait(chopstick[(i+1)%5]);   // 拿右筷
        进餐;
        signal(chopstick[i]);
        signal(chopstick[(i+1)%5]);
        思考;
    }
}
\`\`\`

**问题**：
- ❌ 可能死锁（所有人都拿起左筷）

**解决方案**：
1. **限制同时进餐人数**：最多4人
2. **奇偶策略**：奇数号先左后右，偶数号先右后左
3. **AND信号量**：同时申请两根筷子

### 五、管程（Monitor）⭐⭐ 重点

**定义**：由编译器实现的同步机制，是一种高级同步原语

**组成**：
- **共享数据结构**：管程内部的数据
- **过程（函数）**：对数据进行操作的过程
- **初始化代码**：初始化共享数据
- **条件变量**：用于进程等待和唤醒

**💡 管程的详细工作机制**：

1. **互斥性保证**
   - 管程确保同一时刻只有一个进程在管程内执行
   - 由编译器自动实现，程序员无需关心
   - 进入管程时自动加锁，退出时自动解锁

2. **条件变量（Condition Variable）**
   - **作用**：允许进程在管程内等待某个条件成立
   - **操作**：
     - \`wait(cond)\`：释放管程锁，进入等待队列
     - \`signal(cond)\`：唤醒一个等待该条件的进程
   
3. **管程 vs 信号量**

| 对比项 | 管程 | 信号量 |
|-------|------|--------|
| **实现方式** | 编译器实现 | 操作系统提供 |
| **互斥保证** | 自动保证 | 需要程序员正确使用P/V |
| **易用性** | 更高级，更易用 | 较底层，易出错 |
| **灵活性** | 较低 | 较高 |

**管程的示例结构**：
\`\`\`
monitor ProducerConsumer {
    // 共享数据
    int buffer[N];
    int count = 0;
    condition notFull, notEmpty;
    
    // 过程
    void insert(item) {
        if (count == N) wait(notFull);
        // 插入物品
        count++;
        signal(notEmpty);
    }
    
    item remove() {
        if (count == 0) wait(notEmpty);
        // 取出物品
        count--;
        signal(notFull);
        return item;
    }
}
\`\`\`

**优点**：
- ✅ 将同步问题模块化，易于理解和维护
- ✅ 避免信号量的P/V操作顺序错误
- ✅ 适合复杂的同步场景

**缺点**：
- ❌ 需要语言支持（如Java的synchronized）
- ❌ 灵活性不如信号量

---

 **学习提示**：
- ✅ 信号量的P、V操作是核心考点
- ✅ 三个经典问题必考（会写伪代码）
- ✅ 理解互斥和同步的区别
- ✅ 掌握Peterson算法的原理`,
        },
        {
          id: '2.4',
          title: '死锁',
          content: `## 2.4 死锁 ⭐⭐⭐ 必考

### 一、死锁的概念

**定义**：多个进程因竞争资源而造成的一种**僵局**，若无外力作用，这些进程都将无法向前推进

**生活中的例子**：
- 🚗 **交通堵塞**：四辆车在十字路口互相等待
- 🍽️ **哲学家进餐**：每个人都拿起一根筷子，等待另一根

**死锁 vs 饥饿**：

| 对比项 | 死锁 | 饥饿 |
|-------|------|------|
| **进程状态** | 所有进程都阻塞 | 部分进程长期得不到资源 |
| **原因** | 循环等待 | 调度不公平 |
| **解决** | 需要外力干预 | 调整调度策略 |

**💡 什么是饥饿现象（Starvation）**？

**定义**：某个进程长期得不到所需资源，无法向前推进的现象

**产生原因**：
1. **调度算法不公平**
   - 如SJF算法中，长作业可能永远得不到CPU
   - 优先级调度中，低优先级进程可能饥饿

2. **资源分配策略问题**
   - 读者-写者问题中，写者优先可能导致读者饥饿
   - 反之，读者优先可能导致写者饥饿

3. **信号量等待队列管理不当**
   - 没有采用公平队列（FIFO）

**解决方法**：
- ✅ 使用公平调度算法（如FCFS、RR）
- ✅ 老化技术（Aging）：随等待时间增加优先级
- ✅ 确保有限等待原则

### 二、死锁产生的条件 ⭐⭐ 必考

**四个必要条件**（缺一不可）：

#### 1. 互斥条件

**含义**：资源只能被一个进程独占使用

**示例**：打印机、磁带机

#### 2. 请求和保持条件

**含义**：进程已保持至少一个资源，但又提出新的资源请求

**示例**：
\`\`\`
进程A：持有R1，请求R2
进程B：持有R2，请求R1
\`\`\`

#### 3. 不剥夺条件

**含义**：进程已获得的资源在未使用完之前不能被剥夺

**说明**：只能由进程自己释放

#### 4. 循环等待条件

**含义**：存在进程等待环路

**示例**：
\`\`\`
P1 → 等待 R2 → P2 → 等待 R1 → P1
     (形成一个环)
\`\`\`

**重要结论**：
- ✅ 四个条件**同时满足**才会发生死锁
- ❌ 破坏任一条件即可预防死锁

### 三、死锁的处理策略

**四种策略**：

| 策略 | 思想 | 特点 |
|-----|------|------|
| **预防死锁** | 破坏四个必要条件之一 | 保守，资源利用率低 |
| **避免死锁** | 动态检查，确保系统安全 | 较灵活，但开销大 |
| **检测死锁** | 允许死锁发生，然后检测 | 简单，但需解除机制 |
| **解除死锁** | 发现死锁后采取措施 | 最后手段 |

### 四、预防死锁 ⭐⭐ 重点

**思想**：破坏四个必要条件之一

#### 1. 破坏互斥条件

**方法**：将独占资源改造为共享资源

**示例**：
- 使用SPOOLing技术将打印机虚拟化为共享设备

**问题**：
- ❌ 很多资源本质上不能共享

#### 2. 破坏请求和保持条件

**方法1：资源静态分配（Static Allocation）**

**💡 什么是资源静态分配？**

**定义**：进程在运行前一次性申请其所需的全部资源，只有在所有资源都满足时才分配，否则一个都不分配

**特点**：
- ✅ 破坏了“请求和保持”条件
- ✅ 实现简单，不会发生死锁
- ❌ 资源利用率低（有些资源可能很久后才用）
- ❌ 可能导致饥饿（资源紧张时进程迟迟无法开始）

**示例**：
\`\`\`
进程P需要资源{R1, R2, R3}

情况1：R1、R2、R3都可用 → 全部分配给P，P开始执行
情况2：R1可用，但R2被占用 → P等待，不分配任何资源
\`\`\`

**方法2**：申请新资源前释放已有资源

**问题**：
- ❌ 资源利用率低
- ❌ 可能饥饿

#### 3. 破坏不剥夺条件

**方法**：允许强制剥夺资源

**实现**：
- 进程申请新资源失败时，释放已占有的资源
- 过一段时间后重新申请

**问题**：
- ❌ 实现复杂
- ❌ 可能导致工作重复

#### 4. 破坏循环等待条件 ⭐ 常用

**方法1：资源有序分配法（Ordered Resource Allocation）**

**💡 什么是资源有序分配法？**

**定义**：给所有资源类型编号，进程只能按递增顺序申请资源

**实现步骤**：
1. 将所有资源类型线性排序，赋予唯一编号
2. 进程申请资源时必须按编号递增的顺序
3. 如果需要多个资源，必须一次性按序申请

**示例**：
\`\`\`
资源编号：R1(1) < R2(2) < R3(3)

进程A：先申请R1，再申请R2 ✓ （符合顺序）
进程B：先申请R2，再申请R1 ✗ （违反顺序，不允许）
\`\`\`

**优点**：
- ✅ 简单易实现
- ✅ 能有效预防死锁
- ✅ 不需要运行时检测

**缺点**：
- ❌ 限制进程对资源的自由申请
- ❌ 资源编号可能困难（特别是动态资源）
- ❌ 可能导致资源浪费

**方法2：资源配图化简法（Resource Allocation Graph Simplification）**

**💡 什么是资源配图化简法？**

**定义**：通过简化资源分配图来检测是否存在死锁的方法

**基本原理**：
- 如果资源分配图可以完全化简（所有边都能消除），则系统无死锁
- 如果不能完全化简，存在不可化简的环，则发生死锁

**化简步骤**：

1. **找出不阻塞的进程**
   - 该进程的所有资源请求都能被满足
   - 即：Need ≤ Available

2. **假设该进程执行完毕**
   - 释放其占有的所有资源
   - 删除该进程的所有边（请求边和分配边）

3. **重复上述过程**
   - 继续寻找新的不阻塞进程
   - 直到无法继续化简

4. **判断结果**
   - 如果图中所有边都被消除 → 无死锁
   - 如果仍有剩余边 → 存在死锁

**示例**：
\`\`\`
初始状态：
P1 → R1 (P1请求R1)
R1 → P2 (R1已分配给P2)
P2 → R2 (P2请求R2)
R2 → P1 (R2已分配给P1)

化简过程：
1. 检查P1：Need(P1) > Available → 阻塞
2. 检查P2：Need(P2) > Available → 阻塞
3. 无法找到不阻塞进程 → 存在死锁环

结论：P1和P2形成死锁
\`\`\`

**适用场景**：
- 单实例资源：有环 = 死锁
- 多实例资源：有环 ≠ 一定是死锁（需进一步判断）

**优点**：简单易实现
**缺点**：
- ❌ 限制进程对资源的自由申请
- ❌ 资源编号困难

### 五、死锁避免（银行家算法）⭐⭐⭐ 必考

**思想**：在资源分配前判断是否会导致不安全状态

#### 1. 安全状态 vs 不安全状态

**安全状态**：
- 系统能按某种顺序（安全序列）为每个进程分配资源
- 直至满足每个进程的最大需求

**不安全状态**：
- 不存在安全序列
- **不一定**发生死锁，但可能发生

**关系**：
- ✅ 安全状态 → 一定无死锁
- ❌ 不安全状态 → 可能有死锁

#### 2. 银行家算法的数据结构

假设有n个进程，m类资源：

| 数据结构 | 类型 | 说明 |
|---------|------|------|
| **Available** | vector[m] | 每类资源的可用数量 |
| **Max** | matrix[n×m] | 每个进程的最大需求 |
| **Allocation** | matrix[n×m] | 每个进程已分配的资源 |
| **Need** | matrix[n×m] | 每个进程还需要的资源 |

**关系**：
\`\`\`
Need[i][j] = Max[i][j] - Allocation[i][j]
\`\`\`

#### 3. 银行家算法步骤

**当进程Pi发出资源请求Request[i]**：

\`\`\`
Step 1: 检查 Request[i] ≤ Need[i] ?
        是 → 继续
        否 → 报错（超出最大需求）

Step 2: 检查 Request[i] ≤ Available ?
        是 → 继续
        否 → Pi等待（资源不足）

Step 3: 试探性分配
        Available -= Request[i]
        Allocation[i] += Request[i]
        Need[i] -= Request[i]

Step 4: 执行安全性算法
        安全 → 正式分配
        不安全 → 恢复原状，Pi等待
\`\`\`

#### 4. 安全性算法

**目标**：寻找一个安全序列

**步骤**：
\`\`\`
1. 初始化：
   Work = Available
   Finish[i] = false

2. 寻找满足条件的进程Pi：
   Finish[i] == false 且 Need[i] ≤ Work
   
   找到 → 执行Step 3
   找不到 → 执行Step 5

3. 模拟Pi执行完毕：
   Work += Allocation[i]
   Finish[i] = true
   返回Step 2

4. 如果所有Finish[i]都为true → 安全

5. 否则 → 不安全
\`\`\`

#### 5. 实例分析

**例题**：
\`\`\`
系统有3类资源(A,B,C)，数量分别为(10,5,7)
5个进程P0-P4

当前状态：
进程 | Max    | Allocation | Need   | Available
P0   | 7 5 3  | 0 1 0      | 7 4 3  | 3 3 2
P1   | 3 2 2  | 2 0 0      | 1 2 2  |
P2   | 9 0 2  | 3 0 2      | 6 0 0  |
P3   | 2 2 2  | 2 1 1      | 0 1 1  |
P4   | 4 3 3  | 0 0 2      | 4 3 1  |
\`\`\`

**安全性检查**：
\`\`\`
Step 1: Work=(3,3,2)
        P1的Need=(1,2,2) ≤ Work ✓
        执行P1: Work+=(2,0,0)=(5,3,2)

Step 2: Work=(5,3,2)
        P3的Need=(0,1,1) ≤ Work ✓
        执行P3: Work+=(2,1,1)=(7,4,3)

Step 3: Work=(7,4,3)
        P0的Need=(7,4,3) ≤ Work ✓
        执行P0: Work+=(0,1,0)=(7,5,3)

Step 4: Work=(7,5,3)
        P2的Need=(6,0,0) ≤ Work ✓
        执行P2: Work+=(3,0,2)=(10,5,5)

Step 5: Work=(10,5,5)
        P4的Need=(4,3,1) ≤ Work ✓
        执行P4: Work+=(0,0,2)=(10,5,7)

安全序列：P1 → P3 → P0 → P2 → P4 ✓
\`\`\`

### 六、死锁检测

**思想**：允许死锁发生，定期检测

#### 1. 资源分配图（RAG）

**组成**：
- **进程节点**：圆形
- **资源节点**：矩形
- **请求边**：进程→资源（Pi → Rj）
- **分配边**：资源→进程（Rj → Pi）

**死锁定理**：
- 单实例资源：图中有环 → 死锁
- 多实例资源：图中有环 ≠ 死锁（需进一步判断）

#### 2. 死锁检测算法

**类似安全性算法**：
\`\`\`
1. 简化资源分配图
2. 找出不阻塞的进程
3. 假设其执行完毕，释放资源
4. 重复直到无法简化
5. 剩余进程即为死锁进程
\`\`\`

### 七、解除死锁

**方法**：

#### 1. 资源剥夺法

- 从某些进程剥夺资源
- 分配给死锁进程

**问题**：被剥夺的进程可能需要回退

#### 2. 撤销进程法

- 逐个终止死锁进程
- 直到打破循环等待

**策略**：
- 按优先级终止
- 按代价最小终止

#### 3. 进程回退法

- 让进程回退到之前的检查点
- 释放占有的资源

**要求**：需要保存检查点

### 八、死锁处理策略对比 ⭐ 总结

| 策略 | 资源利用率 | 吞吐量 | 实现难度 | 适用场景 |
|-----|-----------|-------|---------|--------|
| **预防** | 低 | 低 | 简单 | 资源充足 |
| **避免** | 中 | 中 | 复杂 | 通用 |
| **检测+解除** | 高 | 高 | 中等 | 死锁罕见 |

---

 **学习提示**：
- ✅ 死锁四个必要条件必考
- ✅ 银行家算法的计算题常考
- ✅ 会画资源分配图
- ✅ 理解各种预防方法的优缺点`,
        }
      ]
    },
    {
      id: 'ch3',
      number: 3,
      title: '内存管理',
      score: '10-15分',
      importance: 'high',
      mindMap: `mindmap
  root((内存管理))
    内存管理概念
      内存管理的基本原理和要求
      连续分配管理方式
      基本分页存储管理
      基本分段存储管理
      段页式存储管理
    虚拟内存管理
      虚拟内存的基本概念
      请求分页管理方式
        页面置换算法
          OPT
          FIFO
          LRU
          Clock
        页框分配
        页面回收
        内存映射文件
      抖动和工作集
      地址翻译的示例`,
      sections: [
        {
          id: '3.1',
          title: '内存管理概念',
          content: `## 3.1 内存管理概念 ⭐⭐⭐ 必考

### 一、内存管理的基本功能

**内存管理的四大功能**：

| 功能 | 说明 | 实现方式 |
|-----|------|--------|
| **内存空间的分配与回收** | 为进程分配内存，使用后回收 | 分区、分页、分段 |
| **地址映射** | 逻辑地址 → 物理地址 | 重定位寄存器、页表 |
| **内存保护** | 防止进程越界访问 | 界限寄存器、键保护 |
| **内存扩充** | 从逻辑上扩充内存容量 | 虚拟内存技术 |

### 二、程序的装入和链接

#### 1. 链接（Linking）

**目标**：将多个目标模块链接成一个完整的可执行文件

**💡 不同储存管理中程序的链接方式区别** ⭐⭐ 重点

**三种链接方式的对比**：

| 方式 | 时机 | 特点 | 适用存储管理 |
|-----|------|------|-------------|
| **静态链接** | 运行前一次性链接 | 简单，但浪费空间 | 连续分配、基本分页 |
| **装入时动态链接** | 装入内存时链接 | 需要时才链接 | 基本分段、段页式 |
| **运行时动态链接** | 执行过程中链接 | 最灵活，支持共享库 | 虚拟存储管理 |

**详细说明**：

**1. 静态链接**：
- 在程序运行之前，将所有目标模块和库函数链接成一个完整的可执行文件
- **优点**：实现简单，执行速度快
- **缺点**：
  - ❌ 浪费存储空间（每个程序都包含完整的库函数）
  - ❌ 更新库函数需要重新链接所有程序
- **适用**：早期操作系统、嵌入式系统

**2. 装入时动态链接**：
- 在程序装入内存时，才将需要的模块链接起来
- **优点**：
  - ✅ 节省存储空间（多个程序可以共享同一个库）
  - ✅ 便于模块更新
- **缺点**：装入时间稍长
- **适用**：分段存储管理、段页式存储管理

**3. 运行时动态链接**：
- 在程序执行过程中，当需要某个模块时才进行链接
- **优点**：
  - ✅ 最灵活，按需加载
  - ✅ 支持插件机制
  - ✅ 节省内存（只加载需要的模块）
- **缺点**：实现复杂，运行时有一定开销
- **适用**：虚拟存储管理、现代操作系统
- **示例**：Windows的DLL、Linux的.so文件

**💡 链接方式与存储管理的关系**：
- **连续分配**：通常使用静态链接，因为程序需要连续空间
- **分页管理**：可以使用静态链接或装入时动态链接
- **分段管理**：适合使用装入时动态链接，因为段是逻辑单位
- **虚拟存储**：最适合使用运行时动态链接，因为可以按需调入

---

**三种链接方式**：

| 方式 | 时机 | 特点 |
|-----|------|------|
| **静态链接** | 运行前一次性链接 | 简单，但浪费空间 |
| **装入时动态链接** | 装入内存时链接 | 需要时才链接 |
| **运行时动态链接** | 执行过程中链接 | 最灵活，支持共享库 |

#### 2. 装入（Loading）

**目标**：将可执行文件装入内存

**三种装入方式**：

##### （1）绝对装入方式

**原理**：编译时就确定物理地址

**适用**：单道程序环境

**缺点**：
- ❌ 只能装入固定位置
- ❌ 不支持多道程序

##### （2）可重定位装入方式（静态重定位）

**原理**：装入时进行地址转换

**过程**：
\`\`\`
编译时：生成相对地址（从0开始）
    ↓
装入时：加上基地址，转换为绝对地址
    ↓
执行时：直接访问绝对地址
\`\`\`

**特点**：
- ✅ 支持多道程序
- ❌ 一旦装入，不能移动
- ❌ 需要连续内存空间

##### （3）动态运行时装入方式（动态重定位）⭐ 重要

**原理**：执行时才进行地址转换

**硬件支持**：重定位寄存器

**过程**：
\`\`\`
编译时：生成相对地址
    ↓
装入时：不修改地址，记录基地址
    ↓
执行时：每次访问内存都加上基地址
\`\`\`

**优点**：
- ✅ 程序可以在内存中移动
- ✅ 支持虚拟内存
- ✅ 便于内存紧凑

**公式**：
\`\`\`
物理地址 = 逻辑地址 + 重定位寄存器的值
\`\`\`

### 三、逻辑地址与物理地址 ⭐⭐ 重点

#### 1. 基本概念

| 地址类型 | 别名 | 说明 |
|---------|------|------|
| **逻辑地址** | 相对地址、虚拟地址 | CPU生成的地址，从0开始 |
| **物理地址** | 绝对地址、实地址 | 内存单元的实际地址 |

#### 2. 地址空间

**逻辑地址空间**：程序能使用的地址范围

**物理地址空间**：实际内存的地址范围

**关系**：
- 逻辑地址空间 ≤ 物理地址空间（无虚拟内存时）
- 逻辑地址空间 > 物理地址空间（有虚拟内存时）

#### 3. 地址重定位

**定义**：将逻辑地址转换为物理地址的过程

**两种重定位方式对比**：

| 对比项 | 静态重定位 | 动态重定位 |
|-------|-----------|----------|
| **时机** | 装入时 | 执行时 |
| **实现** | 软件 | 硬件（重定位寄存器） |
| **能否移动** | 不能 | 能 |
| **虚拟内存** | 不支持 | 支持 |
| **开销** | 小 | 大（每次访问都要转换） |

### 四、连续分配管理方式

#### 1. 单一连续分配

**原理**：内存分为系统区和用户区，用户区只分配给一个进程

**适用**：单道批处理系统

**缺点**：
- ❌ 内存利用率低
- ❌ 不支持多道程序

#### 2. 固定分区分配

**原理**：将内存划分为若干个固定大小的分区

**特点**：
- ✅ 实现简单
- ❌ 内部碎片严重
- ❌ 分区大小难以确定

**内部碎片**：分配给进程的分区中未被利用的部分

#### 3. 动态分区分配 ⭐⭐ 重点

**原理**：根据进程的实际需要，动态地分配内存空间

**💡 动态分区的建立时机**：
- ✅ **动态分区是在作业装入时动态建立的**
- 当作业需要装入内存时，系统根据其大小在空闲分区表中查找合适的分区
- 如果找到合适分区，则将其分割并分配给作业
- 如果没有合适分区，则可能触发内存紧凑或拒绝分配

**数据结构**：
- **空闲分区表**：记录所有空闲分区
- **空闲分区链**：链接所有空闲分区

##### （1）首次适应算法（FF, First Fit）

**思想**：从链首开始查找，找到第一个满足要求的分区

**示例**：
\`\`\`
空闲分区：100K → 50K → 200K → 30K
请求：80K
结果：分配200K分区（第一个满足的）
剩余：120K
\`\`\`

**优点**：
- ✅ 保留高址部分的大空闲区
- ✅ 查找速度快

**缺点**：
- ❌ 低址部分碎片多
- ❌ 大分区被分割

##### （2）最佳适应算法（BF, Best Fit）

**思想**：查找能满足要求的最小分区

**示例**：
\`\`\`
空闲分区：100K → 50K → 200K → 90K
请求：80K
结果：分配90K分区（最小的满足的）
剩余：10K ← 小碎片！
\`\`\`

**优点**：
- ✅ 保留大空闲区

**缺点**：
- ❌ 产生大量外部碎片（太小无法利用）
- ❌ 查找速度慢（需遍历全部）

##### （3）最坏适应算法（WF, Worst Fit）

**思想**：挑选最大的分区进行分配

**示例**：
\`\`\`
空闲分区：100K → 50K → 200K → 90K
请求：80K
结果：分配200K分区（最大的）
剩余：120K
\`\`\`

**优点**：
- ✅ 减少外部碎片
- ✅ 剩余分区较大，仍可分配

**缺点**：
- ❌ 缺乏大空闲区
- ❌ 不适合大作业

##### （4）邻近适应算法（NF, Next Fit）

**思想**：从上次查找结束的位置开始查找

**特点**：
- 使空闲分区分布均匀
- 避免低址部分碎片集中

##### （5）快速适应算法（Quick Fit）⭐ 补充

**思想**：为常用大小的请求设置专门的空闲队列

**实现**：
- 维护多个空闲链表，每个链表对应一种固定大小
- 例如：8KB链表、16KB链表、32KB链表
- 当有请求时，直接到对应大小的链表中分配

**优点**：
- ✅ 查找速度极快（O(1)）
- ✅ 无需搜索，直接定位

**缺点**：
- ❌ 空间开销大（需要维护多个链表）
- ❌ 只适用于固定大小的请求

**适用场景**：
- 操作系统内核内存分配
- 数据库缓冲池管理

---

### 💡 可重入程序（Reentrant Program）⭐⭐ 重要

**定义**：可重入程序是指可以被多个进程同时调用而不会相互干扰的程序

**特点**：
- ✅ 代码部分是纯代码（不包含任何可变数据）
- ✅ 不修改自身代码
- ✅ 使用局部变量或参数传递数据

**💡 如何通过对换改善系统性能**：
- ✅ **可重入程序可以通过减少对换数量来改善系统性能**
- 原因：可重入程序的代码部分可以被多个进程共享
- 当进行内存对换时，只需要对换数据部分，代码部分可以保留在内存中
- 这样减少了对换的数据量，提高了系统效率

**示例**：
- 编译器、编辑器等工具程序通常是可重入的
- 多个用户可以同时使用同一个编译器编译不同的程序

---

#### 💡 伙伴系统（Buddy System）⭐⭐ 重要

**定义**：一种基于二进制分割的动态内存分配算法

**核心思想**：
- 将所有空闲块按大小分组，每组大小为2的k次方
- 当请求大小为s时，找到最小的2的k次方 ≥ s
- 如果该大小的块不够，就将更大的块分裂成两个"伙伴"

**工作原理**：

初始状态：一个1024KB的大块

请求200KB：
1. 找到最小2的k次方 ≥ 200 → 2的8次方 = 256KB
2. 没有256KB的块，分裂512KB → 两个256KB伙伴
3. 分配一个256KB，另一个成为空闲

释放256KB：
1. 检查其伙伴是否也空闲
2. 如果伙伴空闲，合并成512KB
3. 否则，保持256KB空闲

**伙伴的定义**：
- 两个大小相同的块
- 地址连续
- 由同一个大块分裂而来

**判断伙伴的方法**：

假设块大小为2的k次方，地址为addr

伙伴地址 = addr XOR (1 << k)

示例：
- 块A: addr=0, size=256KB (2的8次方)
- 伙伴B: addr=0 XOR 256 = 256KB

**优点**：
- ✅ 分配和回收速度快（O(log n)）
- ✅ 外部碎片较少
- ✅ 合并操作简单（只需检查伙伴）

**缺点**：
- ❌ 内部碎片严重（向上取整到2的幂）
- ❌ 不适合任意大小的请求

**实际应用**：
- Linux内核内存分配器（slab allocator的基础）
- Windows堆管理器
- 数据库内存池

---

### 💡 快速适应算法（Quick Fit）⭐ 补充

**思想**：为常用大小的请求设置专门的空闲队列

**实现**：
- 维护多个空闲链表，每个链表对应一种固定大小
- 例如：8KB链表、16KB链表、32KB链表
- 当有请求时，直接到对应大小的链表中分配

**优点**：
- ✅ 查找速度极快（O(1)）
- ✅ 无需搜索，直接定位

**缺点**：
- ❌ 空间开销大（需要维护多个链表）
- ❌ 只适用于固定大小的请求

**适用场景**：
- 操作系统内核内存分配
- 数据库缓冲池管理

#### 💡 动态分区的内存回收与合并 ⭐⭐ 重点

**问题背景**：
当进程运行结束释放内存时，会产生新的空闲分区。如果这个空闲分区与已有的空闲分区相邻，就需要进行**合并**，以减少外部碎片。

**内存回收的四种情况**：

设释放的分区为F，大小为size_F，起始地址为addr_F

| 情况 | 条件 | 处理方式 | 示意图 |
|-----|------|---------|--------|
| **情况1** | F上方有相邻空闲区 | 合并到上方空闲区 | [空闲][F] → [更大的空闲] |
| **情况2** | F下方有相邻空闲区 | 合并到下方空闲区 | [F][空闲] → [更大的空闲] |
| **情况3** | F上下都有相邻空闲区 | 三者合并为一个 | [空闲][F][空闲] → [超大空闲] |
| **情况4** | F上下都无相邻空闲区 | F独立成为新空闲区 | [占用][F][占用] → [占用][空闲][占用] |

**判断相邻的方法**：

\`\`\`
// 假设空闲分区表按地址递增排序

情况1：上方相邻
if (空闲表[i].addr + 空闲表[i].size == addr_F) {
    // F紧接在空闲表[i]之后
    空闲表[i].size += size_F;  // 扩展上方空闲区
}

情况2：下方相邻
if (addr_F + size_F == 空闲表[j].addr) {
    // F紧接在空闲表[j]之前
    空闲表[j].addr = addr_F;   // 更新起始地址
    空闲表[j].size += size_F;  // 扩展下方空闲区
}

情况3：上下都相邻
if (情况1 && 情况2) {
    // 三个分区合并
    空闲表[i].size += size_F + 空闲表[j].size;
    删除空闲表[j];  // 下方的空闲区被合并，需删除
}

情况4：都不相邻
// 在空闲表中插入新的空闲分区F
\`\`\`

**示例分析**：

\`\`\`
初始状态（地址从小到大）：
[系统区100K][P1:200K][空闲50K][P2:100K][空闲80K][P3:150K]

场景1：P1结束（释放200K）
- P1上方是系统区（不空闲）
- P1下方是空闲50K（相邻！）
→ 合并：P1(200K) + 下方空闲(50K) = 250K空闲
结果：[系统区100K][空闲250K][P2:100K][空闲80K][P3:150K]

场景2：P2结束（释放100K）
- P2上方是空闲250K（相邻！）
- P2下方是空闲80K（相邻！）
→ 三者合并：250K + 100K + 80K = 430K空闲
结果：[系统区100K][空闲430K][P3:150K]

场景3：P3结束（释放150K）
- P3上方是空闲430K（相邻！）
- P3下方无分区（边界）
→ 合并到上方：430K + 150K = 580K空闲
结果：[系统区100K][空闲580K]
\`\`\`

**实现要点**：

1. **空闲表维护**
   - 通常使用链表或数组存储空闲分区
   - 按地址递增排序便于判断相邻

2. **合并时机**
   - 进程终止时立即回收
   - 调用free()等内存释放函数时

3. **数据结构选择**
   - **链表**：插入删除方便，但查找慢
   - **数组**：查找快，但插入删除需移动元素

4. **边界处理**
   - 第一个分区：只有下方可能相邻
   - 最后一个分区：只有上方可能相邻
   - 中间分区：上下都可能相邻

**重要性**：
- ✅ 减少外部碎片，提高内存利用率
- ✅ 避免大量小碎片无法利用
- ✅ 是大作业能够找到足够大空闲区的关键

#### 4. 四种算法对比 ⭐ 总结

| 算法 | 查找速度 | 碎片情况 | 大分区保护 | 适用场景 |
|-----|---------|---------|-----------|--------|
| **FF** | 快 | 低址碎片多 | 好 | 通用 |
| **BF** | 慢 | 外部碎片多 | 好 | 小作业多 |
| **WF** | 中等 | 碎片少 | 差 | 大作业多 |
| **NF** | 较快 | 分布均匀 | 一般 | 通用 |

### 五、离散分配管理方式概述

**问题**：连续分配会产生外部碎片

**解决**：离散分配（分页、分段、段页式）

**思想**：将进程分散地装入不相邻的内存区域

**三种方式对比**：

| 方式 | 划分单位 | 目的 | 维度 |
|-----|---------|------|-----|
| **分页** | 页面（固定大小） | 提高内存利用率 | 一维 |
| **分段** | 段（可变大小） | 满足用户需求 | 二维 |
| **段页式** | 先分段再分页 | 结合两者优点 | 二维 |

---

### 💡 分段存储管理的关键点 ⭐⭐

**💡 段的数量与段表的关系**：
- ✅ **不管进程有多少个段，系统都为每个进程建立一张段表**
- 段表记录了该进程所有段的信息（段基址、段长度等）
- 段表的大小是固定的，与段的数量无关

**💡 分段的决定因素**：
- ✅ **段式存储管理时，一个程序如何分段是在用户编程时决定的**
- 程序员根据程序的逻辑结构（如主程序、子程序、数据段等）来划分段
- 这与分页不同，分页是由系统自动完成的，对用户透明

---

 **学习提示**：
- ✅ 三种装入方式的对比是高频考点
- ✅ 动态分区分配的四种算法必考（会计算）
- ✅ **内存回收与合并机制**是重点（四种情况要掌握）
- ✅ 理解内部碎片和外部碎片的区别
- ✅ 掌握静态重定位和动态重定位的区别`,
        },
        {
          id: '3.2',
          title: '基本分页存储管理',
          content: `## 3.2 基本分页存储管理 ⭐⭐⭐ 必考

### 一、基本概念

**分页存储管理**：将进程的逻辑地址空间分成若干个大小相等的**页面**，将物理内存分成同样大小的**页框**

**核心思想**：离散分配，提高内存利用率

#### 1. 页面（Page）

**定义**：逻辑地址空间的固定大小片段

**特点**：
- 大小固定（通常为4KB）
- 从0开始编号
- 最后一页可能不足一页（内部碎片）

#### 2. 页框（Page Frame）

**定义**：物理内存的固定大小存储块

**特点**：
- 大小与页面相同
- 从0开始编号
- 也称为页帧、物理块

#### 3. 页表（Page Table）

**定义**：记录页面与页框对应关系的数据结构

**作用**：实现逻辑地址到物理地址的映射

**页表项（PTE）**：

| 字段 | 说明 |
|-----|------|
| **页框号** | 对应的物理页框号 |
| **存在位（P）** | 页面是否在内存中 |
| **修改位（M）** | 页面是否被修改过 |
| **访问位（A）** | 页面是否被访问过 |
| **保护位** | 读/写/执行权限 |

**💡 页表的建立时机**：
- ✅ **页表是在程序装入内存时建立的**
- 操作系统在加载程序时，根据程序的逻辑地址空间大小创建页表
- 页表本身也存储在内存中，由操作系统管理

### 二、地址结构 ⭐⭐ 重点

#### 1. 逻辑地址结构

\`\`\`
┌─────────────┬──────────────┐
│   页号 P     │  页内偏移 W   │
└─────────────┴──────────────┘
\`\`\`

**计算公式**：
\`\`\`
页号 P = 逻辑地址 / 页面大小
页内偏移 W = 逻辑地址 % 页面大小
\`\`\`

**示例**：
\`\`\`
逻辑地址 = 2500，页面大小 = 1KB (1024B)
页号 P = 2500 / 1024 = 2
页内偏移 W = 2500 % 1024 = 452
\`\`\`

#### 2. 物理地址结构

\`\`\`
物理地址 = 页框号 × 页面大小 + 页内偏移
\`\`\`

**示例**：
\`\`\`
页框号 = 5，页面大小 = 1KB，页内偏移 = 452
物理地址 = 5 × 1024 + 452 = 5572
\`\`\`

### 三、地址变换机构 ⭐⭐ 重点

#### 1. 基本地址变换过程

\`\`\`
Step 1: 从逻辑地址中提取页号P和页内偏移W
    ↓
Step 2: 检查页号是否越界（P ≥ 页表长度？）
    ↓
Step 3: 查页表，得到页框号F
    ↓
Step 4: 拼接物理地址 = F × 页面大小 + W
    ↓
Step 5: 访问物理内存
\`\`\`

**注意**：每次内存访问需要**两次**访存
- 第1次：查页表
- 第2次：访问数据

#### 2. 具有快表的地址变换机构

**问题**：每次访问都要查页表，速度慢

**解决**：引入**TLB（快表）**

**TLB（Translation Lookaside Buffer）**：
- 高速缓存，存放最近访问的页表项
- 容量小（通常64-1024项）
- 速度快（硬件实现）

**地址变换过程**：
\`\`\`
Step 1: 提取页号P
    ↓
Step 2: 查TLB
    ├─ 命中 → 直接得到页框号F
    └─ 未命中 → 查页表，更新TLB
    ↓
Step 3: 拼接物理地址
    ↓
Step 4: 访问物理内存
\`\`\`

**有效访问时间（EAT）**：
\`\`\`
EAT = 命中率 × (TLB时间 + 内存时间) 
    + (1 - 命中率) × (TLB时间 + 页表时间 + 内存时间)
\`\`\`

**示例计算**：
\`\`\`
假设：
- TLB时间 = 20ns
- 内存时间 = 100ns
- 命中率 = 90%

EAT = 0.9 × (20 + 100) + 0.1 × (20 + 100 + 100)
    = 0.9 × 120 + 0.1 × 220
    = 108 + 22
    = 130ns

无TLB时：200ns
性能提升：(200 - 130) / 200 = 35%
\`\`\`

### 四、两级页表

**问题**：页表太大，需要连续存放

**示例**：
\`\`\`
32位地址空间，页面大小4KB
页表项数 = 2^20 = 1M个
每个页表项4B
页表大小 = 4MB ← 太大！
\`\`\`

**解决**：将页表分页

**两级页表结构**：
\`\`\`
逻辑地址：┌──────┬──────┬────────┐
         │页目录号│页表号│ 页内偏移 │
         └──────┴──────┴────────┘
            10位    10位     12位
\`\`\`

**优点**：
- ✅ 页表可以离散存放
- ✅ 只需为使用的部分分配空间

**缺点**：
- ❌ 需要多次访存（页目录→页表→数据）

### 五、分页 vs 分段 ⭐ 对比

| 对比项 | 分页 | 分段 |
|-------|------|------|
| **划分单位** | 页面（固定大小） | 段（可变大小） |
| **目的** | 提高内存利用率 | 满足用户需求 |
| **维度** | 一维 | 二维 |
| **共享** | 困难 | 容易 |
| **保护** | 困难 | 容易 |
| **碎片** | 内部碎片 | 外部碎片 |

---

 **学习提示**：
- ✅ 地址变换的计算题必考
- ✅ 理解TLB的作用和命中率计算
- ✅ 掌握页表项的内容
- ✅ 了解两级页表的原因`,
        },
        {
          id: '3.3',
          title: '基本分段存储管理',
          content: `## 3.3 基本分段存储管理 ⭐⭐ 重点

### 一、分段的基本概念

**分段存储管理**：将进程的逻辑地址空间分成若干个**段**，每段是一组逻辑上完整的信息单元

**与分页的区别**：
- 📍 **分页**：物理单位，对用户透明
- 📍 **分段**：逻辑单位，用户可见

**段的划分依据**：
- 主程序段
- 子程序段
- 数据段
- 栈段

### 二、段表（Segment Table）

**段表项**：

| 字段 | 说明 |
|-----|------|
| **段长** | 段的长度 |
| **基址** | 段在内存中的起始地址 |
| **存在位** | 段是否在内存中 |
| **访问权限** | 读/写/执行 |

**段表寄存器**：
- 段表始址：段表在内存的起始地址
- 段表长度：段的数量

### 三、地址结构 ⭐ 重点

**二维地址结构**：
\`\`\`
逻辑地址 = (段号 S, 段内偏移 W)
\`\`\`

**地址变换过程**：
\`\`\`
Step 1: 检查段号是否越界（S ≥ 段表长度？）
    ↓
Step 2: 查段表，得到段长L和基址B
    ↓
Step 3: 检查段内偏移是否越界（W ≥ L？）
    ↓
Step 4: 物理地址 = B + W
\`\`\`

**示例**：
\`\`\`
段号S=2，段内偏移W=100
查段表：段2的基址B=5000，段长L=200
检查：100 < 200 ✓
物理地址 = 5000 + 100 = 5100
\`\`\`

### 四、分段的优缺点

**优点**：
- ✅ 便于编程和模块化
- ✅ 便于共享和保护
- ✅ 动态增长（段可以扩展）

**缺点**：
- ❌ 产生外部碎片
- ❌ 内存利用率低
- ❌ 需要紧凑技术

### 五、分页 vs 分段 ⭐⭐ 必考

| 对比项 | 分页 | 分段 |
|-------|------|------|
| **划分单位** | 页面（固定大小） | 段（可变大小） |
| **目的** | 提高内存利用率 | 满足用户需求 |
| **维度** | 一维（只需页号） | 二维（段号+段内偏移） |
| **对用户透明** | 是 | 否 |
| **共享** | 困难（以页为单位） | 容易（以段为单位） |
| **保护** | 困难 | 容易（每段可设权限） |
| **动态增长** | 不支持 | 支持 |
| **碎片** | 内部碎片 | 外部碎片 |

---

 **学习提示**：
- ✅ 理解分段和分页的本质区别
- ✅ 掌握二维地址结构
- ✅ 了解分段的优点（共享、保护）`,
        },
        {
          id: '3.4',
          title: '段页式存储管理',
          content: `## 3.4 段页式存储管理

### 一、基本概念

**段页式存储管理**：先分段，再分页

**思想**：结合分段和分页的优点

**地址结构**：
\`\`\`
逻辑地址 = (段号 S, 页号 P, 页内偏移 W)
\`\`\`

### 二、地址变换过程

\`\`\`
Step 1: 查段表，得到该段的页表始址
    ↓
Step 2: 查页表，得到页框号
    ↓
Step 3: 拼接物理地址 = 页框号 × 页面大小 + 页内偏移
\`\`\`

**访存次数**：
- 无TLB：3次（段表→页表→数据）
- 有TLB：通常1-2次

### 三、优缺点

**优点**：
- ✅ 兼具分段和分页的优点
- ✅ 便于共享和保护
- ✅ 内存利用率高

**缺点**：
- ❌ 硬件复杂
- ❌ 软件实现复杂
- ❌ 需要更多内存空间（段表+页表）

---

 **学习提示**：
- ✅ 了解段页式的地址结构
- ✅ 理解三次访存的原因`,
        },
        {
          id: '3.5',
          title: '虚拟内存管理',
          content: `## 3.5 虚拟内存管理 ⭐⭐⭐ 必考

### 一、虚拟内存的基本概念

**虚拟内存**：基于**局部性原理**，在程序装入时，只装入当前需要的部分，其余部分留在外存

**本质**：从逻辑上扩充内存容量

#### 1. 局部性原理 ⭐ 理论基础

**时间局部性**：
- 如果某个数据被访问，那么不久后它可能再次被访问
- **原因**：循环、栈操作

**空间局部性**：
- 如果某个数据被访问，那么它附近的数据也可能被访问
- **原因**：顺序执行、数组访问

**应用**：
- Cache：利用局部性提高访问速度
- 虚拟内存：利用局部性减少内存需求

#### 2. 虚拟内存的特征

| 特征 | 说明 |
|-----|------|
| **多次性** | 作业无需一次性全部装入内存 |
| **对换性** | 作业无需一直常驻内存 |
| **虚拟性** | 从逻辑上扩充内存容量 |

**重要结论**：
- ✅ 虚拟内存 = 请求调入 + 页面置换
- ✅ 虚拟内存容量 = 内存容量 + 外存容量
- ✅ **虚拟储存技术的核心在于按需调入和自动换出**
  - **按需调入**：只将当前需要的页面调入内存，其他页面留在外存
  - **自动换出**：当内存不足时，自动将不常用的页面换出到外存

### 二、请求分页管理方式

**实现条件**：

| 条件 | 说明 |
|-----|------|
| **硬件支持** | 页表机制、中断机构、地址变换机构 |
| **软件支持** | 请求调页策略、页面置换算法 |

**页表项的扩展**：

| 字段 | 说明 |
|-----|------|
| **存在位（P）** | 页面是否在内存中 |
| **修改位（M）** | 页面是否被修改过 |
| **访问位（A）** | 页面是否被访问过 |
| **外存地址** | 页面在外存的位置 |

**💡 虚拟存储器的页表项特点**：
- ✅ **虚拟存储器的页表项比基本分页管理的页表项更复杂**
- 增加了存在位、修改位、访问位、外存地址等字段
- 存在位用于判断页面是否在内存中，如果不在则触发缺页中断
- 外存地址用于在页面被换出后，能够重新从外存调入

### 三、页面置换算法 ⭐⭐⭐ 必考

**💡 缺页中断的详细描述**：
- ✅ **缺页中断是一种特殊的中断，发生在访问的页面不在内存中时**
- **触发条件**：CPU访问某个页面时，检查页表中的存在位P=0
- **处理过程**：
  1. CPU发出访存请求
  2. 地址变换机构查页表，发现存在位P=0
  3. 硬件产生缺页中断，暂停当前进程
  4. 操作系统执行缺页中断处理程序
  5. 从外存中找到该页面，调入内存
  6. 如果内存已满，需要选择一个页面换出（页面置换）
  7. 更新页表，将存在位P置为1
  8. 重新执行被中断的指令
- **特点**：
  - 缺页中断是在指令执行期间产生的
  - 一条指令可能产生多次缺页中断
  - 缺页中断处理后，需要重新执行被中断的指令

---

#### 1. OPT（最佳置换算法）

**思想**：选择以后永不使用或最长时间内不再访问的页面淘汰

**特点**：
- ✅ 理想算法，缺页率最低
- ❌ 无法实现（需要预知未来）
- 📍 用于评价其他算法

**示例**：
\`\`\`
物理块数：3
页面序列：7, 0, 1, 2, 0, 3, 0, 4, 2, 3, 0, 3, 2, 1, 2, 0, 1, 7, 0, 1

假设当前内存中有：7, 0, 1
下一个要访问的是2，需要置换

查看未来：
- 7：第18次才用
- 0：第4次就用
- 1：第13次才用

结果：置换7（最晚使用）
\`\`\`

#### 2. FIFO（先进先出）

**思想**：选择最先进入内存的页面淘汰

**特点**：
- ✅ 实现简单
- ❌ 性能差
- ❌ 可能出现**Belady异常**

**Belady异常**：
- 增加物理块数，缺页次数反而增加
- 原因：FIFO不考虑页面的使用情况

**示例**：
\`\`\`
物理块数：3
页面序列：1, 2, 3, 4, 1, 2, 5, 1, 2, 3, 4, 5

缺页次数：9次

如果物理块数增加到4：
缺页次数：10次 ← Belady异常！
\`\`\`

#### 3. LRU（最近最久未使用）⭐ 重要

**思想**：选择最近最长时间未被访问的页面淘汰

**特点**：
- ✅ 性能较好（接近OPT）
- ✅ 不会出现Belady异常
- ❌ 实现开销大

**实现方式**：

##### （1）计数器法

**方法**：为每个页面记录最后访问时间

**缺点**：需要硬件计数器，开销大

##### （2）栈法

**方法**：维护一个栈，最近访问的页面放在栈顶

**优点**：不需要比较，只需移动指针

**示例**：
\`\`\`
物理块数：3
页面序列：7, 0, 1, 2, 0, 3, 0, 4, 2, 3

Step 1: 访问7 → [7]
Step 2: 访问0 → [0, 7]
Step 3: 访问1 → [1, 0, 7]
Step 4: 访问2 → [2, 1, 0]  (7被淘汰)
Step 5: 访问0 → [0, 2, 1]  (0移到栈顶)
...
\`\`\`

#### 4. Clock算法（时钟算法）⭐ 常用

**思想**：LRU的近似算法，使用访问位

**数据结构**：
- 循环队列
- 每个页面有一个访问位A

**💡 Clock算法的具体内容**：

**基本Clock算法步骤**：
\`\`\`
Step 1: 指针指向当前位置
    ↓
Step 2: 检查访问位A
    ├─ A=0 → 淘汰该页面，指针下移
    └─ A=1 → 将A清零，指针下移
    ↓
Step 3: 重复Step 2，直到找到A=0的页面
\`\`\`

**改进型Clock算法**：
- 同时考虑访问位A和修改位M
- 优先淘汰(A=0, M=0)的页面

**四类页面**：

| 类别 | (A, M) | 说明 | 优先级 |
|-----|--------|------|-------|
| 第1类 | (0, 0) | 最近未访问，未修改 | 最高 |
| 第2类 | (0, 1) | 最近未访问，已修改 | 较高 |
| 第3类 | (1, 0) | 最近访问，未修改 | 较低 |
| 第4类 | (1, 1) | 最近访问，已修改 | 最低 |

**扫描轮次**：
- 第1轮：找(0,0)，不清零A
- 第2轮：找(0,1)，不清零A
- 第3轮：找(0,0)，清零A
- 第4轮：找(0,1)，清零A

**💡 Clock算法的优势**：
- ✅ 实现简单，只需一个循环链表和一个指针
- ✅ 性能接近LRU，但开销更小
- ✅ 广泛应用于实际操作系统（如Linux）

#### 5. 页面置换算法对比 ⭐ 总结

| 算法 | 性能 | 实现难度 | Belady异常 | 适用场景 |
|-----|-----|---------|-----------|--------|
| **OPT** | 最优 | 无法实现 | 无 | 理论评价 |
| **FIFO** | 差 | 简单 | 有 | 很少用 |
| **LRU** | 好 | 复杂 | 无 | 通用 |
| **Clock** | 较好 | 中等 | 无 | 实际系统 |

### 四、抖动和工作集

#### 💡 驻留集（Resident Set）⭐⭐ 重要

**定义**：驻留集是指进程在内存中占用的物理页面的集合

**💡 驻留集的大小**：
- 驻留集大小 = 进程当前在内存中的页面数
- 驻留集大小可以动态变化
- 操作系统需要为每个进程维护其驻留集

**💡 驻留集与抖动的关系**：
- 如果驻留集太小，会导致频繁的缺页中断，引发抖动
- 如果驻留集太大，会减少其他进程的可用内存
- 操作系统需要在两者之间找到平衡

---

#### 💡 局部置换和全局置换的区别 ⭐⭐ 重点

**局部置换（Local Replacement）**：
- **定义**：只从发生缺页中断的进程的驻留集中选择页面进行置换
- **特点**：
  - ✅ 不影响其他进程的页面
  - ✅ 性能可预测
  - ❌ 可能导致某些进程驻留集过小
- **适用**：大多数现代操作系统

**全局置换（Global Replacement）**：
- **定义**：可以从所有进程的页面中选择页面进行置换
- **特点**：
  - ✅ 更灵活，可以利用全局信息
  - ❌ 可能影响其他进程的性能
  - ❌ 性能不可预测
- **适用**：早期操作系统

**对比总结**：

| 特性 | 局部置换 | 全局置换 |
|-----|---------|----------|
| **置换范围** | 仅当前进程 | 所有进程 |
| **对其他进程的影响** | 无 | 有 |
| **性能可预测性** | 好 | 差 |
| **实现复杂度** | 简单 | 复杂 |
| **实际应用** | 广泛使用 | 较少使用 |

---

#### 1. 抖动（颠簸）现象

**定义**：页面频繁地在内存和外存之间交换，导致系统大部分时间花在页面置换上

**原因**：
- 多道度过高
- 分配给进程的物理块太少
- 页面置换算法不当

**表现**：
- CPU利用率低
- 磁盘I/O频繁
- 系统响应慢

**解决方法**：
- 降低多道度
- 采用工作集策略
- 增加物理内存

#### 2. 工作集（Working Set）

**定义**：在某段时间间隔Δ内，进程实际访问的页面的集合

**公式**：
\`\`\`
W(t, Δ) = {页面i | 页面i在[t-Δ, t]时间内被访问}
\`\`\`

**工作集大小**：
- Δ太小：工作集不能覆盖全部常用页面
- Δ太大：工作集包含太多不常用页面

**应用**：
- 为进程分配足够的物理块（≥工作集大小）
- 避免抖动

### 五、页面缓冲算法（PBA）

**思想**：将被置换的页面暂时保留在内存缓冲区中

**两个链表**：
- **空闲链表**：未被修改的页面
- **修改链表**：已被修改的页面

**优点**：
- 减少磁盘I/O
- 提高系统性能

---

### 💡 内存映射文件（Memory-Mapped File）⭐⭐ 重要

**定义**：内存映射文件是一种将文件内容直接映射到进程地址空间的技术

**工作原理**：
- 操作系统将文件的一部分或全部映射到进程的虚拟地址空间
- 进程可以像访问内存一样访问文件内容
- 对内存的修改会自动写回文件（或通过msync()手动同步）

**优点**：
- ✅ 简化文件I/O操作（无需read/write系统调用）
- ✅ 提高性能（减少数据拷贝）
- ✅ 支持随机访问
- ✅ 便于文件共享（多个进程可以映射同一个文件）

**应用场景**：
- 数据库系统（如SQLite、MySQL）
- 大型文件处理
- 进程间通信（共享内存）
- 可执行文件的加载

**示例**：
\`\`\`c
// Linux中使用mmap()函数
void *ptr = mmap(NULL, file_size, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);
// 现在可以直接通过ptr访问文件内容
ptr[0] = 'A';  // 修改文件内容
munmap(ptr, file_size);  // 解除映射
\`\`\`

---

### 💡 分配页框（Page Frame Allocation）⭐⭐ 重点

**定义**：分配页框是指操作系统为进程分配物理内存页面的过程

**💡 页框分配策略**：

**1. 固定分配**：
- 为每个进程分配固定数量的页框
- 简单但不够灵活

**2. 可变分配**：
- 根据进程的需要动态调整页框数量
- 更灵活，但实现复杂

**💡 页框分配算法**：

**局部范围**：
- 只从当前进程的驻留集中分配页框
- 不影响其他进程

**全局范围**：
- 可以从所有空闲页框中分配
- 可能影响其他进程

**💡 页框分配与页面置换的关系**：
- 当需要分配新页框但没有空闲页框时，需要触发页面置换
- 页面置换算法选择一个页面换出，释放其页框
- 然后将新页面装入该页框

---

 **学习提示**：
- ✅ 四种页面置换算法必考（会计算缺页次数）
- ✅ 理解LRU和Clock的实现原理
- ✅ 掌握Belady异常的概念
- ✅ 了解抖动的原因和解决方法`,
        }
      ]
    },
    {
      id: 'ch4',
      number: 4,
      title: '文件管理',
      score: '8-12分',
      importance: 'medium',
      mindMap: `mindmap
  root((文件管理))
    文件系统基础
      文件的基本概念
      文件系统结构
      文件的逻辑结构
    目录与文件
      目录的基本概念
      文件控制块和索引节点
      目录结构
      目录的操作
      目录实现
      文件的物理结构
      文件的操作
      文件共享
      文件保护
    文件系统
      文件系统布局
      文件存储空间管理
      虚拟文件系统
      文件系统挂载`,
      sections: [
        {
          id: '4.1',
          title: '文件系统基础',
          content: `## 4.1 文件系统基础 ⭐⭐ 重点

### 一、文件的基本概念

**文件**：具有文件名的一组相关信息的集合，是文件系统管理的**基本单位**

**本质**：
- 📍 逻辑概念：用户视角的信息集合
- 📍 物理实现：存储在磁盘上的数据块

**文件的属性**：

| 属性 | 说明 |
|-----|------|
| **文件名** | 文件的标识符 |
| **类型** | 可执行文件、文本文件等 |
| **位置** | 在磁盘上的存储位置 |
| **大小** | 文件的长度 |
| **保护** | 访问权限 |
| **时间** | 创建时间、修改时间、访问时间 |

---

### 💡 UNIX系统中的文件类型 ⭐⭐ 重要

**在UNIX系统中，文件分为以下几种类型**：

| 文件类型 | 符号 | 说明 | 示例 |
|---------|-----|------|------|
| **普通文件（Regular File）** | - | 包含数据的文件，如文本、二进制文件 | file.txt, program.exe |
| **目录文件（Directory）** | d | 包含其他文件和目录的列表 | /home, /usr |
| **字符设备文件（Character Device）** | c | 按字符流方式访问的设备 | /dev/tty, /dev/null |
| **块设备文件（Block Device）** | b | 按块方式访问的设备 | /dev/sda, /dev/hda |
| **管道文件（FIFO/Pipe）** | p | 用于进程间通信的命名管道 | /tmp/mypipe |
| **套接字文件（Socket）** | s | 用于网络通信或本地进程间通信 | /tmp/mysql.sock |
| **符号链接文件（Symbolic Link）** | l | 指向另一个文件的链接 | ln -s target link |

**💡 如何查看文件类型**：

\`\`\`bash
# 使用ls -l命令查看文件类型
$ ls -l
-rw-r--r-- 1 user group 1024 Jan 1 12:00 file.txt    # - 表示普通文件
drwxr-xr-x 2 user group 4096 Jan 1 12:00 directory   # d 表示目录
crw-rw-rw- 1 root root 5, 0 Jan 1 12:00 /dev/null    # c 表示字符设备
brw-rw---- 1 root disk 8, 0 Jan 1 12:00 /dev/sda     # b 表示块设备
prw-r--r-- 1 user group 0 Jan 1 12:00 mypipe         # p 表示管道
srwxrwxrwx 1 user group 0 Jan 1 12:00 mysql.sock     # s 表示套接字
lrwxrwxrwx 1 user group 10 Jan 1 12:00 link -> target # l 表示符号链接
\`\`\`

**💡 文件类型的重要性**：
- ✅ 操作系统根据文件类型决定如何处理该文件
- ✅ 不同文件类型有不同的操作方式
- ✅ 设备文件允许用户空间程序访问硬件设备
- ✅ 特殊文件类型（管道、套接字）支持进程间通信

**💡 与普通文件的区别**：
- **普通文件**：存储实际数据，可以进行读写操作
- **目录文件**：存储文件名和inode的映射关系
- **设备文件**：不存储数据，而是提供访问硬件设备的接口
- **管道/套接字**：用于进程间通信，数据在内存中传输

---

### 二、文件的逻辑结构 ⭐ 重点

**逻辑结构**：从用户角度看到的文件结构

#### 1. 无结构文件（流式文件）

**特点**：
- 文件内部不再划分记录
- 是一串字符流（字节序列）
- 操作系统不关心文件内容

**代表**：
- UNIX、Linux的文件系统
- C语言的文件操作

**优点**：
- ✅ 灵活，适用于各种应用
- ✅ 实现简单

#### 2. 有结构文件（记录式文件）

**特点**：
- 由一组相似的记录组成
- 每个记录有固定格式

**记录的组织方式**：

| 方式 | 说明 | 适用场景 |
|-----|------|--------|
| **顺序文件** | 记录按关键字顺序排列 | 批量处理 |
| **索引文件** | 建立索引表，快速查找 | 随机访问 |
| **索引顺序文件** | 结合两者优点 | 通用 |

**代表**：
- 数据库文件
- 传统操作系统（如IBM大型机）

### 三、文件控制块FCB ⭐⭐ 重点

**FCB（File Control Block）**：系统为每个文件设置的一个数据结构，用于描述和控制文件

**FCB包含的信息**：

| 类别 | 内容 |
|-----|------|
| **基本信息** | 文件名、物理地址、逻辑结构、物理结构 |
| **存取控制信息** | 访问权限（读/写/执行） |
| **使用信息** | 创建时间、修改时间、访问时间、文件大小 |
| **管理信息** | 文件类型、链接数、所有者 |

**FCB的问题**：
- ❌ FCB太大，检索文件时需要多次访盘
- ❌ 打开文件时，需要将FCB全部读入内存

### 四、索引节点inode ⭐⭐ 必考

**思想**：将FCB中的**文件名**分离出来，剩下的信息构成**索引节点（inode）**

**目录项**：
\`\`\`
目录项 = 文件名 + inode编号
\`\`\`

**优点**：
- ✅ 减少检索文件时访问的磁盘次数
- ✅ 便于文件共享（多个文件名指向同一个inode）
- ✅ 节省内存空间

#### Unix inode结构 ⭐⭐ 必考

**混合索引方式**：

| 索引类型 | 数量 | 说明 |
|---------|-----|------|
| **直接地址索引** | 10个 | 直接指向数据块 |
| **一级间接索引** | 1个 | 指向一个索引块 |
| **二级间接索引** | 1个 | 指向一级索引块的索引块 |
| **三级间接索引** | 1个 | 指向二级索引块的索引块 |

**计算示例**：
\`\`\`
假设：
- 磁盘块大小 = 1KB = 1024B
- 盘块号占4B
- 一个索引块可存放：1024 / 4 = 256个盘块号

直接地址：10个块
一级间接：256个块
二级间接：256 × 256 = 65,536个块
三级间接：256 × 256 × 256 = 16,777,216个块

最大文件大小 ≈ 16GB
\`\`\`

### 五、目录管理

**目录**：为了实现**按名存取**，文件系统为每个文件建立一个目录项

**目录项的内容**：
- 文件名
- inode编号（或FCB指针）

#### 1. 单级目录

**结构**：所有文件都在一个目录下

**问题**：
- ❌ 不能重名
- ❌ 不便分组
- ❌ 检索慢

#### 2. 两级目录

**结构**：
- 主文件目录（MFD）：按用户名索引
- 用户文件目录（UFD）：每个用户一个目录

**优点**：
- ✅ 解决重名问题
- ✅ 提高检索速度

**缺点**：
- ❌ 不能共享文件
- ❌ 不能建立子目录

#### 3. 树形目录 ⭐ 常用

**结构**：多级目录，形成树形结构

**优点**：
- ✅ 层次清晰，便于管理
- ✅ 可以共享文件
- ✅ 检索效率高

**路径名**：
- **绝对路径**：从根目录开始
- **相对路径**：从当前目录开始

**示例**：
\`\`\`
绝对路径：/home/user/document/file.txt
相对路径：./document/file.txt
\`\`\`

### 六、文件的物理结构 ⭐⭐⭐ 必考

**物理结构**：文件在磁盘上的存储方式

#### 1. 连续分配

**原理**：将文件存放在一组**连续**的磁盘块中

**FCB内容**：
- 起始块号
- 文件长度（块数）

**优点**：
- ✅ 支持顺序访问和随机访问
- ✅ 访问速度快
- ✅ 实现简单

**缺点**：
- ❌ 产生外部碎片
- ❌ 文件长度难以动态增长
- ❌ 需要预知文件大小

**适用**：光盘文件系统（CD-ROM）

#### 2. 链接分配

##### （1）隐式链接

**原理**：每个磁盘块包含指向下一个块的指针

**FCB内容**：
- 起始块号
- 结束块号

**优点**：
- ✅ 无外部碎片
- ✅ 文件可动态增长

**缺点**：
- ❌ 只能顺序访问
- ❌ 指针占用存储空间
- ❌ 可靠性差（指针损坏导致文件丢失）

##### （2）显式链接（FAT）⭐ 重要

**原理**：将链接指针集中存放在**文件分配表（FAT）**中

**FAT表**：
- 内存中保存一份副本
- 每个表项对应一个磁盘块
- 表项内容：下一个块的块号

**优点**：
- ✅ 支持随机访问
- ✅ 无需在数据块中存指针

**缺点**：
- ❌ FAT表占用内存空间
- ❌ 访问时需要查FAT表

**代表**：FAT16、FAT32文件系统

#### 3. 索引分配 ⭐⭐ 重点

**原理**：为每个文件建立一个**索引表**，记录文件各逻辑块对应的物理块号

**FCB内容**：
- 索引表的地址

**优点**：
- ✅ 支持随机访问
- ✅ 无外部碎片
- ✅ 文件可动态增长

**缺点**：
- ❌ 索引表占用额外空间
- ❌ 小文件浪费空间

##### 多级索引

**问题**：大文件的索引表太大

**解决**：采用多级索引

| 级别 | 说明 |
|-----|------|
| **一级索引** | 索引表直接指向数据块 |
| **二级索引** | 索引表指向一级索引表 |
| **三级索引** | 依此类推 |

**Unix混合索引**：
- 直接地址（10个）+ 一级间接 + 二级间接 + 三级间接
- 兼顾小文件和大文件的效率

### 七、文件的操作

**系统调用**：

| 操作 | 系统调用 | 说明 |
|-----|---------|------|
| **创建** | create() | 创建新文件 |
| **删除** | delete() | 删除文件 |
| **打开** | open() | 将FCB读入内存 |
| **关闭** | close() | 将FCB写回磁盘 |
| **读** | read() | 从文件读取数据 |
| **写** | write() | 向文件写入数据 |
| **定位** | seek() | 移动读写指针 |

**打开文件的过程**：
\`\`\`
1. 根据文件名查找目录，得到inode编号
    ↓
2. 将inode读入内存
    ↓
3. 在打开文件表中创建表项
    ↓
4. 返回文件描述符（fd）
\`\`\`

### 八、文件共享与保护

#### 1. 文件共享

**硬链接**：
- 多个文件名指向同一个inode
- 删除一个文件名，文件仍存在
- 不能跨文件系统

**软链接（符号链接）**：
- 创建一个特殊文件，包含目标文件的路径名
- 删除原文件，链接失效
- 可以跨文件系统

#### 2. 文件保护

**访问控制列表（ACL）**：
- 为每个文件维护一个列表
- 记录每个用户的访问权限

**权限位**：
\`\`\`
-rwxr-xr--
│ │││ │││└─ 其他用户：只读
│ │││ ││└── 其他用户：无写权限
│ │││ │└─── 其他用户：无执行权限
│ │││ └──── 同组用户：读+执行
│ ││└───── 同组用户：无写权限
│ │└────── 同组用户：无执行权限
│ └─────── 文件所有者：读+写+执行
└───────── 文件类型（-表示普通文件）
\`\`\`

---

 **学习提示**：
- ✅ FCB和inode的区别是高频考点
- ✅ Unix inode的结构和计算必考
- ✅ 三种物理结构的对比常考
- ✅ 理解硬链接和软链接的区别`,
        },
        {
          id: '4.2',
          title: '文件系统',
          content: `## 4.2 文件系统实现 ⭐⭐ 重点

### 一、文件系统布局

**磁盘分区结构**：

| 区域 | 说明 |
|-----|------|
| **引导块** | 存放引导程序，启动OS |
| **超级块** | 文件系统的元数据（大小、空闲块数等） |
| **i节点列表** | 存放所有inode |
| **数据区** | 存放文件数据和目录 |

**超级块的内容**：
- 文件系统的大小
- 空闲块的数量和位置
- inode的数量和位置
- 文件系统类型
- 挂载状态

### 二、文件存储空间管理 ⭐⭐⭐ 必考

**目标**：有效地管理磁盘的空闲空间

#### 1. 空闲表法

**原理**：为每个连续的空闲区建立一个表项

**表项格式**：
\`\`\`
第1个空闲区：起始块号=100，长度=50
第2个空闲区：起始块号=200，长度=30
...
\`\`\`

**优点**：
- ✅ 容易找到连续的空闲区
- ✅ 适合连续分配的文件系统

**缺点**：
- ❌ 表项数量不确定
- ❌ 碎片多时表项太多

#### 2. 空闲链表法

**原理**：将所有空闲块链接成一个链表

**实现方式**：
- 第一个空闲块中存放指向下一个空闲块的指针
- 或者在内存中维护一个空闲链表

**优点**：
- ✅ 实现简单
- ✅ 无额外空间开销

**缺点**：
- ❌ 难以找到连续的空闲块
- ❌ 遍历链表慢

#### 3. 位示图法（Bitmap）⭐ 重要

**原理**：用二进制位来表示磁盘块的使用情况

**规则**：
- 0表示空闲
- 1表示已分配

**示例**：
\`\`\`
磁盘有16个块，字长=8位

位示图：
字0: 1 0 1 1 0 0 1 0  （块0-7）
字1: 0 1 0 0 1 1 0 1  （块8-15）

空闲块：1, 4, 5, 8, 10, 11, 14
\`\`\`

**计算公式**：
\`\`\`
已知块号b，字长w：
- 行号 i = b / w
- 列号 j = b % w

已知行号i，列号j，字长w：
- 块号 b = i × w + j
\`\`\`

**示例计算**：
\`\`\`
假设字长=16位，块号=50

行号 i = 50 / 16 = 3
列号 j = 50 % 16 = 2

所以块50对应：第3字的第2位
\`\`\`

**优点**：
- ✅ 容易找到连续的空闲块
- ✅ 便于计算空闲块总数
- ✅ 实现简单

**缺点**：
- ❌ 位示图需要常驻内存
- ❌ 大磁盘时位示图太大

**位示图大小计算**：
\`\`\`
磁盘容量 = 1TB = 2^40 B
块大小 = 4KB = 2^12 B
块数 = 2^40 / 2^12 = 2^28

位示图大小 = 2^28 bits = 2^25 Bytes = 32MB
\`\`\`

#### 4. 成组链接法 ⭐ Unix采用

**思想**：改进的空闲链表法，将空闲块分组管理

**结构**：
\`\`\`
第一组（100个空闲块）：
  - 第1块：存放第二组的地址和块数
  - 第2-100块：空闲块

第二组（100个空闲块）：
  - 第1块：存放第三组的地址和块数
  - 第2-100块：空闲块
...
\`\`\`

**分配过程**：
\`\`\`
1. 从第一组分配空闲块
2. 当第一组只剩最后一块时
3. 将该块读入内存（包含下一组的信息）
4. 继续从下一组分配
\`\`\`

**优点**：
- ✅ 不需要将整个链表常驻内存
- ✅ 适合大型文件系统
- ✅ 分配和回收效率高

**代表**：Unix文件系统

### 三、四种方法对比 ⭐ 总结

| 方法 | 查找速度 | 空间开销 | 适用场景 |
|-----|---------|---------|--------|
| **空闲表** | 快（连续区） | 中等 | 连续分配 |
| **空闲链表** | 慢 | 无 | 小文件系统 |
| **位示图** | 快 | 较大 | 通用 |
| **成组链接** | 较快 | 小 | 大型系统（Unix） |

### 四、虚拟文件系统（VFS）

**问题**：不同的文件系统有不同的实现

**解决**：VFS提供统一的接口

**VFS的作用**：
- 📍 屏蔽底层文件系统的差异
- 📍 为用户提供统一的API
- 📍 支持多种文件系统共存

**架构**：
\`\`\`
用户程序
    ↓
VFS层（统一接口）
    ↓
具体文件系统（ext4、FAT32、NTFS...）
    ↓
磁盘驱动
\`\`\`

**VFS的数据结构**：
- 超级块对象
- inode对象
- 目录项对象
- 文件对象

**优点**：
- ✅ 支持多种文件系统
- ✅ 用户程序无需关心底层类型
- ✅ 便于扩展新的文件系统

### 五、文件系统挂载

**挂载（Mount）**：将一个文件系统连接到目录树的某个节点上

**挂载点**：目录树中的一个目录

**挂载过程**：
\`\`\`
1. 检查挂载点是否存在
    ↓
2. 读取文件系统的超级块
    ↓
3. 将超级块信息存入内存
    ↓
4. 更新目录树，将挂载点指向新文件系统
\`\`\`

**示例**：
\`\`\`bash
# Linux挂载命令
mount /dev/sdb1 /mnt/usb

# 卸载
umount /mnt/usb
\`\`\`

**卸载（Unmount）**：
- 断开文件系统与目录树的连接
- 要求：没有进程在使用该文件系统

---

 **学习提示**：
- ✅ 位示图的计算题必考
- ✅ 理解成组链接法的工作原理
- ✅ 掌握四种空间管理方法的优缺点
- ✅ 了解VFS的作用`,
        }
      ]
    },
    {
      id: 'ch5',
      number: 5,
      title: '输入/输出管理',
      score: '5-8分',
      importance: 'medium',
      mindMap: `mindmap
  root((输入/输出管理))
    I/O管理概述
      I/O设备
      I/O控制方式
      I/O软件层次结构
      应用程序I/O接口
    设备独立性软件
      设备独立性软件
      高速缓存与缓冲区
      设备分配与回收
      SPOOLing技术
      设备驱动程序接口
    磁盘和固态硬盘
      磁盘
      磁盘的管理
      磁盘调度算法
      固态硬盘`,
      sections: [
        {
          id: '5.1',
          title: 'I/O管理概述',
          content: `## 5.1 I/O管理概述 ⭐⭐ 重点

### 一、I/O设备的分类

#### 1. 按使用特性分类

| 类型 | 说明 | 示例 |
|-----|------|------|
| **存储设备** | 用于存储数据 | 磁盘、磁带、U盘 |
| **输入/输出设备** | 人机交互 | 键盘、鼠标、显示器、打印机 |

#### 2. 按传输速率分类

| 类型 | 速率 | 示例 |
|-----|------|------|
| **低速设备** | KB/s级 | 键盘、鼠标 |
| **中速设备** | MB/s级 | 打印机、扫描仪 |
| **高速设备** | GB/s级 | 磁盘、光盘 |

#### 3. 按信息交换单位分类 ⭐ 重要

| 类型 | 交换单位 | 特点 | 示例 |
|-----|---------|------|------|
| **块设备** | 数据块 | 可随机访问，有缓冲区 | 磁盘、光盘 |
| **字符设备** | 字符流 | 只能顺序访问，无缓冲 | 键盘、鼠标、打印机 |

### 二、I/O控制方式 ⭐⭐⭐ 必考

**目标**：减少CPU对I/O操作的干预，提高系统效率

#### 1. 程序直接控制方式（轮询）

**原理**：CPU不断查询设备状态

**过程**：
\`\`\`
CPU发出I/O命令
    ↓
循环查询设备状态
    ↓
设备完成 → CPU取走数据
\`\`\`

**优点**：
- ✅ 实现简单

**缺点**：
- ❌ CPU利用率低（忙等待）
- ❌ 不能并行执行
- ❌ 实时性差

#### 2. 中断驱动方式 ⭐ 重要

**原理**：设备完成操作后发出中断，CPU响应中断

**过程**：
\`\`\`
CPU发出I/O命令
    ↓
CPU执行其他任务
    ↓
设备完成 → 发出中断
    ↓
CPU响应中断，处理数据
\`\`\`

**优点**：
- ✅ CPU利用率高
- ✅ 支持并行执行

**缺点**：
- ❌ 每次传输都要中断
- ❌ 中断开销大（频繁中断）

**适用**：少量数据传输

#### 3. DMA方式（Direct Memory Access）⭐⭐ 重点

**原理**：由DMA控制器负责数据传输，CPU只需在开始和结束时介入

**DMA控制器**：
- 专门的硬件电路
- 可以直接访问内存

**过程**：
\`\`\`
CPU初始化DMA控制器
    ↓
DMA控制器接管总线
    ↓
DMA直接在设备和内存间传输数据
    ↓
传输完成 → DMA发出中断
    ↓
CPU响应中断，进行后续处理
\`\`\`

**优点**：
- ✅ CPU干预少
- ✅ 适合大批量数据传输
- ✅ 传输速度快

**缺点**：
- ❌ 需要DMA控制器硬件
- ❌ 占用总线

**适用**：磁盘、网卡等高速设备

#### 4. 通道方式

**原理**：通道是专门负责I/O的处理器

**通道**：
- 独立的I/O处理器
- 可以执行通道程序

**过程**：
\`\`\`
CPU发出I/O指令
    ↓
通道执行通道程序
    ↓
通道控制多个设备完成I/O
    ↓
全部完成 → 通道发出中断
    ↓
CPU响应中断
\`\`\`

**优点**：
- ✅ CPU干预最少
- ✅ 可以控制多个设备
- ✅ 实现真正的并行

**缺点**：
- ❌ 硬件复杂，成本高

**代表**：IBM大型机

#### 5. 四种控制方式对比 ⭐ 总结

| 方式 | CPU干预 | 数据单位 | 适用场景 | 性能 |
|-----|--------|---------|---------|-----|
| **程序控制** | 最多 | 字节 | 极少用 | 最差 |
| **中断驱动** | 较多 | 字节 | 少量数据 | 较差 |
| **DMA** | 较少 | 数据块 | 大批量数据 | 好 |
| **通道** | 最少 | 数据块 | 大型系统 | 最好 |

### 三、I/O软件层次结构 ⭐⭐ 重点

**设计原则**：分层设计，上层使用下层提供的接口

**四层结构**：

| 层次 | 名称 | 功能 |
|-----|------|------|
| **第4层** | 用户层I/O软件 | 库函数、Spooling系统 |
| **第3层** | 设备独立性软件 | 设备命名、保护、分配 |
| **第2层** | 设备驱动程序 | 与硬件直接交互 |
| **第1层** | 中断处理程序 | 处理I/O中断 |

**特点**：
- ✅ 上层可以使用下层提供的接口
- ❌ 下层不能调用上层
- ✅ 每层职责明确

### 四、应用程序I/O接口

**系统调用**：

| 调用 | 功能 |
|-----|------|
| **open()** | 打开设备或文件 |
| **read()** | 读取数据 |
| **write()** | 写入数据 |
| **close()** | 关闭设备或文件 |
| **ioctl()** | 设备控制操作 |

---

 **学习提示**：
- ✅ 四种I/O控制方式的对比是高频考点
- ✅ 理解DMA的工作原理
- ✅ 掌握I/O软件的四层结构`,
        },
        {
          id: '5.2',
          title: '设备独立性软件',
          content: `## 5.2 设备独立性软件 ⭐⭐ 重点

### 一、设备独立性

**定义**：应用程序独立于具体使用的物理设备

**实现方式**：
- 设备命名统一化（逻辑设备名）
- 提供逻辑设备与物理设备的映射

**优点**：
- ✅ 提高可移植性
- ✅ 便于设备替换
- ✅ 简化编程

**示例**：
\`\`\`
应用程序使用逻辑设备名：printer
    ↓
系统映射到物理设备：/dev/lp0
    ↓
如果更换打印机，只需修改映射
\`\`\`

### 二、高速缓存与缓冲区 ⭐⭐ 必考

**目的**：缓和CPU与I/O设备速度不匹配的矛盾

#### 1. 单缓冲

**原理**：设置一个缓冲区

**过程**：
\`\`\`
读入：设备 → 缓冲区 → 用户区
写出：用户区 → 缓冲区 → 设备
\`\`\`

**时间分析**：
\`\`\`
T = 输入时间 + 计算时间 + 输出时间
\`\`\`

**缺点**：
- ❌ 串行执行，效率低

#### 2. 双缓冲

**原理**：设置输入缓冲区和输出缓冲区

**过程**：
\`\`\`
输入：设备 → 缓冲区1 → 用户区
      同时：设备 → 缓冲区2 （准备下一次）

输出：用户区 → 缓冲区1 → 设备
      同时：用户区 → 缓冲区2 （准备下一次）
\`\`\`

**优点**：
- ✅ 提高并行性
- ✅ 减少等待时间

**时间分析**：
\`\`\`
如果计算时间 < 输入/输出时间：
T ≈ max(输入时间, 计算时间) + 输出时间
\`\`\`

#### 3. 循环缓冲

**原理**：多个缓冲区组成循环队列

**适用**：多个生产者和消费者

#### 4. 缓冲池

**原理**：将多个缓冲区组织成链表，统一管理

**三个链表**：
- **空闲缓冲链表**：未使用的缓冲区
- **输入缓冲链表**：已填满的输入缓冲区
- **输出缓冲链表**：待输出的缓冲区

**优点**：
- ✅ 提高缓冲区利用率
- ✅ 灵活分配

### 三、设备分配与回收

#### 1. 设备分类

| 类型 | 特点 | 示例 |
|-----|------|------|
| **独占设备** | 一次只能分配给一个进程 | 打印机、磁带机 |
| **共享设备** | 可同时分配给多个进程 | 磁盘 |
| **虚拟设备** | 通过SPOOLing技术改造 | 虚拟打印机 |

#### 2. 设备分配策略

**独占设备分配**：
\`\`\`
1. 检查设备是否可用
    ↓
2. 分配设备
    ↓
3. 修改设备状态为“忙”
    ↓
4. 进程使用完毕后释放
\`\`\`

**共享设备分配**：
- 采用调度算法（如磁盘调度）
- 多个进程可以并发访问

### 四、SPOOLing技术（假脱机技术）⭐⭐ 必考

**全称**：Simultaneous Peripheral Operations On-Line

**思想**：将**独占设备**改造为**共享设备**

**原理**：
- 利用磁盘作为后端存储
- 将I/O操作暂存到磁盘
- 由后台进程实际执行I/O

#### SPOOLing系统的组成

| 组件 | 位置 | 功能 |
|-----|------|------|
| **输入井/输出井** | 磁盘 | 暂存数据 |
| **输入缓冲区/输出缓冲区** | 内存 | 缓冲数据 |
| **输入进程/输出进程** | 系统 | 控制数据流动 |

#### 打印Spooling系统的工作过程

\`\`\`
Step 1: 用户进程请求打印
    ↓
Step 2: 系统将数据写入输出井（磁盘）
    ↓
Step 3: 用户进程继续执行（不等待）
    ↓
Step 4: 输出进程从输出井读取数据
    ↓
Step 5: 输出进程将数据发送到打印机
\`\`\`

**优点**：
- ✅ 提高设备利用率
- ✅ 提高用户响应速度
- ✅ 实现虚拟设备功能

**缺点**：
- ❌ 占用磁盘空间
- ❌ 实现复杂

### 五、设备驱动程序接口

**设备驱动程序**：操作系统内核中直接控制硬件的代码

**功能**：
1. 接收上层软件的抽象I/O请求
2. 检查请求的合法性
3. 向设备控制器发送命令
4. 等待中断或与中断处理程序协同工作

**特点**：
- 📍 与硬件紧密相关
- 📍 每种设备都有对应的驱动程序
- 📍 可动态加载和卸载

**驱动程序的接口**：
\`\`\`
// 标准接口
int open(device);
int close(device);
int read(device, buffer, count);
int write(device, buffer, count);
int ioctl(device, command, arg);
\`\`\`

---

 **学习提示**：
- ✅ 单缓冲vs双缓冲的时间计算
- ✅ SPOOLing技术的原理和组成是高频考点
- ✅ 理解设备独立性的意义`,
        },
        {
          id: '5.3',
          title: '磁盘和固态硬盘',
          content: `## 5.3 磁盘和固态硬盘 ⭐⭐⭐ 必考

### 一、磁盘的结构

**磁盘组成**：
- 多个盘片（Platter）
- 每个盘片有两个面（Surface）
- 每个面有一个磁头（Head）

**磁盘术语**：

| 术语 | 说明 |
|-----|------|
| **磁道（Track）** | 盘面上的同心圆 |
| **扇区（Sector）** | 磁道上的分段，最小存储单位（通常512B） |
| **柱面（Cylinder）** | 所有盘面上相同半径的磁道集合 |

**磁盘地址表示**：
\`\`\`
(柱面号, 磁头号, 扇区号)
\`\`\`

### 二、磁盘的管理

#### 磁盘初始化过程

| 步骤 | 名称 | 说明 |
|-----|------|------|
| **1** | 低级格式化 | 划分磁道和扇区 |
| **2** | 分区 | 将磁盘划分为多个逻辑磁盘 |
| **3** | 高级格式化 | 创建文件系统 |

### 三、磁盘I/O时间 ⭐⭐ 重点

**磁盘I/O时间 = 寻道时间 + 旋转延迟 + 传输时间**

#### 1. 寻道时间（Seek Time）

**定义**：磁头移动到指定磁道的时间

**特点**：
- 📍 **主要因素**（占总时间的70%以上）
- 📍 与移动距离成正比

**平均寻道时间**：通常为3-10ms

#### 2. 旋转延迟（Rotational Latency）

**定义**：等待指定扇区旋转到磁头下的时间

**平均旋转延迟**：
\`\`\`
平均延迟 = 旋转一周时间 / 2

例如：7200 RPM的磁盘
旋转一周 = 60 / 7200 = 8.33ms
平均延迟 = 4.17ms
\`\`\`

#### 3. 传输时间（Transfer Time）

**定义**：数据传输的时间

**计算**：
\`\`\`
传输时间 = 数据量 / 传输速率
\`\`\`

**示例计算**：
\`\`\`
假设：
- 寻道时间 = 5ms
- 转速 = 7200 RPM
- 传输速率 = 100MB/s
- 读取512B数据

寻道时间 = 5ms
旋转延迟 = 4.17ms
传输时间 = 512B / 100MB/s ≈ 0.005ms

总时间 ≈ 9.175ms
\`\`\`

### 四、磁盘调度算法 ⭐⭐⭐ 必考

**目标**：减少平均寻道时间

#### 1. FCFS（先来先服务）

**原理**：按请求到达的顺序处理

**示例**：
\`\`\`
请求序列：98, 183, 37, 122, 14, 124, 65, 67
当前磁头位置：53

执行顺序：98 → 183 → 37 → 122 → 14 → 124 → 65 → 67

移动距离：
53→98: 45
98→183: 85
183→37: 146
37→122: 85
122→14: 108
14→124: 110
124→65: 59
65→67: 2

总移动距离 = 640
\`\`\`

**优点**：
- ✅ 公平
- ✅ 实现简单

**缺点**：
- ❌ 性能差
- ❌ 寻道距离长

#### 2. SSTF（最短寻道时间优先）

**原理**：优先处理距离当前磁头最近的请求

**示例**：
\`\`\`
请求序列：98, 183, 37, 122, 14, 124, 65, 67
当前磁头位置：53

Step 1: 距离53最近的是65 (距离12)
Step 2: 距离65最近的是67 (距离2)
Step 3: 距离67最近的是37 (距离30)
...

执行顺序：65 → 67 → 37 → 14 → 98 → 122 → 124 → 183
总移动距离 = 236 ← 比FCFS好！
\`\`\`

**优点**：
- ✅ 平均寻道时间短
- ✅ 性能好

**缺点**：
- ❌ 可能导致**饥饿**（远处的请求长期得不到服务）

#### 3. SCAN（电梯算法）⭐ 重要

**原理**：磁头沿一个方向移动，处理沿途请求，到达端点后反向

**示例**（假设向磁道号增加的方向移动）：
\`\`\`
请求序列：98, 183, 37, 122, 14, 124, 65, 67
当前磁头位置：53，方向：向外

执行顺序：65 → 67 → 98 → 122 → 124 → 183 → 37 → 14

移动距离：
53→183: 130（向外）
183→14: 169（向内）

总移动距离 = 299
\`\`\`

**优点**：
- ✅ 避免饥饿
- ✅ 性能较好
- ✅ 类似电梯运行

**缺点**：
- ❌ 两端的请求等待时间长

#### 4. C-SCAN（循环扫描）

**原理**：单向移动，回到起点后重新开始

**示例**：
\`\`\`
请求序列：98, 183, 37, 122, 14, 124, 65, 67
当前磁头位置：53，方向：向外

执行顺序：65 → 67 → 98 → 122 → 124 → 183 → 14 → 37

注意：183→14是快速返回，不处理请求

总移动距离 = 391
\`\`\`

**优点**：
- ✅ 提供更均匀的等待时间
- ✅ 避免两端请求等待过长

**缺点**：
- ❌ 返回时不处理请求，效率略低

#### 5. LOOK和C-LOOK

**思想**：改进SCAN和C-SCAN，不需要移动到端点

**LOOK**：只移动到最远的请求位置就反向

**C-LOOK**：只移动到最远的请求位置就返回起点

### 五、磁盘调度算法对比 ⭐ 总结

| 算法 | 平均寻道时间 | 饥饿 | 适用场景 |
|-----|------------|-----|--------|
| **FCFS** | 最长 | 无 | 很少用 |
| **SSTF** | 短 | 有 | 负载轻 |
| **SCAN** | 较短 | 无 | 通用 |
| **C-SCAN** | 较短 | 无 | 要求均匀响应 |
| **LOOK** | 短 | 无 | 实际系统常用 |

### 六、固态硬盘（SSD）

#### SSD vs HDD

| 对比项 | HDD（机械硬盘） | SSD（固态硬盘） |
|-------|---------------|--------------|
| **存储介质** | 磁性盘片 | 闪存芯片 |
| **读写速度** | 慢（ms级） | 快（μs级） |
| **抗震性** | 差 | 好 |
| **功耗** | 高 | 低 |
| **噪音** | 有 | 无 |
| **寿命** | 长 | 有限（写入次数） |
| **价格** | 便宜 | 较贵 |

#### SSD的特点

**优点**：
- ✅ 速度快（无机械部件）
- ✅ 抗震性强
- ✅ 功耗低
- ✅ 无噪音

**缺点**：
- ❌ 价格高
- ❌ 写入次数有限
- ❌ 需要特殊管理

#### SSD的管理技术

##### 1. 磨损均衡（Wear Leveling）

**问题**：闪存有写入次数限制（通常10万次）

**解决**：均匀分配写入操作到所有块

##### 2. 垃圾回收（Garbage Collection）

**问题**：SSD不能直接覆盖写入，需要先擦除

**解决**：
- 后台回收无效数据块
- 整理有效数据
- 擦除整个块

##### 3. TRIM命令

**作用**：通知SSD哪些数据块不再需要

**好处**：
- 提高写入性能
- 延长SSD寿命

---

 **学习提示**：
- ✅ 磁盘调度算法的计算题必考（会计算移动距离）
- ✅ 掌握四种主要算法的特点
- ✅ 理解HDD和SSD的区别
- ✅ 了解SSD的特殊管理技术`,
        }
      ]
    }
  ])

  // 计算属性
  const currentChapter = computed(() => {
    return chapters.value.find(ch => ch.id === currentChapterId.value)
  })

  const currentSection = computed(() => {
    if (!currentChapter.value) return null
    return currentChapter.value.sections.find(sec => sec.id === currentSectionId.value)
  })

  const progress = computed(() => {
    const totalSections = chapters.value.reduce((sum, ch) => sum + ch.sections.length, 0)
    const studiedCount = studiedSections.value.size
    return {
      total: totalSections,
      studied: studiedCount,
      percentage: Math.round((studiedCount / totalSections) * 100)
    }
  })

  // 方法
  function setCurrentChapter(chapterId: string) {
    currentChapterId.value = chapterId
    // 自动选中第一章的第一个小节
    const chapter = chapters.value.find(ch => ch.id === chapterId)
    if (chapter && chapter.sections.length > 0) {
      currentSectionId.value = chapter.sections[0].id
    }
  }

  function setCurrentSection(sectionId: string) {
    currentSectionId.value = sectionId
    
    // 自动更新所属章节ID
    const section = findSectionById(sectionId)
    if (section) {
      const chapter = chapters.value.find(ch => 
        ch.sections.some(sec => sec.id === sectionId)
      )
      if (chapter) {
        currentChapterId.value = chapter.id
      }
    }
  }
  
  // 辅助函数：查找小节
  function findSectionById(sectionId: string) {
    for (const chapter of chapters.value) {
      const section = chapter.sections.find(sec => sec.id === sectionId)
      if (section) return section
    }
    return null
  }

  function toggleSectionStudied(sectionId: string) {
    if (studiedSections.value.has(sectionId)) {
      studiedSections.value.delete(sectionId)
    } else {
      studiedSections.value.add(sectionId)
    }
    saveProgress()
  }

  function isSectionStudied(sectionId: string): boolean {
    return studiedSections.value.has(sectionId)
  }

  function getChapterProgress(chapterId: string): number {
    const chapter = chapters.value.find(ch => ch.id === chapterId)
    if (!chapter) return 0
    const studiedInChapter = chapter.sections.filter(sec => 
      studiedSections.value.has(sec.id)
    ).length
    return Math.round((studiedInChapter / chapter.sections.length) * 100)
  }

  function saveProgress() {
    localStorage.setItem('os_studied_sections', JSON.stringify([...studiedSections.value]))
  }

  function resetProgress() {
    studiedSections.value.clear()
    saveProgress()
  }

  return {
    // 状态
    chapters,
    currentChapterId,
    currentSectionId,
    studiedSections,
    
    // 计算属性
    currentChapter,
    currentSection,
    progress,
    
    // 方法
    setCurrentChapter,
    setCurrentSection,
    toggleSectionStudied,
    isSectionStudied,
    getChapterProgress,
    saveProgress,
    resetProgress
  }
})
