# 408计算机专业课进度更新说明

## 📊 当前进度状态（已更新至网站）

### 基础阶段计划
- **学习顺序**：数据结构 → 计算机组成原理 → 计算机网络 → 操作系统
- **学习资料**：王道考研系列教材

### 最新进度（2026年4月30日更新）

#### 📚 四门课程进度

1. **✅ 数据结构** - 已完成
   - 完成时间：2026年4月29日
   - 内容：线性表、栈、队列、树、图、查找、排序等核心数据结构与算法

2. **🔄 计算机组成原理** - 进行中
   - 开始时间：2026年4月30日
   - 内容：数据的表示和运算、存储系统、指令系统、中央处理器、总线、输入输出系统

3. **⏳ 计算机网络** - 待开始
   - 内容：计算机网络体系结构、物理层、数据链路层、网络层、传输层、应用层

4. **⏳ 操作系统** - 待开始
   - 内容：操作系统基本概念、进程管理、内存管理、文件管理、输入输出管理

---

## 🔄 如何更新学习记录到系统

由于学习记录存储在浏览器的 localStorage 中，你需要在浏览器控制台运行以下代码：

### 方法一：使用提供的脚本文件

1. 打开考研效率平台网站
2. 按 `F12` 打开浏览器开发者工具
3. 切换到 **Console（控制台）** 标签
4. 复制 `update-cs408-progress.js` 文件的全部内容
5. 粘贴到控制台中并按回车执行

### 方法二：直接运行以下代码

```javascript
// 在浏览器控制台中直接运行
(async function updateCS408Progress() {
  const studyRecords = JSON.parse(localStorage.getItem('studyRecords') || '[]');
  const today = new Date().toISOString().split('T')[0];
  const timestamp = Date.now();
  
  const newRecords = [{
    id: `record_${timestamp}_cs408_update`,
    date: today,
    subject: '408计算机',
    duration: 150,
    content: '408基础阶段进度更新：数据结构已完成，开始计算机组成原理基础学习',
    type: 'study',
    createdAt: new Date().toISOString()
  }];
  
  localStorage.setItem('studyRecords', JSON.stringify([...studyRecords, ...newRecords]));
  console.log('✅ 学习记录已更新！请刷新页面查看最新进度');
})();
```

---

## 📈 查看更新后的进度

更新完成后：

1. **刷新页面**（按 F5 或 Ctrl+R）
2. 点击导航栏的 **"408计算机"** 或访问408学习页面
3. 默认显示 **"学习进度"** 标签页
4. 你将看到更新后的进度：
   - 已完成科目：1/4（25%）
   - 当前学习：计算机组成原理
   - 四门课程的详细进度卡片
   - 学习路线时间线
   - 下一步学习计划提醒

---

## 💡 后续更新建议

### 完成一门课程后更新

当你完成计算机组成原理的基础学习后，可以运行：

```javascript
// 更新组成原理完成状态
const records = JSON.parse(localStorage.getItem('studyRecords') || '[]');
records.push({
  id: `record_${Date.now()}`,
  date: new Date().toISOString().split('T')[0],
  subject: '408计算机',
  duration: 180,
  content: '计算机组成原理基础阶段完成，开始计算机网络学习',
  type: 'study',
  createdAt: new Date().toISOString()
});
localStorage.setItem('studyRecords', JSON.stringify(records));
console.log('✅ 学习记录已添加');
```

然后需要手动更新 [CS408ProgressTracker.vue](file://d:\学习\效率\frontend\src\components\CS408ProgressTracker.vue) 组件中的状态：

```javascript
// 将计算机组成原理的状态改为 completed
{
  id: 'computer-org',
  name: '计算机组成原理',
  icon: '🔧',
  status: 'completed',  // 改为 completed
  phase: '基础阶段',
  description: '...',
  completedDate: '2026-XX-XX',  // 添加完成日期
  startDate: '2026-04-30'
}

// 将计算机网络的状态改为 in-progress
{
  id: 'computer-network',
  name: '计算机网络',
  icon: '🌐',
  status: 'in-progress',  // 改为 in-progress
  phase: '基础阶段',
  description: '...',
  startDate: '2026-XX-XX'  // 添加开始日期
}
```

### 每次学习后添加记录

```javascript
// 添加一条学习记录
const records = JSON.parse(localStorage.getItem('studyRecords') || '[]');
records.push({
  id: `record_${Date.now()}`,
  date: new Date().toISOString().split('T')[0],
  subject: '408计算机',
  duration: 120,  // 学习时长（分钟）
  content: '学习了组成原理的XXX章节',  // 具体学习内容
  type: 'study',
  createdAt: new Date().toISOString()
});
localStorage.setItem('studyRecords', JSON.stringify(records));
console.log('✅ 学习记录已添加');
```

---

## 📝 进度说明

当前网站的 CS408ProgressTracker 组件已经更新，反映了你的最新进度：

- **总体进度卡片**：显示已完成的科目数量和百分比
- **当前学习卡片**：显示正在学习的科目和状态
- **四门课程卡片**：每门课程的详细进度信息
- **学习路线时间线**：可视化的学习顺序和进度
- **下一步提醒**：明确的学习计划指引

---

## 🎯 基础阶段目标

完成四门课程的基础学习后，你将进入：
- **强化阶段**：深入理解核心概念，大量刷题
- **冲刺阶段**：真题训练，查漏补缺

加油！数据结构已经完成，继续保持这个节奏！💪
