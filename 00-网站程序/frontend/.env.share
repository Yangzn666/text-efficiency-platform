# 共享版（公开知识库）构建模式
# 仅当 `vite build --mode share` 时加载，用于把单树构建切到 share 变体：
#   - vite.config 依据 mode==='share' 换 base / PWA 名 / 路由表 / 云同步替身
#   - 构建后裁剪 personal 私有静态资源
# 个人版默认 `vite build`（mode=production）不加载本文件，行为保持不变。
VITE_APP_MODE=share
